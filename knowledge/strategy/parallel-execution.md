# Parallel Execution

Companion to `plan.md`. That file says **what** and **why**; this file
says what can happen **at the same time**.

> The previous lane queues are archived at
> `archive/parallel-execution-17.08.2026.md`. This is a fresh board. Add
> lanes as work is scoped.

---

## Rules

1. **Lane ownership** — each lane owns its directories. No lane edits
   another lane's files. Cross-lane seams go through `.context/` notes.
2. **Merge small, merge often** — branches live days, not weeks.
3. **Every branch updates the changelog** — add a `CHANGELOG.md` line,
   tick the `plan.md` item, tick your lane item here.
4. **Bite sizing** — one bite = one PR ≈ 20-40 focused minutes.
   If an item needs an "AND", split it.

## Status Key

| Mark | Meaning |
|------|---------|
| `[✓]` | Merged |
| `[~]` | In flight in another workspace |
| `[ ]` | Open |

---

## Lanes

<!--
Format per lane:

### Lane: <name> (Phase N)
**Owns:** `<dir>`

- [ ] <bite>
-->

### Lane: hello-demo (Phase 1)
**Owns:** `marketing/marketing-ui/public/docs/hello/`

- [✓] Auth flow completes without errors
- [✓] Greeting shows correct username
- [✓] Groups listed correctly
- [✓] Session restores on page reload
- [✓] No console errors in any flow
- [✓] Full E2E test with popup + log sequence verification

### Lane: notes-demo (Phase 1)
**Owns:** `marketing/marketing-ui/public/docs/notes/`

- [✓] Auth flow completes without errors
- [✓] Create note works
- [✓] Read/list notes works
- [✓] Update note works
- [✓] Delete note works
- [✓] Data persists after page reload
- [✓] No console errors in any flow
- [✓] Full E2E test with popup + log sequence verification

### Lane: auth-consent-forks (Phase 1)
**Owns:** `ui/src/components/Consent/`, `ui/src/interfaces/`, `e2e/tests/auth-popup-roundtrip.spec.ts`

The authenticator consent screen is the shared seam every demo drives. The
round-trip spec drove only the single "Allow" + "Close window" forks. The
**approve-all** fork had a real bug — it dropped the app-contract response, so
the opener's callback fired late, mis-delivered from the group's response —
that no test could see, because the bug lived only in the `approveAll` code
path. The **fork rule** in the AI Use Theory (`knowledge/ai-use-theory/testing.md`)
is the fix: a feature is covered only when the seam is driven through *every*
fork. This lane drives every remaining fork through the real popup.

- [✓] Approve-all fork: app + group contract via "Approve all" (not single Allow) — fixed the dropped app-contract response (shared `approveOne` helper so the two forks can't diverge) + E2E with the bug-catcher assertion
- [✓ 3.3.0] Deny fork: single "Deny" (X) on the app contract → demo handles `denied` gracefully (message, no crash, fix-access appears)
- [✓ 3.3.0] Deny fork: single "Deny" on the group contract → demo proceeds (group is optional, `initApp` still runs)
- [✓ 3.3.0] Skip fork: "Continue without sharing" → token sent, no contracts granted, demo shows fix-access on first CRUD
- [✓ 3.3.0] All-set fork: already-granted ACR is filtered out → "You're all set" renders → "Close window" closes the popup
- [✓ 3.3.0] Logout fork: "Not you? Log out" → returns to login, pending contracts cleared, no stale state
- [✓ 3.3.0] Details fork: expand/collapse chevron renders the permission diff (added/removed/same chips) for a re-request
- [✓ 3.3.0] Mixed fork: approve app + deny group in one session (and the reverse) — each response matches its own contract
- [✓ 3.3.0] Edge fork: approve-all with zero pending contracts → `goToApp` early return, no crash
- [✓ 3.3.0] Fix-access fork: revoke → "Fix access" through the REAL popup (retire the `popup.close()` + raw-API workaround in `notes-demo.spec.ts` — a seam-rule violation the theory now names)
- [✓ 3.75.0] Account switch + remembered-accounts picker (operator: "no opportunity to switch accounts … zero ui … just puts the cookie in" + "google lets you pick one from multiple emails … still asks you for password" + "lets you sign in as a new user too") — the popup stops auto-completing on a stale session when the opener is signed out (`freshLogin` + scoped `nothingToShow` override → login screen; signed-in opener still one-tap; ungranted contract still shows consent). Google-style "Choose an account" picker (last 5, localStorage, identifiers only) pre-fills the form (password still required); "Continue as {username}" = the no-password fast path; "Create a new account" untouched. Cross-lane touch: `ui/src/components/CredentialPage/LoginForm.tsx` + new `ui/src/lib/rememberedAccounts.ts` (the picker is a login-screen feature, not a consent-fork). `rememberedAccounts.test.ts` (8) + `consentAccountSwitch.test.tsx` (5) + `auth-popup-roundtrip` return-run e2e re-pinned.
- [✓ 3.77.10] Consent popup login form no longer smashes into the "Signed in as" footer (operator screenshot: the signed-in + account-picker login screen rendered so tall the password field collided with the "Signed in as … Log out" footer). The login-form branch of `ConsentView.tsx` was a plain `<div className="p-6 sm:p-8">` (no overflow) while the consent branch scrolls internally — a tall form grew the card past the `calc(100vh - 4rem)` cap and pushed the `shrink-0` footer into the password field. Fix: the login-form branch is now the card's internal scroll region (`min-h-0 flex-1 overflow-y-auto p-6 sm:p-8`, the consent branch's idiom). `consentAccountSwitch.test.tsx` +1 (form in a scroll region; footer a sibling, not a descendant). 230 ui tests green, `tsc -b` clean.

### Lane: messages-demo (Phase 1)
**Owns:** `marketing/marketing-ui/public/docs/messages/`

- [✓ 3.5.0] DM group contract: create invite_only group with deterministic ID
- [✓ 3.5.0] Send message via CRUD (attach to DM group)
- [✓ 3.5.0] Receive/read messages via group read
- [✓ 3.8.0] WebRTC: initP2P with token, connect to peer
- [✓ 3.8.0] WebRTC: send data over P2P channel
- [✓ 3.8.0] WebRTC: receive data via onInbound callback
- [✓ 3.8.0] E2E test: DM CRUD + WebRTC P2P round-trip (DM CRUD half done in 3.5.0; WebRTC P2P half done in 3.8.0)

### Lane: groups-demo (Phase 1)
**Owns:** `marketing/marketing-ui/public/docs/groups/`

- [✓ 3.2.0] Create group with roles
- [✓ 3.2.0] Join policy: open (instant join)
- [✓ 3.2.0] Join policy: request (approve/deny)
- [✓ 3.2.0] Invite member with role
- [✓ 3.2.0] Leave group
- [✓ 3.2.0] Remove member
- [✓ 3.2.0] E2E test: group lifecycle + join policies

### Lane: media-demo (Phase 1)
**Owns:** `marketing/marketing-ui/public/docs/media/`

- [✓ 3.6.0] Upload image to MinIO (presigned URL)
- [✓ 3.6.0] Create document with minio type in body
- [✓ 3.6.0] Read document back, verify presigned URL resolved
- [✓ 3.6.0] Display image from presigned URL
- [✓ 3.6.0] E2E test: upload → create → read → display

### Lane: feed-demo (Phase 1)
**Owns:** `marketing/marketing-ui/public/docs/feed/`

- [✓ 3.7.2] Create/join discover group (open, auto-join)
- [✓ 3.7.2] Post to discover group
- [✓ 3.7.2] Create followers groups for 2-3 test users
- [✓ 3.7.2] Follow creators (join followers groups)
- [✓ 3.7.2] Read combined feed from discover + followers
- [✓ 3.7.2] E2E test: post → follow → read combined feed
- [✓ 3.17.6] Fix: use the node-default discover group — the demo was still on the stale per-user model from 3.7.2 (it asked users to "set up your discover group" through the consent popup). Now it reads the universal board `web10.app/groups/web10/discover` under the `posts` service (the same service + group the social app and marketing site use), no setup step; e2e re-aligned (contains-assertions, since the board is a shared node default)

### Lane: sharing-demo (Phase 1)
**Owns:** `marketing/marketing-ui/public/docs/sharing/`

- [✓ 3.7.0] Create group, post to it
- [✓ 3.7.0] Block sharing per group (toggle off)
- [✓ 3.7.0] Verify posts hidden from members when sharing blocked
- [✓ 3.7.0] Unblock sharing (toggle on), verify posts reappear
- [✓ 3.7.0] User-wide blacklist: block a user
- [✓ 3.7.0] Per-group blacklist: block a user from one group
- [✓ 3.7.0] E2E test: share → block → verify hidden → unblock → verify visible
- [✓ 3.8.1] Fix: "sharing group ready" printed twice (root cause: `authListen` re-fired on a redundant same-user token — now deduped in the SDK, D45 + demo idempotency + anti-test)

### Lane: retire-obsolete-e2e (Phase 1)
**Owns:** `e2e/tests/`

Pre-v3 e2e specs testing removed endpoints / ghost demo locations / broken
flows. Approach: **cut the broken bodies**; STUB (`test.skip` + comment) where
the feature still belongs in v3 (the stub documents the v3 intent + rewrite
path); DELETE where the feature is gone in v3. Coverage absorbed by the Phase
1 per-demo E2E lanes + `api/tests/test_v3_conformance.py` (I3 contract
enforcement).

**DELETED (whole spec — ghost location / removed endpoints):**
- [✓ 3.0.61] `demo-cr-flow.spec.ts` — ghost `sdk.localhost/demos/` location (demos moved to `marketing.localhost/docs/`) + old `/v3/documents/*` paths. Groups browser flows → `groups-demo` lane. (PR #648)
- [✓ 3.0.61] `consent-grant.spec.ts` — legacy `/signup` + removed `/certify`/`/posts`/`/services`. v3 consent → `auth-popup-roundtrip.spec.ts`. (PR #648)
- [✓ 3.0.61] `terms-revoke.spec.ts` — removed `/posts`/`/services`. v3 contract-revoke → `auth-popup-roundtrip.spec.ts`. (PR #648)

**GUTTED + STUBBED (feature belongs in v3 — rewrite later):**
- [✓ 3.0.61] `app-store.spec.ts` — "token handoff" (v2 `/certify` + `/{username}/posts`) → v3 app-contract + `/v3/create`. (PR #648)
- [✓ 3.0.61] `social-post-feed.spec.ts` — "signup+token+CRUD" (v2 `/certify`) + social render. (PR #648)
- [✓ 3.0.61] `studio-metering.spec.ts` — 3 metering tests (v2 star-record `credits_spent`) → v3 billing. "aggregate" DELETED (v2-only `/aggregate`, gone in ClickHouse). (PR #648)
- [✓ 3.0.61] `exporter.spec.ts` — marketing-api `/health` + `/import` pipeline (v2 node API). (PR #648)
- [✓ 3.0.61] `social-full.spec.ts` — posts/comments/reactions/DM (v2 `/{username}/*`) → v3 service-based CRUD + DM groups; social render. (PR #648)
- [✓ 3.0.61] `gauntlet.spec.ts` — 5 social-app render tests + 2 v3-API tests (join-approval, cross-user isolation I3) that were FAILING on correct v3 login → investigate (possible real bugs). (PR #648)

### Lane: hls (Phase 2)
**Owns:** `api/app/v3/endpoints/media.py`, `api/app/services/{transcode,hls}.py`, `marketing/marketing-ui/public/docs/media/` (demo player), `e2e/tests/hls.spec.ts`

HLS is v3 (D44) — "the one feature that makes this legit legit youtube vs
bs." Upload → in-process ffmpeg worker (dedicated daemon threads, NOT the
FastAPI request pool, bounded concurrency) → HLS renditions (360p/720p/
1080p) + thumbnails → MinIO → signed manifest + JWT on every segment
(bifurcated auth) → hls.js playback (Safari native). The KB is aligned and
is the spec: `knowledge/knowledge-base/web10-v3/media/`
(`transcoding-foundation.md` = the model, `transcoding.md` = the pipeline,
`minio-auth-bifurcated.md` = the auth split, `streaming.md` = the layers).
P2P stays v4 — do not build it here.

- [✓ 3.9.0] Transcode worker: video upload → ffmpeg (subprocess) → 360p/720p/1080p HLS renditions + thumbnails → MinIO → document updated with `transcoding_settings` (status: processing → done|failed; the doc is the status surface) — `api/app/services/transcode.py` + `POST /v3/media/transcode`
- [✓ 3.9.0] Signed manifest + segment serving: a read mints a 10-min JWT (sig) bound to (reader, doc, hls prefix); the manifest endpoint verifies the sig AND re-checks access (author or group membership) — the expiry is the re-check cadence. Master manifest synthesized from `transcoding_settings.variants` (doc is source of truth, manifest is a view); variant manifests rewrite every segment to a signed URL; segments stream from MinIO sig-only (no DB, traversal rejected) — `api/app/services/hls.py` + `GET /v3/media/hls/{manifest,variant,segment}`
- [✓ 3.9.0] Player in the media demo (the HLS unit test): upload → queue transcode → poll the doc → hls.js playback (Safari native fallback, vendored hls.js) — `marketing/marketing-ui/public/docs/media/`
- [✓ 3.9.0] E2E: API floor (upload → transcode → manifest → variant → segment bytes, MPEG-TS sync byte) + anti-tests (no sig / EXPIRED sig / cross-doc sig / non-member sig / traversal) + browser gauntlet (real demo: upload → "HLS ready" → hls.js manifest parsed → video duration > 0, log sequence) — `e2e/tests/hls.spec.ts` + 40 API unit tests in `api/tests/test_hls.py`
- [✓ 3.10.0] Aspect-ratio policy + social-style demo: renditions planned per-source (target by height, preserve source ratio, never upscale, even dims — the squashed-9:16 bug); thumbnails ratio-preserving; fps probed; two ffprobe bugs the new e2e caught (csv unpack, webm `format.duration = N/A` → stream fallback). Demo: upload-style toggle (Original / TikTok 9:16 / Instagram 4:5 / Square 1:1) + client-side reframe before upload (canvas cover-crop + MediaRecorder — node gets the finished file) + player spec (muted autoplay, quality dropdown via hls.js levels, speed, fullscreen, vertical layout). KB: new `video-experience.md`. E2E: vertical + landscape fixtures, ratio/no-upscale assertions, tiny-source test, style-toggle gauntlet — `api/app/services/transcode.py`, `marketing/marketing-ui/public/docs/media/`, `e2e/tests/hls.spec.ts`, `api/tests/test_hls.py`
- [✓ 3.9.1] web10-social adoption: moved to the `social-v3` lane (Phase 3) — the demo proves the pipeline, the app is the integration test
- [✓ 3.99.6] Anon HLS streaming (the marketing /trending "can't be played" bug): the manifest re-check's carrier-post rule used a pre-D58 literal `group_members` JOIN — the D58 backfill renamed the discover board's `anon` row to `anyone`, so every anon-minted sig 403'd at the manifest (the social app reads with a token = a real membership row, which is why only the marketing site broke). The re-check now uses the read path's own D58 gate (`can_read_carrier_post` → `can_read_group`: membership OR the group's `anyone`/`authenticated` grant) + the sig carries an `authenticated` flag (minted at read time) so the re-check evaluates the same principal classes the read did — it confirms, never escalates. Revocation still kills a copied sig within one 10-min TTL. `api/app/services/hls.py`, `api/app/v3/services/clickhouse.py`, `api/app/v3/endpoints/{documents,media,query}.py` + 5 new tests, 945 API green. KB `minio-auth-bifurcated.md` rule (3) aligned.

### Lane: social-v3 (Phase 3)
**Owns:** `marketing/web10-social/`

The social app is the integration test (Phase 3). It ran on two legacy
seams — `web10-npm@1.0.8` (v1 auth) and the hand-rolled `src/data/v3.ts`
(data) — and the convergence is on the SDK the demos already run on.
Both seams are now retired (3.11.0 auth, 3.12.0 data); what remains is
the hls.js player and the e2e gauntlet that proves it end-to-end.
 The decision bite gated the seam bites — docs first.

- [✓ 3.157.0] **Repost: the repeat icon opens the composer in repost mode on EVERY surface (unify onto the post model)** (operator, 23.09.2026, lightbox screenshot: "when i click the repost button it just toggles it 1 0 1 0, that is WRONG, it should open you up on the feed, or in a post composer or something somewhere else, with that post as the repost to let you make a post sharing that post, so the behavior is outright wrong, definitely adds logistics how to count reposts etc, but lets get that properly working"). **The bug (the documented open follow-up, `social/reposts.md` line 75):** 3.110.0 made a repost a **post** (not a reaction) and wired the **feed** repeat icon → the composer's repost mode (`repostingTo`), but **Discover / PostLightbox / ProfileFeed / Groups still call the legacy `toggleRepost`** (a `type:'repost'` reaction write) — so on those surfaces the repeat icon just flips a reaction doc (the "1 0 1 0" toggle) instead of opening a composer with the post as the repost. **And the count is still legacy:** those surfaces count reposts as `type:'repost'` reactions (`readReactions`/`countReactions`), so even a post-based repost wouldn't move their tally. **The fix:** (1) **write** — route every surface's repeat icon to the composer's repost mode (the feed's `onRepost` → `repostingTo` pattern, lifted to a shared seam) instead of `toggleRepost`; the composer's `createRepost` is the single write. (2) **count** — count reposts as `posts` whose `body.repost_of` points at the post (the feed query's I3-scoped join is the reference), not as `type:'repost'` reactions; the "I reposted this" fill comes from the reader's own repost post (the `readFeedReactions` own-post read is the reference). (3) **logistics** — the repost count is the number of `repost_of` posts (one per reposter, self-healed); a repost is a real post in the reposter's followers group so it resurfaces in their feed as a "reposted" card. **Gates:** the composer's repost mode must be reachable from Discover / Lightbox / Profile / Groups (a shared `onRepost` seam or a global repost state, not four copies); the count + fill re-derive on a post-based write; the legacy `type:'repost'` reaction stays a read-only fallback for old data. **Acceptance bar:** tapping the repeat icon on ANY surface opens the composer with the post as the repost (context block + "Add a comment…"), never a silent toggle; submitting creates a repost post that shows in the reposter's feed + bumps the original's count; the count is stable across refresh (no 1 0 1 0). Lane: `social-v3`.

- [~] **The discover / trending video wall becomes the hero — chrome recedes, chunky thumbnails, posts-first search** (operator pass, 23.09.2026: "this is taking up so much space on the discover page, this post maker" + "sooo teeny compared to youtube … three of them fill the viewport horizontally, just GIGANTIC. we need this to be chunky." + "this is hideous, we have search on the top bar we dont need a search here! the moment you search … it should show you the discover posts being searched, and then chunky icon to switch it to people and groups search" + "the marketings discover, all that shit on the top is not needed" + "show dont tell, that is the objective for the video cards to be that big" + "dont have to show that top 10 thing on the home page on the marketing, on the hot gossip it is good" + "that is just too small too hard to see, want to keep the youtube stuff big, the explore also isnt an explore, it is the People … make it a person Icon"). The Home wall is full-width (social 3-across desktop, marketing 3-across full-width); the social Discover drops its redundant header search field (search is the top bar; `?q=` still filters) + the "Discover" header (the chunky sticky **Posts | People** tab row is the primary nav — "Explore" renamed **People** with a person icon) + the composer is `compact` (single-line until focused); the marketing `/trending` cuts its giant hero to a slim sticky control row + the Top 10 sidebar is Hot Gossip only; the top-bar `GlobalSearch` is posts-first with a chunky Posts | People & Groups mode toggle; the shared `HomeCard` title is `text-base`. `discoverScreen` / `globalSearch` / `socialScreens` / `follow` re-pinned. 906 social + 271 marketing green, both `tsc` clean.

- [~] **The Home view — the Explorer + marketing /trending open on a YouTube-style video wall (the default), "Video" → "Home", "Hot Gossip" second** (operator pass, 23.09.2026: "we need to be able to go pound for pound with these other guys, more of the video is filling the screen for them" + "video view should be first hot gossip second, to compete. video should be renamed home view. hot gossip is like our version of threads" + "youtube view is preferable less brainrot, the videos all have a good title, thumbnail, and have the attribution of who put them up. can show the post text if it is short enough, otherwise … truncate the text to a certain char limit"). A new shared `HomeCard` (`@web10/discover`) — a 16:9 thumbnail (fills the frame), a truncated title (80-char limit + ellipsis), and the author's attribution. The view is renamed + made the **default** in both apps (`?view=`: `home` bare / `grid` = Hot Gossip; legacy `?view=youtube` → `home`), the Home wall is a responsive 1/2/3/4-col grid that fills the desktop (the Explorer widens to `max-w-5xl` in the Home view), and the social card navigates to the post's permalink (`/u/:username/p/:postId` via a new `navigate-post` event). `discoverScreen.test.tsx` + `Trending.test.tsx` re-pinned (Home default; board tests on `?view=grid`; the Home card's thumbnail/title/attribution). 906 web10-social + 272 marketing-ui green, both `tsc` clean. KB `discover-card.md`.

- [✓ 3.145.0] **Messages surface is chat-only — Mail + CRM views deleted** (operator: "can we hide the mail and the crm thing from the messages part, it is just distracting" → "can actually just delete those views!"). D79. `DmsScreen.tsx` loses the Chat/Mail/CRM toggle (all four render sites), the `activeView`/`?view=` state machine + `switchView`; the screen renders list / empty state / thread directly and stale `?view=mail|crm` links land on chat. `MailView.tsx` + `CrmView.tsx` deleted. Data layer untouched (`contacts.ts`, DM `subject`, `dms`/`contacts` collections — the bugbot D70 flow still writes `subject`). `socialScreens.test.tsx` re-pinned (6 toggle/mail/crm cases + 2 describe blocks removed, no-toggle regression pin added); harness `capture.mjs` default views now chat + settings (ready `dm-new-message-btn`, screen-load `dms-screen`), README/`vite.config.ts`/`mock-data.ts` comments updated, stale `mail-*.png`/`crm-*.png` deleted. 905 web10-social tests green, `tsc` clean, harness capture green at desktop + 375.

- [✓ 3.149.0] **Profiles are public by default — the missing `anyone` grant on the followers group** (operator, Followers-tab screenshot: "followers pfps arent rendering, it is because i have to follow to see pfp?" → "they should be public unless made private, why make a profile then"). The followers group (where a user's `profile` face doc lives) granted `owner`→`*` + `member`→`posts:readAll` but **no `anyone` grant**, so the face was unreadable to a stranger/anon → the Followers/Following tabs + "someone else's profile" rendered a fallback initial, not the pfp. **Client-side fix (D60, zero node surface):** publicness = the node's generic role-grant primitive — `FOLLOWER_ROLES` gains a `reader` role (`profile:readAll`, face-only, not `posts`); `ensureFollowers` creates the group public-by-default (`anyone`→`reader`) + heals a pre-existing private group on mount (runs via the session-health recovery in `access.ts`); `setProfilePublic` (new, `profile.ts`) toggles the row via `addGroupMember`/`removeGroupMember`. Public community groups were already fine (they add the `anyone`→`reader` row for the `web10-social-group-identity` face). Tests: `follows.test.ts` re-pinned (create + heal + no-op), `profile.test.ts` +4 (`setProfilePublic`), `contacts.test.ts` re-pinned. 911 web10-social green, `tsc` clean. **Open (deferred, D62):** the private toggle needs a persisted `visibility` preference so the mount-heal doesn't re-publicize an opted-private profile (until then the heal is a one-way ratchet to public). D58 amendment in `decisions.md`; the D60 guardrail is now a first-class AGENTS.md section.

- [✓ 3.144.0] **Group chats in Messages read as groups — the explicit "Group" badge** (operator, screenshot of the Messages list: "on the messages tab, this should be clear it is a group message!"). A group-chat conversation row looked like a DM row (the only tell was the tiny `Users` glyph on the avatar corner), and the thread header showed name + "N members" with nothing that said *group*. `DmsScreen.tsx`: the group-chat **list row** now carries an explicit "Group" badge (shared `Badge`, `variant="brand"` + `Users` glyph, next to the name — DM rows never render it) and the **group thread header** carries the same badge next to the name. 918 web10-social green (+1 new DM-negative test, 2 re-pinned), `tsc` clean. KB `social/group-chat.md` (list + header badge). Screenshots `chat-{desktop,375}` + new `chat-group-thread-{desktop,375}` (harness `chat-group-thread` screen entry).

- [✓ 3.137.0] **Messaging from a profile goes straight to the DM thread** (operator: "when clicking to message someone from their profile, it should fill in the provider, it is clear their provider from their profile, and it should just show the dm view … why is there this weird message view should go straight to dm" + "it could take you straight here but with an empty chat … if the first time messaging them"). The profile's Message button dropped the recipient's provider on the `/messages?to=` deep link, so DmsScreen fell back to the "New message" compose picker (username + "Provider (optional)" + message box) — redundant when the profile already knows the full identity. `UserProfileScreen.tsx` Message button → `/messages?to={username}&provider={provider}`; the DmsScreen `?to=` handler, when a provider is present, derives the canonical conversation key and navigates straight to `/messages/{conv}` (the DM thread view — empty chat for a first message), no compose/picker. Bare `?to=` keeps the old fallback. 894 web10-social green (incl. the deep-link regression), `tsc` clean.

- [✓ 3.116.1] **Groups carry a platform-level `tags` column — web10-social classifies its groups by tag (D78)** (operator, after the "followers groups showing in My Groups" bug: "is this a fundamental protocol flaw? should groups themselves have tags? … this would be an issue for any app architecture not just social media. we could add tags to the groups in clickhouse, make a clickhouse upgrade right?"). **KB-first (`groups/overview.md` "Group Tags" + D78):** `group_contracts` gains a **`tags` column** (`Array(String)`, default `[]`) — the group-primitive analog of `documents.tags`; the platform stores + matches them (`has(tags, …)`), the app decides what they mean. The group-side of the existing doc-tag primitive; the `discoverable` flag is the precedent. It makes group *selection* a server-side tag read instead of a client-side id-pattern blocklist (which leaked followed users' followers groups + group chats). web10-social scheme (namespaced): `web10-social-group` / `-followers` / `-dm` / `-chat`. The group **tag** is the *selection* classifier; the face **`kind`** (D77) is a *render* hint. **Built (cross-lane: api + sdk + web10-social):** (1) ClickHouse — `group_contracts.tags` (boot ALTER); `create_group`/`update_group`/`get_group` carry tags; `get_user_groups(member, tags)` server-side `has(gc.tags, …)` filter. (2) API — `CreateGroup`/`UpdateGroup`/`ListMyGroups` models + create/update/list endpoints. (3) SDK — `V3Group.tags`; `createGroup`/`updateGroup`/`getMyGroups({ tags })`. (4) web10-social — `GROUP_TAG` constants; tag at creation (followers/dm/community/chat); `getMyCommunityGroups` = `getMyGroups({ tags: ['web10-social-group'] })`, `getMyGroupChats` = `getMyGroups({ tags: ['web10-social-chat'] })` — server-side tag reads, no client-side pattern matching (supersedes the 3.115.1 `isFollowersGroup` fix for the Groups surface). (5) Migration — one-time, sentinel-gated `_migrate_group_tags_backfill` seeds legacy untagged groups from their id shape. **Tests:** API 961 green (create/update/list-by-tag + the migration), SDK 125 green (tag threading), web10-social 818 green + `tsc` clean (select-by-tag + create tagging). Lane: `social-v3` + platform (api/sdk).

- [✓ 3.115.0] **Group chat — N-person messaging in the Messages surface** (operator: "do we have group messaging or no? group would have a name some kind of chat name. group chat is pretty major thing i feel to just basic cover then our messaging is straight up golden."). **KB-first (`social/group-chat.md`, D77):** a group chat is an N-member group (the same primitive a DM is) with **`kind:'chat'` on its face** (the `web10-social-group-identity` doc), a user-chosen name, a member list (bare-username, invite_only), and per-sender messages (`posts` in the group, each carrying `sender_username`). Zero node changes. **Slice 1 (CRUD-only real-time):** `src/data/groupChat.ts` (`createGroupChat` / `getMyGroupChats` / `readGroupChatFace` / `readGroupChatMessages` / `sendGroupChatMessage`), `GroupIdentity.kind`, the Messages list shows DMs + group chats (unified, recency-sorted), a "New group" create flow (name + add members), the group thread view (name header + member count + per-sender bubbles), deep-linkable at `/messages/group/{groupId}`. The P2P group nudge + the `getMyCommunityGroups` chat filter are follow-ups. 8 new tests (`groupChat.test.ts` 6 + `socialScreens.test.tsx` 2); 802 social green, `tsc` clean.
- [✓ 3.114.0] DM compose profile preview — type a username, see the account before you message (operator: "messaging ui should show image of the username like if you type username pull up the actual acct before you message … flagship social media makes it clear as you type with debouncing searches the profile that you would message, so you know it is a user that exists" + "show some kind of card with the profile picture or something for the user so you know it is the intended recipient based on their profile"). The "New message" composer's type-a-username flow now does a debounced (400ms) lookup of the recipient's public face as you type, rendered as a confirmation card (avatar + display name + @handle + bio + a success check) under the username input — you see the real account (and its profile picture) before you hit send. **Built:** (1) data — `lookupUserProfile(username, provider?)` + a new `UserFace` type (`src/data/profile.ts`): reads the target's profile doc (name/bio/avatar+banner refs) via the now-provider-aware `readUserProfile` + resolves the refs to presigned `public_media` URLs (the profile screen's cross-user path); `null` when the account has no readable profile (unknown username or no profile set) — the preview degrades, never blocks the send. (2) UI — `DmsScreen.tsx` `ContactPicker` compose mode: a `ProfilePreview` card (skeleton while the debounce is pending, found card, neutral "no profile found — you can still message them" state) + a sequence counter that drops stale in-flight results as you keep typing; all tokens/states, `data-testid` hooks. 7 new tests (`lookupUserProfile.test.ts` 5 + `socialScreens.test.tsx` 2); screenshot harness `mock-data.ts` gains a `lookupUserProfile` stub + `capture.mjs` a `--fill SEL VALUE` flag. 794 web10-social green, `tsc --noEmit` clean, harness capture green at desktop + 375.
- [✓ 3.113.0] Comments + replies can carry photos — an attach control in the comment box + a compact photo grid under each comment (operator: "Cool if comments / replies can have photos in them. twitter does that right?"). **KB-first (`social/comments.md` gains "Photos in comments"):** a comment's photos ride in `body.media_refs` (media doc_ids, the post's own convention) — **zero node changes** (the read path's `resolve_media_urls_in_docs` already resolves `media_refs` in every doc, keyed on the doc's author, gated by the same group read — I3 holds). **Built:** shared `CommentThread` gains an `uploadMedia` seam → a photo-attach control (image icon, `accept="image/*"`, multiple, capped at 4) + a removable preview tray, uploads on send (the `PostComposer` idiom), a photo-only comment is sendable, and `CommentNodeRow` renders the photos as a compact grid below the text (1 → full-width capped, 2–4 → 2-col square grid, `object-cover`, lazy, broken refs dropped); absent the seam (remote marketing mode) it degrades to text-only. web10-social data: `CommentRecord.media_refs` (mapped in `fromV3DocToComment`), `createComment` writes `media_refs`, `createThreadComment` threads `mediaRefs`, `readThreadComments`/`readThreadReplies` map resolved refs to `MediaItem[]`, new `uploadCommentPhoto` (images only, downscale + thumbnail + `public_media`); the social wrapper injects `uploadMedia={uploadCommentPhoto}`. 10 new tests (`data/comments.test.ts` 5, `commentThread.test.tsx` 5) + `commentAnchor.test.tsx` re-cut to the Proxy lucide mock. 797 web10-social + 264 marketing-ui green, both `tsc` clean. **Open:** video in a comment (transcode + a player in a compact row) is a separate build.
- [✓ 3.111.0] The video player pass — the feed's portrait video fits the card again, the marketing /trending controls actually show, and Shorts can be heard (operator, three surfaces: "feed the video player still looks out of date...." + "on the marketing page not on social media looks great and consistent too, but the controls straight up dont show, i can click it to pause and play but that is it on marketing discover, should be the same where you can see the controls on the vid....." + "tik tok view looks great but muted default, should he a mute unmute speaker icon, no way to hear the video"). Three roots, one shared package (`marketing/shared/discover/`), one disease (the package is only half-integrated into its two apps): (1) the feed's hls path dropped `maxHeight` (3.105.0's full-bleed redesign threaded `maxWidth` to `HlsVideoPlayer` but not `maxHeight`) — a transcoded 9:16 clip (the default for every upload) reserved a full-width box ~1.78× the card tall, rack buried below the fold; `HlsVideoPlayer` now accepts `maxHeight` (portrait capped + centered in a black letterbox, landscape no-op) and `VideoPlayer` threads it to the hls path (the groups feed's dropped prop fixed for free). (2) Tailwind v4's automatic content detection never scans the shared package (outside each app's directory, reached only via the Vite alias) — classes appearing ONLY in the package (the rack's `inset-x-0`/`bottom-0` positioning, the gradient scrim, the RackMenu popover, the volume slider) were missing from the builds: the marketing /trending rack rendered unpositioned + invisible ("i can click it to pause and play but that is it"), the social rack landed but unstyled (the "out of date" look). `@source "../../shared/discover/src";` in each app's entry CSS — verified by rebuild + grep (all rack classes in both shipped CSSes). (3) Shorts muted-by-default with no escape hatch — screen-level mute state in `ShortsScreen` (muted by default, a module-level flag so the choice survives leaving + returning within the session) + a speaker icon on the action rail (top item, above the heart, `VolumeX` ↔ `Volume2`, `stopPropagation`) + a `muted` seam on `<VideoPlayer immersive>` (default `true`; `ImmersiveHls` + `InlineVideo` apply it on mount + on every flip). `hlsVideoPlayer.test.tsx` +2, `videoPlayer.test.tsx` re-pinned +1, `shortsScreen.test.tsx` re-pinned +1; harness: the feed seed gains a transcoded portrait post + `readFeedPage` serves pre-resolved media refs + a fake `window.Hls` (offline hls path). 791 web10-social + 264 marketing-ui green, both `tsc` + `vite build` clean. Screenshots `feed-{desktop,375}.png` (the "looks great" reference) + `shorts-375.png` (the speaker icon) + `discover-desktop.png` (no regression). KB `video-player.md` (the `maxHeight` paragraph + the `muted` seam + the `@source` note) + `shorts.md` (playback spec + decision #7). Work order: `knowledge/strategy/video-player-fixes.md`.
- [✓ 3.110.0] Reposts are real posts — the composer's repost mode + the feed's "reposted" card (operator: "reposts dont work when you hit repost, it should let you add some kind of comment, where it shows up in your feed as reposted … needs a special ui thing when you repost, in the post composer, where it has the post that you are reposting, then you are adding your text i.e. why you are reposting it kind of a thing. you get it, what a repost actually is."). **Model change (KB-first, `social/reposts.md` rewritten):** a repost is **a post, not a reaction** (the 3.104.0 `type:'repost'` reaction is retired as the primary mechanism). A repost is a real `posts` doc in the reposter's followers group with `repost_of` (the original's doc_id) + `text` (the optional comment) — it resurfaces in the reposter's followers' feed as a "reposted" card with the original embedded. **Built:** (1) data — `repost_of` on `PostRecord` + `createPost` body + `createRepost(original, comment)` (`posts.ts`); the D73 feed query counts reposts as `posts` with `repost_of = p.doc_id` (a new I3-scoped join) instead of `type='repost'` reactions; `readFeedReactions` marks `reposted` from the reader's own repost posts (legacy `type:'repost'` reactions still count for old data). (2) composer — `PostComposer` `repostingTo` mode: a `RepostContext` block (original author + text + first media + cancel X), "Add a comment…" placeholder, "Repost" button, plain-repost postable, submit → `createRepost` (never uploads the user's own media). (3) feed — `FeedRoute` (App) holds `repostingTo`; the repeat icon reports the tapped post up via `onRepost` and opens the composer in repost mode; `PostCard` renders a "reposted" badge + the reposter's comment (the quote) + `RepostedEmbed` (the original by doc_id via `readPostById`, I3-scoped, degrades to "Original post unavailable"). 17 new tests (`repostComposer.test.tsx` 8, `repostFeed.test.tsx` 4, `posts.test.ts` 3, `feed.test.ts` 2); 766 web10-social green, `tsc --noEmit` clean. **Open:** unify Discover/Lightbox/Profile/Groups (still the legacy reaction boost signal) onto the post model + the author nudge.
- [✓ 3.109.0] Clicking a comment author's username takes you to their profile (operator: "when i click a username on a comment, it should bring me to their profile"). A comment's author was a plain `<span>` (the shared `CommentThread` rendered `{c.author_username || 'you'}` as inert text) — every other author surface (post header, discover card, notification rows) is already a tappable profile link, so the comment author was the one dead end. **One shared seam, both apps:** (1) shared `CommentThread` gains optional `onAuthorClick?: (username, provider?) => void` — when wired the author is a `<button>` (`data-testid="comment-author-{id}"`, `aria-label`, `stopPropagation`); absent (remote/anon) it stays a plain span (an anon visitor can't navigate in-app — same rule as the card's own author). (2) shared `PostActions` threads `onAuthorClick` to its mounted `CommentThread`; shared `DiscoverCard` gains a distinct `onCommentAuthorClick` (the card's own author already had `onAuthorClick`; the comment author is a *different* user) and passes it to its `PostActions`. (3) web10-social wiring — every comment surface navigates to `/u/:username`: `PostCard` (feed + the profile's feed lens via `ProfileFeed`), `PostLightbox` (gains `useNavigate`), `GroupPostCard` (gains `useNavigate`), `ShortsScreen` (gains `useNavigate`), and the Discover grid + video-view cards (via `navigateToUserProfile`). The social `CommentThread` wrapper exposes the new prop. `postActions.test.tsx` +2 (the author is a button reporting `onAuthorClick('alice', undefined)`; absent the handler it's plain text, not a dead button); `postLightboxDelete`/`postLightboxVideo` re-pinned to wrap `PostLightbox` in a `MemoryRouter` (it now calls `useNavigate`). 750 web10-social + 263 marketing-ui green, both `tsc` clean.
- [✓ 3.108.0] The feed + discover preset rack is four plain-English badges — Most recent / Most liked / Most commented / Balanced (operator, rack screenshot: "i like this but should have most liked most commented, most recent as those badges, and balanced so 4 of those to pick from!"). The three chips (Newest / Most loved · all time / Balanced) are re-cut into the four signals a user actually asks for. **The presets (`powerMean.ts`, both apps — the verbatim copies stay in sync):** `newest` → `most-recent` (recency-only, chronological — the feed default, same state), `most-loved` → `most-liked` (likes-only, all-time — same state), a new `most-commented` (comments-only, all-time: `recency 0, likes 0, comments 5`), and `balanced` (the mixed default, unchanged). The preset *ids* are renamed to match the labels; `?knobs=` deep links + the persisted `feedKnobs` store the 5-field state, not the id, so existing links + saved tunings still resolve (`presetIdForState` matches on state, not id). **No node change:** comments-only ranking already works — `knobStateToSort` sends `{recency:0, likes:0, comments:1}` and the node's `_power_mean_score` / `_power_mean_score_sql` already weight comments (the recency-only shortcut fires only when both likes AND comments are 0). **Screens:** `FeedScreen.tsx` (feed default is now `getPreset('most-recent')`) + `DiscoverScreen.tsx` (the `activePreset === 'most-recent'` chronological guard) + both `KnobRack.tsx` render the four chips. **Tests:** `powerMean.test.ts` (both apps — 4 presets, the most-commented state, distinct mix codes), `feedKnobs.test.tsx` + `discoverScreen.test.tsx` (the four chips + renamed testids + preset-switch re-reads), `Trending.test.tsx` (four chips + renamed preset clicks). 748 web10-social + 264 marketing-ui tests green, both `tsc` clean.
- [✓ 3.107.0] **Comments page the Facebook way — `ref_value` is the parent, cursor paging at both levels, "view more comments" / "view more replies"** (operator, after 3.106.0: "is this the right design though? if there are 10000 comments? will need to page" → "what would facebook do" → "do it right here, how facebook would do it!"). The 3.106.0 single-read design breaks at 10k — a hot post can't be read in one call; the thread must page, and paging needs the server to pull "page N of X" for the post's top-level comments AND one comment's replies. `ref_value` is the column the server filters on, so it must point at the thing being paged. **Model change (KB-first, `social/comments.md` rewritten):** a comment's `ref_value` is now its PARENT — the post for top-level, the parent comment for a reply (was: always the post). Cost: the post's TOTAL count is no longer one `GROUP BY ref_value` (top-level stays exact; total = top-level + summed replies, or a maintained counter at scale — the count is decoupled from the read). **Built:** node `read_docs_by_ref` keyset cursor paging (`ORDER BY created_at, doc_id` + `cursor` "created_at|doc_id"; `ReadDocuments` gains `cursor`+`order`; the safe-query engine already passes ORDER BY / the keyset WHERE through); SDK `read()` gains `cursor`+`order`; data layer `readComments`/`readReplies` return a `CommentPage`, `readReplies` keys on the parent, new `countRepliesByComment`, `createComment` writes `ref_value = parent_id` for a reply, new `readThreadComments` (page + likes + replyCounts) + `readThreadReplies`; the shared thread rewritten to the paged model (opens with the first top-level page, pre-fetches each comment's first reply page driven by `replyCounts`, renders the two hyperlink pagers "View more comments" + "View more replies"); the seam is paged (`CommentPageResult`), the marketing public-ledger reader returns the whole flat conversation as one page + the thread groups on `parent_id` (both apps, one component). API +3, SDK +2, `data/comments.test.ts` + `commentThread.test.tsx` re-cut to the paged model. 948 API + 121 SDK + 759 web10-social + 261 marketing-ui green, all `tsc`/`ruff` clean.
- [✓ 3.106.0] **Comments are threaded — replies nest under their parent, and every comment has a like** (operator: "i want the comments app wide to have thread like replies, and all have likes, at the comment level, not seeing that in the social app"). The shared thread (`marketing/shared/discover/`) rendered a flat list — no nesting, no per-comment like; the data layer already carried the shape (`body.parent_id`, reactions keyed to any `ref_value`) but the reply read filtered on the comment id while the write ref'd the post (latent mismatch). **The model (KB-first, `social/comments.md`):** a comment's `ref_value` is ALWAYS the post (top-level *and* reply) so the post's comment count stays true + one read returns the whole conversation; a reply carries `body.parent_id` and the thread groups by it client-side (the convention the YouTube importer already writes). A comment like is a `reactions` doc (`type:'like'`, `target_service:'comments'`, `ref_value` = the comment id) — same shape as a post like, zero node changes. **Built:** data layer (`readReplies` fixed; new `readThreadComments` = whole conversation + per-comment `likeCount`/`likedByMe`, independently degrading; new `createThreadComment` with `parentId`); `setReaction`/`toggleReactionKind` gain `targetService`; the shared thread rewritten to a recursive tree (`buildCommentTree`/`countCommentTree`) with per-comment like + Reply (one retargetable compose box, not a per-comment input); `onToggleCommentLike` seam on the thread + `<PostActions>`; the social wrappers inject the seams + wire the like to `toggleReactionKind(id,'like',groups,'comments')`. `commentThread.test.tsx` (new, 9) + `data/comments.test.ts` re-pinned + `postActions.test.tsx` re-pinned. 757 web10-social + 261 marketing-ui green, both `tsc` clean. KB `social/comments.md` + README index.
- [✓ 3.105.3] The social Discover caps a portrait (9:16) video too — consistent with the marketing /trending card, not full-bleed on desktop (operator, after 3.105.2 capped the lightbox + the marketing card but left the social Discover full-bleed: "dont let the social discover full bleed please, too big on desktop, make it consistent thanks"). The social Discover's two shared-card usages (`DiscoverScreen.tsx` — the board card `discover-card` + the "Video" view card `discover-youtube-card`) now pass `videoMaxWidth="min(50vh, 100%)"`, the same cap the marketing `TrendingCard` sets (3.105.2). A portrait clip (`width < height`) is capped to a square-ish frame + centered in a black letterbox (a full-width 9:16 box is ~1.78× the card tall — too big on desktop, and it buries the control rack at its bottom); landscape is unaffected. The following Feed stays full-bleed. `discoverScreen.test.tsx` +1 (a portrait clip in the discover card is capped to `min(50vh, 100%)` + centered `mx-auto`, the source ratio still reserved). 749 web10-social green, `tsc` clean. KB `video-player.md` (the `maxWidth` paragraph + the surface map: Discover grid + the "Video" view are portrait-capped).
- [✓ 3.105.2] The 9:16 video fits its frame again (lightbox + trending), the rack's two dials are distinct, and a mid-playback quality switch no longer stalls (operator, four screenshots across the social lightbox + the marketing `/trending`: "i actually liked when the 9/16 videos were put in a square frame, in the insta view. currently when i see a video from someones profile that is tick tok dimensions, it is not fitting right" + "tick tock 9/16 vid is forced into a square and controls arent visible" + "two meter icons, doesnt make sense for resolution control, also the resolution changing seems straight broken if i change resolution in the middle of playing" + "the marketing sites trending still doesnt have the video controls … in the social app the controls are there though! just the marketing app"). Four roots, one shared player (`marketing/shared/discover/`). (1) The lightbox's portrait frame — 3.105.0's full-bleed frame is right for a single-column feed but the lightbox is a bounded modal: a 9:16 clip reserves a full-width box ~1.78× the viewport tall, clipped by the `max-h-[88vh]` modal, the control rack stranded off-screen at its bottom. (2) The trending card's portrait frame — the same full-bleed 9:16 box in the marketing card wall dwarfs the card and buries the rack (the social single-column feed reads the tall box as "the video"; the card wall doesn't — hence "controls in the social app, not the marketing app"). (3) Two identical dials — the speed *and* quality `RackMenu` triggers both used `Gauge`, so the rack showed two identical meters. (4) The quality switch stalled playback — picking a level set `hls.currentLevel` but a mid-playback switch re-buffers; if the current buffer drained before the new level's first segment landed, the `<video>` stalled and never resumed. **Fixes:** (1)+(2) a new `maxWidth` layout prop on `<VideoPlayer>` (threaded to `HlsVideoPlayer` + `InlineVideo`): a bounded surface passes it for a portrait clip (`width < height`), the player caps the frame to that width + centers it (`mx-auto`) in a full-width black letterbox — the whole clip + rack stay in view (width-based: for a portrait clip, capping the width forces the taller height to shrink in; landscape never capped). `PostLightbox` `LightboxVideo` + the marketing `TrendingCard` set `videoMaxWidth="min(50vh, 100%)"`; the social single-column Discover/Feed leave it unset (full-bleed, unchanged). The pointer-hold handlers move to the player's outer box (not the capped frame) so reaching across the letterbox never hides the rack mid-reach. (3) distinct glyphs — speed = `Gauge`, quality = `MonitorPlay`; `RackMenu` gains an `icon` prop. (4) the quality effect nudges playback after the level switch (resume if playing + re-arm `hls.startLoad()` if `autoStartLoad` is disengaged); `HlsInstance` type gains `autoStartLoad`/`startLoad`. The marketing `feedPostToMediaItems` now carries the source `width`/`height` (it dropped them, so the portrait check never fired). **Tests:** `hlsVideoPlayer.test.tsx` +4 (distinct icons; mid-playback quality switch resumes; `maxWidth` caps + centers; no `maxWidth` → full-width) + new `postLightboxVideo.test.tsx` (2) + `Trending.test.tsx` +2. 748 social + 263 marketing-ui green, both `tsc` clean. KB `video-player.md` (the `maxWidth` portrait-cap paragraph, the distinct-glyph + quality-switch notes, the surface map).
- [✓ 3.105.1] Feed like / dislike / comment counts load again (operator, feed screenshot of a liked post with a red heart but no number: "like counts didnt even load this time.... weird"). The D73 feed query (`buildFeedQuery`, `src/data/feed.ts`) counts engagement with in-query `LEFT JOIN`s over `reactions` + `comments`, but runs with `groups: feedGroups` (followers groups — the feed is followers-minus-discover). Reactions and comments are *written* to the discover group (`createReaction` / `createComment` default), and the safe-query engine scopes every service CTE to the caller's groups — so the joins saw no data and returned 0. The heart filled (the `readFeedReactions` "did I like this?" read was fixed in 3.101.0 to read the discover group) but the count next to it was a dead 0. Fix: a new `readFeedEngagementCounts(postIds, feedGroups)` helper runs two server-side `GROUP BY ref_value` queries (reactions split by `countIf(type='like'/'dislike')` + comments) scoped to `[...feedGroups, discover]` (the same union `readFeedReactions` reads); `readFeedPage` merges the real tallies over the page's posts (the in-query joins stay for the ranked-score path). Exact, no cap. `data/feed.test.ts` re-pinned (feed read = feed query + two count queries scoped to the discover group; counts come from the count queries, not the feed row) + a regression for the second latent bug in the same 3.101.0 change: the power-mean score still referenced the pre-split `eng.reaction_count` column, so any tuned preset with a likes weight emitted a query referencing a non-existent column → ClickHouse error → the whole feed failed to load; the score now uses the total (`like_count + dislike_count`). 690 web10-social tests green, `tsc --noEmit` clean.
- [✓ 3.105.0] Discover video: full-bleed frame + a flagship-grade control rack, and every video (hls *and* file) shows the same controls on both apps (operator, three screenshots: marketing `/trending` video "just full size and no video controls"; social Discover "fitted to a square frame, and has video controls. i would prefer if consistent, both have video controls"; a zoom of the rack "these video controls just look kinda gross, lopsided or whatever, this doesnt look so creme de la creme flagship. make the best design decisions here"). One shared component, both apps (`marketing/shared/discover/`). (1) The control rack redesigned (`HlsVideoPlayer.tsx`): the old two-row rack had a gross row of text-labeled native `<select>`s ("speed [1x ▾] quality [Auto ▾]") — now a single clean row (scrubber on top; play/pause · mute · volume · time left, speed · quality · fullscreen right), no text labels, even spacing, token colors; speed + quality are a new `RackMenu` (icon button + designed popover, closes on pick/outside/Esc) instead of the native `<select>`. (2) The frame is full-bleed: the old `max-w-[280px]` phone column + `bg-elevated` letterbox gutters are gone — the frame reserves the source ratio and fills the card width (`object-contain` on `bg-black`), matching the marketing "full size" look. (3) The file path (`VideoPlayer.tsx` `InlineVideo`) now renders the same overlaid rack while playing (auto-hide + pointer-hold) + is full-bleed — so "both have video controls" is structurally true for every video, transcoded or not. `DiscoverVideo` drops the `maxHeight="60vh"` cap. `hlsVideoPlayer.test.tsx` re-pinned (RackMenu popover drives quality/speed; the vertical case asserts the full-bleed frame, no phone column). 689 social + 259 marketing-ui green, both `tsc` + `vite build` clean. KB `video-player.md` (surface map + trace + component section).
- [✓ 3.104.0] Reposts are real — the repeat icon is a tappable, countable signal on every post surface (operator: "the repost function is straight up not working, i dont think we planned it or anything in the kb how reposts would work or visually show"). **It was never built:** the "repost" write was a no-op (`recordRepost` in `wapi.ts`), the feed/lightbox "Share" called it (recorded nothing), the count was hardcoded `0`, the Discover repeat icon was a dead `<span>`, and the one `recordRepost` test tested the mock, not the code. **KB-first (`social/reposts.md`):** a repost is a `reactions` doc with `type: 'repost'` + `ref_value` = the target post's doc_id (same shape as a like, D62 — zero node changes). **Independent of like/dislike** (a user can like AND repost; `toggleRepost` matches `type === 'repost'` alone), **username-alone ownership** (3.87.2 class), **self-heals** duplicates. **Not a boost** (no resurfacing — a follow-up) and **not the share/link action** (Share is now a pure `navigator.share`/clipboard; the two were conflated). **Built:** `toggleRepost` (data); the feed query counts `type='repost'` + `readFeedReactions` returns `reposted`; `PostActions` gains a fourth axis `repost` (`interactive`/`display`/`none`, default none) + `reposted`/`repostCount`/`onToggleRepost` (a `Repeat2` button, brand-filled when active); `DiscoverCard` moves the repost from the dead `trailing` span to the axis; Feed / Discover / PostLightbox / ProfileFeed / Groups all wire it `interactive` (optimistic toggle + rollback). Discover's engagement loop now counts each reaction type separately (latent bug: reposts were counted as likes via the `else` branch). The no-op `recordRepost` is deleted. `reactions.test.ts` (the fake `recordRepost` test → a real `toggleRepost` suite), `postActions.test.tsx` +6, `feed.test.ts` `readFeedReactions` +`reposted`, `hlsFeed.test.tsx` mocks re-pinned. 700 web10-social + 259 marketing-ui green, both `tsc` clean; harness re-captured feed/discover/profile-feed/groups-detail. **Follow-ups (open, in `reposts.md`):** the author nudge, the resurface/boost, the ranking weight (the power-mean's `reposts` signal is still weighted 0).
- [✓ 3.103.0] People screen — find profiles by mutual follows, the groups-tab card shape for people (operator: "this groups is a plain poor representation of groups… would be cool if their was also a way to view peoples profiles, like a tab to find profiles, showing a preview of their prof pic and banner like this groups tab. based on mutual follows or something like that"). New `/people` destination (desktop sidebar + mobile More sheet, not a bottom-bar tab): a card list in the groups-tab shape — banner strip (real banner or accent gradient) with the profile pic overlapping, name + @handle, "N mutuals · M followers" meta, Follow/Following button. **Mutuals = "how much in common":** `mutuals(X) = |followers(X) ∩ my-following|` (the "N mutuals" signal); no node change needed (follows are group membership). Sort tabs: Mutuals (default) / Popular / A–Z, deep-linkable via `?sort=`. Data layer `src/data/people.ts` (`fetchPeople`, pure `computeMutuals` + `sortPeople`); candidate pool = discover-board authors + community-group members, deduped, self excluded, capped. `people.test.ts` (17) + `peopleScreen.test.tsx` (14); 720 social green, `tsc` clean. Screenshots `people-{desktop,375}.png`. **Open:** the groups-tab polish the operator flagged is a separate lane item.
- [✓ 3.112.0] **Groups tab polish — flagship pass** (operator, with the 3.103.0 people request: "this groups is a plain poor representation of groups, just doesnt look like flagship social media" + "it is the same issue just as bad on the people tab, lets make them both look great great!"). The My Groups row (3.67.4) is a thin card (a 36px accent strip + a 48px avatar + name + member count + Leave) — it reads as a settings list, not a flagship social surface. Bring it up to the screenshot test (design.md §1): richer group face (the real cover + avatar when the group has a face, not just the hash accent), a presence/last-active signal, a hover glow + lift, and a denser meta line (members + a "N in common" / shared-members hint reusing the 3.103.0 mutuals primitive). Acceptance bar: a `groups-desktop.png` that holds between a Kick and a Twitch screenshot. Gated on nothing; the 3.103.0 `computeMutuals` is the seed for the "in common" hint. **Done in 3.111.0** (real group faces + gradient fallbacks + richer card + stronger hover, both the Groups and People screens). **Open follow-ups:** the "N in common" shared-members hint (needs a member-list intersection read) + the presence / last-active signal (no presence data yet).
- [✓ 3.100.2] Discover video renders like the feed — natural ratio, no letterbox borders (operator, after the 3.100.1 rack fix: "i want them to be visually like the same way"). The shared `DiscoverCard` forced a uniform 16:9 tile (`fit="cover" ratio={16/9}`, the 3.77.1 youtubey layout) — a 9:16 clip rendered as a centered phone column in a wide frame (the borders) / center-cropped (file source). Now the card's single-video media renders through a `DiscoverVideo` helper mirroring the feed exactly: `sourceFromMedia` + `mode={hls?'full':'inline'}` + `fit="contain"` + `maxHeight="60vh"` (3.34.0 feed behavior). 9:16 = a tall 9:16 box (no borders/crop), landscape = full-width — same product as the feed, both apps. Carousels keep the 16:9 frame. `marketing/shared/discover/src/DiscoverCard.tsx`; `discoverScreen.test.tsx` re-pinned (natural ratio + object-contain, not aspect-video/object-cover). 688 social + 259 marketing-ui green, both `tsc` + `vite build` clean.
- [✓ 3.100.1] Discover/trending video: the control rack no longer vanishes mid-reach (operator, social Discover screenshot of a 9:16 clip: "the video controls arent showing … the volume, the fullscreen" + "when i try to full screen the controls go away because my mouse goes off the video and into the borders which hides the controls"). Both apps' discover cards compose the shared `HlsVideoPlayer`; its auto-hide hid the rack the moment the pointer left the video's aspect box — but for a 9:16 clip in the wide card the box is the full-width 9:16 frame while the video is the centered `max-w-[280px]` column, so the rack's right half (quality + fullscreen) sits over the letterbox border. Reaching for fullscreen crossed the border → `onMouseLeave` → `setControlsVisible(false)` → the rack hid before the click landed. Fix (`marketing/shared/discover/src/HlsVideoPlayer.tsx`): a `pointerOver` hold — the rack stays up while the pointer is over the player (the auto-hide timer now also requires `!pointerOverRef`), `onMouseLeave` drops the hold + arms the idle window (a playing video still fades out once the pointer is truly gone; a paused one keeps the rack). `hlsVideoPlayer.test.tsx` +1 regression (rack persists while the pointer is over the player across the idle window; hides after the pointer leaves; returns on re-enter). 688 social + 259 marketing-ui green, both `tsc` clean.
- [✓ 3.9.2] Decision: converge on the SDK (`knowledge/strategy/decisions.md`) — D46. Retire both legacy seams, adopt the SDK the demos already run on (the reference implementation).
- [✓ 3.11.0] Auth: D42 login through the real consent popup (the same flow the demos run) — the LoginScreen's one-tap survives via auto-complete
- [✓ 3.11.0] Auth: sign-out scrubs token + cookie; session restores on reload
- [✓ 3.12.0] Data: `getV3Client()` returns the SDK's `createV3Client` — retire the hand-rolled fetch client, data modules keep their API
- [✓ 3.28.1] Media + profile persistence (operator bug: "uploading a profile photo gives me error … the whole profile page is extremely broken, i cant even change my name / website"): `uploadMedia` is now the real presigned flow (form → MinIO POST → confirm with `object_key`, never a URL); `refreshMediaUrls` presigns via `getMediaReadUrl`; `resolveMediaRefs` handles the dual ref shape (API-resolved objects = the cross-user path, string doc_ids = `listMedia(doc_ids)` + presign); `mediaRefId` at every `mediaMap` indexing site; `saveProfile` calls `ensureFollowers` before writing (the 3.25.3 settings pattern — profile.ts was the named unfixed sibling); persistent hidden file input (`profile-file-input`) makes the upload seam e2e-drivable. Cross-lane touches: the API's `media/confirm` + `media/list` now return document envelopes (the SDK's `V3Document` was the contract; the API was the drifted side) + `list_media` `doc_ids` filter + `resolve_media_urls` fresh-presign-from-object_key (hls lane's `media.py`/`clickhouse.py`), SDK `listMedia` doc_ids
- [✓ 3.38.2] Profile screen heals broken node state (operator: "still having issues … confused how much it doesnt work" — the dev node's pre-fix state: phantom-member followers group + stale app contract): `ensureFollowers` now heals the phantom-member state (group exists but user not a member — the pre-3.25.1 `web10.app/users/{username}` member key — via `getMyGroups` check + `joinGroup`; no join when already a member, which would downgrade the owner role on merge); the profile owner path's `loadData` is per-read isolated (one `members/list` 401 degrades the Following tile, never blanks the screen — the feed's 3.25.x pattern)
- [✓ 3.39.0] Session health — the confirmatory `verifySession` oracle + the SessionGuard (operator: "is there any way we can recover from bad state? … the social app could sign out! … wouldnt want to over do it … how would we detect the state is just bad"): `POST /v3/session/verify` runs the real checks (token decode incl. the custom `expires` claim, user lookup, app-contract check, followers-group membership) and returns a typed verdict + ordered `actions` — the node is the oracle, the client never guesses from status codes. The load-bearing rule (**definite NO vs. UNKNOWN**): an UNREADABLE store yields `unknown` (no action), a readable+EMPTY store yields the decisive negative (action) — a deploy window must not look like "contract missing". The app's SessionGuard (`src/data/session.ts`) executes the `actions` (`reauth` replace-on-arrival / `heal_followers_group` / `signout`) with a 5-min cooldown (the loop-breaker → "Log in again" banner). Wired on mount (reauth deferred — no popup on page load) + on a reactive 401/403. Cross-lane: API endpoint + model, SDK `w.verifySession()` + types, KB `sdk/api.md` "Session Health" section, `e2e/tests/session-verify.spec.ts` (the dirty-node seatbelt — seeds each bad state, asserts the verdict + the browser heal gauntlet)
- [✓ 3.31.0] Mobile bottom nav "More" tab (operator: "the mobile responsive view is too crammed at the bottom, because we have so many coming soon things … maybe a burger where some of the things are on the side" → chose the More sheet over a burger): the mobile bottom bar is now **four core tabs (Feed, Discover, Messages, Profile) + a "More" tab** (never >5 icons, room to grow); the More sheet (the `AdPicker` bottom-sheet idiom) holds **Settings** + the **coming-soon list** (Flares/Takes/Livestream/Games/Marketplace) so the roadmap is discoverable on mobile; the **"Help" (report-bug) tab moves from the bottom bar to the mobile top header** (already holds New-post + Log-out). Desktop sidebar unchanged. `src/components/Social/Layout.tsx` + `socialScreens.test.tsx` (the "coming-soon NOT in mobile bottom nav" test → two tests: bar = 4 core + More, sheet holds Settings + coming-soon; Help in the header, not the bar).
- [✓ 3.32.0] Groups screen: the "coming soon" Groups tab is now real — My Groups (community memberships, infra groups filtered) + Discover (the D53 directory, search + topic filter) + the deep-linkable detail (`/groups/:id`). Data layer: `readGroupDirectory` + `readGroupDetail` (the public GET endpoints) + the community-group filter. 22 unit tests + screenshot harness routes. Groups is a real destination: desktop sidebar + mobile More sheet (alongside Settings), NOT a bottom-bar core tab.
- [✓ 3.67.4] Groups screen overhaul — first-class, feed-forward (operator: "this groups ui is so weak … showing non web10 social groups … no create group button … see the feed from this screen … starpower ui"). (1) My Groups drops the per-user **app-storage groups** (`media-{u}`/`notes-{u}`/`sharing-{u}` — the private groups that power the other apps; `isAppStorageGroup` = slug ends with `-{username}`) on top of discover/followers/DM. (2) **New group** CTA → **CreateGroupSheet** (cover + avatar upload, name, about, Who-can-read = the D58 read grant, tags, website); `createCommunityGroup` creates the group + writes the face (D60 identity doc) → navigates in. (3) **Feed-forward detail** — compact header + slim banner (only when set) + compact about (no Facebook hero) + member **composer** + posts with **real media** (images + inline video, batch-resolved); non-members keep I3 "join to view". My Groups cards gain a per-group cover accent. `groupsFilter.test.ts` + `createGroupSheet.test.tsx` + 3 `groupsScreens` cases; harness gains `mediaRefId`/`resolveMediaRefs` stubs. 350 social tests green, `tsc` clean.
- [✓ 3.38.0] Feed knobs (D36 amendment — operator lifted the "knobs on the chronological feed" reject): the feed carries the same D36 rack as Discover (presets + rotary knobs, power-mean re-ranking, client-side). Default = the Newest preset (chronological until tuned — the delivery pitch survives as the out-of-the-box experience). Knob state is deep-linkable (`?knobs=`, same encoding as Discover) AND persisted to the user's web10 `settings` service (`feedKnobs` on the settings doc — the 3.25.3 pattern; URL > saved > default). `readFeedEngagement` (the ref pattern) gives the likes/comments knobs real signal. 16 unit tests.
- [✓ 3.73.0] Feed/discover knobs rank server-side + the Character knob is gone (operator: "i have to scroll sideways, what is the character control? i dont even know what that means" + "the feed should re rank on api call, not client side"). (1) The **Character knob (power-mean exponent p) removed** — fixed at the middle (p = 0, weighted geometric mean — `FIXED_CHARACTER_DETEENT`); `character` stays in `KnobState` so `?knobs=` + saved settings keep their 5-field shape; the rack is 4 knobs (no mobile sideways scroll). (2) **Server-side ranking**: a knob twist is a debounced (400ms) re-read carrying the D36 power-mean `sort` config (the node already computed it since 3.18.2/3.21.1) — not a client-side shuffle of the same 50. `knobStateToSort()` (powerMean.ts) maps detents → the node's `PowerMeanSort` (Newest preset → `null`, the plain chronological default). The **feed** rides dev's D69 `readFeedPage` (one request per page, cursor-paged) with `knobState` passed through so the node ranks the page server-side (a knob twist is a debounced re-read of page one, the cursor resets); **Discover** uses `readDiscoverFeed(sort)` (the node ranks the board, no client re-sort when a sort is present). The node ranks across the full group membership (so "Most loved · all time" can surface an old post the top-50 chronological read never fetched); a re-read keeps the previous feed on screen (no skeleton flash). **SDK cross-lane touch** (`sdk/src/v3.ts`): `read()` gains `sort?: PowerMeanSort` + the type export (the API already accepted it), dist rebuilt. **API fix the e2e exposed** (`api/app/v3/services/clickhouse.py`): the ranked read path 500'd with `IndexError: tuple index out of range` — the outer `SELECT` projected 6 columns but the parser reads 8 (`ad_mode`/`ad_target`); the mock returned 8-column rows so it only bit on a live node. Outer `SELECT` now projects all 8; `TestPowerMeanRead` +1 pinning the projection. `powerMean.test.ts` (new) + `feed.test.ts` +4 + `feedKnobs.test.tsx` reworked + `discoverScreen.test.tsx` preset test reworked. 387 social + 97 SDK tests green, API read/sort suite green, `ruff` + `tsc` clean.
- [✓ 3.87.0] The feed + discover rack is just Recency / Likes / Comments — the Time knob is gone, the half-life is fixed at 1 day (operator: "the tune your own feed and discover should be just recency likes, and comments! the time knob is too complex… we should just set that to something sensible for the user!"). The **Time knob (the recency half-life) is removed** — the same move as the Character knob (3.71.0): the obfuscated math dial (1h / 4h / 12h / 1d / 7d / ∞) with no plain-English concept. The half-life is **fixed at the middle detent — 1 day** (`FIXED_HALF_LIFE_DETEENT` / `FIXED_HALF_LIFE_MS` in `powerMean.ts`); `halfLife` stays in `KnobState` so `?knobs=` + saved settings keep their 5-field shape, but `scorePost` + `knobStateToSort` pin it to the fixed value (the `FIXED_CHARACTER_P` pattern). The rack is now the three signals a user understands (no mobile sideways scroll). "Most loved · all time" is unaffected (all-time = recency weight 0, not the half-life). **The node contract is unchanged** (`PowerMeanSort.half_life_ms` still sent, now always 1 day; the server's `_power_mean_score` / `_power_mean_score_sql` untouched). **Cross-lane touch:** the verbatim copies `marketing/marketing-ui/src/lib/powerMean.ts` + `src/components/KnobRack.tsx` (+ `powerMean.test.ts` + `Trending.test.tsx` re-pinned to 3 knobs, `knob-time` absent) kept in sync. `powerMean.test.ts` (the fixed-half-life pins) + `feedKnobs.test.tsx` +1 (the rack is 3 knobs, `knob-time` absent). 497 social + 260 marketing-ui tests green, `tsc` clean. D36 amendment.
- [✓ 3.86.1] Clicking "Newest" on the feed now lights up — an explicit preset click beats the saved tuning (operator: "when i click newest button, it doesnt light up like the other two"). Feed-only precedence bug exposed by 3.87.0: `setKnobUrl` omits `?knobs=` for the feed default (Newest), so `knobState` (URL > saved > default) fell back to the saved tuning and the chip showed the saved preset, not Newest. Fix (`FeedScreen.tsx`): an explicit click that IS the feed default clears the saved tuning (`savedKnobsRef` → null) so the click wins; non-default presets untouched. `feedKnobs.test.tsx` +1. Discover + the other two presets were already correct.
- [✓ 3.34.0] Feed media renders at natural aspect ratio, not a 1:1 crop (operator: "all the videos were in this 1:1 ratio … it should be the dims of the video ideally … in the profile view when i click the vids, they look great though!"): the read path was dropping the stored dimensions — **API (cross-lane touch, `api/app/v3/services/clickhouse.py`)** `resolve_media_urls` now carries `width`/`height`/`duration_seconds` + a presigned `thumbnail_url` (additive); **client** `types.ts` `fromResolvedMediaRef` maps them, `FeedScreen.tsx` `MediaItem` renders `object-contain` at the natural ratio (capped `max-h-[60vh]`, measure-on-load fallback for legacy), and `MediaGrid` is **option (b)** — first item at natural ratio + a count badge (the lightbox carousel handles the rest). 816 API + 236 social tests green.
- [✓ 3.39.0] Real-time messages (WebRTC P2P) + presence: the social app adopts the messages-demo's P2P pattern (CRUD = source of truth, P2P = the fast path). `src/data/p2p.ts` (the seam: initP2P / sendP2P / onP2PInbound / presence set) wraps the SDK's `web10-npm/rtc` (PeerJS). On sign-in, when `p2pEnabled` is on (default), App opens the peer; a sent DM is also pushed over the data channel, and an inbound nudge re-reads the open conversation. Presence = the peer is open: the other party shows Online/Offline (green/gray dot + label) from live connections + a "Real-time" status chip. Opt-out (Settings → Real-time Messages toggle) tears down the peer — CRUD-only, shown offline. `peerjs` added as a dep; `v3.ts` sets `rtcServer`; `AppSettings.p2pEnabled` (default on); screenshot harness aliases `@/data/p2p` to a no-op (no real signaling connection). 11 unit tests (`p2p.test.ts`) + screenshot harness (settings toggle + presence dots verified).
- [✓ 3.40.0] Presence offline detection: the presence dot flips gray when a peer disconnects (3.39.0 stuck green). `src/data/p2p.ts` — connection-close hooks (send path opens the channel via `connect()` + hooks `close`; inbound path hooks the sender's conn) for immediate offline + a TTL backstop (per-peer `lastSeen` + 15s sweep expiring peers idle past 60s) for missed close events. `markOffline` notifies presence subscribers (DmsScreen dot/label flip automatically, no component change); `teardownP2P` stops the sweep + clears `lastSeen`. 5 new unit tests (`p2p.test.ts`), 292 green.
- [✓ 3.66.0] Video editor before posting: client-side **trim** (in/out points + re-encode) + **resize/crop** — the operator's "we can do more! all those features to edit the video trim, resize before posting." A client-side editor (canvas / MediaRecorder or ffmpeg.wasm for re-encode); partly dissolves once the feed renders natural ratios (resize-to-fit stops being a problem), trim is the piece that stays. Its own lane item, gated on the hls.js player.
- [✓ 3.67.0] Video: hls.js player for video posts in the feed (Safari native fallback, vendored hls.js) — moved from the `hls` lane. The media demo's player adopted (`HlsVideoPlayer.tsx`): muted autoplay, quality dropdown (hls.js levels), speed, fullscreen, 9:16 phone column. The read path carries `transcoding_settings` into resolved media refs + mints a per-reader `manifest_url` (cross-lane API touch); native `<video>` fallback for non-transcoded video (Phase-2 import path). The e2e exposed + fixed a worker bug (hardcoded `service="media"` split `media_metadata` docs across collections).
- [✓ 3.68.0] Feed drops the lightbox modal (operator: "i click a video, the modal opens up, and the video starts playing not in the modal … i dont think we need a modal card to pop open … not in the feed, comments can just be below"): the feed's `PostCard` no longer opens the `PostLightbox` (it stays on the insta-style profile grids + the `/u/:username/p/:postId` deep link). The video plays inline (tap toggles play/pause in place, `stopPropagation` so the tap never reaches the card — the old bug: a tap both played the video in the card *and* popped the modal, leaving it playing behind); comments were already inline below. Owner actions that lived in the lightbox move to a **kebab (⋯) menu** on own posts (Share / Edit-inline / Make private-public / two-tap Delete) in a small popover; `onPostUpdated` re-reads the feed after a mutation; `flatMediaMap` (fed only the lightbox) dropped. `FeedScreen.tsx` + `socialScreens.test.tsx` (+3 tests: inline play never opens the lightbox, own-post owner menu, non-own post has neither). 346 social tests green.
- [✓ 3.69.0] Video: **cross-user HLS streaming** (D68 — stream access tracks post access): `can_view_doc` now passes a reader of a post that carries the media (dedup-then-filter carrier-post lookup, author-scoped, 10-min sig re-check cadence preserved). The post read already grants post readers a presigned raw-URL, so this leaks nothing new. Same pass fixed a read-path bug the e2e exposed: `resolve_media_urls` + `list_media` had no version dedup on the ReplacingMergeTree — a stale `processing` row dropped the `transcoding_settings` and the feed fell back to the raw MP4.
- [✓ 3.70.5] Feed: **the following feed — followers groups only** (operator: "my feed pulled up all these people that i didnt follow"): `getFeedGroups` (`src/data/groups.ts`) returned all the user's groups minus discover, so the feed read posts from DM groups (a DM message is a `posts` doc — it rendered as a feed card from the DM recipient), community groups (every member's posts), and close-friends. Now it filters to **followers groups only** (`endsWith('/followers')`) — the user's own + the ones they follow (following = joining their followers group, so membership is exactly "I follow them"). DM / community / close-friends / app-storage groups no longer reach the feed. This supersedes 3.70.2 (which kept community + close-friends). 2 new tests in `feed.test.ts` + the 3.70.2 `groupsFilter` case re-pinned. 371 social tests green, `tsc` clean.
- [✓ 3.70.0] Video: **composer upload path + lightbox player** — 3.67.0 left two gaps, both now closed: (1) `uploadMedia` (posts.ts) writes the `video: {type:'minio'}` leaf + queues the best-effort `POST /v3/media/transcode` after confirm (3.70.0 — `media.test.ts` pins the leaf, the token+doc_id queue, the images-get-neither, the failure-never-fails-upload, and the no-token-skip); (2) `PostLightbox` routes transcoded video through `HlsVideoPlayer` (3.70.0), and the overlaid control rack (play/pause, scrubber, time, volume, speed, quality, fullscreen) landed in 3.71.0.
- [✓ 3.70.2] Feed drops DMs + app-storage posts (operator: "my dms are showing up as empty posts on the feed … it should only be posts, their comments, and reactions"): DMs are `posts`-service docs in a 2-member `dm-…` group, and `getFeedGroups` only excluded discover — so a DM's `posts` row (body has `message`, not `text`) leaked into the feed and rendered as an empty card. `getFeedGroups` now also drops DM groups (`isDmGroup`) + app-storage groups (`isAppStorageGroup`); followers / close-friends / community stay. `groupsFilter.test.ts` +1 (the feed group set). 369 social tests green, `tsc` clean.
- [✓ 3.71.0] Feed video player: overlaid, auto-hiding control rack (operator: "it should have the video player controls, fullscreen the speed controls same stuff that is in the modal, modal is just not how to go with a feed"): `HlsVideoPlayer` reworked from a below-video control bar (quality/speed/fullscreen, muted-autoplay-loop) into a YouTube/TikTok-style **overlay** — play/pause, scrubber, time, volume toggle + slider, speed, quality, fullscreen — that fades out ~2.5s after playing + idle (stays up while paused). Serves both the feed card and the lightbox. `hlsVideoPlayer.test.tsx` +6. 375 social tests green, `tsc` clean.
- [✓ 3.77.1] Discover video tiles are a uniform 16:9 (youtubey) — operator: "the discover on the social app and the trending on the web10 marketing page dont look totally the same, the videos layout doesnt look youtubey in the discover on the social app." The grid view's `MediaVideo` rendered each clip at its **natural aspect ratio** (`object-contain`, letterboxed, `maxHeight: 50vh`) — a portrait clip made a tall box, so the grid looked ragged. Now every video tile is a **fixed 16:9** (`aspect-video`) with **`object-cover`** (crops to fill — a portrait clip shows its center frame), matching the marketing trending's `TrendingMedia` (the youtubey reference). The play button is now always visible when paused (a real affordance, not hover-only) + the duration badge stays. (The FEED keeps natural ratio per 3.34.0 — Discover is the uniform-tile surface.) `discoverScreen.test.tsx` +1 (a portrait clip renders a 16:9 `object-cover` tile, not `object-contain`). Screenshot harness seeded with a discover video wall (`mock-data.ts` `readDiscoverFeed`/`resolveMediaRefs` + `readFeedPage` stub). 450 social tests green, `tsc` clean.
- [✓ 3.78.2] A clear way to log out — the desktop sidebar's account row + user menu (operator: "there is no clear way to log out", feed screenshot). The only desktop log-out was a ghost button at the very bottom of a long sidebar (below "Report a bug"), at the screen's edge / below the fold on 1080p. Now the sidebar bottom is an **account row** (avatar + display name + `@username` + chevron) that opens a **user menu** (Profile / Settings / Report a bug / **Log out**) — the `ui/` `TopBar` `AccountMenu` idiom (click-outside + Escape, `role="menu"`), opening upward. The account row loads the profile once (`readProfile` + `resolveMediaRefs` for the avatar; degrades to the token username + an initial). `socialScreens.test.tsx` re-pinned to drive the menu (trigger + closed-by-default, Log out fires `onLogout`, the three nav items reachable, outside-click closes). 465 social tests green, `tsc` clean. `screenshots/user-menu-open-desktop.png` + re-captured `feed-{desktop,375}.png`. Mobile unchanged (top-header log-out icon stays).
- [✓ 3.79.3] Feed own-post owner menu restored (operator: "on the feed, i cant edit the posts like the discover and the profile, like delete edit"): the feed's `isOwnPost` compared `author_provider === token.provider`, but v3 `author_key` is the **bare username** (provider implicit) so `extractProvider` returns the v2 `'web10'` fallback — never the token's real provider — and the kebab (⋯) never rendered on own posts. Now keys off username alone (the KB's `author_key = token.username` rule). Discover/Profile only "worked" by skipping the check (lightbox `token !== null` fallback / hardcoded `isOwner`). `FeedScreen.tsx` + `socialScreens.test.tsx` (+1: the `'web10'`-fallback author still shows the menu) + harness feed seed gains an own post. 472 social tests green, `tsc` clean. `screenshots/feed-owner-menu-{desktop,375}.png`.
- [✓ 3.79.4] Discover lightbox no longer offers edit/delete on other people's posts (follow-up to 3.79.3): the `PostLightbox` fallback was `isOwner = isOwnerProp ?? token !== null` — "a token exists, therefore I own this post." DiscoverScreen omits `isOwner`, so while signed in the lightbox showed Make private / Edit / Delete on *every* discover post (strangers' included). The fallback now derives ownership from the post's author (`token !== null && post.author_username === token.username`, the same v3 rule as the feed); DiscoverScreen unchanged, explicit-prop call sites untouched. `PostLightbox.tsx` + `postLightboxDelete.test.tsx` (+2: own post shows owner actions, someone else's shows none). 474 social tests green, `tsc` clean. `screenshots/discover-lightbox-{desktop,375}.png`.
- [✓ 3.80.0] Video editor controls are live (operator: "the video edit controls are really not working well at all", Edit-video-sheet screenshot). The 3.66.0 editor worked but felt dead — picking a crop did nothing visible until Apply, the trim timeline was a static bar (no drag), the playhead crawled in 4Hz `timeupdate` steps, and the preview played the whole clip. Now: (1) **live crop preview** (the frame locks to the picked ratio + `object-cover` — the exact center crop the re-encode produces; Original = natural frame); (2) **draggable in/out handles** (pointer-drag, clamped to the other handle ± the 0.5s min trim so the window never inverts; the out-of-window regions are dimmed); (3) a **smooth rAF-driven playhead** (ref-updated DOM `left`, no 60fps re-renders); (4) **playback loops inside the selected window** so the preview shows exactly what ships. `VideoEditorSheet.tsx` + `videoEditor.test.tsx` +9. 480 social tests green, `tsc` clean.
- [✓ 3.87.1] Trending video posts stop rendering other posts' media (operator: "on the trending tab, a posted video is blacked out dont know why, greyed out"). The per-author media grouping in `DiscoverScreen.loadDiscover` aliased the first post's `media_refs` array as its ref accumulator and `push`ed later posts' refs into it — mutating the first post — so the per-post filter matched ALL of the author's media against the first post (a single-video post rendered a carousel of the other post's media; the carousel's raw-source-file video slides are unplayable HEVC/AV1 for transcoded uploads → the black tile). Fix: copy the array (`refs: [...(p.media_refs || [])]`). `discoverScreen.test.tsx` +1 regression (same-author video+image posts keep their own media, no carousel, no mutation) — fails pre-fix. 495 social tests green, `tsc` clean.
- [✓ 3.87.3] Discover card's engagement bar padded to the card edges (operator: "discover the padding is really off" — screenshot: the like/comment/repost/share row flush against the card's left/right/bottom border). 3.86.0 regression: the shared `<PostActions>` moved the bar out of the card's `p-4` wrapper (so the `border-t` divider spans the card) but the `bar` layout chrome had no horizontal/bottom padding — icons flush on three sides while the content above sat 16px in. Fix (`PostActions.tsx` `bar` layout, Discover is its only consumer): `px-4 pt-3 pb-3` (icons align with the badge/avatar/text, share gets 16px off the right edge, 12px bottom air) + drop the `mt-3` (outside the `p-4` wrapper it made 28px above the divider vs 12px below). `postActions.test.tsx` `layout="bar"` re-pinned (border-t + px-4 + pb-3). 541 social tests green, `tsc` clean. `screenshots/discover-{desktop,375}.png` re-captured.
- [✓ 3.89.0] Profile face lightbox — click the profile picture or banner to view it enlarged; the owner can set it from any of their posts (operator: "when i click profile picture or my banner, it should enlarge them, even show a modal, they should be posts but selected as the profile picture! that would be pretty facebook like"). New shared `ProfileMediaLightbox` (`src/components/Bio/ProfileMediaLightbox.tsx`) — one modal, two jobs: **view** (everyone, the face enlarged) + **pick** (owner, a grid of their own posts' media → tap to `saveProfile` with the media `doc_id` as `avatar_ref`/`banner_ref`, so the face becomes a real post's media). Wired into `UserProfileScreen.tsx` + the dead-code sibling `ProfileScreen.tsx` (kept in sync): avatar + banner are now `role="button"` (clickable + keyboard), the owner's hover upload buttons `stopPropagation`. The picker source is a `useMemo` over the owner's posts' resolved media (a `ResolvedMediaRef` per tile — not the `MediaRecord`, which has no `doc_id`), placed before the `if (loading) return` early return (hook-order). `profileMediaLightbox.test.tsx` (9) + harness `?screen=profile` route + face-media seed. 552 social tests green, `tsc` clean. `screenshots/profile-{face,banner}-{desktop,375}.png`. **Still open:** cross-node/imported-media picker; the "the face IS a `posts` doc" data-model change.
- [✓ 3.96.0] Profile face picker crop step — the Facebook-style "how it displays" framing before a picked photo becomes the face (operator: "pfp and banner picker should let you do some resizing / cropping how facebook lets you do that for the actual how it displays as pfp"). The pick grid's tap now opens a **crop step** (a sub-step of the `ProfileMediaLightbox`): the image in the actual display frame (a circle for the avatar, the ≈4.36:1 band for the banner), pointer-drag pan + wheel/pinch/button zoom + reset, pan clamped to the cover invariant. Confirm encodes the visible window client-side (`src/lib/faceCrop.ts` — one shared transform model for preview + crop, so what you see is what ships; the image is fetched as a blob → `createImageBitmap` → canvas → JPEG, the `mediaProcessing.ts` idiom) and uploads it as a NEW media doc through the normal `uploadMedia` path — the profile points at the crop, the original post's media is untouched. Output: avatar 512×512, banner 1536×352. `faceCrop.test.ts` (14) + `profileMediaLightbox.test.tsx` re-pinned to the crop flow (12 new) + `ResizeObserver` setup mock. 631 social tests green, `tsc` clean. `screenshots/capture.mjs` repeatable `--click`. KB `web10-social-v3/your-profile.md` "The Face" section. `screenshots/profile-crop-{avatar,banner}-{desktop,375}.png`.
- [✓ 3.87.2] Liking a post N times no longer stacks N reactions — the toggle is a toggle again (operator: "i liked this post 28 times, it isnt detecting that … 0, 1, 0, 1, 0, 1, shouldnt get to i liked 28 times"). The "is this my reaction?" match in `setReaction`/`toggleReactionKind` (`src/data/reactions.ts`) ran the v2 rule (`author_provider === token.provider`), but v3 `author_key` is the **bare username** so `extractProvider` returns the `'web10'` fallback — never the token's real provider — "mine" was never found, and every tap CREATED a new reaction doc (28 taps → 28). Same class as the feed's `isOwnPost` fix (3.79.3). Fix: username-alone match in `setReaction` + `toggleReactionKind` + the two initial-state loads (`PostLightbox.tsx`, `GroupDetailScreen.tsx` — the heart was also un-filled on already-liked posts). `reactions.test.ts` re-pinned to the real v3 shape (bare-username `author_key` — the old v2-shaped seed is what let the bug through) + 2 regressions (the 28-likes oscillation, verified failing pre-fix; the v2-shaped key still matches). 543 social tests green, `tsc` clean. KB `post-actions.md` gains the v3 ownership rule. **Closed (3.94.0):** the feed's `likedMap`/`dislikedMap` are now seeded from the reader's own reactions (`readFeedReactions`) — the "forgot my like" gap is gone.
- [✓ 3.88.1] Stacked duplicate reactions self-heal on the next interaction (follow-up to 3.87.2). 3.87.2 fixed the *cause* (the v2 provider compare) but docs already stacked in the DB survive — a post with 5 stacked likes still reads 5. The "mine" match in `setReaction` changes from `find` to `filter` (sees ALL of the user's reactions, not just the first); a per-type dedup pass keeps the newest doc and deletes the rest (`Promise.all`). Runs on every like/dislike tap on every surface (feed, lightbox, groups) — the moment the user next interacts, duplicates are gone. Idempotent (no-op with ≤1 doc per type). Covers both likes and dislikes. `toggleReactionKind` also `find`→`filter` so the toggle decision sees the full picture. `reactions.test.ts` +7 (5 stacked likes → 0; 5 likes → dislike swap; 3 stacked dislikes → 0; 3 dislikes → like swap; idempotent no-dup path; `setReaction` direct ×2). 558 social tests green, `tsc` clean.
- [✓ 3.86.0] **Post Actions: one shared engagement bar + the like/dislike pair** (KB-first: `knowledge/knowledge-base/web10-v3/social/post-actions.md` — the spec, mirroring `video-player.md`). The engagement row (like + counts + comment entry) was triplicated and drifted: the feed's `PostCard` (interactive like + `CommentThread`), the `PostLightbox` (a *second* copy of the same optimistic toggle + burst + rollback), Discover's `DiscoverCard` (like was a **dead `<span>`** — the drift bug), and Groups' `GroupPostCard` (**no engagement at all**). The rule: **no surface owns the engagement row; every surface composes `<PostActions>`.** Three axes: `like` / `dislike` / `comments` (each `interactive` | `display` | `none`; comments `inline` | `none`) + `layout` (`row` | `bar` | `bare`) + the `trailing` / `groups` / `defaultOpen` seams. **The like/dislike invariant (one reaction per user — like XOR dislike):** `ReactionRecord.type` was already a free string (`'dislike'` worked today, zero node changes), but `toggleReaction` only toggled one type — a user could hold both. New data-layer helpers `setReaction` (the primitive) + `toggleReactionKind` (the tap handler) in `src/data/reactions.ts` enforce the mutual exclusion; the component calls it. The heart shows the like count; the thumb is tappable but its count stays off the bar (a D69 nudge, not a tally). **Built:** (1) `setReaction`/`toggleReactionKind` in `src/data/reactions.ts` + 15 unit tests (the state table + tap table + stranger's-reaction + signed-out); (2) new shared `src/components/Feed/PostActions.tsx` (controlled component — the surface owns `liked`/`disliked`/counts, the component renders the pair + counts + mounts the already-shared `<CommentThread>`; optimistic toggle + burst + rollback live once) + 13 tests; (3) the four surfaces compose it — Feed (interactive/interactive/inline, row), Discover (display/—/inline, bar + trailing repost/share — the dead-like became an explicit prop), PostLightbox (interactive/interactive/inline + trailing share + defaultOpen), Groups (interactive/interactive/inline, group-scoped via `groups` — the new row). `CommentThread` gains a `groups` prop. `postActions.test.tsx` (new) + `socialScreens.test.tsx` +1 + `groupsScreens.test.tsx` +3. 536 social tests green (post-merge with dev), `tsc` clean.
- [✓ 3.88.1] **Profile feed view — the posts tab is a lens, not a fixed grid** (operator: "the profile pages of people should have a facebook feed view, to see the posts like the feed, but just for the profile, right now it has insta view only!"). The "view lenses everywhere" idea (`later.md`) promoted — **rendering only, never ranking**: the profile's Posts tab (own + other — `UserProfileScreen` at `/u/:username`, since `/profile` redirects there) gains a **Grid | Feed** view toggle on the tab row. Grid = the existing insta-shaped 3-column grid (the default, unchanged); Feed = the profile's posts as a vertical stream of the feed's own `PostCard` (text + media + tags + the shared `<PostActions>` engagement bar) — the data is the SAME `posts` the grid renders, the lens only changes the rendering. `PostCard` exported from `FeedScreen.tsx` (a `testId` prop, default `'post-card'`, keeps the feed's testids); new `ProfileViewToggle` (segmented Grid/Feed, `role="tablist"`) + `ProfileFeed` (per-post engagement loaded isolated — like count + own reaction + comment count; one bad read degrades that card, never the view; likes toggle optimistically through `toggleReactionKind` with rollback). The URL holds the lens (`?view=feed`, the `?knobs=`/`?tab=` idiom — refresh-safe, shareable, default = bare URL); the toggle is a posts-tab concept (hidden on the Media tab); someone else's profile shows no owner menu. 8 unit tests (`profileFeed.test.tsx`); screenshot harness gains the `/u/:username` route + `profile`/`profile-feed` screens + the creator-page seeds (+ the two missing barrel stubs `countUserFollowing`/`readUserPublicPosts`). 551 social tests green, `tsc` clean. `screenshots/profile-{desktop,375}.png` + `screenshots/profile-feed-{desktop,375}.png`.

- [✓ 3.90.0] **Shorts — the vertical short-form feed** (KB-first: `knowledge/knowledge-base/web10-v3/social/shorts.md`). A short is a `posts` doc on the discover group whose single media is a **real 9:16 video** — no new collection, no new group (the feed is a *view* over the discover group, filtered to shorts). **The write:** `createPost` writes `tags` (`posts.ts` — the field was a first-class column the read side already mapped but the create path never populated); the composer sets `['short']` on a 9:16 video (`PostComposer.tsx`, auto-detect, no toggle). **The read + the security seam** (`feed.ts` `readShortsFeed`): read discover, resolve media, keep only genuine single 9:16 videos — the **render-time gate** re-derives 9:16 from the *resolved* media (`video/*` AND `width < height`), not the client-asserted `short` tag (MinIO returns the *declared* Content-Type; `confirm_media_upload` stores client dims as-is — a faked short is dropped at render). **The surface** (`ShortsScreen.tsx`, `/shorts` + `/shorts/:postId`): full-screen vertical scroll-snap feed, `<VideoPlayer>` per slide, author/caption overlay, like/comment/share rail, deep-link scroll-to-post. **Nav:** Shorts promoted from coming-soon to a real destination (desktop sidebar + the mobile More sheet; the bottom bar stays at five). **Tests:** `feed.test.ts` +2 driving the gate (real 9:16 kept; faked image / lying-ratio / multi-media dropped; untagged 9:16 kept) + `socialScreens.test.tsx` re-pinned. 545 social tests green, `tsc` clean, `screenshots/shorts-{desktop,375}.png`. **v1.5 follow-up:** the server-side `has(tags, 'short')` filter (the idiom the node-ad read already uses, `clickhouse.py:2419`) + optional ffmpeg-stamped dimension verification.
- [✓ 3.89.1] **Nav names signed off — Flares → Stories, Takes → Shorts** (operator: "okay, stories and shorts it is"). The two provisional coming-soon labels (`Layout.tsx`) renamed to the operator-chosen **Stories** (ephemeral posts) + **Shorts** (short-form vertical video). `socialScreens.test.tsx` re-pinned. 543 social tests green, `tsc` clean.
- [✓ 3.88.0] **Every video is re-encoded before upload + live encode/upload %** (operator: "when i edit video it works, if i dont edit the video it says failed to fetch … no progress x% … video should always go through the editor at some default setting … where pre upload it is technically 'edited'"). The raw fast-path in `PostComposer.handleSubmit` violated `video-experience.md` ("the node never sees the original") — an unedited video uploaded the raw camera file (often HEVC, often too large for the presigned POST) → `Failed to fetch`. Now: `AttachedMedia.edited` guards a **default encode** (`editVideo(file, {})` — Original, full duration) at post time for any video never run through the editor; edited videos upload the editor output as-is (no double-encode). `editVideo` + `uploadMedia` gain `onProgress` (the upload POST switches `fetch`→`XMLHttpRequest` only when progress is requested); the tray shows `Encoding N%` → `Uploading N%`. Editor stays opt-in (no forced modal). KB `video-experience.md` gains the enforced-default paragraph. `videoEditing.test.ts` +1, `media.test.ts` +3, `videoEditor.test.tsx` +4. Screenshot harness gains a `/composer` view. 551 social tests green (full-suite load-flaky on this box; 10 touched/adjacent files = 198 green together), `tsc` clean. `screenshots/composer-{desktop,375}.png`.
- [✓ 3.94.0] **The feed remembers the reader's own like across a reload** (operator: "i liked this post, but the feed doesnt have the memory that i liked it when it loads the feed, it loads with the feed unliked always, then i like it it shows 2 likes i refresh it shows 0 likes"). The D69 feed payload carries the like **count** but not whether the *reader* liked the post, so `FeedScreen` seeded `reactionMap` from `p.likes` but never `likedMap`/`dislikedMap` — every heart rendered empty on a cold load (even on a post the reader had already liked), and the next tap created a second doc ("shows 2"). This was the "still open" note on 3.87.2. Fix (client-side, no node change): a new `readFeedReactions(postIds)` data-layer helper (`src/data/feed.ts`) — one batched ref read over the feed's followers groups, filtered to the reader's own docs (v3 ownership by **username alone** — the same rule as 3.87.2/3.79.3) — returns `{ liked, disliked }` maps; `loadFeed` seeds the maps, `loadMore` merges them; a read failure degrades to empty (the 3.25.x pattern). Now an already-liked post shows a filled heart on load and tapping it clears (not stacks). `data/feed.test.ts` +4 + `hlsFeed.test.tsx` +2 regressions (filled heart on load; tap clears, count 1→0). 612 social tests green, `tsc` clean.
- [✓ 3.90.1] **Uploaded videos keep their audio — the client re-encode no longer ships a silent webm** (operator: "no uploads should be made silent, everything with sound should keep sound"). Follow-up to 3.88.0's enforced default re-encode. Root: `editVideo` set `video.muted = true` unconditionally, and in Chrome/WebKit a muted element silences its `MediaElementAudioSourceNode` too — so the audio track added to the `MediaRecorder` stream was silent, and every re-encoded upload (now ALL uploads) had no sound. Also `new AudioContext()` can boot suspended (no fresh gesture on the default-edit path) and produce no audio. Fix (`src/lib/videoEditing.ts`): element no longer muted up front (once `createMediaElementSource` is called the element's audio is already diverted into the WebAudio graph, so the user hears nothing); `await audioCtx.resume()` before recording; `video.muted = true` only in the catch fallback if routing fails. `videoEditing.test.ts` +2 (routing succeeds → element not muted + audio track on the recorded stream; routing throws → element muted, no audio track). 577 social tests green, `tsc --noEmit` clean.
- [✓ 3.96.1] **The video re-encode is now deterministic — the "Darth Vader" audio + A/V drift are gone (ffmpeg.wasm engine + no-op fast path)** (operator: "the audio sounds like darth vader" + "shit falls out of sync all the time" + "this must be better so lets do it"). Follow-up to 3.88.0 (enforced default re-encode) + 3.90.1 (silent audio). **The root (a real-time-capture artifact in `editVideo`):** the `canvas.captureStream(30)` + `AudioContext` + `MediaRecorder` re-encode is a **real-time capture** — it plays the source and records each frame + audio in wall-clock time. Two failure modes: (1) main-thread stutter → the audio capture drops/mis-times samples (the "Darth Vader" pitch distortion); (2) audio on a clean Web Audio clock vs video via rAF → dropped frames drift the A/V. The node's ffmpeg + hls.js player are both clean (verified: a 440Hz/120Hz tone survives the node pipeline at the correct pitch) — the distortion + drift were baked in at the client re-encode. **The fix (two parts):** (1) **the no-op fast path** — the default edit (no trim, no crop, the common case) re-encodes nothing; the browser uploads the original directly and the node transcodes it to clean HLS (new `isNoopEdit(opts, duration)` helper, `PostComposer.handleSubmit` calls it). (2) **the ffmpeg.wasm engine** (`src/lib/ffmpegEngine.ts`) — the edit path runs the full FFmpeg binary in a Web Worker as a **deterministic file-to-file op**: no playback, no real-time capture, frame-accurate trim (`-ss` before `-i` + re-encode), a `crop` filter for the ratio reframe, `-c:a aac -ar 48000` for clean audio, guaranteed A/V sync. The ~32MB core is **self-hosted** (imported `?url` → Vite emits it as an asset from our origin, not unpkg) and **lazy-loaded on editor open** (the intent signal; a "Preparing…" state gates Apply). Single-threaded core (no COOP/COEP headers). `editVideo` is now a thin wrapper: read metadata → compute trim + crop geometry → build the `-vf` filter → delegate to the engine. **Tests:** `videoEditing.test.ts` re-pinned to the engine seam (the `isNoopEdit` state table + `editVideo` hands the engine the right trim + filter — crop vs scale, clamping, progress passthrough, error propagation; the old canvas/MediaRecorder/AudioContext mocks are gone); `videoEditor.test.tsx` re-pinned (the editor pre-warms the engine on open, the edited output is now `-edited.mp4`, the unedited no-op path uploads the original directly). 69 touched/adjacent tests green, `tsc --noEmit` clean, `vite build` ships the core as separate assets (111KB JS + 32MB WASM + worker, fetched at runtime not on page load).
- [✓ 3.96.2] **Desktop HLS audio is no longer choppy — vendored hls.js upgraded 1.5.13 → 1.7.3** (operator: "the audio is actually fine on mobile on desktop it is botched so wondering if it was never a problem after all … and just desktop playing the hls in a super choppy retarded way"). **The reframe:** the *same* HLS stream plays clean on mobile (native HLS) but botched on desktop (hls.js / MSE) → the stream is fine, the **desktop player** is the problem. The vendored hls.js was **1.5.13**, which predates **v1.6.18 — "bind initPTS to the lowest DTS across tracks to prevent negative timestamps" (fixes #7932)** — the textbook cause of this exact symptom (1.5.x bound initPTS per-track → audio/video tracks get negative/mismatched timestamps → choppy audio in MSE, while native HLS tolerates it). **The fix:** upgrade the vendored `public/hls.min.js` to **1.7.3** (the UMD build attaching to `window.Hls` — a drop-in, API-stable swap; 1.6→1.7 notes confirm no breaking changes for our usage). Carries the initPTS fix + v1.7.0's smoother audio handling + parallel init-segment loading. Player code unchanged (same `new Hls()` / `loadSource` / `attachMedia` surface). **Verification:** the node's HLS segments are clean (AAC-LC 48kHz stereo, 6s segments, ~5ms A/V offset — normal); 37 player/feed/shorts tests green, `tsc --noEmit` clean, `vite build` ships the new asset. `src/types/hls.d.ts` notes the 1.7.3 pin + why. **Operator to confirm on desktop** (the one place I can't listen automatically) — if still choppy, next suspect is the manifest not declaring `CODECS=`.
- [✓ 3.93.1] **Own profile is recognized as yours no matter how you arrived — the "sometimes I can't edit my pfp" bug** (operator: "sometimes entering my profile i cant edit my pfp … hitting the profile button then app knows my profile link is editable by me"). Same v2/v3 provider-mismatch class as 3.79.3 (feed `isOwnPost`) + 3.87.2 (reactions): `UserProfileScreen.loadData` decided ownership with `token.username === username && token.provider === provider`, but a profile reached via a feed/Discover author click carries the post's `author_provider` in `location.state` — and a v3 `author_key` is the **bare username**, so `extractProvider` returns the v2 **`'web10'` fallback**, never the token's real provider. `token.provider === provider` was `false` on exactly those arrivals → the user's OWN profile rendered as a stranger's (Follow/Message, no Edit profile / camera / banner), while the profile button (no `state.provider` → falls back to the token's own) worked. Fix: `isOwn` keys off `token.username === username` alone (the KB's v3 ownership rule — the provider is implicit). `UserProfileScreen.tsx` + `follow.test.tsx` (+2 regression, the v2-fallback-provider arrival verified failing pre-fix). 608 social tests green, `tsc --noEmit` clean.
- [✓ 3.95.0] **Shorts plays like a short — the video fills the screen + the TikTok swipe works** (operator: "definitely some work to be done how shorts are being displayed, if video component needs some special treatment for these cases, + allowing the tik tok swiping to happen too" — screenshot: a small 9:16 video floating in a black void on desktop). Two roots: `VideoPlayer` with an `hls` source delegated unconditionally to the full-rack `HlsVideoPlayer` (the `max-w-[280px]` column + scrubber/quality/speed rack — every transcoded short rendered small with a rack colliding with the overlay), and the swipe had no playback state. Fix (KB-first — `shorts.md` frame/playback/swipe spec + decision #6; `video-player.md` `immersive` layout prop): (1) `immersive` on `<VideoPlayer>` — the video fills the frame the surface gives it (`absolute inset-0 object-cover`, no own ratio, no phone column); the `hls` source renders video-only (new `ImmersiveHls` — hls.js, muted/autoplay/loop, designed error, tap-to-pause that never escapes) with NO control rack; `InlineVideo` gains the fill + an `active` seam; the full-rack `HlsVideoPlayer` untouched. (2) The frame: on a phone the slide IS the 9:16 frame (full-bleed); on desktop a centered 9:16 column filling the viewport height (`h-full md:w-auto md:aspect-[9/16]` — the designed letterbox; `h-full w-full md:aspect-[9/16]` is a no-op, aspect-ratio loses when both dims are explicit). (3) The ambient loop: the active slide (≥60% visible) autoplays muted, off-screen slides pause, tap toggles in place. (4) The swipe: native scroll-snap + `ArrowUp`/`ArrowDown`/`PageUp`/`PageDown` one-slide keyboard nav. `videoPlayer.test.tsx` +4 + new `shortsScreen.test.tsx` (8: slides + frame, the hls video-only fill, the ambient loop, the swipe handing autoplay over, the keyboard swipe, the overlay + rail, the optimistic like, the empty state). Harness: `hls.min.js` loads + `dm-portrait2` seeded transcoded so the capture exercises the hls path. 618 social ... (line truncated to 2000 chars)
- [✓ 3.98.2] **Liking (and disliking) now works on Discover — the board takes live reactions** (operator: "liking isnt working for me on the discover, on the feed it works but discover no"). 3.86.0 made Discover's engagement bar `display`-only (the heart a dead `<span>`), with the KB's "open question" of whether the public board should take reactions. The report settles it: yes. `DiscoverCard`'s `<PostActions>` flips `like`/`dislike` to `interactive` (feed parity — post-actions.md's "one way of reacting everywhere"); the screen gains `likedMap`/`dislikedMap` seeded from the discover-group reaction read it already performs (v3 username-alone ownership, the 3.87.2 rule) so the heart/thumb render filled on load; `handleToggleReaction` (the feed's optimistic-toggle + rollback) calls `toggleReactionKind`. No node change (reactions default to the discover group, D62 — the same group the board reads). `discoverScreen.test.tsx` +3 (interactive button not a span; like → `toggleReactionKind('p1','like')` + optimistic fill; already-liked post shows a filled heart on load — `getV3Client` mock now controllable). 609 social tests green, `tsc` clean. KB `post-actions.md` surface map + open question updated.
- [✓ 3.98.0] **The feed is a query, not an endpoint — the query engine gains a prepare pass (read + resolve), `POST /v3/feed` is retired (D73)** (operator: "i dont think there should be a feed.py… breaks the kb standards" → "we added the general query engine to do something like this 267 requests in one… for any app… then you dont need an endpoint… the social app serves as an example" → "could you bake it into the query? the prepare step? so improve the engine?" → "i just hope it does the exact same thing but using the engine!"). The D60 leak: `feed.py` hardcoded the social read (the node reading the `profile` service + resolving the `avatar_ref`). Fix (KB-first: `query-engine.md` Phasing 4 + D73): (1) **engine prepare pass** (`query.py` `_prepare_rows` + `PrepareSpec`/`PrepareFace`) — after the SELECT, mint the rows: `media` (presign + per-reader HLS sigs) + `ads` (pinned + node) + `face` (the author's face media, caller-declared field); reuses the read path's passes verbatim, no escalation (the boundary CTEs already gated the rows); the CTE columns gain `ad_mode`/`ad_target` (`safe_query.py`). (2) **SDK** — `w.query(sql, { groups, prepare })` + `V3Prepare` types; `w.feed`/`V3FeedResult` retired. (3) **client** (`data/feed.ts`) — `readFeedPage` = one `w.query()` (the app's query names `profile`; the node hardcodes nothing) + the prepare pass; `buildFeedQuery` mirrors `read_feed`'s SQL, `fromFeedQueryRow` maps the rows. **Retired:** `feed.py` + `FeedRequest` + `ch.read_feed` + `w.feed`/`V3FeedResult` + `test_feed_endpoint.py` + `TestReadFeed`. Behavior-preserved (same ranking/cursor/counts/ads/media/HLS/face); engagement counts + the face are now group-scoped (more correct, matches in practice). Cross-lane: node + SDK (this lane owns the client). `test_query_endpoint.py` `TestPrepare` (5) + `data/feed.test.ts` `readFeedPage` (4). 990 API + 117 SDK + 613 social tests green, `tsc` clean.
- [✓ 3.97.0] **The generic server-side tag filter on `read` (`has(tags, …)`) — and the Shorts feed pulls only shorts** (Shorts v1.5, the plan's open item). The `read` endpoint takes a `tags` param that filters to docs carrying **every** given tag — a **platform primitive, not a social concept** (operator: "tags are a generic thing? there are tags across all web10 it isnt just a web10 social thing"): `tags` is a first-class column on the universal `documents` table, so the filter lives on the generic read path and any app can filter its own reads by its own tags with zero new infra. web10-social is the first consumer (`['short']`). **API** (`api/app/v3/`): `ReadDocuments.tags` → `read_documents_in_groups` → `_group_docs_query`/`_group_docs_ranked_query` → `_board_base_sql` (`AND (has(p.tags, %(tagN)s) AND …)` — shared by the no-sort + ranked paths). The `feed` endpoint is unchanged (no consumer). **SDK** (`sdk/src/v3.ts`): `read()` opts + the interface gain `tags?: string[]`; `dist/` + `public/wapi.js` rebuilt + re-synced. **Client** (`src/data/feed.ts`): `readDiscoverFeed(sort, limit, tags?)`; `readShortsFeed` sends `['short']`. **The two-layer model (shorts.md) is now explicit:** the tag is the server-side inclusion rule (cheap + indexable); the render-time 9:16 gate stays the backstop that drops fakes. Consequence: an *untagged* 9:16 video is no longer a short (the composer auto-tags every 9:16 upload, so app-created shorts are unaffected). **Tests:** API `test_v3_clickhouse.py` +4 (`TestReadTagFilter`) + SDK `v3.test.ts` +2 + client `feed.test.ts` re-cut to the two-layer seam (mock `read` simulates the server tag filter). 1006 API + 119 SDK + 630 social tests green, `tsc` clean. **KB:** `shorts.md` (two-layer read + decision #2 + trust-boundary row), `sdk/api.md` (tag-filter section). **Still open:** the optional ffmpeg-stamped dimension verification in the transcode worker (the "truly bulletproof" version).
### Lane: pwa-install (Phase 3)
**Owns:** `marketing/web10-social/public/serviceWorker.js`, `marketing/web10-social/src/components/shared/InstallPrompt.tsx`, `marketing/web10-social/src/lib/pwa.ts`, `marketing/web10-social/src/lib/analytics.ts` (the install events), `e2e/tests/social-pwa.spec.ts`; cross-lane: `knowledge/knowledge-base/web10-v3/social/pwa.md` (the spec).

**Decision: D72** — web10-social becomes a serious PWA: a real service worker + an install prompt, fired at the moment of value, never on a timer. The reel/shorts surface (`/shorts`) is where the web gap is widest on a phone; installability is what makes the PWA (D41) deliver a native-feeling experience without an app store. The SW is the keystone — the browser won't fire `beforeinstallprompt` without a functioning service worker, so the prompt bites are gated on it. The trigger-policy, telemetry, and e2e bites are parallel once the SW + prompt surface exist.

- [✓ 3.92.0] **KB** (`knowledge/knowledge-base/web10-v3/social/pwa.md`) — the installability bar, the SW design (shell-only precache + offline fallback + update flow; the line: the cache is the app shell, never user content, the feed is always re-read from the node), the `beforeinstallprompt` → custom-surface wiring, the trigger policy (Shorts-on-phone / post-engagement, one dismissible surface, never a modal wall, never on a timer), and the D56 line (the "installed" event is a content-free GA4 action). **Gates the rest.**
- [✓ 3.92.0] **The service worker (keystone)** (`public/serviceWorker.js`) — the stub (`const assets = []`) is replaced with a real app-shell precache (the known files + the hashed Vite assets discovered by parsing `/index.html` at install), an offline fallback for the SPA `index.html` route (deep links like `/shorts` / `/u/:username` load the shell offline), and an update flow (versioned cache name + `skipWaiting`/`clients.claim`). The shell-only cache is the invariant. Registration moves into `main.tsx` (prod-only); the dead `registerWorker.js` is deleted. **Gates the prompt + e2e bites.**
- [✓ 3.92.0] **The install prompt surface** (`src/components/shared/InstallPrompt.tsx` + `src/lib/pwa.ts` `useInstallPrompt`) — the hook intercepts `beforeinstallprompt` (module-level), stashes the event, exposes `canInstall` / `isIOS` / `promptInstall()` / `dismiss()`; the surface is one tasteful dismissible card (mobile bottom sheet / desktop floating card) — "install to keep your feed + go offline." iOS shows Share → Add to Home Screen instructions (Safari can't be triggered in code). Dismissal remembered in `localStorage` so it never re-nags.
- [✓ 3.92.0] **The trigger policy** (wired into `ShortsScreen.tsx` + the follow handlers + Settings) — show the surface when (a) a phone user opens `/shorts` and stays ~3s, or (b) a user follows a creator, or (c) the explicit "Install app" affordance in Settings. Never on page load, never on a timer, never past the one remembered dismissal. Automatic triggers are mobile-only; desktop gets the explicit affordance.
- [✓ 3.92.0] **Telemetry (the D56 line)** (`src/lib/analytics.ts`) — content-free GA4 `pwa_install_prompt_shown` + `pwa_installed` actions (a count + the trigger context: `shorts` / `engagement` / `manual`). No user content, no PII.
- [✓ 3.92.0] **Unit tests** — `pwa.test.ts` (11) + `installPrompt.test.tsx` (6): the hook's `canInstall` transition + `promptInstall` outcome + dismissal persistence, `requestInstallPrompt`'s no-op-when-not-installable / dispatch+track-when-installable / no-op-when-dismissed / no-op-when-installed, the surface's render-on-trigger / dismiss-remembers / iOS-instructions variants. Screenshot harness: the install surface on a 375px Shorts view + desktop (`install-prompt-{desktop,375}.png`). 598 web10-social tests green, `tsc --noEmit` clean, `vite build` ships the SW.
- [ ] **E2E** (`e2e/tests/social-pwa.spec.ts`) — the installability bar holds on a live node (manifest served, SW registered + controlling, `beforeinstallprompt` fires in an installable Chromium context), the offline fallback loads the shell with the network offline, the prompt appears on the Shorts trigger + is remembered after dismissal. API floor (manifest + SW + shell assets served) + browser gauntlet (installability + offline + prompt, log-sequence verified).

### Lane: notifications (D69)
**Owns:** `marketing/web10-social/src/data/notifications.ts`, `src/components/Notifications/`, `src/components/Social/Layout.tsx` (badge/bell), `src/App.tsx` (route + banner); cross-lane: `api/` + `sdk/` (the `getPendingRequests` read), `knowledge/knowledge-base/web10-social-v3/notifications.md` (the spec).

The always-on signal (D69): derived events the app owns, CRUD is truth, the
P2P data channel is the nudge (the DMs' pattern — no server push, no node
table, no new endpoint). The substrate exists (the app-wide `onP2PInbound`
bus, the `readRefCounts` ref-count primitive, the followers group, the
`Layout` shell, the `sessionAlert` banner). The operator's framing: "moment
you look at messages it should probe online guy … whatever app state they are
in, that is when we have a notification on the screen that is always on! for
unread messages and unread replies to your posts." The decision bite gates
the rest — docs first.

- [ ] **Decision: D69** (`knowledge/strategy/decisions.md`) — P2P nudge + CRUD re-read, app-owned `notifications` service, node stays stateless. Amends the KB's WebSocket push model (contradicts D66). **Gates the rest.**
- [ ] **KB: amend `notifications.md`** — replace the WebSocket push model with the P2P-nudge + CRUD-re-read model (D69); fix the stale API shapes (`getPendingRequests` unbuilt, D58 group ids); add the DM-unread + reply-to-comment types; keep the "app-owned, derived events" framing.
- [✓ 3.74.0] **Notification store** (`src/data/notifications.ts`) — app-wide singleton (the `p2p.ts` listener-set idiom): live unread count + recent items, `onNotificationChange` + `unreadCount()`. Subscribes to `onP2PInbound` so a nudge bumps the badge from *any* screen. Seed from a CRUD read on sign-in (truth; the nudge is the fast path). Wired into `App.tsx` sign-in/sign-out. 11 unit tests.
- [✓ 3.74.0] **Read side — derive from reads** (`src/data/notifications.ts`) — `deriveNotifications` derives reactions/comments on my posts (read my posts → filter author === me → read with `ref = myPostIds` → filter out mine) + the latest DM from each other party. Unread via a single `last_seen` cursor (a `notifications` doc in the followers group); `markAllRead` advances it. (Follow requests — gated on `getPendingRequests` — + group joins + replies-to-my-comments are follow-ups.)
- [✓ 3.74.0] **Write side — the nudge** (`src/data/{reactions,comments,dms,follows}.ts`) — on an action that targets someone, push a typed payload over P2P (react → post author; comment → post author; reply → comment author; DM → recipient; follow → followed user). `sendNotification` helper (self-guard, best-effort, no durable write). 9 unit tests. (Group-join nudge is a follow-up.)
- [ ] **Unread state — app-owned** (`src/data/notifications.ts`) — a `notifications` service in the followers group (D60): `{type, from, ref_doc_id, read, created_at}`. Badge = unread count; mark-read on open. DM unread = a per-conversation last-read cursor (the `settings.ts` pattern).
- [✓ 3.74.0] **Always-on surface** (`src/components/Social/Layout.tsx`, `src/App.tsx`) — a bell with a live unread badge (desktop sidebar + mobile top-header) + an app-wide "N new" banner (clears on /notifications). Bell → `/notifications`. `useNotifications` hook + `NotificationBell`.
- [✓ 3.74.0] **The `/notifications` screen** (`src/components/Notifications/`, route in `App.tsx`) — history list (avatar + "did X" + time-ago), deep-linkable (`/notifications`), mark-read on open, designed empty state. (Avatar resolution is a follow-up.)
- [✓ 3.85.0] **Row deep links — a notification takes you to the place it's about** (`src/data/notifications.ts`, `src/components/Notifications/NotificationsScreen.tsx`, `src/components/Bio/UserProfileScreen.tsx`) — `notificationHref` (reaction/comment → post permalink + `?comment=`; dm → conversation; follow_request → profile; group_join → group) + `resolveReplyHref` (parent comment → post → author chain). Resolvable rows are buttons, unresolvable stay plain `<li>`. The profile's posts/media tab gains `?tab=` (deep-link rule). KB destination table in `web10-social-v3/notifications.md`.
- [✓ 3.105.4] **Follow notifications — derived from the followers-group member list** (`src/data/notifications.ts`, `src/components/Notifications/NotificationsScreen.tsx`) — operator: "not seeing notification if someone follows you." The read side never derived follows (only the P2P nudge existed, which is lost when the recipient is offline). The followers group is open join (following is immediate), so `deriveNotifications` now reads `getGroupMembers(followersGroupId(me))` and emits one `follow_request` row per follower (excluding self + reserved principals), `joined_at` as the event time, `last_seen` cursor marks read/unread. Label corrected to "X followed you." Unblocks follow notifications **without** `getPendingRequests`. 2 unit tests; 750 social green, `tsc` clean.
- [✓ 3.110.1] **Reply notifications — derived from my comment ids (offline recovery)** (`src/data/notifications.ts`) — operator, on the D69 model: "using the existing webrtc to notify then api pull the new stuff, so no page reload needed … otherwise they will get the notification when they reload the api." The read side derived reactions/comments/DMs/follows but **never replies** — a reply's `ref_value` is the parent *comment's* doc_id (not the post's), so the comment derivation (`ref: myPostIds`) never selected it; a reply made while the recipient was offline was lost (the nudge is fire-and-forget). `deriveNotifications` now reads the comments in my groups, collects the doc_ids I authored, and emits one `reply` row per comment whose `ref_value` is in that set (excluding self). `ref_doc_id` = the parent comment's doc_id (matches the write-side nudge + `resolveReplyHref`). 2 unit tests; 772 social green, `tsc` clean.
- [✓ 3.115.1] **Badge + banner clear when you view them — the "viewed but still says new" race** (`src/components/Notifications/NotificationsScreen.tsx`) — operator (screenshot, on Groups with the "2" badge + "2 new notifications" banner still lit): "i have viewed my new notifications and the thing that says they are new hasnt updated, it hasnt changed that i viewed them." The screen fired `markAllRead()` **once** on mount (`useEffect` `[]`), but `initNotifications` → `seed` is async (~7+ CRUD reads); opening `/notifications` mid-seed meant `items` was empty → `markAllRead` no-opped (`items.every(read)` true on `[]`) → the `last_seen` cursor never advanced → when the seed landed the rows unread, the one-shot effect never re-fired → badge + banner stuck at "2." Fix: the effect now reacts to `unread` (`useEffect(() => { if (unread > 0) markAllRead(); }, [unread])`) — re-runs the instant the seed (or a new nudge) bumps it; `markAllRead` no-ops when all-read so it never loops. `notifications.test.tsx` mock re-renders on a controllable emit + a new race regression test (verified to fail on the old one-shot code). 819 social green, `tsc` clean.
- [ ] **The missing primitive — `getPendingRequests`** (`api/` + `sdk/`) — a read of pending join requests for **request-join** groups (e.g. close-friends); `requestJoin` exists, the read does not. Note (3.105.3): NOT required for follow notifications — follows are open-join and derived from the followers-group member list.
- [ ] **E2E: two-user gauntlet** (`e2e/tests/social-notifications.spec.ts`) — A posts → B reacts/comments/replies → A's badge bumps in real time + the history shows the events; A opens `/notifications` → marked read; a DM to A bumps the unread-DM count. API floor (read-side derivation + the pending-requests read) + browser gauntlet (badge + banner + screen, log-sequence verified).

**Groups: In-App Management** (operator, 07.09.2026 — "my friend wasnt able to see my public esports grinder group" + "the ui is really bare … doesnt look like a flagship social media app"). The social app's group page is read-only; all management lives in the authenticator. Port the authenticator's `GroupCard` + four dialogs into the social app behind a manager-only "Manage" entry point, and re-cut the detail hero. The model is unchanged (D58 + D53 + D60); the API already exposes every op. Plan: `plan.md` "Groups: In-App Management". The SDK seam is the keystone (gates the social bites); the five social bites are parallel (one workspace each, each owns its own component + data fn + test file; only the **surface** bite touches `GroupDetailScreen.tsx`).
- [✓ 3.71.0] **SDK seam (keystone, cross-lane `sdk/`)** — `createGroup` + `updateGroup` accept `discoverable?: boolean` (the API already accepts it); new `deleteGroup(groupId)` → `groups/delete` (the endpoint exists); `V3Group` gains `discoverable?: boolean` (the `/manages` + `/get` endpoints already return it). `sdk/src/v3.test.ts` +5. **Gates the four social bites below.**
- [✓ 3.71.0] **Surface: "Manage" entry point + detail hero re-cut** (`src/components/Groups/GroupDetailScreen.tsx`) — the keystone social bite. A manager-only **Manage group** kebab (gated on `getGroupsManages()`) opens the `ManageGroupSheet` (the tab/section shell the other bites mount into). The **hero re-cut**: a face-present group renders the Facebook-shaped banner hero (cover + avatar + name + about + tags + website); a face-less group shows a designed empty-face state (a "Add a cover & about" CTA for managers, a clean minimal header otherwise) — the page never looks bare. `groupsScreens.test.tsx` +7 (entry-point gate: manager ✓ / member ✗ / bystander ✗; the sheet opens with its tabs; hero renders the face; empty-face state).
- [✓ 3.71.0] **Profile: the face editor** (`src/components/Groups/ManageGroup/ProfileSection.tsx`, `src/data/groups.ts`) — port the authenticator's `GroupProfileDialog` **plus cover + avatar editing** (the authenticator's profile dialog is name/about/website/tags only — the cover/avatar are set at create and *never editable anywhere today*; reuse the `CreateGroupSheet`'s `uploadMedia` + preview pattern). Save via `writeGroupIdentity` (D60); the detail re-reads on save. `groupsScreens.test.tsx`.
- [✓ 3.71.0] **Settings: join policy + who-can-read + list-in-directory** (`src/components/Groups/ManageGroup/SettingsSection.tsx`, `src/data/groups.ts`) — port the authenticator's `GroupSettingsDialog`: join policy (`updateGroup({join_policy})`), who-can-read (public / signed-in / private = add/remove the `anyone`/`authenticated` `reader` reserved rows, the D58 read grant), and the **"List in directory" toggle** (`updateGroup({discoverable})` — the D53 blasting flag, the fix for the original "friend can't find my group" bug). `groupsScreens.test.tsx`.
- [✓ 3.71.0] **Members: manage members + join requests** (`src/components/Groups/ManageGroup/MembersSection.tsx`, `src/data/groups.ts`) — port the authenticator's `GroupMembersDialog`: member list, add/remove, and the **join-request queue** (pending → approve / deny via `getJoinRequests` / `approveJoinRequest` / `denyJoinRequest` — the SDK already has these; the data layer wraps them). `groupsScreens.test.tsx`.
- [✓ 3.71.0] **Roles + Delete** (`src/components/Groups/ManageGroup/RolesSection.tsx`, `src/data/groups.ts`) — port the authenticator's `GroupRolesDialog` (the per-service role-map editor, D58 shape — `COMMUNITY_ROLES` is the template) + **Delete group** (two-tap confirm → `deleteGroup` → navigate to `/groups`; gated on the `deleteGroup` op, owner only). `groupsScreens.test.tsx`.
- [✓ 3.71.0] **Create-flow fix: a "public" group is listed** (`src/data/groups.ts`, `src/components/Groups/CreateGroupSheet.tsx`) — the original bug's root cause: `createCommunityGroup` never sets `discoverable`, so a "public" group is publicly *readable* but **never findable** in the directory. **Decision: a `public` group is auto-listed** — `createCommunityGroup` passes `discoverable: visibility === 'public'` (the Settings section's toggle stays the manual override). `groupsCreate.test.ts` (public → `discoverable: true`; signed-in / private → `false`).
- [✓ 3.74.2] **Group create/edit 403s — the app contract was missing the two services the data layer writes** (`src/interfaces/auth.ts`, `src/data/access.ts`, `src/data/groups.ts`, `src/components/Groups/CreateGroupSheet.tsx`) — operator: "getting constant 403s … pretty rough actually creating group or editing group" + "the create group doesnt have nearly all of the options of the edit group." **(1) The 403s:** the login contract's `SOCIAL_SERVICES` was missing `web10-social-group-identity` (the D60 face — create-group + face editor) + `notifications` (the D69 cursor) → every face write 403'd at the app-contract gate. Both lists (login contract + session-health oracle) gain the two services, in lockstep: the oracle list makes a pre-fix user's partial contract detectable (`degraded` → `reauth` → `applyACR` merges the missing grants — self-heal, no full re-login). **(2) The options gap:** the create sheet had no **join policy** — `CreateGroupInput` gains `join_policy?` (default `open`), threaded to `createGroup` (was hardcoded `'open'`), + a **How people join** selector. `authContract.test.ts` (new, 2) pins the contract's service list + the lockstep; `groupsCreate.test.ts` +3 + `createGroupSheet.test.tsx` +2 pin the join policy. 444→449 social tests green, `tsc` clean.
- [✓ 3.78.0] **Create-group sheet gains the "List in directory" setting — create/edit parity** (`src/data/groups.ts`, `src/components/Groups/CreateGroupSheet.tsx`, `screenshots/capture.mjs`) — operator: "in create group i am not seeing the settings all the settings i am supposed to see." The create sheet was missing the **List in directory** control (the D53 `discoverable` flag) that the edit surface's `SettingsSection` has — `createCommunityGroup` *derived* it from visibility, so a signed-in/private group couldn't be opted into the directory at create time. **Fix:** `CreateGroupInput` gains `discoverable?` (`createCommunityGroup` uses `input.discoverable ?? visibility === 'public'` — default-preserving); the sheet gains a **List in directory** toggle (the `SettingsSection` idiom) whose default tracks the visibility until the operator flips it (then sticky via `listedTouched`), threaded as `discoverable: listed`. Closes the last create-vs-edit options gap (3.74.2 added join policy). `groupsCreate.test.ts` +2 (explicit override both directions) + `createGroupSheet.test.tsx` +4 (render/default/track/sticky/thread). `capture.mjs` gains `--click` (click before `--ready`) to shoot the sheet. 468 social tests green, `tsc` clean.
- [✓ 3.116.0] **Group detail: profile-shaped hero (always) + the real feed** (`src/components/Groups/GroupDetailScreen.tsx`, `src/components/Feed/{FeedScreen,PostComposer}.tsx`, `screenshots/harness/`) — operator (screenshot of a face-less group): "when clicking on a group it should look like an actual feed…… but also have profile elements at the top, the banner and the profile pic like a user profile." Two roots: the hero only rendered the profile shape when the group had a face (face-less → bare header), and the group feed re-implemented its own card + chat-style composer. **Built:** (1) the hero is ALWAYS profile-shaped — banner always present (cover or the brand gradient fallback, the profile's fallback) + overlapping avatar (cover or hash-color initial) + name/badges/meta + about; the `hero-empty` variant is retired, the manager "Add a cover & about" CTA survives as `!hasFace && canManage`. (2) `PostCard` (the reference card) gains a `groups` prop → `PostActions`; the group screen composes `PostCard` via a thin `GroupFeedPost` wrapper (group-scoped engagement state, `isOwnPost` for the owner menu) — the local `GroupPostCard`/`PostMedia` are gone. (3) `PostComposer` gains a `groups` prop → `createPost`; the group screen mounts the flagship composer (media/visibility/ad) instead of the chat-style box. `groupsScreens.test.tsx` re-pinned +2 (hero fallbacks; group-scoped like write); harness gains the `uploadCommentPhoto` stub + a param-driven group-detail route + the `groups-detail-noface` screen. 819 social green, `tsc` clean, captures green. KB `post-actions.md` (the `groups` seam on the reference card).

### Lane: engine-group-metadata (Phase 2 — THE KEYSTONE, do first)
**Owns:** `api/app/v3/services/safe_query.py` + the query endpoint, `sdk/src/v3.ts`. **Plan:** `knowledge/strategy/engine-group-metadata.md` (operator pass, 18.09.2026 — "joining with groups table in secure way for reads, reading what you are allowed to read is essential thing for the web10 engine" + "it should be the first thing we do since it is a big addition to the protocol"). **The keystone of the discover / groups / search cluster.** Extend the D73 query engine so a caller's query can JOIN **group metadata** (`member_count` from `group_members`, `join_policy`+`discoverable` from `group_contracts`) into a content read — gated by the **existing I3 read gate** (`can_read_group` / `readable_groups`, `clickhouse.py:1125,1155`), with **NULL-out-and-sort-last** semantics for groups the reader can't read. **The wall extends, it doesn't weaken:** `group_meta` is a new API-built, visibility-enforced boundary CTE (parallel to the service CTEs); the raw tables **stay blocked** (the caller references `group_meta`, never `group_members`/`group_contracts`); the AST walk + re-parse backstop are unchanged in structure. **Opt-in** (`withGroupMeta` on `w.query`). **Behavior change (QE-7, decided):** metadata is visible iff you can read the group (tightens the D53 "metadata always returned by ID" model — more consistent I3; requires a KB update to `groups/detail.md`). This is what makes "sort people by legit followers / groups by legit members" a first-class, secure, queryable, sortable capability (the feed's sort customizability applied to audience size). **Gates D0 (people read) + D3 (groups browser)** — they sort by legit metadata via the `group_meta` join; their node-function versions (if shipped meanwhile) use the same visibility semantics, so this is a mechanism upgrade, not a semantics change.

- [✓ 3.121.0] **QE-A0: expose `group_id` on the content CTEs** (the join key — the prerequisite, `safe_query.py`) — add `dg.group_id` to the boundary CTE's outer SELECT (the one that JOINs `doc_groups`); a doc in N readable groups surfaces N rows (one per group, each with that `group_id`) — the multi-group shape is pre-existing (the JOIN already produces N rows), now visible + meaningful; the exposed group_ids are all readable groups (the `WHERE dg.group_id IN (readable_groups)` filter), so no I3 leak. Update the prepare pass / row serialization. **Gates QE-A** (the `group_meta` join is unusable without the key).
- [✓ 3.123.0] **QE-A: the node change** (`api/app/v3/services/safe_query.py` + the query endpoint) — the `_group_meta_cte_sql` builder (pre-aggregated counts + deduped contracts + CASE-based NULLing on the readable set — **NOT** the service CTE's WHERE-filter); the opt-in flag; the endpoint computes the readable groups (via `readable_groups`) and passes them to the CTE builder (parallel to `readable_groups_by_service`); the `_validate` + `allowed`-gate extension (recognize `group_meta` when opted in — `query.py:168` computes `allowed` from the app contract's readAll services, so the opt-in must inject `group_meta` into the allowed set; **keep the raw tables blocked**); the compiled query re-parses (the existing backstop).
- [✓ 3.126.0] **QE-B: the SDK change** (`sdk/src/v3.ts`) — `w.query(sql, { groups, prepare, withGroupMeta })` + the query types. `dist/` + `public/wapi.js` rebuilt + re-synced.
- [✓ 3.127.0] **QE-C: the I3 anti-tests** (the seatbelt — the most important bite) — a reader can't see/infer a group's metadata they can't read (the value is NULL; the sort reveals no rank; the NULL pattern reveals no count; a private group's `member_count` is absent to a non-reader, present to a member); **the raw tables are still rejected** (a direct `group_members` reference is rejected even with `withGroupMeta` on — the wall holds); the completeness guarantee (the AST walk catches a raw-table reference inside a caller CTE / subquery, opt-in on).
- [~] **QE-D: the migration** — the D0 people read + the D3 groups browser expressed as queries using `group_meta` (sort by legit follower/member count); the node-function versions (if shipped first) are replaced (same semantics, now app-writable). **Gated on QE-A/B/C (done).** **Handed off to the `discover-reorg` lane** (this lane owns the query engine + SDK, not the social data layer the migration rewires) — filed there as **QE-D1 (groups browser, clean) + QE-D2 (people read, blocked on a group-scoping design decision)**. See the `discover-reorg` lane for the full context + query expressions.

### Lane: global-search (Phase 2)
**Owns:** `marketing/web10-social/src/components/Social/Layout.tsx` (the top bar / header search slot), `marketing/web10-social/src/components/Search/` (new), `marketing/web10-social/src/data/search.ts` (new). **Plan:** `knowledge/strategy/global-search.md` (operator pass, 18.09.2026 — "maybe good to have an everything search on the topbar, people groups, everything" + "on insta you can search profiles, so we are trying to get parity on that"). The **top-bar everything-search** (people + groups + posts) is the *front door*; the Discover subtabs (`discover-reorg.md`) are the *browsers* you land on via "see more" (`?q=`). The social app has **no top bar today** (desktop sidebar-only, mobile 56px header) — this is a new chrome surface. **Anon vs signed-in is a behavior split, not a data split** (the operator's correction) — same card shape, same principal-based read; anon just can't follow/join (the app's existing sign-in idiom). This lane does NOT touch `DiscoverScreen.tsx` (discover-reorg) — the hand-off is the `?q=` deep link (a contract, not a shared file).

- [✓ 3.118.0] **S1: the search surface in the chrome** (`Layout.tsx` + new `src/components/Search/GlobalSearch.tsx`) — the **expanding-icon** state machine (`icon (rest) → expanded field → results → collapse`): the slim search icon in the top bar / header (always there, like the logout — the bar stays slim at rest so it never crowds a per-screen sticky header), the **animate-open** to a full-width field, the **animate-collapse** on done/Escape/X, the debounced (400ms) query state, and the results container (**dropdown on desktop / full-screen view + X on mobile**). Renders the shell + a "type to search" state (no results logic yet). A new `globalSearch.test.tsx` (the icon is on every screen; tap expands the field; Escape/X collapses; the query is debounced; mobile renders the full-screen results view, not a dropdown). **Sequence S1 before discover-reorg D4** (both touch `Layout.tsx` — S1 adds the top bar, D4 only edits the nav item arrays).
- [✓ 3.124.0] **S2: the fan-out + results + "see more"** (new `src/data/search.ts` + `GlobalSearch.tsx`) — `globalSearch(query)` (the three-way fan-out: people + groups + posts, merged, top ~5 each); the dropdown sections (People/Groups/Posts); tap → navigate (person → `/u/…`, group → `/groups/…`, post → the post deep link); "see more" → the Discover browser with `?q=` (people → `/discover?tab=people&q=…`, groups → `/discover?tab=groups&q=…`, posts → `/discover?q=…`). `globalSearch.test.ts` (the fan-out merges + caps; the link targets). **People scale gated on `discover-reorg` D0** (v1 floor: `fetchPeople`).
- [✓ 3.146.0] **S3: the desktop field is always expanded** (`GlobalSearch.tsx`) — operator pass (23.09.2026, two screenshots): "i want the search bar to look like this all the time … like its clicked in form, because i think that is alot more informative to the user." The desktop top bar permanently renders the full field (glyph + "Search people, groups, posts…" placeholder + X) instead of the collapsed icon; `open` now scopes to the **results dropdown** (focus → open; Escape / click-outside / navigate → close, 150ms fade); the X **clears the query** (keeps focus in the field) instead of collapsing; the typed query persists across close/reopen and navigation (debouncedQuery always tracks the field). **Mobile unchanged** (icon → full-screen results view). `globalSearch.test.tsx` re-pinned (field always present, no trigger; focus opens; Escape / click-outside close keep the field; X clears; row-navigation closes the dropdown, keeps the field; Layout counts one trigger — mobile — plus the desktop field). 906 web10-social tests green, `tsc` clean, harness capture green at desktop + 375.
- [✓ 3.160.0] **S5: Enter stays on the active tab + the labels are "Trending and People"** (`GlobalSearch.tsx` + a small `DiscoverScreen.tsx` chip) — operator pass (24.09.2026, the search dropdown screenshot): "i would like if i hit enter that the search happens whether on posts or the people tab, like it searches / stays on both. then you can X it from either one in the discover! also that text is just wrong, it is supposed to be Trending and People, not Posts and People". **(1)** Enter / the CTA navigate to Discover **with the query, staying on the active tab** (`/discover?q=…` → Trending; `/discover?tab=explore&q=…` → People) instead of always forcing `?tab=explore` — the active tab is read from the router location. **(2)** The **query chip (with its X)** now renders on the **Trending tab too** (`DiscoverScreen.tsx`, the discover-reorg lane's file — a one-line hand-off, not a shared edit) so the search can be X'd from either tab. **(3)** The **labels are corrected** — the mode toggle reads **Trending | People** (was Posts | People & Groups), the posts section header reads **Trending** (testid `global-search-section-trending`), the CTA reads "See all results in Discover", and the Discover tab row's posts tab is labeled **Trending** (the `trending`/`explore` ids + `?tab=` deep links are unchanged). `globalSearch.test.tsx` re-pinned (Enter → `/discover?q=…`; a new Enter-on-People-tab case keeps `?tab=explore`; the CTA → `/discover?q=…`; the section testid rename) + `discoverScreen.test.tsx` +3 (the Trending label; the query chip on the Trending tab; X clears `?q=`). **(4) The "People" rename override (both apps):** a parallel PR (#1005, `discover-ia-consistency`, 3.158.0) had just merged renaming the tab to "Profiles"; the operator overrode it (24.09.2026: "trending People makes more sense") — the tab is relabeled **People** in the social app + the marketing Discover (the two apps stay in sync), tab *ids* unchanged. Merged + renumbered against #1005 (this entry was 3.158.0, now 3.160.0). 946 web10-social + 271 marketing-ui tests green, `tsc` clean, harness captures green at desktop + 375. **KB:** `global-search.md` S5 + the pinned URL contract; `discover-reorg.md` current state (two tabs, Trending | People; the query chip on both); `discover-ia-consistency.md` (the 24.09 "People" name override note).
- [✓ 3.161.0] **S6: the Facebook-style search — the unclipped wide dropdown + the Profiles/Groups subtabs** (`GlobalSearch.tsx` + `Layout.tsx` + `DiscoverExploreTab.tsx` + `DiscoverScreen.tsx`) — operator pass (25.09.2026, the search dropdown + Discover screenshots + two Facebook references): "that search bar looks chopped though" + "this dropdown looks terrible too" + "facebooks looks much better" + "in the subtab of people groups and profiles instead of groups and people! but people should be the logo of the two people … in the subtab, it should be profiles and groups, i.e. profiles is individual people profiles. and should be just one person logo!". **(1) The unchopped field** — the sidebar's `overflow-hidden` (a decorative-glow clip) was clipping the dropdown to the 256px sidebar; the glow now clips in its own inner container and the dropdown is a **wide panel** (`w-[26rem]`, `rounded-xl`) that overflows into the content, Facebook-style. The field is a **rounded-full pill** (h-10, `bg-elevated`), the placeholder shortens to "Search web10" (it fit), and the **X only renders when there's a query** (an empty field has nothing to clear). **(2) The Facebook-style rows** — each result row is a round glyph chip (person / hash / search) + the title + a subline; **person rows carry the account's own avatar on the RIGHT** (the Facebook suggestion row); the mode toggle is a slim segmented control (no chunky pills). **(3) The subtabs are Profiles + Groups** — the Explore tab's visibility chips read **Profiles** (the ONE-person glyph — individual profiles) + **Groups** (hash), and the section header reads **Profiles**; the **People top tab carries the TWO-people glyph** (it holds profiles + groups — "people should be the logo of the two people"). Tab *ids* + `?tab=`/`?show=` deep links unchanged. `globalSearch.test.tsx` re-pinned (the X is query-gated; the pill + wide-panel assertions) + `discoverScreen.test.tsx` +1 (the two-people People tab vs the one-person Profiles chip). 957 web10-social tests green, `tsc` clean, harness capture green at desktop + 375. **KB:** `global-search.md` S6 + `plan.md` S6.
- [✓ 3.162.0] **S7: the search is people-first — opposite to Discover, live as you type, no "see results" hop** (`GlobalSearch.tsx` + a small `DiscoverScreen.tsx` / `DiscoverExploreTab.tsx` padding fix) — operator pass (25.09.2026, the search dropdown + Discover screenshots): "for search purposes, people should be selected firstly in the search, people first, opposite in discover in discover the trending tab is first, and shouldnt have to hit see results! the people tab should pull up automatically with the search as you type as well as showing the small results" + "little things the controls have no padding in the web10 social discover" + "on trending and people tabs!". **(1) People-first** — the dropdown's default mode is **People** (people + groups, live as you type), **opposite to Discover** (where Trending is the first tab — the search is the front door for finding accounts, Discover is the posts hero). The reads load together on the debounced query (the mode only picks which sections are shown), so a tab flip is instant (no re-skeleton). (2) **The "see all" lands where the small results came from** — Enter / the CTA in People mode → the People tab (`/discover?tab=explore&q=…`); in Trending mode they keep the S5 active-tab hand-off. (3) **The control-row padding** (cross-lane, discover-reorg's files — a one-line hand-off, not a shared edit) — the Trending tab's query chip / KnobRack / topic chips / Home|Hot Gossip toggle rows + the People tab's query chip / Profiles|Groups toggle rows were flush at `md:px-0` (3.155.0's "video wall is the hero" pass over-applied the full-bleed to the control rows); they now carry the content column's desktop gutter (`md:px-4 lg:px-6`), mobile stays `px-4`. `globalSearch.test.tsx` re-pinned to the people-first model + `discoverScreen.test.tsx` +2 (the control rows keep the desktop gutter, both tabs). 959 web10-social tests green, `tsc` clean, harness capture green at desktop + 375. **KB:** `global-search.md` S7 + `plan.md` S7.

### Lane: discover-reorg (Phase 2)
**Owns:** `marketing/web10-social/src/components/Discover/` (the shell + the subtab components), `marketing/web10-social/src/components/People/PeopleScreen.tsx`, `marketing/web10-social/src/components/Groups/GroupsScreen.tsx` (the directory move + the redirect), `marketing/web10-social/src/data/{people,groups}.ts`, `marketing/web10-social/src/App.tsx` (the redirects), `marketing/marketing-ui/src/pages/Trending.tsx`, `api/` + `sdk/` (the D0 people read only). **Plan:** `knowledge/strategy/discover-reorg.md` (operator pass, 18.09.2026). Discover becomes *the* discovery surface — **Posts | People | Groups** subtabs (`?tab=`, posts bare) as **browsers** (paged + sortable). **Search is NOT here** — it's the top-bar everything-search (`global-search.md`); each subtab is reached *with a query* via "see more" (`?q=`). The Groups tab keeps only My Groups; the People tab is retired (`/people` → `/discover?tab=people`); the marketing `/trending` mirrors the three (the dead `PATCH /discover/users` row is replaced by a real read). **The subtabs are separate component files** (D1 owns the shell, D2 owns `DiscoverPeopleTab.tsx`, D3 owns `DiscoverGroupsTab.tsx`) so the bites don't share a file. **Open decision (resolve before D2/M1):** the people pool — (a) widen the client pool + page client-side (v1 floor) vs (b) a node "public users" read (D0 — principal-based, the same read the marketing site needs; the dead `/discover/users` call becomes real). Leaning (b). **The People browser is the highest-risk, lowest-certainty bite** (its value is contingent on node population — hollow on a small node).

- [✓ 3.119.0] **D0: the public "people" read** (keystone, cross-lane `api/` + `sdk/`; only if the decision → (b)) — a **server-side composition** (one round-trip, not N client reads): `list_users()` (exists) → batch the profile faces → `_get_group_member_counts` on the followers groups (the **unspoofable** membership aggregate — a user can't fake their follower count, it's `count(group_members)`, not a stored field) → rank by followers → `limit`/`offset`. Principal-based (anon = the public subset, signed-in = more — the *same* card shape, different permissions). A user with no public profile is absent, not shown-with-fallback (I3). API floor test (I3 — no private data; anon vs signed-in; the count is a membership aggregate) +   SDK passthrough. **Gates D2 (people data) + M1.** If the decision → (a), skip
  D0 and D2 pages the widened client pool. **Today** this is a node function
  (the query engine is walled off from contract tables — `safe_query.
  py:60-78`); **once the `engine-group-metadata` keystone (QE) lands**, D0 is
  expressed as a query using the `group_meta` join (sort by legit follower
  count) — same semantics, now app-writable (QE-D, the migration). Don't write
  it as a `w.query(…)` before QE lands.
- [✓ 3.120.0] **D1: the Discover subtab shell** (`src/components/Discover/DiscoverScreen.tsx`) — the Posts | People | Groups tab row (`?tab=`, posts bare) + it holds `?q=` and passes it to the active subtab (no per-subtab search field — search is the top bar). Mounts `DiscoverPeopleTab` / `DiscoverGroupsTab` (designed placeholders until D2/D3 land). The "People to follow" rail is retired from Posts. **Posts view is byte-identical** (no regression). `discoverScreen.test.tsx` (tab switch + `?tab=` + `?q=` pass-through + Posts no-regression).
- [✓ 3.122.0] **D2: the People browser** (new `src/components/Discover/DiscoverPeopleTab.tsx` + `PeopleScreen.tsx` + `people.ts`) — the People cards as a Discover subtab over the D0 read: sorts (`?sort=`: Popular + A–Z — mutuals dropped, the D0 read returns the unspoofable follower count, not mutuals) + **pagination** ("view more", the D0 `offset`) + a `?q=` filter (name/handle). **"It's quiet here" state**: if the node has fewer than ~10 public people, show a designed "It's quiet here" empty state instead of a degenerate ranked list. `people.test.ts` + `peopleScreen.test.tsx` re-pinned + new (sorts; paging appends; `?q=` filters; the quiet-here state under ~10 people). **Gated on D0** (or the (a) pool).
- [✓ 3.128.0] **D3: the Groups browser** (new `src/components/Discover/DiscoverGroupsTab.tsx` + `GroupsScreen.tsx`) — the D53 directory as a Discover subtab: cards (face+name+owner+members+join policy+tags) + `?tag=` (deep-linkable topic chips) + **pagination** (the `offset` threaded through the directory read; "view more" appends the next page, hides when exhausted) + a `?q=` filter (name/owner/tags, query chip). **All states designed** (skeleton / error+retry / no-groups / no-results). The Groups tab's Discover tab is removed (`/groups` = My Groups only; the empty-state CTA → `/discover?tab=groups`); `/groups?tab=discover` → `/discover?tab=groups` (a render-time redirect carrying `?q=`/`?tag=` over). `groupsScreens.test.tsx` re-pinned + a new `DiscoverGroupsTab` suite (paging appends + hides when exhausted; the redirect; the filters; the states). **No node change** — the D53 read already paged (`readGroupDirectory(limit, offset)`); this threads the `offset`. The `groups.ts` data layer is untouched.
- [✓ 3.130.0] **D4: nav + redirects** (`Layout.tsx`*, `App.tsx`) — People removed from the sidebar + More sheet; `/people` → `/discover?tab=people`; `/groups` = My Groups only. `socialScreens.test.tsx` (nav shape; the redirects). *`Layout.tsx` is also touched by `global-search` S1 — **sequence S1 before D4** (S1 adds the top bar, D4 only edits the nav item arrays) so the two lanes don't collide.
- [✓ 3.139.0] **D6: Explorer rename + Groups tab retirement + My Groups in the subtab + Following/Followers sub-routes** (operator pass, 22.09.2026) — the operator: "we should get rid of the groups tab if it is in the discover tab anyways! … lets rename discover tab to explorer tab! that language makes more sense" + "same compass icon though :)" + "yeah like my groups is in explorer, and people you are following and your followers are in the people tab! yupp like sub routes in the explorer tab." **Built:** (1) **Discover → Explorer** (`Layout.tsx` + `DiscoverScreen.tsx`) — the nav label + the screen header + the subtab aria-label all rename; the compass icon is unchanged. (2) **Groups tab retired from the nav** (`Layout.tsx`) — the Groups row is removed from the desktop sidebar + the mobile More sheet (it lives in the Explorer/Groups subtab now). (3) **My Groups in the Groups subtab** (`DiscoverGroupsTab.tsx`) — a "My Groups" section above the public directory: the user's community groups (`getMyCommunityGroups`) with their faces (`readGroupIdentity` + `resolveMediaRefs`), rendered as `MyGroupRow` cards (banner + avatar + name + member count + join policy + Leave button for non-owners); skeleton / error / empty states. (4) **Following/Followers sub-routes in the People tab** (`DiscoverPeopleTab.tsx`) — a secondary tab row (`?personTab=`: `following` | `followers` | `discover` (bare)): "Following" lists the people you follow (`readFollows` → resolved profiles), "Followers" lists the people who follow you (`listFollowers` → resolved profiles), "Discover" is the existing public directory (D0 read). Each person renders as a `PersonCardRow`. **Tests:** `discoverScreen.test.tsx` +4; `socialScreens.test.tsx` re-pinned. 915 web10-social tests green, `tsc --noEmit` clean.
- [✓ 3.140.0] **D7: the sidebar gets lean — notifications to the top bar, monetization to the More popover, "New post" retired (the composer lives in the profile + the explorer + the feed)** (operator pass, 22.09.2026) — the operator: "notifications could go in the top right next to the other thing on the top right in the topbar! and the monetization stuff could go in the more tabs. also new post doesnt need to be a thing, that should go in your profile, you can make a new post from your profile, from feed, or from the explorer too! three places to post! not on the side bar." **Built:** (1) **Notifications to the top bar** (`Layout.tsx`) — the notifications bell + unread badge move from the desktop sidebar to the desktop top bar, next to the account row. (2) **Monetization to the More popover** (`Layout.tsx`) — the Monetization + Node Monetization rows leave the desktop sidebar and move into the More popover (they were already in the mobile More sheet). (3) **"New post" retired from the nav** (`Layout.tsx`) — the `nav-new-post` sidebar row + the `new-post-button-mobile` top-bar button are removed. (4) **The composer in three places** — the **feed** (existing `FeedRoute` composer), the **profile** (a new `PostComposer` at the top of the owner's Posts tab, `UserProfileScreen.tsx`), and the **explorer** (a new `PostComposer` at the top of the Explorer/Posts subtab, `DiscoverScreen.tsx`). **Tests:** `socialScreens.test.tsx` +2 (the notifications bell is in the top bar; the "New post" button is absent) + the three monetization tests re-pinned. 917 web10-social tests green, `tsc --noEmit` clean.
- [✓ 3.148.0] **D8: Monetization back to a permanent desktop sidebar row** (operator pass, 23.09.2026) — "lets take monetization out of the more menu on desktop, there is space for it!" **Built (`Layout.tsx`):** (1) `monetizationItem` → `sidebarNavItems` (after Messages) — permanent desktop sidebar row for every signed-in user; mobile unchanged (Monetization stays in the mobile More sheet). (2) The Monetization button leaves the desktop More popover — the popover now carries only the admin-only Node Monetization row (`useNodeAdmin`-gated) + the coming-soon list; the divider above "Coming soon" renders only when the Node row is present. (3) `isActive('/monetize')` = `isMonetizeCreator` — the sidebar row highlights on the Creator section; on `/monetize?tab=node` only the popover's Node row highlights (never both). **Tests:** `socialScreens.test.tsx` re-pinned (Monetization is a sidebar row without opening More; the More popover contains no Monetization row; the two highlight tests assert sidebar row vs popover Node row). 906 web10-social tests green, `tsc --noEmit` clean. **KB:** `monetization.md` "The Nav" updated.
- [✓ 3.141.0] **M1: `/trending` mirrors the three** (`marketing-ui`) — Posts | People | Groups subtabs on the public ledger (anon). The dead users row → the D0 read. No node change (D0 + D53 already existed); no marketing-api change (the marketing-ui reads the node directly, the existing pattern). **Built:** (1) the subtab row (`Trending.tsx`) — Posts | People | Groups (`?tab=`, posts bare). (2) the People subtab (`TrendingPeople.tsx`) — the D0 read (anon) + people cards. (3) the Groups subtab (`TrendingGroups.tsx`) — the D53 read (anon) + group cards. (4) the dead code fix — the `PATCH /discover/users` call → the D0 read. (5) three new funnel events. **Tests:** `Trending.test.tsx` +8. 272 marketing-ui tests green, `tsc -b` clean. **Finishes the discover-reorg lane.**
- [✓ 3.153.0] **D9: the people directory is populated from birth + the Explore tab's People / Groups visibility toggle** (operator pass, 23.09.2026 — "this should show some pages of people though right?" + "if there were a ton of people, wouldn't the groups get drowned out? … a fat chunky people icon, and a groups icon … both selected, neither, or one or the other") — two follow-ups to D2 + the D80 Explore tab. **(1) The bug:** the D0 directory only lists users with a readable `profile` face; the face was only ever written on a manual profile save, so a fresh node's directory was empty ("It's quiet here") despite 54 users. `ensureProfile()` (`src/data/profile.ts`) seeds a minimal public face (`display_name` = username) on sign-in when none exists — idempotent + non-clobbering; wired into `App.tsx` sign-in. Client-side (D60). **(2) The UI:** a chunky **People | Groups** icon+label chip row on `DiscoverExploreTab` — each section shows/hides independently (both / one / neither; neither = a neutral empty state), deep-linkable via `?show=`; the people-only sort row hides with the section. **Tests:** `profile.test.ts` +4; `discoverScreen.test.tsx` +5. 916 web10-social green, `tsc` clean.
- [✓ 3.159.0] **D10: the People browser gets its "N mutuals" back (derived client-side from the generic membership primitive — the node stays generic, D60) + the "easy filters" (People: All | Following | Mutuals | Followers; Groups: All | My Groups | Discover) + the "It's quiet here" root-cause fix** (operator pass, 24.09.2026 — "why is people tab saying that, it should show the people on the web10 node … not just people you follow" + "there used to be a thing that said how many mutuals you had, and showed a lot more people!!!" + "would be cool to be able to see your followers + who you are following! like some easy filters" + "same thing with groups filters" + "could you implement this generically … get rid of this backend destruction you did?"). **(1) The "It's quiet here" root cause:** the D0 directory (`list_public_users`) only listed users with a readable `profile` face — a faceless user was *absent*; `ensureProfile()` (D9) only seeds a face on sign-in *after* it shipped, so pre-existing users were absent → empty directory. Fix (node, generic): a faceless user is now **listed with a username-only face** (a username + follower count is public identity — the share preview, 3.90.0, already exposes it; the I3 read-gate is still the access boundary). The directory is populated from birth. **(2) Mutuals, CLIENT-side (the node stays generic, D60):** `mutuals(X) = |followers(X) ∩ my-following|` derived in `src/data/people.ts` from the **generic membership primitive** — a user's followers = `getGroupMembers(followersGroupId(X))` (the same read the profile's Followers/Following screens + the old 3.102.0 version use), intersected with the reader's following set. One `getGroupMembers` per card, bounded by page size; anon/no-follows → 0. The node returns only the universal primitives (user, follower count, profile face) — it never computes an app's social signal (reverses the first cut that baked `_reader_following_set` + `_mutual_counts` into the node). `PersonCard` carries `mutuals`; `PersonCardRow` shows "N mutuals · M followers" (only when > 0). **(3) The "easy filters":** the Explore tab's People section gains a **People filter chip row** (`?personFilter=`): All (bare URL, the directory) | Following | Mutuals (directory ∩ mutuals > 0) | Followers. Following/Followers are the reader's own graph (the directory can't express them) → new `fetchMyFollowingCards()` (`byUserGroups`-backed, D80 M1) + `fetchMyFollowersCards()` (`getGroupMembers` on your own followers group), face-enriched, cached; Mutuals reuses the directory. The Groups section gains a **Groups filter chip row** (`?groupFilter=`): All (bare URL, the directory) | My Groups (`getMyCommunityGroups` + `MyGroupRow` + Leave) | Discover (the directory). Both chip rows sit in the section header, tokens-only, deep-linkable. **(4)** the profile's clickable Following/Followers counts (3.151.0, D80) already exist — this pass adds the same *filters* to the discovery surface. **Tests:** node `test_people_directory.py` re-pinned (faceless user listed; **no `mutuals` field** — node stays generic; I3 anon-vs-signed-in unchanged); `people.test.ts` re-pinned + new (mutuals = followers(X) ∩ my-following, client-side; 0 when following no one, no membership read); `discoverScreen.test.tsx` +7; harness `mock-data.ts` + `mutuals` + the two graph-read stubs. 943 web10-social + 271 marketing-ui + 131 sdk + 1015 node green, `tsc` clean. **I3 note:** the faceless-user listing does not loosen the access boundary (the read-gate is unchanged).
- [ ] **D5: the screenshot pass** (`marketing/web10-social/screenshots/`) — `discover-{posts,people,groups}-{desktop,375}.png` + the marketing trending captures; the screenshot test (design.md §1) on every subtab; the harness seeds (group faces + people cards + posts) so all three subtabs render with content. **Gated on D1–D4.**
- [ ] **QE-D: the group_meta migration** (the `engine-group-metadata` keystone's payoff — express D0 + D3 as **queries using `group_meta`**, replacing the node-function versions; same visibility semantics, now app-writable + sortable by legit audience size). **Gates: the keystone is done — QE-A 3.123.0 / QE-B 3.126.0 / QE-C 3.127.0, all merged.** The query engine now exposes `group_meta` (`member_count`/`join_policy`/`discoverable`, NULL-out-and-sort-last, I3-gated) via `w.query(sql, { withGroupMeta: true })`. Split into two bites — **D3 is clean, D0 is blocked on a design decision**:
  - **QE-D1: the groups browser (D3) as a query** (`src/data/groups.ts` + `DiscoverGroupsTab.tsx`) — replace `readGroupDirectory` (the `GET /v3/groups/directory` node function) with `w.query('SELECT gm.group_id, gm.member_count, gm.join_policy, gm.discoverable FROM group_meta gm WHERE gm.discoverable = 1 ORDER BY gm.member_count DESC NULLS LAST LIMIT <n> OFFSET <o>', { withGroupMeta: true })`. The query gives the sortable metadata + `group_id`; the client derives slug/owner from the group_id (the `_parse_group_id` shape) and fetches the face separately (as it already does). **Gap:** `permission_summary` (needs `roles`, which `group_meta` doesn't expose) — either drop it from the card or keep the node read for that one field.
  - **QE-D2: the people read (D0) as a query** (`src/data/people.ts` + `DiscoverPeopleTab.tsx`) — replace `w.listPeopleDirectory()` (the `POST /v3/users/directory` node function) with `w.query('SELECT pr.author_key AS author_key, pr.body AS profile_body, gm.member_count AS follower_count FROM profile pr JOIN group_meta gm ON pr.group_id = gm.group_id ORDER BY gm.member_count DESC NULLS LAST, pr.author_key LIMIT <n> OFFSET <o>', { withGroupMeta: true })`. **BLOCKED on a design decision:** the query engine scopes to the reader's groups (or explicit groups) — there is no "all groups" mode — but D0 needs *all public users*. As-is the query returns *people you already follow + self* (signed-in) or nothing (anon: `get_user_groups(anon)` ≈ empty) — the wrong set for a discovery surface, and it breaks the anon marketing read. Needs a query-engine change (a way to scope to all public users' followers groups / a "public" group scope) before D0 can be a clean query. **Decide the scoping approach first** (query-engine extension vs. keep the node function for D0 and migrate only D3).
   **Acceptance bar:** the people read + groups browser sort by legit audience size via the `group_meta` join; the node-function versions are retired (or explicitly kept with a note); existing social tests green; `tsc` clean; screenshots per design.md §12. **Handed off from the `engine-group-metadata` lane** (it owns the query engine + SDK, not the social data layer).

### Lane: discover-ia-consistency (Phase 3)
**Owns:** `marketing/web10-social/src/components/Discover/DiscoverScreen.tsx` (the tab row labels/icons + the desktop padding), `marketing/web10-social/src/components/Discover/DiscoverExploreTab.tsx` (the People/Groups section icons), `marketing/web10-social/src/components/Social/Layout.tsx` (the keys-only mark + the search in the sidebar + the top-bar tabs), `marketing/web10-social/src/components/Search/GlobalSearch.tsx` (the sidebar anchor), `marketing/marketing-ui/src/components/Navbar.tsx` (Trending→Discover, drop Groups), `marketing/marketing-ui/src/pages/Trending.tsx` (Trending|Profiles + the Profiles browser), `marketing/marketing-ui/src/components/TrendingPeople.tsx` + `TrendingGroups.tsx` (retired in favor of the shared browser), `marketing/shared/discover/` (the extracted PersonCard + GroupCard). **Plan:** `knowledge/strategy/discover-ia-consistency.md` (operator pass, 23.09.2026, ~13 screenshots). **The fixed point:** Discover reads the same in the social app and the marketing site — **Trending | Profiles** tabs (both apps), search in the **sidebar** (desktop) / header (mobile), the **Profiles** browser mashes **People + Groups** (mirrored verbatim on marketing), the marketing nav's "Trending" is renamed **Discover**, and the cards come from the shared `@web10/discover` package so the two apps can't drift. **No node change (D60 — all client-side).** **Operator sign-off GIVEN (23.09.2026)** on the three flagged decisions: (1) the Trending|Profiles tabs in the *global* top bar (Discover-only), (2) dropping the marketing **Groups** nav row + the standalone `/groups` page (redirects to `/trending?tab=profiles`), (3) "Profiles" as the tab name (it holds People *and* Groups). **Shipped in 3.158.0.** **Sequencing:** C1 (shared cards) gates C3 (marketing mirror); A (social Discover) + B (social chrome) + C (marketing) are three independent workspaces; sequence B3 after A1 (both touch the Discover tab row).

- [✓ 3.158.0] **A1: `Posts` → `Trending`, `People` → `Profiles` (the social tab row)** (`DiscoverScreen.tsx`). The `trending` tab's label `Posts` → **Trending** (icon stays `Flame`); the `explore` tab's label `People` → **Profiles** (icon stays `User`, one person). Tab *ids* unchanged (`trending`/`explore`) so `?tab=` deep links + the `?tab=explore&q=` search hand-off keep working — only the visible label moves. `data-testid`s unchanged. `discoverScreen.test.tsx` re-pinned (the two label assertions).
- [✓ 3.158.0] **A2: the People section icon = two-overlapped; the Profiles tab = one-person** (`DiscoverExploreTab.tsx`). Verify the "swap" the operator asked for: the **Profiles tab** carries the one-person glyph (`User`), the **People section** carries the two-overlapped glyph (`Users`); Groups keeps `Hash` ("groups is just fine"). Icon/label only, no behavior change.
- [✓ 3.158.0] **A3: side padding on the Discover screen** (`DiscoverScreen.tsx`). The operator: "no padding at all on the sides." The content wrappers run `md:px-0` (full-bleed, the 3.153.0 "video wall is the hero" pass). Restore a desktop gutter — `md:px-4 lg:px-6` on the content column, keeping the 1/2/3-col grid; mobile (375px) stays full-bleed. Screenshot at 1440 + 375 to confirm the gutter reads as designed, not a regression of the "fill the viewport" pass.
- [✓ 3.158.0] **B1: the keys mark only (no wordmark) in the sidebar** (`Layout.tsx`). The desktop sidebar's top row drops the "web10" text, keeping just the `/keys-mark.png` glyph (Facebook-style). The mobile header keeps the full lockup. A `compact`/`markOnly` `Wordmark` variant, not a deletion. Layout wordmark test re-pinned.
- [✓ 3.158.0] **B2: the search field moves from the top bar to the sidebar (desktop)** (`Layout.tsx` + `GlobalSearch.tsx`). The desktop `<GlobalSearch variant="desktop" />` leaves the top bar and renders at the top of the sidebar (under the keys mark, above the nav rows). The top bar keeps the bell + account row. The results dropdown anchors to the sidebar (it already positions `absolute top-full` off its wrapper). The always-expanded desktop form is unchanged (`global-search.md` S3). Mobile untouched (icon → full-screen results view). `globalSearch.test.tsx` + the Layout cases re-pinned (the desktop field is in the sidebar, not the top bar).
- [✓ 3.158.0] **B3: the Trending | Profiles tabs move into the top bar (desktop, Discover screen only)** (`Layout.tsx` + `DiscoverScreen.tsx`). The Discover screen's sticky tab row moves into the top bar's left side when the active screen is Discover; on non-Discover screens the top bar shows only the bell + account. `Layout.tsx` reads the route and, on `/discover`, renders the two tabs; `DiscoverScreen.tsx` drops its own sticky tab row on desktop (keeps it on mobile). The tabs stay `?tab=`-driven. **Flagged to operator** (couples the top bar to the Discover route). `socialScreens.test.tsx` + `discoverScreen.test.tsx` re-pinned. **Sequence after A1** (both touch the tab row).
- [✓ 3.158.0] **C1: extract the People + Groups cards into `@web10/discover` (the keystone)** (`shared/discover` + both apps). Move the social `PersonCardRow` (banner+avatar+name+@handle+followers+Follow) + `DiscoverGroupCard` (banner+avatar+name+owner+members+join-policy+Join) into the shared package as **remote-mode** components (link-out to the social app, no in-app follow/join — the marketing is anon). The social re-imports them (interactive mode) — one source. `index.ts` exports them; both apps' `@source` already scans the package (3.111.0). **Gates C3.**
- [✓ 3.158.0] **C2: the marketing nav — `Trending` → `Discover`; drop `Groups`** (`Navbar.tsx` + `App.tsx` + the three footer/Join/Exporter links). `{ path: '/trending', label: 'Trending' }` → **Discover**; remove the `{ path: '/groups' }` row (groups fold into Discover). The `/groups` route redirects to `/trending?tab=profiles`. The operator: "this should be home discover app store." Navbar/route tests re-pinned. **Flagged to operator** (dropping the Groups nav row + the standalone page).
- [✓ 3.158.0] **C3: the marketing Discover page = the social Discover (Trending | Profiles)** (`Trending.tsx`). The tab row becomes **Trending | Profiles** (chunky, icon+label pills, matching the social) — was `Posts | People | Groups`. The `posts` tab → **Trending** (flame); the `people` tab → **Profiles** (one-person); the `groups` tab is **removed** (groups fold into Profiles). The **Profiles** tab renders the **mashed People + Groups browser** (the social Explore tab's shape: the `?show=` People/Groups toggle — People = two-overlapped, Groups = hash — People first then Groups, each paged, using the **shared** cards from C1). The **Trending** tab is unchanged (already mirrors the social). `?tab=` values: `posts` (bare) / `profiles` (was `people`) / `groups` (retired → `profiles`). `Trending.test.tsx` re-pinned. **Gated on C1.**
- [✓ 3.158.0] **C4: marketing card parity (the "super old" groups render is gone)** (`TrendingPeople.tsx` + `TrendingGroups.tsx` retired). With C1 + C3, the marketing People + Groups cards are the shared banner+avatar shapes (the same as the social Discover). The old `TrendingGroups` flat card + the standalone `/groups` `GroupDirectory` card are retired. Confirm the marketing People card matches the social `PersonCardRow`.
- [✓ 3.161.1] **C5: the marketing Discover search + X + card width catch up to the social app (the "it's just different" drift)** (operator pass, 25.09.2026, four screenshots: "the search is just not working like on the social app" + "x ing out my search doesnt work" + "the groups and profiles are rendering so small compared to the social app" + "the social app is just different"). Three client-side roots, all in `marketing-ui` (no node change, D60): **(1) the search was a phantom endpoint** — `fetchSearchResults` called `PATCH /discover/search`, a route that never existed (every search 404'd into "Search unavailable"); the node has no multi-entity search endpoint (the documented v1 floor, `global-search.md`), so the marketing now mirrors the social `searchPosts` — a new `searchDiscoverPosts` (`FeedPreview.tsx`) reads the same 200-post board pool + filters text/author/tag client-side (`#tag` → tags only). **(2) the X was a no-op on the Profiles tab** — the query was `useState`-only, but the Profiles query-chip X clears `?q=` from the URL; the query is now URL-driven (`?q=`, the deep-link rule, the social Discover's own model) so the field's X + the chip's X + a deep link all flow through one debounced `?q=` effect. **(3) the cards rendered tiny** — the `ProfilesBrowser` ran the shared cards through `grid-cols-1 sm:grid-cols-2` (half width on desktop); the social renders them single-column, so the four lists are now `space-y-3`. `Trending.test.tsx` +5 (no `/discover/search` call; X clears field + `?q=`; `?q=` deep link pre-fills + searches; the Profiles chip X clears the field — the operator's exact bug; the lists are single-column) + the 17 bare `<Trending />` renders wrapped in a `MemoryRouter`. 276 marketing-ui green, `tsc -b` clean.
- [✓ 3.158.0] **D1: keep the KB true** (`discover-reorg.md` — the tabs are now `Trending | Profiles`, groups is a section not a tab; `global-search.md` — the search's home is the sidebar on desktop; this doc's ticks). A CHANGELOG line (feature → minor bump) + the lane ticks. `AGENTS.md` only if a stack/auth fact changed (it doesn't — pure UI).

### Lane: membership-visibility (D80)
**Owns:** `api/app/v3/services/clickhouse.py` (the `membership_visibility` contract field + the `by-user` read + the `members/list` gate), `api/app/v3/endpoints/groups.py` (the `by-user` route), `api/app/v3/services/safe_query.py` + `api/app/v3/endpoints/query.py` (the `memberships` CTE, M2 — still open), `sdk/src/v3.ts` (`byUserGroups` + the `withMemberships` query flag), `marketing/web10-social/src/data/{groups,follows}.ts`, `marketing/web10-social/src/components/Bio/` (the profile list screens), `marketing/web10-social/src/components/Discover/` (the DiscoverScreen Trending|Explore rework + `DiscoverExploreTab` + the DiscoverPeopleTab Following/Followers removal), `marketing/web10-social/src/components/Social/Layout.tsx` (the Discover rename + the nav reorder + the Profile-name item), `marketing/web10-social/src/components/Search/GlobalSearch.tsx` (Enter opens Explore). **Plan:** `knowledge/strategy/decisions.md` D80 (operator pass, 23.09.2026). **Group membership visibility is group policy:** the contract gains `membership_visibility` (`public` | `hidden`); a membership row `(G, M)` is publicly enumerable **iff `G.membership_visibility == public`** — one rule, every read surface. web10-social sets it per group type at creation (followers/community = `public`, dm/close-friends = `hidden`). The social graph (who follows whom) is public (the thesis — "your number here is real", "a real network, not a dark forest"); DM relationships are private (DM groups are `hidden`). **The IA fixed point (the anti-limbo rule, locked this pass, revised by the operator 23.09.2026):** the social graph is a property of a *person* → it lives on the *profile* (clickable follower/following counts → dedicated list screens → back to profile), **never** on a discovery surface; **Discover** = find things you don't know yet — **Trending** (the posts board) | **Explore** (people + groups mashed into one browser, "people are groups in web10"), pure discovery; the **Groups nav tab is NOT restored** (the operator dropped it — your communities live in Discover → Explore); the **nav order** is Profile (your name) · Shorts · Discover · Feed · Messages · Monetization; the **search bar opens Explore** (Enter → `/discover?tab=explore&q=`). **Sequence:** M0 (the field) gates M1/M2/M3 (the reads); M4 (the app) is independent of M2 (the CTE) — it rides M1 (the endpoint). M5/M6 (the IA) are independent of the node work.

- [✓ 3.151.0] **M0: the `membership_visibility` contract field** (keystone, `api/app/v3/services/clickhouse.py`) — add `membership_visibility String DEFAULT 'hidden'` to `group_contracts` (the `ALTER TABLE … ADD COLUMN IF NOT EXISTS` + the ensure-schema, the same pattern as `discoverable`); `create_group` / `update_group` accept + persist it (default `hidden`); `get_group` returns it. **Backfill:** set `public` on every existing followers group (`tags` contains the followers tag, D78) + community groups; leave dm / close-friends `hidden`. API floor test (the field round-trips; the default is `hidden`; the backfill flips followers groups to `public`). **Gates M1, M2, M3.**
- [✓ 3.151.0] **M1: `POST /v3/groups/by-user`** (the primary read, `api/app/v3/endpoints/groups.py` + `clickhouse.py`) — input `{ user: "provider/username", tag?: string, limit, offset }`; output `[{ group_id, role, joined_at, tags, join_policy, discoverable }]` — the user's memberships in **`public`-visibility groups only**, deduped (tombstone read invariant), optional `tag` filter (D78), paged, anon-readable. The group metadata is joined so the app renders rich cards in one round-trip. API floor tests (a public group's membership is returned anon; a `hidden` (dm) group's membership is **absent** — the I3-positive test; the `tag` filter; paging; an unknown user → empty). **Gates M4.**
- [ ] **M2: the `memberships` query CTE** (the composition read, `api/app/v3/services/safe_query.py` + `endpoints/query.py` + `sdk/src/v3.ts`) — a read-only, **opt-in** (`withMemberships` on `w.query`) API-built boundary CTE over `group_members` (deduped, not-deleted) exposing **`public`-visibility groups' rows only**: `group_id, member_key, role, joined_at`. Same category as `group_meta` (a view over the contract tables, not a writable app service); the raw tables stay blocked; the AST walk + re-parse backstop unchanged. **No visibility NULLing** — hidden groups' rows simply aren't in the view (the gate is the `membership_visibility == public` filter, decided public-by-policy in D80). SDK: `w.query(sql, { withMemberships: true })` + the type. I3 anti-tests (a `hidden` group's rows are absent even with the opt-in; the raw `group_members` is still rejected; a `public` group's rows are present anon). **Independent of M1** (the app rides M1; M2 is for app composition). **NOT built in 3.151.0** — the profile lists ride M1 (the endpoint); the CTE is the power-user composition read, still open.
- [✓ 3.151.0] **M3: `members/list` honors the field** (`api/app/v3/endpoints/groups.py`) — re-gate from bare `is_group_member` to the rule: `membership_visibility == public` → any reader (anon included); `hidden` → member/owner only (the existing `is_group_member`). API floor test (a public followers group's member list is readable anon; a hidden dm group's is member-only — no regression). **Gates M4's followers read (the app can use either M1 or this).**
- [✓ 3.151.0] **M4: the profile following/followers list screens** (`marketing/web10-social/src/components/Bio/` + `src/data/{follows,groups}.ts` + `App.tsx`) — the profile's **Follower** + **Following** counts become **clickable** → `/u/:username/followers` + `/u/:username/following` (deep-linkable, refresh-safe, the URL-holds-state rule) → a person list (the existing `PersonCardRow` shape: banner + avatar + name + @handle + follower count + Follow/Unfollow) → **back to profile**. **Following** = `by-user { user: X, tag: "followers" }` (M1) → derive the followed username from each `group_id` → resolve faces. **Followers** = `members/list` on X's followers group (M3) or `by-user` — pick one, paged. **Kill the `countUserFollowing` bug** (`follows.ts:104` — it counts X's *followers*, not who X follows); the real count = `by-user { user: X, tag: "followers" }` length (or a `count` param on M1). All states designed (loading / empty / error / paged) at desktop + 375px. `data-testid` hooks. **Gated on M1 (+ M3 for followers).**
- [✓ 3.151.0] **M5: the Discover People tab loses Following/Followers** (`marketing/web10-social/src/components/Discover/DiscoverPeopleTab.tsx`) — the `?personTab=` Following | Followers | Discover sub-row is **removed**; the People tab is **pure discovery** again (the D0 directory: sorts + paging + `?q=` + the quiet-here state). The personal lists now live on the profile (M4). `discoverScreen.test.tsx` + `peopleScreen.test.tsx` re-pinned (the person sub-tabs are absent; the People tab is the directory). **Independent of the node work.**
- [✓ 3.151.0] **M6: the IA fixed point — the operator's revised IA (23.09.2026)** (`marketing/web10-social/src/components/Social/Layout.tsx` + `src/components/Discover/DiscoverScreen.tsx` + new `DiscoverExploreTab.tsx` + `src/components/Search/GlobalSearch.tsx`) — the operator **dropped the Groups nav restore** and revised the IA: (1) **Explorer → Discover** (nav label + screen header; the compass icon is unchanged). (2) **Nav reorder** — the desktop sidebar is now **Profile (your name) · Shorts · Discover · Feed · Messages · Monetization**; the sidebar **Profile item shows the user's own name** (not the word "Profile") so it reads as "your profile"; **Monetization** is a first-class sidebar item (out of the More popover; Node Monetization stays in the popover, admin-only). (3) **Discover = Trending | Explore** — the subtabs collapse from three (Posts | People | Groups) to two: **Trending** (the posts board, the default bare URL) and **Explore** (people + groups **mashed into one browser** — "people are groups in web10"; both paged independently, `?q=`-filterable). (4) **The search bar opens Explore** — typing shows the live dropdown; **Enter (or the "See all results in Explore" CTA) navigates to `/discover?tab=explore&q=`**; the old per-section "See more" links are retired. `/people` → `/discover?tab=explore`; `/groups?tab=discover` → `/discover?tab=explore` (carrying `?q=`/`?tag=`). `socialScreens.test.tsx` + `discoverScreen.test.tsx` + `globalSearch.test.tsx` + `groupsScreens.test.tsx` re-pinned. **Independent of the node work.**
- [✓ 3.151.0] **M7: the screenshot pass** (`marketing/web10-social/screenshots/`) — the profile with clickable counts, the `/u/:username/followers` + `/following` list screens (back-to-profile), the Discover Trending + Explore tabs, the reordered nav (Profile-name first), all at desktop + 375px via the self-booting harness (`bun run screenshots` / `capture.mjs --name …`). The harness gains the follow-list routes + the D80 follow reads (seeded from the peer set). design.md §12 acceptance bar. **Gated on M4–M6.**

### Lane: group-as-profile (Phase 2)
**Owns:** `marketing/web10-social/src/components/Groups/` (detail + ManageGroup + the create entry), `marketing/web10-social/src/data/groups.ts` (the draft/publish fns), the D60 identity doc shape. **Plan:** `knowledge/strategy/group-as-profile.md` (operator pass, 18.09.2026). **The group is a profile:** the group page is the profile page (banner + avatar + about + tabs) with **Feed** front and center + **Media** (insta grid) second; an admin edits via a **pencil → inline edit mode on the page** (face *and* settings — the "Manage" sheet's Profile+Settings fold in; Members+Roles move to a secondary **kebab**); creating a group opens that same page **in edit mode as a draft** (auto-saved) → **Publish** / **Delete**. **Decisions locked (operator, risk pass):** (1) **no duplicate groups — a create-time *slug* check** (the node's `create_group` has no collision guard — bare `INSERT` — so the client checks `get_group` before create; after create the slug is the identity, the display name is free); (2) **one edit model, atomic commit — for drafts *and* published groups** (edit mode stages face + settings; Save/Publish commits them atomically together — the "atomic go"; the live state is frozen at the last commit while editing; Cancel discards — no torn states, no divergent stores); (3) **uploads** — a nav-away mid-upload shows a warning, no auto-save mid-upload; (4) **the slug is a vanity identifier, not a uuid** (the group's "domain name" — shareable/sellable/owner-namespaced, changeable with a redirect from the old slug). This lane does NOT touch `DiscoverScreen.tsx` (discover-reorg) or `Layout.tsx` (global-search). The one shared file is `groups.ts` — **sequence G0 before discover-reorg D3**.

- [✓ 3.117.0] **G0: the draft state + slug guard** (keystone, `src/data/groups.ts` + the D60 identity doc) — `GroupIdentity` gains `status?: 'draft'|'published'` (default `published` for existing) **and staging-setting fields** (who-can-read / how-join / list-in-directory staged in the face during edit — decision 2); `createCommunityGroup` gains a `draft` mode (group `discoverable=false` + `status:'draft'` face); a `slugTaken(slug)` helper (the **create-time** `get_group` collision check — decision 1); `publishGroup` / `saveGroup` (the **atomic** commit: face `status→published` + `update_group` join_policy/discoverable/read-grants — one ordered sequence, decision 2); `deleteGroup` (exists). My Groups shows the owner's drafts, marked "Draft" (the directory is already safe — `discoverable=1`). Unit tests (a draft is absent from the directory + others' lists, present to the owner; `slugTaken` true on an active group / false on a tombstone; the commit flips it live + listed atomically; delete discards). **Gates G2, G4. Sequence before discover-reorg D3.**
- [✓ 3.125.0] **G1: the group page tabs** (`GroupDetailScreen.tsx`) — the **Feed** (default, bare URL) | **Media** (`?tab=media`) tab row, feed first; the Media tab is a **paged** insta grid of the group's media posts (**infinite scroll** — next page on scroll, not "pull everything") **+ a count** (total media posts); reuse the profile's media grid + lightbox. Needs a paged group-media read (`limit`/`offset` + count). Hero + feed unchanged (no regression). `groupsScreens.test.tsx` (tab switch + `?tab=` deep link + the media grid pages on scroll + the count).
- [✓ 3.129.0] **G2: the inline edit mode** (`GroupDetailScreen.tsx` + new `ManageGroup/GroupEditMode.tsx`) — the manager **"Edit" pencil** in the hero → the page flips to edit mode: face fields (name/about/website/tags/banner/avatar) **and** settings (who-can-read / how-join / list-in-directory) as inline inputs, Save / Cancel. **One model for a draft and a published group** (decision 2): edit mode **stages** the changes (the live group is frozen at the last commit); **Save/Publish is the atomic commit** (face + settings together); **Cancel** discards. **No auto-save mid-upload** + a **nav-away upload warning** (decision 3). (The slug-taken guard is create-time only — decision 1; in edit mode the display name is free.) The Manage sheet's Profile + Settings sections fold in and are retired. `groupsScreens.test.tsx` (pencil manager-only; edit mode shows face + settings; the commit is atomic; the upload warning; no auto-save mid-upload; Cancel restores the live state; works the same on a draft and a published group).
- [✓ 3.131.0] **G3: the members/roles secondary surface** — the manager **kebab** (secondary to the pencil) opens the existing Members + Roles sections (unchanged components, new home) + the **published-delete** two-tap confirm (distinct from the lightweight draft-delete). `groupsScreens.test.tsx` (the kebab is manager-only; it opens the sections; the published-delete is two-tap).
- [✓ 3.134.0] **G4: create = the group page in edit mode (draft)** (`GroupsScreen.tsx` + `GroupDetailScreen.tsx` + `groups.ts`) — "New group" opens the group page in edit mode for a **draft** (G0); changes **auto-save** (the face doc); the action row is **Publish group** / **Delete** (the lightweight draft-delete); the name-taken guard is live. The `CreateGroupSheet` is retired (fields + tests re-pointed). `groupsCreate.test.ts` + `createGroupSheet.test.tsx` re-cut (a new group is a draft — absent from the directory until Publish; auto-save persists the face; name-taken blocks; Publish flips it live + listed; Delete discards). **Gated on G0, G2.**
- [✓ 3.136.0] **G5: the screenshot pass** (`screenshots/`) — `group-{feed,media}-{desktop,375}.png`, `group-edit-{desktop,375}.png`, `group-create-{desktop,375}.png`; the screenshot test on every state; the harness seeds (a group with media posts, a manager for the pencil, a draft for the create flow). **Gated on G1–G4.**

### Lane: social-e2e (Phase 3)
**Owns:** `e2e/tests/` — each bite owns its own spec file, so the bites
parallelize across workspaces (breadth, not depth).

The social app is the integration test (Phase 3). The retired social specs
(3.0.61: `social-post-feed`, `social-full`, `gauntlet`) are the rewrite
path. Same pattern as the demo specs: API floor + anti-tests + browser
gauntlet with log-sequence verification. **Organized by surface, not by
test layer** — each surface is an independent spec (separate route,
separate `src/data/` module, no shared code under test), so six workspaces
can run six specs at once. The only shared seam is login (the D42 popup) —
already torture-tested by `authenticator-torture` + `auth-popup-roundtrip`
(gate cleared, 3.11.0); each spec uses it as infrastructure, never
re-tests it. The platform-primitive API floors already live in the demo
specs (`feed-demo` = multi-group read, `groups-demo` = group lifecycle,
`messages-demo` = DM CRUD, `sharing-demo` = block/share) — the social API
floors stay thin: they pin the app's exact read pattern (what the app
actually queries), not a re-proof of the primitive. I3 anti-tests are
per-surface: each spec proves isolation for its own surface. The discover
board is a shared node default — posts to it get contains-assertions, not
exact counts (the `feed-demo` pattern).

- [✓ 3.26.0] Feed (`e2e/tests/social-feed.spec.ts`) — API floor: the app's exact feed read (discover + followers multi-group, sort config) + I3 (a non-follower's group post is absent). Browser gauntlet: real D42 login → feed renders → post → reload persists.
- [✓ 3.25.1] Groups (`e2e/tests/social-groups.spec.ts`) — the app's groups surface: follows (followers groups) as the app drives them — follow → the creator's posts enter the feed → unfollow → they leave. API floor: the follow/unfollow group ops + the feed-read delta. Browser gauntlet: follow/unfollow through the app, feed reflects it. (Group *management* — create/roles/invite — is the authenticator + marketing directory surface; its floors live in `groups-demo`.)
- [✓ 3.26.3] Profiles (`e2e/tests/social-profile.spec.ts`) — API floor: profile doc + posts read + follower count; I3 (a stranger's private data is not readable). Browser gauntlet: own profile (edit persists) + another user's public profile + the `/u/:username/p/:postId` deep link.
- [✓ 3.28.1] Profiles torture layer (`social-profile.spec.ts`) — the 3.26.3 gauntlet pre-created the followers group via API and only edited the bio, so the cold-start save path and the upload seam were never driven. New: API floor (the app's exact upload pattern — upload-url → presigned POST → confirm envelope → list(doc_ids) → read-url → the blob serves back the exact uploaded bytes; profile write round-trip through the app contract + no-contract-origin 403 anti-test) + browser gauntlet (COLD START — no group, no doc: edit name/website/bio → the app creates the group (node-level assert) → persists across reload; AVATAR UPLOAD — real file via setInputFiles → presigned avatar renders (naturalWidth > 0) → persists across reload).
- [✓ 3.25.2] Messages (`e2e/tests/social-messages.spec.ts`) — API floor: DM group contract + CRUD (deterministic DM group ID). Browser gauntlet: two-user DM round-trip through the app (send → receive → reply).
- [✓ 3.132.0] Messages P2P real-time (`e2e/tests/social-p2p.spec.ts`) — the real app's WebRTC P2P fast path, e2e-proven with two live accounts (the gap the gauntlet punted on + the stale "no P2P" comment). Two SEPARATE browser instances (P2P needs two browsers, not two contexts) + the mDNS flag, both pre-authed, both open the same DM conversation. Witnessed in the DOM with NO reload on the recipient: A sends → B's `nav-notifications-badge` pops to "1" (the D69 nudge) + the message lands in B's open thread (the onP2PInbound re-read); the reverse for B's reply. Asserts the recipient logged the inbound P2P handler, so a green run is direct evidence the fast path fired (the app has no polling — P2P is the only mechanism that can update the DOM without a reload). Verified against the live e2e stack (seam logs: both peers READY, bidirectional presence, `sent over open channel`, `p2p inbound — refreshing`, re-read `got 0` → `got 1`). Precursor to the two-user notifications gauntlet (plan.md).
- [✓ 3.133.0] Notifications real-time gauntlet (`e2e/tests/social-notifications.spec.ts`) — the notifications version of social-p2p (D69: CRUD truth, P2P nudge). API floor (the derivation's CRUD read: B's reaction on A's post is a `reactions` doc with `ref_value` = A's post, readable by A) + browser gauntlet (B reacts to A's post in the real app → A's `nav-notifications-badge` pops to 1 in REAL TIME, no reload → A opens /notifications → the history shows the event → marked read). **Caught + fixed a real bug:** the reaction/comment nudges used the derived `author_provider` (v3 author_keys are bare usernames → `extractProvider` falls back to the `'web10'` default) as the target provider, so the nudge targeted a non-existent peer and the real-time notification silently never landed (the channel "queued on open" never opened). The DM nudge was immune (it uses the conversation key's provider). Fix: target `token.provider` (the node's provider, v3 same-node) in `reactions.ts` + `comments.ts`. 891 social tests green, `tsc` clean.
- [✓ 3.25.3] Settings (`e2e/tests/social-settings.spec.ts`) — API floor: settings doc read/write round-trip. Browser gauntlet: change a setting → persists across reload + sign-out/sign-in.
- [✓ 3.26.2] Trending (`e2e/tests/social-trending.spec.ts`) — the `/discover` board surface (the in-app trending: D36 knobs over the node-default discover group). API floor: anon board read + engagement counts. Browser gauntlet: the board renders seeded posts, the knobs re-rank, deep-linkable state. (Complements the `discover-board` lane's board gauntlet — that one owns the moderation ops: seed → anon read → hide/restore round-trip.)
- [✓ 3.32.0] Groups directory (`e2e/tests/social-groups-directory.spec.ts`) — the app's Groups screen (distinct from the follows surface in 3.25.1): the D53 directory + detail reads. API floor: directory lists discoverable + excludes non-discoverable; detail reachable for an existing group / a ghost 404s; **I3** — a non-member detail read returns NO posts; join/leave membership. Browser gauntlet: Discover → join an open group → it appears in My Groups → the detail deep link renders, log-sequence verified.
- [ ] Capstone gauntlet (`e2e/tests/social-gauntlet.spec.ts`) — one journey across all screens (login → feed → post → profile → DM → follow → settings → reload), log-sequence verified. **Gated on all six surface specs above.**

### Lane: discover-board (Phase 3)
**Owns:** `api/app/v3/` (discover group + board read + admin discovery), `persona-orchestration/`, `marketing/marketing-ui/src/components/FeedPreview.tsx` + `src/pages/Trending.tsx`, `ui/src/components/Config/ConfigPage.tsx`

The node-default universal public board. The discover group
(`web10.app/groups/web10/discover`) is a NODE DEFAULT — created at boot,
anon + every user a member, auto-enroll at signup, backfill pre-existing
users. Discovery IS a group read: the board is the discover group in the
`groups` list, read anon through the anon-capable `/v3/read` (no separate
discover endpoint — the v2 `/discover/posts` is not resurrected). Persona
seeding posts to the group so the marketing trending page + in-app Discover
look alive.

- [✓ 3.16.2] Node-default discover group: boot-time `ensure_discover_group()` (create + anon + backfill), auto-enroll in `create_user`, idempotent
- [✓ 3.16.2] Anon-capable `/v3/read`: missing token reads as `anon` (the board), app-contract gate for real users only, I3 holds (anon can't read non-member groups)
- [✓ 3.16.2] `ref_value` on create (the ref pattern was broken on the write path — reactions/comments couldn't reference their target)
- [✓ 3.16.2] Board moderation as a group op: `POST /v3/groups/{hide,unhide,hidden}` (gated by `hideAll` OR node admin — the public board has no moderator role), `get_hidden_docs`, anti-join dedup-then-filter
- [✓ 3.16.2] Persona seeding for v3: `seed_personas.py` rewritten off v2 (terms/schemas/ledger) onto groups — posts + reactions/comments via `ref_value`, idempotent, `--verify`
- [✓ 3.16.2] Marketing trending + admin board rewired to the normal group read (anon); trending computes engagement client-side from the reactions/comments groups
- [✓ 3.18.2] Power-mean ranking in the backend (D36, feed-lens-integration) — `/v3/read` gains an optional `sort` config (recency/likes/comments weights, `half_life_ms`, `character` p); the node computes the score over the full group membership and returns pre-sorted results (mirrors `marketing-ui/src/lib/powerMean.ts` so client + server rank identically). Also un-reds the 12 stale `Trending.test.tsx` reds (the mock was still on the v2 `/discover/posts` shape — re-aligned to the v3 `/v3/read` discover-group shape)
- [✓ 3.21.1] Power-mean ranking v1 scale-up — the sort path moves from Python (full membership fetch + in-process sort) into ClickHouse: shared `_board_base_sql` fragment, exact engagement counts via one grouped scan of reactions + comments (option B, no counter table — operator: "this is clickhouse"), SQL power-mean score, `ORDER BY` + `LIMIT`/`OFFSET` in the DB. Real-CH equivalence check proves the SQL score matches the Python reference to ≤1e-9
- [✓ 3.38.1] fix(api) + fix(marketing-ui): /trending consumes the v3 read shape + the API serializes datetimes as ISO 8601 UTC (the 3.16.2 rewire's two regressions, root-caused). (1) Negative times: `/v3/read`'s naive `str(datetime)` `created_at` was parsed as local time (west-of-UTC → future) — **API** now emits ISO 8601 UTC via `_iso_utc()` (every `str(row[N])`/`now.isoformat()` datetime site; `_from_iso_utc()` for the update round-trip), fixing every client at once; **marketing-ui** `parseCreatedAt()` stays as a defensive fallback. (2) Empty Video view: the v3 read serves `media_refs` pre-resolved (objects with `mime_type` + `read_url`), but the page treated them as strings + hardcoded `first_attachment_mime: undefined`, so media was tag-detected only; now `first_attachment_mime` derives from the resolved ref and `TrendingMedia` uses `read_url` directly (no token-gated presign round-trip). 6 marketing-ui tests (241 green) + 835 API tests green.
- [✓ 3.65.1] E2E: board gauntlet — seed → anon reads the board → a real user's post appears → remove/restore round-trip (gated on the social-e2e stack)
- [✓ 3.74.3] fix(marketing-ui): /trending video card — the resolved `thumbnail_url` (the v3 read mints it alongside `read_url`) now feeds the `<video poster>` + the reduced-motion `<img>` (was null → the reduced-motion image pointed at the MP4 → dark void), and the reduced-motion play badge is a real button that opens the full view (was a dead `<div>`). `FeedPreview.tsx` + `Trending.test.tsx` (+3). 257 marketing-ui tests green, `tsc -b` clean.
- [✓ 3.98.0] **The discover card is one shared component — `@web10/discover` (D74) — and the greyed-out mobile-Chrome video is fixed.** The marketing `/trending` card rendered the **raw source file** (HEVC/AV1) that mobile Chrome can't decode → greyed-out tile; the social app played the transcoded HLS. A new **source** package `marketing/shared/discover/` owns the presentational discover card + its deps (`DiscoverCard`, `VideoPlayer`/`HlsVideoPlayer`/`MediaCarousel`, `PostActions`/`CommentThread`, `RankBadge`/heat glow, types) — the **data seam is injected** (wapi for social, public ledger for marketing), **two modes** (`interactive` logged-in / `remote` anon with link-outs), and the **video view is videos-only** (competing with YouTube). Both apps' cards (`DiscoverCard`/`DiscoverYouTubeCard` + `TrendingCard`/`YouTubeCard`) are now thin wrappers over the shared `DiscoverCard`. The package has no node_modules of its own (single-React) — its imports resolve to the consuming app's deps via a Vite alias + tsconfig `paths`. Social 656 + marketing 259 green, both `tsc` + `vite build` clean. KB `social/discover-card.md`, D74.

### Lane: content-moderation (D59)
**Owns:** `api/app/v3/services/moderation.py`, `api/app/v3/endpoints/moderation.py`, `api/app/v3/models/moderation.py`, `api/app/v3/endpoints/documents.py` (the create hook), `api/app/services/config.py` (defaults), `api/app/models/config.py`, `api/tests/test_moderation.py`, `ui/src/components/Config/ConfigPage.tsx` (the Moderation card), `ui/src/__tests__/configModeration.test.tsx`, `clickhouse-init/001-init-v3-schema.sql.template` (moderation_flags)

Content moderation (D59): sensitive-language detection + discover suppression,
built on the existing `group_hidden_docs` mechanism. A whole-word,
case-insensitive blocklist in `node_config` is checked on the post-create path;
a hit on a discover-group post is auto-hidden (the existing hide) + flagged.
A user on `auto_hide_users` is always auto-hidden. The review queue is
human-in-the-loop (the operator suppresses; the machine only flags). D41 holds
— suppression is board curation, not secrecy.

- [✓ 3.41.0] Decision: D59 (`knowledge/strategy/decisions.md`) + KB (`social/content-moderation.md` + `social/sensitive-words-default.md`, the ~50-word default list)
- [✓ 3.41.0] Config: four `node_config` fields (`sensitive_words`, `auto_moderate`, `moderation_enabled`, `auto_hide_users`) + `effective_config` defaults (no DDL — JSON blob) + the shipped default blocklist
- [✓ 3.41.0] Detection: `app.v3.services.moderation` — `check_text` (whole-word, case-insensitive), `moderation_config`, `should_auto_hide`, `record_flag` (best-effort)
- [✓ 3.41.0] Write-path hook: `create_document` moderates `posts` on the discover group — flag + (auto_moderate OR listed) `hide_doc_from_group(DISCOVER_GROUP_ID, …)`; best-effort (a moderation failure never fails the post)
- [✓ 3.41.0] `moderation_flags` table (DDL template + boot self-heal) + `insert_moderation_flag`/`get_moderation_flags` (the queue is a GROUP BY view)
- [✓ 3.41.0] Admin endpoints: `POST /v3/moderation/flags` (the queue) + `POST /v3/moderation/auto-hide` (add/remove from `auto_hide_users`)
- [✓ 3.41.0] UI: the Node Config "Content Moderation" card — master switch + auto-hide toggle (diff-only save), blocklist tag input, the review queue with "Keep hiding"/"Hiding"
- [✓ 3.41.0] Tests: 27 API (`test_moderation.py`) + 7 UI (`configModeration.test.tsx`) — detection, the hook, I3 scoping, the endpoints, the card
- [✓ 3.67.5] E2E: moderation gauntlet — post with a flagged word → hidden from the board → operator keeps-hiding → next post auto-hidden → operator removes → next post visible. `e2e/tests/moderation.spec.ts` (API floor + I3 anti-test + browser gauntlet over the Node Config "Content Moderation" card). Caught + fixed a real bug: `get_moderation_flags`'s `arrayJoin(groupArray(...))` split a multi-flag user into one row per flag (duplicated in the queue, slow under load) — now `arrayFlatten` (one row per user) + unit test + KB re-aligned; also made `ConfigPage.nodePost` port-aware (isolated e2e ports).
- [ ] v1: profile name/bio detection (flag-only, not scanned on the post path in v0) + a retroactive-scan admin command + a user notification on auto-hide

### Lane: contact-auth (D61)
**Owns:** `api/app/v3/endpoints/recovery.py`, `api/app/v3/endpoints/auth.py`, `api/app/v3/services/clickhouse.py` (get_users_by_contact), `api/app/services/twilio.py` (channel-aware), `api/app/v3/models/auth.py`, `api/app/models/config.py` (require_contact), `api/app/services/config.py`, `api/app/exceptions.py`, `api/tests/test_recovery.py`, `ui/src/interfaces/Interface.tsx`, `ui/src/components/CredentialPage/ForgotForm.tsx`, `ui/src/interfaces/MockInterface.tsx`, `ui/src/__tests__/recoveryFlow.test.tsx`

Contact-anchored auth (D61): the account is anchored on a phone OR email,
verified by a 6-digit code. The contact is the front door (enter contact →
code → pick an account or create one → signed in). Sign-up, sign-in, and
password-change are the same flow. Node-config-gated (D10): `require_contact`;
web10.app turns it on. The 3.47.0 UI already calls the three endpoints — they
were never built (the changelog's "the API is 3.37.0" was wrong).

- [✓ 3.51.0] Decision: D61 (`knowledge/strategy/decisions.md`) + KB (`knowledge-base/web10-v3/auth/auth.md`)
- [✓ 3.51.0] API keystone: the three endpoints (request/verify/complete) + `get_users_by_contact` (phone OR email) + Twilio channel-aware (sms/email) + the `verify_token` gate + create-on-complete (unified signup)
- [✓ 3.51.0] Node config flag: `require_contact` (D10) + enforced in `POST /v3/signup` (401 `CONTACT_REQUIRED`)
- [✓ 3.51.0] Tests: `api/tests/test_recovery.py` (request/verify/complete, contact mismatch, bad code, node-config gate)
- [✓ 3.51.0] UI: the contact input (phone OR email) + verify_token plumbing + "create a new account" option + primary-sign-in routing
- [✓ 3.51.1] Refinement: the recovery send drops the username (the message is the console-configured Twilio Verify template — a username-less "your code is {{code}}, if you didn't request this ignore it"); `send_verification` no longer takes a username
- [✓ 3.56.1] E2E: the contact-anchored auth gauntlet (`e2e/tests/recovery.spec.ts`) — phone + email paths, create-on-complete, password-change, the anti-tests (contact mismatch, bad/expired/wrong-purpose verify_token, send rate-limit); the e2e stack runs the API in local-Twilio mode (`TWILIO_E2E`) so the fixed code "123456" completes the flow without real credentials
- [✓ 3.58.1] Fix: the recovery password-change 401s the new password — a same-second `updated_at` race. The five users-table mutators (`change_password`/`change_phone`/`set_email`/`verify_phone`/`verify_email`) wrote `updated_at` from ClickHouse `now()` (second precision) while `create_user`/`migrate_user` used Python microsecond `_now()`; in the same second the new row's `.000` ms tied/lost to the old row's real ms, so `get_user`'s `ORDER BY updated_at DESC LIMIT 1` returned the stale old-hash row. The mutators now use `_now()` (bound `%(updated_at)s`) so the new row strictly outranks the old one

### Lane: ads (monetization)
**Owns:** `marketing/web10-social/src/components/Monetization/` + `marketing/web10-social/src/data/ads-catalog.ts` (the app-owned ad surface, D75), `api/app/v3/services/clickhouse.py` + `api/app/v3/endpoints/documents.py` + `api/tests/test_ads.py`, `e2e/tests/ads.spec.ts`, `marketing/web10-social/src/components/Feed/PostComposer.tsx` + the ad block

The creator-owned ads layer (D55 + the v3/v4 dissemination split): an ad is a
**`posts` document tagged `ad`** (the post's own text + media + a leaf-typed
`offer` + a `status`). **v3 is mad simple** — a document's `ad_preference` (a
column on `documents`) is `pinned` | `none`: the creator pins a specific ad to a
post, and the **read serves the doc with the pinned ad inline** (100% of the
time, I3-checked). Ads are organized into **albums** (Apple-Photos-style,
first-class; an ad in a few via a tag-like field). The full curation engine
(`round_robin` / `greedy` / `random`, the node-level density, the `signal` ×
`strategy` enums) is the **v4 vision**. The KB is the spec — read it first:
`web10-v3/social/ads.md` (the object), `web10-v3/social/ads-dissemination.md`
(the v3 design), and `web10-v4/social/ads-dissemination.md` (the v4 engine).

- [✓ 3.16.1] KB: the standard ad object + the Dissemination section + the two-layer note (`social/ads.md`) + D50 + D51
- [✓ 3.22.0] KB: the Ad Catalog + the composer integration (`social/ads-catalog.md`) + D54
- [✓ 3.23.0] KB: D55 — an ad is a `posts` doc tagged `ad`, not a service
- [✓ 3.25.0] The tagged-post ad (API conformance): the ad object through the existing posts CRUD + the feed read returns it + I3 — pinned by `api/tests/test_ads.py`
- [✓ 3.26.1] KB: the v3/v4 dissemination split — v3 is `pinned` | `none` (data-layer, the read serves the pinned ad inline, I3-checked, 100% density), the curation engine is the v4 vision; the three v3 questions resolved (`ad_preference` column on `documents`, albums first-class + tag-like ad→album link, inline read)
- [✓ 3.27.4] **v3 API: the read serves the pinned ad** — the `ad_preference` column on `documents` (`pinned` | `none` + `target`); the feed read joins to the pinned ad and returns the doc **with** the ad inline (100% of the time); the **I3 check** (serve the pinned ad only if the reader is a member of the ad's group, else no ad); the **albums** (first-class, an ad in a few via a tag-like field). Pinned by `api/tests/test_ads.py`. The foundation — gates the UI
- [✓ 3.28.0] **Ads tab (authenticator)** — the Studio's new Ads surface: **ads upload** (the ingest — create ads: media + offer + status → a `posts` doc tagged `ad`), **album making** (Apple-Photos-style: make albums, sort by album or all, add an ad to a few albums), **pin an ad to a post** (pick an ad → set the post's `ad_preference`). All states designed (empty → CTA, skeleton, error). `ui/src/components/Studio/`
- [✓ 3.29.0] **Composer pin control (web10-social)** — the "Pin an ad" control in `PostComposer`: pick an ad (from an album or all) to pin to the post, or none (sets the post's `ad_preference`); the ad block renders under the post (creative + offer + disclosure, disclosure never hidden). `marketing/web10-social/src/components/Feed/`
- [✓ 3.30.0] **E2E: the torture gauntlet** — create an ad → pin it to a post → follower sees the post with the ad block + disclosure → unpin → it's gone → non-follower never sees the ad (I3) → an ad in two albums shows in both. `e2e/tests/ads.spec.ts`
- [✓ 3.53.0] **Monetization bootcamp guide** (`knowledge/knowledge-base/web10-v3/social/monetization-bootcamp.md`) — the creator-facing ramp: which affiliate programs to join (the shortlist table), how to sign up (the website-list / 180-day rule, node-account vs creator-account), how the ad maker turns a link into a post that pays (offer kind/partner/link/cta/disclosure + pin-to-post + albums), and the "do it genuinely" principles. Docs only — derives from `ads.md` + `ads-catalog.md`, no code.
- [✓ 3.54.0] **Studio: the Affiliate Programs card** (`ui/src/components/Studio/AffiliateProgramsCard.tsx` + `studio-data.ts`) — the bootcamp factored into the Studio: the "START HERE" card in Rung 0 pointing a creator at the programs worth joining (each row = program + niche + commission + why + external sign-up link, new tab). Sits above the Ads card. 8 new Studio tests.
- [✓ 3.55.0] **Studio: retire the AmazonTagCard** (`ui/src/components/Studio/AmazonTagCard.tsx` deleted) — the single-global-tag "auto-affiliate-everything" card D55 rejected (the platform never rewrites the link; the tag lives in each ad's `offer.link`, set in the ad maker). Removed from the Studio + 4 tests.
- [✓ 3.62.0] **Bootcamp page (marketing-ui)** — surface the guide as a `/docs/monetization` page (or a Studio link-out) so a first-time ad-maker lands on it. Gated on a docs-page surface in marketing-ui.
- [✓ 3.96.3] **Shortlist: PartnerStack** — the SaaS/B2B marketplace added to the affiliate shortlist on all four surfaces (KB bootcamp table + lane, public `affiliate-programs` table + lane, public bootcamp lane, Studio `AFFILIATE_PROGRAMS` with sign-up `dash.partnerstack.com/marketplace`). One signup, one dashboard, hundreds of SaaS programs — the network the shortlist was missing.
- [✓ 3.99.0] **D75: monetization is app-owned** — (1) **node service-agnostic ad mechanism** (`api/app/v3/services/clickhouse.py`): drop `collection_name = 'posts'` from `resolve_pinned_ads`/`get_active_node_ads`/`can_read_carrier_post` — the `ad`/`node_ad` tag + `ad_preference` pointer + I3 are the whole model; a non-social app can pin an ad that is any doc. (2) **web10-social `/monetize` surface** (`src/components/Monetization/` + `data/ads-catalog.ts`): Creator (ad catalog + affiliate onboarding, ported from the Studio) + Node (node-ad inventory + density, node-admin gated via `POST /am_admin` + `useNodeAdmin`); the "Node Monetization" nav icon (sibling of Groups) renders only for the node admin. (3) **authenticator Studio removed** (`ui/`): `StudioPage` + the six cards + `ads-data.ts`/`studio-data.ts` + the "studio" nav mode + the obsolete `ensureSelfContract` self-grant. "An ad is a post" is a web10-social shape, not a protocol rule. KB: new `social/monetization.md` + `ads.md`/`ads-dissemination.md`/`node-ads.md`/`ads-catalog.md` updated. 996 API + 685 social + 164 authenticator green, all `tsc` clean.
- [✓ 3.100.0] **The "Monetization" nav entry (every user)** (`src/components/Social/Layout.tsx`) — the `/monetize` surface is now reachable from the nav for every signed-in user (the "Monetization" entry, `$` icon, `/monetize` → the Creator tab); "Node Monetization" (`/monetize?tab=node`) stays node-admin-only. Desktop sidebar + mobile "More" sheet. +2 Layout tests (the `checkNodeAdmin` seam mocked via `vi.hoisted`); the harness `mock-data.ts` gains the missing `readFeedReactions` stub. KB `monetization.md` "The Nav" section updated.
- [✓ 3.101.2] **The Monetization nav stops double-highlighting + the in-page Creator/Node switcher is gone** (`src/components/Social/Layout.tsx` + `src/components/Monetization/MonetizationScreen.tsx`) — operator: "when i hit the monetize it highlights both, should be one or the other" + "this creator node switch is no good. should be separated by the clicking the icon." (1) The two `$` rows' active state was a pathname-only match (`isActive('/monetize')`), so both lit on `/monetize*`; it now reads the URL's `?tab` param (`isMonetizeCreator` / `isMonetizeNode`), so exactly one row highlights (desktop sidebar + mobile "More" sheet). (2) The surface's Creator/Node segmented `TabButton` bar is removed — the nav IS the switcher; the screen renders whichever section the URL points at (still `useNodeAdmin`-gated, non-admin falls back to Creator). `monetization.test.tsx` re-pinned +3 Layout highlight tests. 691 green, `tsc` clean. KB `monetization.md` "The Surface" + "The Nav" updated.
- [~ 3.134.0] **Two ad formats (inline / post) + edit + media + pin-from-edit + post-ad render** (operator pass, 20.09.2026 — `ad-improvements.md`) — (1) **data layer**: `AdRecord.format` (`inline`|`post`) + `PostRecord.ad_target`; `buildOfferBody`/`parseAd`/`buildNodeAdBody` write/read `format` + `media_refs`; `updateAd` (same `doc_id`, pins survive); `updatePost` gains `ad_preference`; the feed/discover read **drops `ad`-tagged docs as standalone ranked posts** (the leak fix — ads are attached, never ranked). (2) **monetization surface**: `AdForm` (create **and** edit) + the Edit button; CTA suggestion chips + neutral "Learn more" fallback; optional `kind` (`none` hides partner); a media attach (one image/video); the **format toggle** (inline/post). (3) **pin from edit**: the feed `PostCard` + `PostLightbox` edit flows gain a "Pin an ad" control (reuses `AdPicker`), save passes `ad_preference`. (4) **post-ad render**: new `PostAdCard` (a full post-like card — media, copy, offer CTA, disclosure, Ad/Sponsored badge, a like) + `AttachedAd` (renders per format); the feed `PostCard` + the shared `DiscoverCard` (via a `renderAd` seam) render the attached ad per format. 908 social + 264 marketing-ui + 992 API green, `tsc`/`ruff` clean. KB `ads.md` "Two Formats" + `ads-catalog.md` + `monetization.md` updated.
- [~ 3.159.0] **Post ads render as just another post — next in line after the pinned post, not inside it** (operator pass, 24.09.2026 — `ad-improvements.md` Issue 6 revision: "the post ad renders within the post its attached too, would be nice if in the discover trending, the feed, where it is being viewed it gets rendered as just another post" + "but with the ad disclosures, but like a post that is in the feed next in line after the pinned post, not indicating its pinned to that post"). The attachment model is unchanged (still attached via the join, never ranked); only the rendering moves: a `post`-format ad renders as **its own card in the stream, next in line after the post it's attached to** (full feed-card chrome — post header with the Ad/Sponsored badge + author + time, media, copy, offer, like + disclosure) with **nothing indicating the pin** (no ring, no "attached to" marker). **Build:** `PostAdCard` gains a `standalone` seam (default = the own-card form; `standalone={false}` = the compact attached variant with the ring, for the lightbox); `AttachedAd` threads it; the feed list + the discover Hot Gossip board `flatMap` post → its post-format ads (inserted after the post's card); the feed `PostCard` + the shared `DiscoverCard` slot skip the `post` format (inline only); the lightbox renders per format (attached variant); `AdRecord.created_at` + `DiscoverAd.created_at` (the post header's time). 925 social + 271 marketing-ui green, `tsc` clean. KB `ads.md` "Two Formats" + `discover-card.md` "The ad slot".

### Lane: node-ads (D57)
**Owns:** `api/app/v3/services/clickhouse.py` (node ad query + read-time attachment), `api/app/v3/endpoints/documents.py` (the read enrichment — the third join: `doc.ad` + `doc.node_ad`), `api/app/services/config.py` + `api/app/models/config.py` (`node_ad_percentage`), `ui/src/components/Studio/` (Ad Inventory card), `marketing/web10-social/src/components/Feed/` (renderer: `node_ad` tag → "Sponsored" label, both ads on the same post), `e2e/tests/node-ads.spec.ts`, `api/tests/test_node_ads.py`

The node operator's ad layer (D57, the second layer of the two-layer ad
model). **v3 is ads only** — no Stripe, no memberships, no tips (the
payment model is v4). A node ad is a `posts` doc on the discover group,
tagged `ad` + `node_ad`, authored by the node operator. The read attaches
active node ads to posts at the operator's configured percentage (default
10%). The attachment is read-time — the creator's `ad_mode` column is
never modified. The response is a **third join**: `doc.ad` (the creator's
pinned ad, if `ad_mode = 'pinned'`) + `doc.node_ad` (the node's ad, if
selected by the percentage). Both can be present on the same post — the
creator's monetization is never suppressed by the node's. The KB is the
spec — read it first: `web10-v3/social/node-ads.md`.

- [✓] **Decision: D57** (`knowledge/strategy/decisions.md`) — two-layer ad model (creator + node); v3 is ads only (no Stripe, no memberships, no tips — the payment model is v4); read-time attachment at a percentage; the third join (`doc.ad` + `doc.node_ad`, both can be present); usage-based pricing (MongoDB model); the v3/v4 split rationale (Stripe Connect = migration lock-in + onboarding friction)
- [✓] **KB: `node-ads.md`** (`knowledge/knowledge-base/web10-v3/social/node-ads.md`) — the node ad object, the read-time attachment (the third join), the density control, the renderer (both ads on the same post), the operator's revenue model (hosting + node ad revenue 85-90%), the "what this is NOT" (not a payment processor, v3 is ads only), security invariants. (3.64.0: prose drift fixed — both ads coexist, not "only fills the gap.")
- [✓ 3.37.0] **`node_ad_percentage` config** (`api/app/models/config.py`, `api/app/services/config.py`) — new field on `NodeConfig` + `ConfigUpdate` (integer, 0-100, default 10); `effective_config()` defaults it; the Node Config UI exposes it
- [✓ 3.37.0] **Node ad query** (`api/app/v3/services/clickhouse.py`) — `get_active_node_ads()`: the bounded query (discover group, `tags ∋ 'node_ad'`, `status = 'active'`, LIMIT 20); called once per read, cached for the read's duration; defensive try/except (returns [] on any error)
- [✓ 3.37.0] **Read-time attachment (the third join)** (`api/app/v3/endpoints/documents.py`) — after the pinned-ad resolution, for each doc: hash(doc_id + reader_key) → if < percentage, attach a node ad as `doc.node_ad` (round-robin); the response carries both `doc.ad` (creator's, if pinned) and `doc.node_ad` (node's, if selected); the creator's `ad_mode` column is never written
- [✓ 3.57.0] **Renderer: both ads on the same post** (`marketing/web10-social/src/components/Feed/AdBlock.tsx`) — the post renders with up to two ad blocks: the creator's ad (`doc.ad`, their disclosure) + the node's ad (`doc.node_ad`, "Sponsored" label + node disclosure); both visible, neither suppressing the other. Ads render as posts (media-aware, creator violet / node amber dressing, disclosure names the author)
- [✓ 3.64.0] **Ad Inventory card (authenticator)** (`ui/src/components/Studio/`) — the operator's surface: percentage slider (0-100, writes `node_ad_percentage`), list of active node ads (creative preview, offer, status), create / pause / resume / retire node ads (writes `posts` docs tagged `ad` + `node_ad` to the discover group); all states designed (empty → CTA, skeleton, error)
- [✓ 3.64.0] **Tests: unit** (`api/tests/test_node_ads.py`) — node ad query (returns active node ads from discover group, excludes paused, excludes non-node_ad, bounded at 20); read-time attachment (percentage 0 = no node ads, percentage 100 = all posts get a node ad, percentage 10 = ~10% get one, deterministic per (doc, reader), a `pinned` post gets BOTH `doc.ad` AND `doc.node_ad`, round-robin cycles through active node ads); I3 (node ad visible to all members of discover group, which is everyone)
- [✓ 3.64.0] **Tests: e2e** (`e2e/tests/node-ads.spec.ts`) — API floor: operator creates a node ad → a reader's feed read returns a post with `doc.node_ad` (the `node_ad` tag present, the "Sponsored" disclosure) → a `pinned` post returns BOTH `doc.ad` (the creator's ad) AND `doc.node_ad` (the node's ad) → percentage 0 = no node ads → percentage 100 = all posts get a node ad. Browser gauntlet: operator creates a node ad via the Ad Inventory card → a follower's feed renders a post with the "Sponsored" ad block → a creator's pinned post shows BOTH the creator's ad AND the node's ad → no pageerror. (Serial — mutates the shared `node_ad_percentage`; `afterAll` resets it to 0.)
- [✓ 3.78.1] **The authenticator's self app-contract** (operator: "noticing those errors for ads accross the board in the authenticator!") — the node console is a CRUD client (the Studio ad cards read/write `posts`/`ads`), and the documents endpoint enforces an app contract per origin; with no contract for the auth origin every Studio ad read 403'd ("No app contract for https://auth.web10.app to readAll on posts"). The e2e masked it (it manually seeds an AUTH-origin contract). On sign-in the authenticator now grants itself a contract for its own origin (the only origin allowed to call `app-contracts/add`), idempotent + best-effort. Cross-lane touch: `ui/src/interfaces/Interface.tsx` (owned by `auth-consent-forks`) — the self-grant lives in the shared Interface, not the card. `selfContract.test.tsx` (4).
- [~ 3.134.0] **Node ads show up nowhere — the discover-group-id fix + the overwrite knob** (operator: "I am not seeing node ads on any posts, should be on 10% of posts" + "write over creator ad? yes no?") — (1) **the bug**: `get_active_node_ads` derived the discover group id from the editable `node_config.provider` field (which drifts from `settings.PROVIDER` on deployed nodes → the query matched nothing → node ads never attached). Fixed to use the **canonical `DISCOVER_GROUP_ID`** (same source that creates the group). Pinned by `test_node_ads.py::test_uses_canonical_discover_id_not_config_provider` (the config provider is deliberately different; the query must still target `DISCOVER_GROUP_ID` and never call the config). (2) **the overwrite knob**: new `node_ad_overwrite` node-config field (default `false`); `attach_node_ads` drops the creator's `doc.ad` when a node ad fires and overwrite is on (only the node ad shows); off = both show (the D57 non-steal default). Format-agnostic. The toggle lives in the Node Monetization surface (next to the density slider) + writes via `/config/update`. `test_node_ads.py` +3 (overwrite on drops the creator ad, off keeps both at 100%, the canonical-id regression). 992 API green, `ruff` clean. KB `node-ads.md` "The Overwrite Knob" + the canonical discover-id note.
- [~ 3.163.0] **Node ads are editable — full parity with creator ads** (operator: "something is off with node ads :( no ability to edit them, should feel just like creator ads!" + "talking about editability, options for inline or post based ad, should have parity i.e. the same format!") — 3.134.0 gave the creator's catalog the full `AdForm` (create AND edit, format toggle, CTA chips, optional `kind`, media attach) but the Node Monetization surface kept the old create-only `NewNodeAdForm`. **The build:** (1) `ads-catalog.ts` — new `updateNodeAd(ad, offer, text, status, mediaRefs?, format?)` → `w.update(doc_id, buildNodeAdBody(…))`; same doc_id (an update is a new version), so the read-time attach picks up the new creative/offer/format on the next read; no albums (a node ad is the operator's inventory). (2) `NodeMonetization.tsx` — `NewNodeAdForm` → `NodeAdForm` (create AND edit, the same shape as the creator's `AdForm`): the **Edit button** on every inventory row, edit mode pre-fills copy / offer / status / format / existing media (kept by doc_id unless replaced), the **format toggle** (inline / post) is editable, the **CTA suggestion chips**, the **`none` offer kind** (hides partner), the row's badges match the creator's row (ACTIVE/PAUSED + INLINE/POST AD + kind). `adsCatalog.test.ts` +1 (`updateNodeAd` same doc_id + `node_ad` tag), `monetization.test.tsx` +3 (Edit opens pre-filled; save calls `updateNodeAd` same doc_id + format; create flow keeps CTA chips). 961 green, `tsc` clean. KB `node-ads.md` updated.

### Lane: query-engine (D62)
**Owns:** `api/app/v3/services/safe_query.py`, `api/app/v3/endpoints/query.py`, `api/app/v3/models/query.py`, `api/app/v3/services/clickhouse.py` (`execute_query` + the boundary CTE), `api/tests/test_query_endpoint.py` + `test_safe_query.py`, `sdk/src/v3.ts` (`w.query`), `e2e/tests/query-engine.spec.ts`, `knowledge/knowledge-base/web10-v3/{query-engine,safe-query}.md`

The **flexible read** (D62): a caller writes a ClickHouse `SELECT` over their
services and the node runs it — read-only by construction. The safe-query
engine rewrites each service to an API-built boundary CTE (group-filtered +
block/sharing/hidden), so joins/aggregations/subqueries/CTEs all work and none
can leak past the caller's groups (raw tables unreachable — a wall, not a
membrane). Exposed as `POST /v3/query` + `w.query(sql, { groups? })`. Anon-
capable (D41), app-contract-gated, `LIMIT 1000` + 10s timeout. KB is the spec —
read it first: `web10-v3/query-engine.md` + `safe-query.md`.

- [✓ 3.52.0] **The boundary** (`safe_query.py`) — parse → validate → rewrite to boundary CTEs; `read_docs_by_ref` is the first consumer (the `ref` filter).
- [✓ 3.56.0] **Server-side engagement counts** (`read_ref_counts_by_ref`) — `GROUP BY ref_value` through the engine (exact, no cap).
- [✓ 3.58.0] **`query_services()` + `max_limit`** (`safe_query.py`) — the pre-flight (which services the query touches) + the `LIMIT 1000` performance bound (a caller `LIMIT` is honored; a union's trailing limit is seen).
- [✓ 3.58.0] **`POST /v3/query`** (`api/app/v3/endpoints/query.py` + `models/query.py`) — anon-capable, app-contract-gated (`query_services` before any group work), per-service D58 read gate, D42 "not a member" 403, caller-SQL → 400. `clickhouse.py` `execute_query()` + `QueryExecutionError`.
- [✓ 3.58.0] **`w.query()`** (`sdk/src/v3.ts`) — `w.query(sql, { groups? })` → `{ rows, count }`; token-less (anon); JSDoc examples + the ClickHouse `LEFT JOIN` count gotcha. `dist/` + `wapi.js` rebuilt.
- [✓ 3.58.0] **The ClickHouse 24.8 CTE-inlining fix** — the boundary CTE's block/sharing/hidden `LEFT ANTI JOIN`s broke CTE inlining when combined with a `JOIN` (`UNKNOWN_IDENTIFIER`), which also broke `read_docs_by_ref` + `read_ref_counts_by_ref` on a real node. Rewritten as `NOT IN` / tuple-`NOT IN` subqueries (semantically identical, verified live: block / group-block / sharing-pause self-exempt / hidden; empty → keep all).
- [✓ 3.58.0] **Tests** — API `test_query_endpoint.py` (20) + `test_safe_query.py` (+12) + `e2e/tests/query-engine.spec.ts` (the seam gauntlet: the power — self-join + aggregation + CTE + JSON; I3 — a non-member reads nothing; the contract gate; the membrane; anon).
- [✓ 3.59.0] **v1: a query playground demo** (`marketing-ui/public/docs/query/`) — an interactive SQL box over the signed-in user's groups (the "go crazy" showcase), reusing the demo auth pattern. Five clickable example queries (recent posts, trending self-join, reaction breakdown, hot-posts CTE, comments by author), a result table + loading/empty/error states, ⌘/Ctrl+Enter to run. Registered in `DEMO_APPS`; `tests/demos/query.spec.ts` (6) + a `query` method/branch in the demo mock.
- [✓ 3.60.0] **v1: per-user query rate limiting** (D65) — `/v3/query` rate-limited per user, keyed on the verified `user_key` (not IP — D49/D64), in-memory per-worker (the recovery idiom), 429 when exceeded. No Redis (D66).
- [ ] **v1: query governance (remainder)** — result caching for repeat queries, `EXPLAIN`-style cost hints in the response. (Redis deferred to the social real-time work — D66.)

### Lane: app-store-metrics (D49)
**Owns:** `api/app/v3/services/clickhouse.py`, `api/app/endpoints/`, `api/app/services/config.py` (n/a — D48), `sdk/src/`, `marketing/marketing-ui/src/pages/`, `clickhouse-init/`, `e2e/tests/`

The store's raw ping-count `visits` is retired as a metric (D49). Replaced
with real-user activity: one `app_visits` table, gated at ingest (1 row per
(app, real user) per 3h, anon dropped), metric-as-query (no counters to
race or pile on). `apps` becomes a stable registration record. Headline +
sort = `users_30d`. The `/stats` node macro shows the same active-user set
across all apps. The decision bite is done (D49); the build follows.

- [✓] Decision: D49 — real-user windowed metrics, anon dropped at ingest, `users_30d` headline, pagination, sign-in re-ping required (`knowledge/strategy/decisions.md`)
- [✓ 3.15.0] Table: `app_visits (app_url, username, seen_at)` + DDL template + boot self-heal
- [✓ 3.15.0] Ingest: gated append per (app, user) if latest `seen_at` > 3h (or first); anon dropped at ingest — verified token only (I2, no unsigned decode)
- [✓ 3.15.0] `apps`: stop appending per ping — append on first registration or real metadata change only; retire the `visits` counter column as a store metric
- [✓ 3.15.0] Metrics: `visits` (count of windowed rows) + `users_1d/30d/90d/1y` (distinct real users, trailing windows) — realtime over `app_visits`
- [✓ 3.15.0] Store: paginated app list (`limit`/`offset`, sort `users_30d` desc, `visits` tiebreak); grid card shows `users_30d` headline, detail page shows the full set
- [✓ 3.15.0] `/stats` macro: node-wide `users_1d/30d/90d/1y` (all apps, same query minus `GROUP BY`); homepage stat bar leads with the all-time `users` count (3.27.5 — operator preference: all-time, not the 30d window)
- [✓ 3.15.0] SDK: token in the register ping + re-fire the ping on the sign-in transition (required — else the metric means "returning users")
- [✓ 3.15.0] Hardening (folded in): #4 URL normalization in `register_app` (lowercase host, one trailing slash); #7 manifest byte cap in `/pwa_listing`
- [✓ 3.15.0] Tests: unit (gated ingest, anon-drop, forged-token I2 anti-test, metrics, pagination) + e2e (real signed-in user → active count; pagination boundary)
- [✓ 3.15.0] KB: `app-store/overview.md` metrics section + `db/clickhouse.md` `app_visits` table
- [✓ 3.17.2] D52 (spec, PR #682): decision + `app-store/endpoints.md` — page not modal, `GET /v3/apps/detail?url=` (public, pure read), URL is the key, reviews = rating + comment, D49's metric set + node macro
- [✓ 3.17.3] D52 (build): `GET /v3/apps/detail` (app + metrics + ratings + node macro; 404 unknown/unapproved; pure read)
- [✓ 3.17.3] D52 (build): `app_ratings.comment` (DDL + boot ALTER, named-column insert, 1000-char cap, canonical-url keying) + dedup-then-filter read in `get_app_ratings` + admin aggregate
- [✓ 3.17.3] D52 (build): `list_store_apps` drops the blanked `web10apps_post_id`
- [✓ 3.17.3] D52 (build): UI — card → `/app-store/app/{urlencoded-url}`, AppDetail rewritten (metrics, reviews, rate form with token-cookie session + SDK auth popup, node context)
- [✓ 3.17.3] D52 (build): tests — 12 API unit + AppDetail/AppCard unit rewrites + 4 e2e (detail payload, 404s, rating round-trip dedup, card → page seam)
- [✓ 3.61.1] Plug slots back to the first-party catalog — **Flagship** (web10 social) + **Core** (node console), not "Most Popular"; the grid dedupes a registered flagship copy (no duplicate web10 social); the flagship metric matches its real registration by display name. (marketing-ui `AppStore.tsx` + tests; KB `app-store/overview.md` made precise.)
- [✓ 3.67.2] **web10 hub** — the Core plug slot (the node console / authenticator) renamed "The node console" → "web10 hub" (the manifest name the operator prefers) and made to show its **real** `users_30d` (it registers like any app via the SDK auto-ping; the metric is captured from the list fetch before the known-host-root grid filter drops it). The grid dedupes the auth-host registration so web10 hub never renders twice. (marketing-ui `AppStore.tsx` + `AppStore.test.tsx`; KB `app-store/overview.md` plug-slot prose + dedup rule; public docs `app-store.md`.)
- [✓ 3.70.3] **Flagship plug slot → product page** — the flagship (web10 social) plug slot opened the app directly instead of its D52 product/review page, unlike every grid tile. Root cause: the card links to the product page only with an `appId`, and the flagship's `appId` came from a lookup in the grid-filtered `apps` list — which drops the canonical `social.web10.app` (a known-host root) — so it was almost always undefined. Fix: fall back to the canonical `SOCIAL_ORIGIN` as the detail key (`appId: flagship?.appId ?? SOCIAL_ORIGIN`). (marketing-ui `AppStore.tsx` + `AppStore.test.tsx`.)
- [✓ 3.77.11] **Flagship plug slot → real user count** — the flagship plug slot showed "0 users · 30d" while the detail page showed the real count (5). Root cause: the canonical registration (`social.web10.app` at its root) is a known-host root, so the grid filter drops it from `apps` and the `flagship` lookup in `firstParty` misses → `users_30d` = 0. Fix: a `flagshipMetric` state captures the metric from the list fetch before the filter (same pattern as `consoleMetric`, 3.67.2); the `firstParty` useMemo falls back to it. (marketing-ui `AppStore.tsx` + `AppStore.test.tsx`.)

### Lane: groups-directory (D53)
**Owns:** `api/app/v3/endpoints/groups.py`, `api/app/v3/services/clickhouse.py`, `clickhouse-init/`, `ui/src/components/` (group management), `marketing/marketing-ui/src/pages/` (or `web10-social/`), `e2e/tests/`

The group store (D53): a public, anon-browsable directory of the groups that
are listed, plus a flexible by-ID detail. Two orthogonal controls: a
**`discoverable` boolean** on `group_contracts` is the *blasting* flag (listed
or not) — **discoverable by default** (default `true`), except `invite_only`
groups (default `false`) and the discover group (explicit `false`);
**membership** controls whether a reader can see the posts (I3). The
directory is a **minimal, canonical view** (no table) over `group_contracts`
⋈ `group_members` ⋈ `group-identity-service`; tags + rich display live in
`group-identity-service`; topic search is a composition. The **detail** is a
flexible, principal-based read (unlisted-model): any existing group reachable
by ID, posts gated by the *reader's* membership, only a non-existent group
404s. I3 holds end to end. The KB is the spec:
`knowledge-base/web10-v3/groups/discoverability.md` + `detail.md`.

- [✓] Decision: D53 — `discoverable` boolean (blasting flag, **default `true`**, `invite_only` + discover group `false`) separate from membership (readability); directory = minimal view (no table); detail = flexible principal-based read (unlisted-model, no 404 for non-discoverable); tags in `group-identity-service`; search by composition (`knowledge/strategy/decisions.md`)
- [✓] KB: `groups/discoverability.md` (two controls, discoverable-by-default, minimal directory view, `group-identity-service` + tags, composition search, invariants) + `groups/detail.md` (unlisted model, listing/reachability/content split, principal-based read, metadata vs posts, why no constrained detail, invariants)
- [✓] Schema: `discoverable UInt8 DEFAULT 1` on `group_contracts` (DDL template + boot-time `ALTER ... ADD COLUMN IF NOT EXISTS`) + `create_group` default logic (True except `invite_only`→False, named-column insert) + `get_group`/`update_group`/`delete_group` carry it + `CreateGroup`/`UpdateGroup` models + create/update endpoints + discover group created `discoverable=False` + unit tests (default logic, named-column insert, discover-group non-discoverable, endpoint pass-through)
- [✓] API: `GET /v3/groups/directory` (anon, paginated) — the **minimal** list of `discoverable = true` groups: id, name (identity, else slug), owner, join policy, member count, tags, permission summary. No posts. View over `group_contracts` ⋈ `group_members` ⋈ `group_identity`
- [✓] `group_identity` table + read path — public display metadata (name, description, banner, avatar, website, tags), group-keyed, append-only; a table (not an I3-gated documents collection) because it's public; `get_group_identity` + `get_group_identities` (batch) feed the directory name + detail display. **⚠️ SUPERSEDED by D60 (3.44.0):** the table is deleted — the face is now documents in an app-named service (`web10-social-group-identity`); the directory/detail are generic (name = slug, no face).
- [✓] API: the group detail (`GET /v3/groups/detail?group_id=`) — public, principal-based (token optional, `user_or_anon`): metadata always for an existing group; posts only if the *reader* is a member (else "join to view"); only a non-existent group 404s (unlisted-model)
- [✓] Opt-in toggle in the authenticator — "List in directory" switch on each managed group card, controls `discoverable` only (anon readability stays a separate Manage-members action); `get_groups_manages` returns `discoverable`, `v3UpdateGroup` accepts it; unit tests (API manages shape + UI toggle on/off/managed-only)
- [✓] UI: the directory screen (`marketing/marketing-ui/`) — `/groups` grid (from `GET /v3/groups/directory`, search + tag filter) + `/groups/:id` detail (from `GET /v3/groups/detail`, posts when member else "join to view", 404 state); `GroupCard` + Navbar link; 14 UI tests (card, directory, detail)
- [✓] Tests: unit (identity read + slug fallback, directory query filters `discoverable=1`, directory endpoint shape, detail: ghost 404 / non-discoverable reachable / member sees posts / non-member "join to view" / anon reads as anon) + e2e (`groups-demo.spec.ts`: directory lists discoverable + excludes non-discoverable; detail 404s ghost / reaches non-discoverable; member sees posts, non-member "join to view")
- [✓ 3.24.0] D53 amendment: NOT discoverable by default — `create_group` defaults `discoverable` to `False` for every join policy (listing is an opt-in; `invite_only` special-case subsumed); DDL default `0` (template + boot `ALTER`); KB amended (decisions.md + discoverability.md)
- [✓ 3.24.0] Backfill (one-time, sentinel-gated) — `_migrate_discoverable_default_flip` delists groups created under the earlier discoverable-by-default rule; a `node_config` sentinel marks completion; only ever moves groups OUT of the directory; concurrent-safe; 3 API unit tests
- [✓ 3.24.0] Contract policy editors work (authenticator) — the "Settings" TODO becomes a real `GroupSettingsDialog` join-policy editor; roles editor + discoverable toggle verified end-to-end; `groupDisplayName` bug fixed (returned `users/<username>`, now the slug); 5 UI unit tests
- [✓ 3.24.0] Torture tests — `e2e/tests/group-contract-editors.spec.ts` (11 tests): API floor (join_policy/roles/discoverable update persists; I3 anti-test: non-member update rejected, the `CRUD` 401) + browser gauntlet (join-policy change → persisted + badge; cancel fork; save-failure fork → status-bar error, no crash; roles add → persisted; empty-role-name anti-test; discoverable toggle ON → listed / OFF → delisted)

### Lane: d58-backend (Stage 0 — the keystone, sequential)
**Owns:** `api/app/v3/endpoints/groups.py` + `services/clickhouse.py` + `models/` (role shape + gates + backfill + identity write), `api/tests/` (conformance re-pin).
**Task blocks + kickoffs:** `strategy/v3-groups-overhaul/stage-0.md` (umbrella: `strategy/v3-groups-overhaul.md`).

D58 replaces the group permission model the KB described but the code never
built. Roles become **per-service permission maps** (the `services` array was
decorative / unenforced). Access is granted to three **nested principal
classes** — `anyone` / `authenticated` / `member` (retiring the `anon`
misnomer) — stored as reserved keys in `group_members`. A principal's
effective role is the **union** of the grants on every class they belong to.
**Reads are role-gated** (content); **identity stays public** (the face).
Public / private = a role grant to `anyone` / `authenticated` — no new flag.
Management ops live under the reserved `'group'` service key. One role per
person (already the code). Closes the attach hole (the write side gets the
same per-service gate). Stays **v3** (operator: pre-prod). The KB is the spec:
`groups/access.md` + D58.

**This is the one stage that does NOT parallelize** — it is a single
coordinated change across `groups.py` + `clickhouse.py`. One workspace, done
in order. It gates Stage 1 and Stage 2. **Gated on the in-flight PRs that
touch these same files landing first** (#734 node-ads, #727 create-group) —
re-base off dev before starting.

- [✓] Decision: D58 (`knowledge/strategy/decisions.md`) — per-service role maps + principal classes + union semantics + reserved `group_members` keys + role-gated content reads + public identity + public/private via class grants + the `'group'` management key + one-role-per-person + the attach-hole fix + conservative backfill; stays v3
- [✓] KB — new `groups/access.md` (canonical model reference) + `identity.md` / `overview.md` / `discoverability.md` / `social-contracts.md` / `requests.md` / `detail.md` re-aligned to the per-service map shape + principal classes (the "service-scoped roles" + "multiple roles per user" fiction retired; `anon`-as-member → `anyone`/`authenticated` grants; membership-gate → effective-role-gate)
- [✓ 3.42.0] **1. Role shape + read gate + write gate** — roles stored as per-service maps; the read path computes the reader's **effective role** (union over `anyone` / `authenticated` / member role) and gates content reads on per-service `readAll` (replaces the membership-only check); the write/attach path gates on the effective role granting the op on the service (closes the attach hole); management ops check the `'group'` key. Both role shapes normalized on read (old clients keep working until Stages 1–2 migrate).
- [✓ 3.43.0] **2. Backfill (one-time, sentinel-gated)** — fan the old flat `permissions` out across the old `services` list (`['*']` → `'*'` key) over `group_contracts`; rename the discover board's `anon` member row → `anyone`; **conservative visibility default** (no existing group besides discover becomes `anyone`-readable — owners opt in). Role-shape fan-out in 3.42.0; the `anon` → `anyone` rename + the `ensure_discover_group` enrollment change in 3.43.0.
- [✓ 3.42.0] **3. Identity write endpoint** — the group's face (name, description, banner, avatar, website, tags) written to the public `group_identity` table, gated by a role grant on `group-identity-service` (owner / `page-curator`); lands *on* the D58 model
- [✓ 3.43.0] **4. Conformance re-pin** — I3 re-pinned from "membership grants access" to "effective role grants access"; stronger anti-tests (anon vs private group, signed-in vs signed-out, member ⊇ stranger ⊇ visitor monotonicity; the attach-hole anti-test). Pinned in `test_v3_access.py` (all five principal-class forks; the monotonicity invariant added in 3.43.0). The stub `test_v3_conformance.py` is a separate, larger effort.

### Lane: d58-demos (Stage 1 — parallel, one workspace per demo)
**Owns:** `marketing/marketing-ui/public/docs/<demo>/` — each demo owns its own dir, so the lanes never touch each other.
**Task blocks + kickoffs:** `strategy/v3-groups-overhaul/stage-1.md`.

**Gated on `d58-backend`.** The demos are the reference implementation (D46) —
they run the real SDK consent flow, so getting them green proves the backend
end-to-end before the social app (the integration test, Stage 2) builds on it.
Each demo below is an **independent lane** — N workspaces run N demos at once.
Per demo: adopt the per-service role-map shape in its `createGroup` role
literals (the old `{services, permissions}` → `{permissions: {service: [ops]}}`),
and drive a **public/private + identity fork** in its e2e (set the group's face
+ grant/revoke the `anyone` read role → assert a bystander's read).

- [✓ 3.45.0] **media-demo** — `docs/media/` (creates `media-{username}` with roles)
- [✓ 3.45.0] **notes-demo** — `docs/notes/` (creates `notes-{username}` with roles)
- [✓ 3.45.0] **sharing-demo** — `docs/sharing/` (creates `sharing-{username}` with roles)
- [✓ 3.45.0] **groups-demo** — `docs/groups/` (the richest — `ROLE_PRESETS`, create/join/roles/invite; the reference for the new shape)
- [✓ 3.45.0] **messages-demo** — `docs/messages/` (DM groups with roles)
- [✓ 3.45.0] **feed-demo** — `docs/feed/` (discover/followers groups with roles)
- [✓ 3.45.0] **tasks-demo** — `docs/tasks/` (user-named groups with roles)
- [✓ 3.45.0] **SDK role type** — `sdk/src/` `V3GroupRole` → the per-service map shape (the shared type the demos + social app both reflect; small, can run alongside)

### Lane: d58-social (Stage 2 — parallel, one workspace per feature)
**Owns:** `marketing/web10-social/` (fan-facing) + `ui/src/components/Groups/` (admin-facing) + `sdk/src/` (role type). Each feature below is an **independent lane** — different files, so they run in parallel.
**Task blocks + kickoffs:** `strategy/v3-groups-overhaul/stage-2.md`.

**Gated on `d58-backend`** (and ideally `d58-demos` green as the proven
reference). The social app is the integration test — it wires up what the
backend + demos already prove. Fan-facing (web10-social) and admin-facing
(ui/) are separate apps → separate parallel lanes.

- [✓ 3.46.0] **role definitions** — `web10-social/src/data/groups.ts` `FOLLOWER_ROLES` / `COMMUNITY_ROLES` / `DM_ROLES` → the per-service map shape (the shared seam; small, do early)
- [✓ 3.46.0] **group profile (fan-facing)** — `GroupDetailScreen.tsx` renders the group's face: banner (cover) + overlapping avatar + name + about + tags + website (the Facebook-shaped hero), from the `web10-social-group-identity` service (D60)
- [✓ 3.46.0] **public/private (fan-facing)** — the detail shows a public/private badge; the create-group dialog gains a visibility control (public / signed-in-only / private) that carries the initial `anyone`/`authenticated` grant
- [✓ 3.46.0] **group profile editor (admin-facing)** — `ui/src/components/Groups/` a profile editor (name, description, website, tags) next to the existing Settings/Roles/Members dialogs → writes the face via the normal CRUD path (D60)
- [✓ 3.46.0] **public/private control (admin-facing)** — `ui/src/components/Groups/` a "Who can read" control (public / signed-in-only / private = grant/revoke the `anyone` / `authenticated` read role)
- [✓ 3.46.0] **feed + detail effective-role read** — the detail renders what the role-gated read returns (a bystander on a private group sees the face + "join to view"; on a public group sees posts) — the API does the gating

### Lane: admin-console (Phase 3)
**Owns:** `ui/src/components/Config/`, `api/app/endpoints/system.py`, `api/app/services/config.py`

The node console's operator surfaces (Node Config panel first). The
panel is the node's control surface — it must show what the node
actually runs, and every control on it must work.

- [✓ 3.16.0] Node Config: effective config in the form (settings defaults ← saved overlay — no more blanks; ClickHouse URL + MinIO values default to the docker-network settings) + field trimming (Node Identity → provider/CORS/token-expiry; Stripe → mode + keys) + the dead Save button fixed (PATCH /config 405 → POST /config/update)

### Lane: platform-telemetry (D56)
**Owns:** `marketing/marketing-ui/src/lib/analytics.ts`, `marketing/web10-social/src/lib/analytics.ts`, `ui/src/lib/analytics.ts`, the three frontends' `main.tsx` + `Dockerfile`, `ubuntu-deployment/docker-compose.ecosystem.yml` (frontend build args), `knowledge/knowledge-base/web10-v3/telemetry.md`

Full-platform telemetry (D56): GA4 + Hotjar on every user-facing
surface, the recording content-blind by construction (maskAllText +
blockAllImages), GA4 events content-free by convention, max tracking
with `advertising_id: 'OFF'` as the single kept flag. The trade is
terms-level, not a consent popup. Supersedes the old "platform surfaces
stay recording-free" rule. The KB is the spec — read it first:
`knowledge/knowledge-base/web10-v3/telemetry.md`.

- [✓ 3.27.1] Decision: D56 (`knowledge/strategy/decisions.md`) — every surface tracked; recording content-blind by construction; GA4 events content-free by convention; `advertising_id: 'OFF'` kept; the trade is terms-level
- [✓ 3.27.1] KB: `knowledge-base/web10-v3/telemetry.md` — the why (compete with Meta/TikTok on UX), the use case, the technical how (GA4 + masked Hotjar, env-gated, per-app `src/lib/analytics.ts`), the line it does not cross, logistics
- [✓ 3.27.1] Build: all three surfaces — web10-social gains masked Hotjar (GA4 already there, max-tracking config); marketing-ui gains GA4 (in-house beacon + Hotjar already there, Hotjar moved to the canonical masked init); the authenticator gains both (new `ui/src/lib/analytics.ts` + initial pageview — query-parameter-driven, no router); `hotjarIdentify(username)` on login in web10-social; unit tests per app (no-op without env, script load, masking config pinned, idempotency, identify)
- [✓ 3.27.1] Deploy wiring: `VITE_GA4_MEASUREMENT_ID` + `VITE_HOTJAR_SITE_ID` baked at build time — Dockerfile ARG/ENV on all three frontends, compose passes `GA4_MEASUREMENT_ID` / `HOTJAR_SITE_ID` per environment (empty = tracking off), env examples updated
- [✓ 3.27.2] Positioning realignment: the docs stop reading "anti-analytics" — thesis.md gains the "and it tracks hard (D56)" section; the manifesto's "nobody is mining you" is narrowed to content (never scanned/sold/fed to the ad machine) + the candid telemetry parenthetical; AGENTS.md gains the Telemetry (D56) operating rule; the README premise table gains the "Built like the best, owned like yours" row; design.md drops the stale "privacy-first" justifications
- [✓ 3.27.3] Runtime-configurable IDs: the GA4/Hotjar IDs live in `node_config` (ClickHouse), set in the Node Config UI (Telemetry card), resolved at page load via a public `GET /telemetry` (node authoritative, build-time env is the dev fallback) — no rebuild to change them. Also fixed the Node Config save (flat body vs the API's `{token:{token}, update:{...}}` — every save 422'd)
- [ ] Terms copy: the tracking disclosure on the marketing site (the "wrong platform for you if you arent ok with that" line) — gated on a terms surface existing (there is no terms page yet)

### Lane: public-docs (audience model)
**Owns:** `marketing/marketing-ui/public/docs/`, `marketing/marketing-ui/src/pages/Docs.tsx`, `marketing/marketing-ui/public/docs/schemas/`

The public docs are the audience-tuned surface of the KB (the KB is the root of
trust — `knowledge/knowledge-base/web10-v3/`). The current docs are thin, not
organized by audience, and drifted (`sdk.md` teaches a non-existent Mongo-style
API). The reframe: an explicit **audience model** — sections for **Users**,
**Developers**, **Node Operators / Influencers**, **Monetizers** (+ the pitch).
Each doc names its reader and speaks clearly to that reader. Full breakdown in
`plan.md` → "Public Docs Overhaul (audience model) — Docs". **Sequencing:** the
drift fix first (the docs are actively wrong), then the audience sections
(Users → Developers → Node Operators → Monetizers), then the rendering/UX.

- [✓ 3.60.3] **Drift fix: rewrite `sdk.md` to the real SDK** — every example to the actual surface (`createV3Client`, `openAuthPortal`+`authListen`, `read(collection, {groups, limit, offset, ref})`, `update(docId, body)`, `delete(docId)`, `query(sql, {groups})`, the group/contract/media/account ops). Source: `KB sdk/api.md` + `sdk/src/v3.ts`. Acceptance: every code block matches the real SDK; copy-paste works. (The KB `sdk/api.md` was aligned to the same surface first — it carried the same drift.)
- [✓ 3.60.4] **Drift fix: rewrite `KB sdk/implementation.md`** — the "How Layer" (SDK call → ClickHouse SQL) carries the same Mongo-style drift: `w.update(collection, {_id}, {$set}, {$groups})` → `update(docId, body, {groups})`, `w.delete(collection, {_id})` → `delete(docId)`, `w.read(collection, {_id, groups})` → `readById(docId, collection)`, `w.createGroup({…})` → `createGroup(name, joinPolicy, roles, members)`, `w.getGroups({member})`/`getGroups({manages})` → `getMyGroups()`/`getGroupsManages()`, `w.getMembers`/`removeMember` → `getGroupMembers`/`removeGroupMember`, `w.blockSharing` → `setSharing`, `w.upload`/`getReadUrl`/`list` → `requestMediaUploadUrl`/`getMediaReadUrl`/`listMedia`, `w.getServiceContracts`/`revokeServiceContract`/`revokeAllServiceContracts` → `listAppContracts`/`revokeAppContract` (the table is `app_contracts`, not `service_contracts`); drop the v4 power-mean `$sort` + cross-node addressing sections (v4 features misplaced in v3). Source: `sdk/src/v3.ts` + `api/app/v3/`.
- [✓ 3.60.5] **Drift fix: bring `protocol-spec.md` current** — add the query engine (D63), recovery (D61), engagement model (D62), ads (D55), D58 role shape, the read `ref`+`count` shape, the rate limit (D65); fix the token format; un-draft it.
- [✓ 3.60.6] **Drift fix: `conventions.md` + `groups.md`** — real `read` opts (drop `$match`/`$sort`/`$limit`); D58 role shape; the engagement model (D62).
- [✓ 3.60.7] **For Users** — `getting-started`, `groups-in-plain-terms`, `your-data` (export / kill switch / opt-out), `account-recovery` (D61), `import-from-other-platforms` (expand the placeholder `export-guidance` + the web10 import flow).
- [✓ 3.60.8] **For Developers** — `query-engine` (NEW), `app-contracts` (NEW), `media` (NEW), `scaffolding` (verify the CLI exists).
- [✓ 3.60.9] **For Node Operators / Influencers** — `start-a-node` (NEW), `node-config` (NEW), `app-store` (NEW), `your-audience` (NEW), `being-a-creator` (NEW).
- [✓ 3.60.10] **For Monetizers** — `ads` (NEW), `ad-catalog` (NEW), `affiliate-programs` (NEW), `payment-rails` (NEW), `monetization-bootcamp` (link the existing guide).
- [✓ 3.61.0] **Rendering / UX** — reorganize the `Docs.tsx` sidebar by audience; a "who are you?" landing that routes the reader to their section.

### Lane: import (port your YouTube)
**Owns:** `api/app/services/importers/`, `api/app/v3/services/import_worker.py`, `api/app/v3/endpoints/imports.py`, `api/app/v3/models/imports.py`, `ui/src/components/Settings/Import.tsx`, `knowledge/knowledge-base/web10-v3/social/import.md`

The first import target: port a YouTuber's catalog (via Google Takeout) onto
their node. The pipeline runs **on the node** (not a sidecar) — an in-process
durable worker (the transcode-worker idiom, no Redis per D66) that streams the
export parts from MinIO, extracts tar/zip, and writes in order (media → posts →
comments → profile) into `staging_posts` (D30, owner-only, followers group),
then deletes the raw export. The honest gaps: no video files (Takeout has no
bytes), no subscriber list (no export path). Root of trust:
`KB social/import.md`. Full breakdown: `plan.md` → "YouTube Importer".

- [✓ 3.63.0] **The import mapping** — the pure YouTube Takeout parser (`importers/youtube.py`): videos → `staging_posts` (full record + original publish date + thumbnail), comments-on-own-videos → comments (D62 `ref_value` join), channel → profile. Unit-tested (`test_importers_youtube.py`).
- [✓ 3.63.1] **The parser reads the REAL Takeout format** — validated against a live export ("jacobs multimedia"): Takeout exports YouTube as **CSV** (`video metadata/videos.csv`, `comments/comments.csv`, `channels/channel.csv`), not the Data-API JSON the 3.63.0 parser assumed (which found zero records). Rewritten for CSV: the `Comment Text` JSON-sequence flatten, precise-path classification (not loose "video" substring — `playlists/*-videos.csv` would misfire), thumbnail derived from the video id, no view/like counts (not in the CSV). Worker extractor now pulls CSV+JSON (not the 27GB of MP4s). Verified: 81 posts (15 public/66 private) + 72 comments + 1 profile from the real export.
- [✓ 3.63.0] **The node pipeline** — `POST /v3/imports` (presigned per-part upload) + `/start` + `/status`; the durable `import_jobs` ClickHouse queue + in-process worker (`import_worker.py`); `insert_document` gained a `created_at` param (backdate the catalog); ordered writes + `origin_id` idempotency + the D62 comment join. Unit-tested (`test_import_worker.py` + `test_imports_endpoint.py`).
- [✓ 3.63.0] **The authenticator UI** — Settings → Import from YouTube card (`ui/src/components/Settings/Import.tsx`): pick Takeout files (tar/zip, multiple) → upload → start → poll; all states designed (idle/ready/uploading/processing/complete/error), tokens-only, `data-testid` hooks. Unit-tested (`import.test.tsx`).
- [✓ 3.63.0] **The docs** — `import-from-other-platforms.md` (honest mapping table + 2GB-tar instruction), `export-guidance.md` (the 2GB-tar note), `KB social/import.md` (the root of trust).
- [ ] **The video pipeline (Phase 2)** — the export includes the actual MP4s (`videos/*.mp4`, ~27GB for the operator's channel). Stream each from the export to MinIO (not to memory), create a media doc, wire it to the post (`media_refs` + `video: {type: minio, value: object_key}`). Playback is a direct presigned MP4 read (`web10-social`'s `<video src={read_url}>`) — **no HLS transcode required** for native playback. Gated on the operator's go-ahead (disk + upload-time implications of a multi-GB upload).
- [ ] **The "port your YouTube" landing** — a creator-facing marketing page (the reach gap + the owned-audience reframe + the import flow). Feeds the `For Monetizers` / `For Node Operators` doc sections.
- [✓ 3.67.1] **The real import e2e** — `e2e/tests/yt-import.spec.ts` (a dedicated spec, not a re-pin of the gutted `exporter.spec.ts`): drives the real node pipeline against the operator's **actual** Takeout export (`e2e/fixtures/yt-takeout.mjs` reads the real CSVs out of the `takeout-*.zip` parts, packs a small zip, computes expected counts via the parser's own logic; a committed synthetic Takeout is the CI fallback; `YT_IMPORT_LIMIT` bounds the chunk, default 12). API floor (real Takeout → fresh account: catalog + D30 public/private split + D62 comment join + profile; idempotent re-import; I3 stranger 403) + browser gauntlet (the real Import card, default-port-gated). Also fixed the `ensure_bucket` concurrent-create race (`BucketAlreadyOwnedByYou` → idempotent).

### Lane: bugbot (D70)
**Owns:** `api/app/v3/services/bugbot.py`, `api/app/endpoints/system.py` (the `submit_bug_report` hook), `api/tests/test_bugbot.py`, `knowledge/knowledge-base/web10-v3/social/bug-reports.md`

Bug reports push to the node's admins as a DM from the `bugbot` user (D70).
The report is already durable in the `bug_reports` table; this is the
delivery. The bot is a real node user (lazy-provisioned, idempotent); the DM
is the standard 2-member `dm-bugbot-{admin}` group + a `posts` doc (the
`sendDm` body shape + `report_id` + screenshot count). Best-effort: a
delivery failure logs and never blocks the submit. Recipients = the node
admin list (`config.list_admins()` — the same list `check_admin` gates on).
Spec: `knowledge/strategy/plan.md` → "Bug Reports: The Bugbot (D70)".

- [✓ 3.79.0] **Decision: D70** (`knowledge/strategy/decisions.md`) — DM push from `bugbot`, not email / not a push channel / not a console UI; lazy idempotent provisioning; recipients = the admin list; best-effort never blocking.
- [✓ 3.79.0] **KB** (`knowledge/knowledge-base/web10-v3/social/bug-reports.md`) — the model (table = the record, DM = the pointer), the bot's identity, the message shape, the invariants (the bot is a user; I1/I2/I3 hold).
- [✓ 3.79.0] **The bot service** (`api/app/v3/services/bugbot.py`) — `ensure_bugbot_user()` (idempotent, random unguessable password, no contact) + `deliver_bug_report(report)` (per-admin: ensure the DM group — both creator-embedded id shapes, the admin may have DM'd the bot first — then the `posts` doc authored by `bugbot`, gated by `can_write_group`).
- [✓ 3.79.0] **The hook** (`api/app/endpoints/system.py`) — `submit_bug_report` calls `deliver_bug_report` after the insert, wrapped so a failure logs + swallows (the submit is already durable; the response is unchanged).
- [✓ 3.79.0] **Tests** (`api/tests/test_bugbot.py`, 19) — provisioning idempotency; delivery to every admin (group contract = the DM shape, doc authored by `bugbot`, body shape, `report_id` + screenshot count present); the admin-created-first group shape is found, not re-created; no admins → no-op; delivery failure → submit still 200 + logged; the I3 boundary pinned compositionally (the doc attaches only to the DM groups; the group's members are bot + admin; the D58 read gate is conformance-pinned).
- [ ] **v1** — the admin review queue UI (console: browse `bug_reports`, fetch screenshots via the detail endpoint); email as a second channel (gated on a provider decision).

### Lane: share-preview (D71)
**Owns:** `api/app/v3/endpoints/share.py`, `api/tests/test_share_endpoint.py`, `marketing/web10-social/nginx.conf` + `Dockerfile` (the edge split), `marketing/web10-social/index.html` (static tags), `ubuntu-deployment/docker-compose.ecosystem.yml` (the `API_PROXY_TARGET` env), `api/app/settings.py` (`SOCIAL_ORIGIN`), `knowledge/knowledge-base/web10-v3/social/share-preview.md`

A shared post link is rich: the node renders the post's Open Graph / Twitter
Card tags (title, description, **thumbnail**) for link-preview crawlers, while
the browser keeps the SPA. The edge split (the social nginx proxies the
post-permalink to the node for known crawler User-Agents only) is the seam.
The privacy floor is the D58 read gate — only what an anon visitor could
already read may preview; a private / followers-only post renders a generic
card. Spec: `knowledge/strategy/plan.md` → "Share Preview: Rich Post
Permalinks (D71)".

- [✓ 3.83.0] **Decision + KB** (`knowledge/strategy/decisions.md` D71, `social/share-preview.md`) — the node renders the preview, the browser gets the SPA; browser-default User-Agent split; the D58 read gate is the privacy floor.
- [✓ 3.83.0] **The endpoint** (`api/app/v3/endpoints/share.py`, `GET /v3/share/post/{username}/{post_id}`) — public, no token; read-by-doc_id + the public-readability gate + the thumbnail selection (first-media-with-image → image `read_url` / video `thumbnail_url` / author avatar / brand mark) + the generic-card floor + ghost → 404. `SOCIAL_ORIGIN` setting for the canonical `og:url`.
- [✓ 3.83.0] **The edge split** (`nginx.conf` → template, `Dockerfile` → `/etc/nginx/templates/`, `API_PROXY_TARGET` in the deploy compose, `resolver 127.0.0.11`) + static `og:`/`twitter:` tags in `index.html` for the app root / non-post routes.
- [✓ 3.83.0] **Tests** (`api/tests/test_share_endpoint.py`, 8) — public → og tags + thumbnail; video → `video.other` + poster; **private → generic, no leak** (I3 anti-test); ghost / non-post → 404; text-only → brand image; truncation; avatar fallback. 986 api + 494 social green, `tsc --noEmit` clean; nginx split verified via `nginx -t` + a docker e2e (crawler → proxied, browser → SPA, non-post never proxied).
- [✓ 3.90.0] **Profile permalink** (`GET /v3/share/profile/{username}`, `api/app/v3/endpoints/share.py` + `nginx.conf` + `api/tests/test_share_endpoint.py`) — the profile's face (avatar as `og:image`, display name, bio, `og:type=profile`) for crawlers; the nginx edge proxies `/u/:username` (mutually exclusive with the post permalink) to it for known link-preview bots. No anon-read gate (a profile's face is public identity — the avatar is already on the discover board; no private accounts); a no-profile user previews as a generic `@username on web10` card. `test_share_endpoint.py` +4 (og tags, no-avatar → brand mark, unknown user → generic card, bio truncation). 990 api tests green.
- [✓ 3.91.0] **Generic thumbnailing utility + the social card moves to the social app's preview server (D60, Option A)** (KB-first: `media/thumbnailing.md`) — operator: "this share.py is totally against the knowledge base…… all the functionality of web10 is supposed to be generic." `share.py` baked social concepts (posts, profile, the `/u/:username` permalink, the author-avatar → brand-mark fallback) into the universal platform — a D60 leak. Now the platform keeps only universal primitives: the pure `pickThumbnail` (schema-free selection over resolved media) + `POST /v3/media/thumbnail {doc_id}` (access-checked; a doc with media or a media doc itself) + `POST /v3/preview/render` (the generic card renderer — spec in, OG/Twitter HTML out, zero social knowledge). `share.py` is **deleted** — the node has zero social concepts. The social card is the social app's tail: a small Node preview server (`marketing/web10-social/preview/` — `card.mjs` + `server.mjs`) in the social container alongside nginx maps the social permalink → doc → `media/thumbnail` → the social fallback → `preview/render`. The social nginx User-Agent split proxies crawlers to it; browsers get the SPA. Deploy: Dockerfile installs Node + `docker-entrypoint.sh` (preview server + nginx); nginx → `${PREVIEW_TARGET}`; compose drops `API_PROXY_TARGET`, adds `PREVIEW_TARGET`/`API_ORIGIN`/`SOCIAL_ORIGIN`/`PREVIEW_PORT`. `test_thumbnail.py` (14) + `test_preview_render.py` (10) + `v3.test.ts` +7 + `previewCard.test.ts` (8). 1001 api + 117 sdk + social green, `tsc` clean.
- [✓ 3.102.0] **Follow-up** — group permalink (same preview-server pattern: the group's face — cover → avatar → brand mark from the `web10-social-group-identity` doc, D60 — as the image). A new route in the same social preview server. The SPA encodes the group id (which contains slashes) into a single path segment; `preview/server.mjs` gains `/groups/:groupId` (captures `[^/]+`, decodes), `nginx.conf` gains the crawler edge split for `/groups/<segment>` (mutually exclusive with the `/u/` permalinks). A group whose face isn't anon-readable (or has no face) previews a generic web10 card (the I3 floor). `previewCard.test.ts` +4 (cover / avatar / brand fallbacks + no-read → generic, no leak). 693 social tests green, `tsc` + `vite build` clean; e2e-verified against the live production API. KB `social/share-preview.md` (new "The group permalink" section) + `media/thumbnailing.md` (open question closed).

### Lane: ci-infra (cross-cutting)
**Owns:** `.github/workflows/` (ghcr-mirror.yml, e2e.yml, docker.yml, cd.yml), `e2e/mirror-images.json`, the `FROM` base-image lines in every `Dockerfile`, the runtime `image:` lines in the compose files, `ubuntu-deployment/docker-compose.ecosystem.yml`

CI + deploy pull every external image from our own GHCR namespace
(`ghcr.io/jacoby149/base-*`) so Docker Hub's anonymous pull rate limit
(100/6h per shared runner IP) and third-party registry availability are out of
the runtime path. `e2e/mirror-images.json` is the single source of truth;
`ghcr-mirror.yml` republishes each source → target (manifest copy, retry on the
source pull). To bump an image: edit its `source` in the manifest, re-run the
mirror.

- [✓ 3.84.0] **GHCR mirror + repoint** (`ghcr-mirror.yml`, `e2e/mirror-images.json`, 8 Dockerfiles, 3 compose files, e2e.yml + docker.yml GHCR login) — the e2e shards flaked on `pull access denied for minio/minio` (Docker Hub anonymous rate limit on shared runner IPs; probe PR #887). Mirror every external image (minio, clickhouse 24.8/24.3/latest, nginx-proxy, bun, nginx, python, static-web-server, uv) to `ghcr.io/jacoby149/base-*`; repoint every `FROM` + runtime `image:`; authenticated pulls (GITHUB_TOKEN, 1000/h). No `docker login`, no Artifactory — GHCR is the built-in lightweight registry.
