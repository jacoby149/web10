# Schema-id reuse state (per-provider, environment-specific IDs).
# Generated by seed_personas.py to avoid re-registering Reaction/Comment
# schemas every run (register_schema has no dedup).
.seed-state.json