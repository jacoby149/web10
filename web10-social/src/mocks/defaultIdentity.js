import head from "../assets/images/Contact.png"

const defaultIdentity = function (web10) {
        return {
                name: "add a name ...",
                web10: web10,
                pic: head,
                bio: "add a bio ...",
        }
}

export default defaultIdentity;