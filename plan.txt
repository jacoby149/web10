web10 plan
==========

PRIORITY ONE (operator, 22.07.2026): THE KILLER APP, PROUD AND
WORKING, BEFORE ANYTHING ELSE. supersedes priority zero (19.07) —
its baseline chain is ticked (A7 real data, B6 auth, D16.1/D17
docs, D16 mostly), but the honest state is still "a shell of a
non-working platform": the pieces exist and the whole doesn't
hold, and the board keeps surfacing work that sounds like an
infrastructure company (sdk rewrite, ux telemetry, provisioning)
instead of a social platform. reprioritization: web10-social IS
the product until further notice. every task is judged by ONE
question — "does this make the social app something the operator
opens on his phone, in front of someone, and is proud of?" if the
answer is no, it waits, explicitly and without guilt (see PARKED
in parallel execution.txt: C2/C3/C3.5, D11, E4, E8, docs prose).
these are good ideas whose time is not now (D20 said protocol
serves platform; this is that decision enforced on the task
board, logged as D29).

[✓ 1.0.140] the bar is THE GAUNTLET — run from the operator's phone against
      dev, top to bottom, no apologies. step one of the work is running
      it and writing the honest fail list (docs/gauntlet-23.07.2026.md); fixes
      land in order of demo impact:
  1. sign up + log in on the social app without a broken screen
  2. post a photo -> it appears in the feed immediately
  3. follow a persona -> their posts land in your feed
  4. like + comment -> counts update, feel instant
  5. dm a persona -> the thread reads like a real messenger
  6. your profile reads as a creator page you'd screenshot
  [✓ 1.0.145] 7. trending/discover shows a real, alive feed (personas keep it
      from ever demoing empty)
  8. nothing white-screens; every screen is design.md-grade at
     375px, on a real phone, on dev AND prod
each step that passes gets encoded as a playwright journey in e2e/
so it can't regress (C6 continued — the referee for "works").
[✓ 1.0.145] gauntlet.spec.ts landed: 8 step-journeys (passing = real
      assertions; failing = test.fixme scaffolds with documented blockers).
      HONESTY NOTE (23.07): the 1.0.140 gauntlet run was a DESK run (api
      probes + code audit — the report's own method line says "NOT a real
      phone"). the operator's phone passes are the real referee: a
      STANDING weekly calendar event until the M0 gate, findings filed as
      lane items same-day (the 22-23.07 find→file→fix loop). journeys
      must assert the BAR, not the happy path — the profile-media-refresh
      bug got past step 6's save+read assertion.
      HONESTY NOTE 2 (24.07 audit): steps 2-3 pass on SEEDED personas
      only. a real account's post reaches NO ONE: the composer sets no
      visibility (PostComposer.tsx:379 → posts.ts:37 routes to
      private_posts — owner-only, never discovery-indexed), and nothing
      fans out on write anywhere (the app never writes a follower's
      inbox; only persona-orchestration/seed_personas.py does,
      out-of-band). the "100% delivery by architecture" pitch is
      unimplemented in product code — fix is D-post-delivery (lane D
      queue). separately, cross-user media has NO read path: the media
      service terms are owner-only and the presign endpoint denies
      non-owners, so a follower can never render an author's photo and
      another user's avatar 403s on dev — fix is A12 + the
      D-public-media-client follow-up (decisions.md D35).
[✓ 1.0.167] ws-C journey hardening: step 6's assert-the-bar gap from
      HONESTY NOTE (above) is closed — the e2e pin now reproduces the
      1.0.157 duplicate-profile-record bug condition and asserts the
      correct record's avatar/banner refs are what the app actually
      requests, verified live by reverting the fix locally and watching
      the pin go red. Step 3 (follow -> feed, follower count) and step 5
      (new-conversation compose) flipped from test.fixme to real,
      UI-driven journeys — gates (follow UI #254, /u/:username 1.0.155,
      follower count 1.0.158, D-dm-compose 1.0.155) were already merged.
      Three findings filed in .context/ for other lanes, not fixed here
      (e2e/** only): registerDefaultSchemas() (data/feed.ts) is dead code
      — nothing calls it, so reactions/comments/follows never actually
      mirror to the public ledger; createPublicEntry() sends a body with
      no token field, so the mirror would 401 even if the schema cache
      were fixed (silently swallowed); generic CRUD read() never
      ObjectId-casts a queried _id, so resolveMediaRefs() — used for
      every avatar/banner/post image app-wide — always returns empty.
when the gauntlet passes end to end, THEN low-hanging fruit in
order of demo impact (realtime feed refresh, notifications, search
your own stuff, D16(3) store curation) — judged by the same
question.

the thesis (why this plan):
- THIS IS A PRODUCT FOR INFLUENCERS. the proposition is OWNERSHIP ON
  THE INTERNET: own your node, your brand, your domain, your audience
  relationship, your revenue. no platform between you and your
  followers. (D4: the customer is the creator; D20: this is the pitch.)
- NO SHADOW BAN, structurally: the inbox pattern (fan-out on write)
  delivers a post to 100% of followers. no reach throttling, no
  algorithm demoting you, no deplatforming. email-newsletter delivery
  semantics for social. this is architecture, not a policy promise --
  it cannot be quietly revoked.
- users own their data in mongo-model collections; apps are just
  lenses. sovereignty is what makes the creator pitch TRUE -- it
  rides along invisibly, it is not the lead pitch.
- the killer app must STAND ON ITS OWN as a plain good social app
  (instagram-shaped: post, feed, dms, media, video). no gimmicks.
  the feed is chronological + a sort dropdown -- "no algorithm" IS
  the feed feature (D20; customizability/lens/chatbox: later.md).
- exporters dump your facebook/insta/youtube life into your services
  so day one isn't empty. the app proves the thesis; the exporters
  seed it; the creator brings the audience.
- influencers/creators run nodes. accounts are free for users, paid
  for by the node operator's marketing revenue. web10 takes a small %
  of revenue flowing through its rails (dev pay already works this
  way in stripe.py).
- the self-host escape hatch has to be EASY or the ownership story
  isn't credible. one person should be able to stand up a node in
  minutes with a setup UI, no settings.py copying, no README
  archaeology.
- multi-service is fine. it's modular, it's good design. the problem
  was never the number of services, it's the lack of a setup
  experience.
- the stack is years old. fix up first, build features on modern
  ground. (largely done -- see phase 0 ticks.)


THE STORY (the pitch to influencers -- M0 must make this credible)
------------------------------------------------------------------
this is largely a story business: a killer pitch that lands as
"oh shit... this is the only way." the beats:

  1. you already don't own your audience -- and you have the number
     that proves it. 1m subscribers, and the video does 300k. subs
     are not delivery; they're permission for the platform to MAYBE
     show you. the reach gap IS the shadow ban, visible in your own
     analytics, and the rent goes up every year (organic decay,
     pay-to-boost, throttles you can't even see).
  2. and time is running out: AI influencers are arriving in volume.
     infinitely prolific synthetic creators that never sleep, never
     charge, never negotiate -- and the algorithm has no loyalty to
     humans. it optimizes engagement, and synthetic content is
     cheaper engagement. the platform doesn't need YOU anymore.
     own your persona and your channel NOW, before you're competing
     with fakes of everyone -- eventually including fakes of you.
  3. the only defense is ownership: your node, your domain, your
     brand, your audience relationship. a post reaches 100% of your
     followers BY ARCHITECTURE (inbox pattern, fan-out on write) --
     nobody can throttle you, demote you, or replace you on your own
     node. it cannot be quietly revoked, because it isn't a policy.
  4. and the ask is ADD, not MOVE. creators already post to 5
     platforms -- this is #6, except they own it. the exporters
     make onboarding a 24-hour thing: whole back catalog imported
     in one shot, then it's just one more surface in the posting
     routine they already have. nobody is asked to move their
     livelihood; the home base fills up while everything else
     continues. platforms become distribution, not landlord.
  5. and it's THE COOL THING. kick/twitch-grade slick, not fediverse
     jank. a creator's node reads as an upgrade to their brand --
     their name, their look, a place their audience brags about
     being early to -- never a protest app the audience tolerates
     out of loyalty. production quality IS part of the story; if it
     looks like peertube, the pitch dies on the first screenshot.
     (this is what phase 2.5 is for.)

the fear is real and the fear is the wedge. every month of AI-slop
growth makes this story stronger. M0's job is a product real enough
that the "oh shit" survives contact with the demo.

the user's side (not a second wedge -- a CONVERSION MULTIPLIER on
the creator sale. the number the creator watches is "what % of my
audience joined." the fan's hell-yeah moves it):
  - the fan's first sentence: "never miss a post from [creator]
    again -- you see 100% of what they make, always" (the same
    delivery guarantee, felt from the fan side), plus inner-circle
    status: founding member of the creator's OWN place, closer to
    them than any platform allows. status + proximity is what
    converts diehards, and diehards convert first.
  - the quiet second sentence (safety -- retains, never acquires):
    delete actually deletes; posts can default to expiring unless
    pinned (the honest answer to old-posts-ruining-lives -- D16's
    timed access, user-facing); your data isn't mined or sold, and
    post-phase-11 not even the operator can read what's private.
  - the wannabe-creator segment (most active users ARE aspiring
    creators, and the platforms jerk them on purpose: one post
    randomly hits, the next ten get 200 views -- a slot machine).
    honest position: we can NEVER out-dopamine tiktok and must not
    try (rebuilding the lottery = rebuilding the algorithm = thesis
    deleted). the wannabe pitch is the LADDER: keep playing the
    lottery on the platforms -- best discovery engines ever built --
    but bank every win here, where the house can't take it back.
    lottery reach resets at the platform's whim; banked followers
    compound. beat 4, one rung down the ladder.
  - and get the psychology right: users aren't fanboys, they're
    aspirants (slightly delulu -- not everyone can be famous, but
    everyone wants to be poppin, or at least have their 5k). the
    deliverable version of poppin: (a) YOUR NUMBER IS REAL HERE --
    the throttle hits small accounts hardest, so 100% delivery
    flatters them MOST: 5k followers = 5k people actually see you,
    vs. instagram showing you to 400 of them. status pitch, not
    just fairness pitch. (b) local fame: status inside a 10k
    community is legible and achievable (known regular, creator
    spotlights -- curated by a human, same philosophy as the ad
    records, never an algorithm). discord/subreddits prove people
    chase local status hard. what we never promise: lottery fame
    from strangers (see above -- no slot machine).
  - onboarding consequence: joining must hand the fan THEIR OWN
    space in the same breath ("you're not joining the crowd -- you
    get your own page here too"). the aspirant identity is honored
    at signup, not discovered later.
  - the flywheel this buys: every account is already creator-shaped
    (own space, own followers -- one collection per user gives this
    free), so the user base is the CREATOR PIPELINE. today's 5k
    wannabe is next year's 100k platform-burned prospect who is
    already home. the wordpress flywheel exactly: nobodies start
    blogs; the ones who get big never migrate.
  - the ladder's top rung -- GRADUATION: grow to 100k on rogan's
    node, then move to your own node with your data AND your
    audience intact, zero friction. platforms hand you a takeout
    zip and keep your followers; here followers auto-migrate
    (phase 10 graduation item; full story lands with M3
    federation). graduation is the ownership story proven from the
    inside -- even the node you're on can't hold you -- and every
    graduate mints a new node (a new hosting customer): the
    pipeline literally converts.
  - THE RISE (the aspirant's arc -- tell it as a story, because it
    is one. the economics map to every step: people are too broke
    to afford infra until fame pops off, so NOBODY PAYS BEFORE FAME
    PAYS THEM FIRST):
      1. start broke, start free: an account on your favorite
         creator's node. their revenue pays for your infra -- a
         free apartment in the coolest building in town.
      2. post. your number is real: every follower you earn
         actually sees you. no throttle, no lottery, no begging an
         algorithm for your own audience.
      3. get known in the scene: the regulars know your name, the
         creator spotlights you, your 5k is really 5k.
      4. pop off: your following outgrows the scene -- and by now
         it's paying you (members, sponsors).
         (the failure branch, and it's a feature: NOT popping off?
         maybe you're just not getting famous in the framework you
         subscribed to. switch nodes -- identity, content, and
         followers come with you (same phase 10 move mechanism,
         pointed sideways). scene-market fit becomes searchable.
         on the platforms this option does not exist: one house,
         one dealer, grind the same machine or quit. side effects:
         nodes COMPETE for rising talent -- a node known for
         launching people attracts the next wave -- and operators
         stay honest, because mistreated members walk with their
         followers.)
      5. graduate: your own node, your own domain, your own
         sponsors. your data AND your audience move with you, zero
         friction (phase 10 graduation). now you're the landlord
         hosting the next wave of broke kids. the flywheel turns.
    every stage upgrades only when success pays for it. this arc is
    the fan-facing story skeleton (manifesto, explanation pages,
    onboarding) -- rise-to-fame is the product's hero's journey.
  - shape consequence: M0 is CREATOR-COMMUNITY-shaped (the
    creator's space, feed, comments, members) -- not friend-graph
    shaped. your 150 real people aren't here yet; the private-life
    story is what a node grows into, never the day-one pitch.


MILESTONES (the route through the map)
--------------------------------------
the phases below are the map. this is the route -- wedge first, prove
things in an order that generates demos, users, and revenue. each
milestone is a stopping point that stands on its own.

M0 "stands on its own" (the demo + the pitch):
    phase 0 toolchain + one-container node + setup wizard + a vertical
    slice of the killer app on ONE node: post, feed (chronological +
    sort dropdown, D20), profile, dms -- PLUS the studio's
    monetization-menu screen (rung-0 cards: memberships, amazon tag,
    direct deals -- the pitch is money, so the video shows the money
    screen, D21). no federation, no encryption, no llm anything.
    deliverable: (a) a node you'd show a creator without apologizing;
    (b) the pitch: THE STORY above as a deck + a 2-minute demo video
    shaped for a creator and their manager, not hacker news -- post
    once, watch it reach 100% of followers. the kill test is ONE
    HUNDRED creator pitches, not a viral video (D20) -- run in
    batches of 20 (batch one iterates the pitch; the verdict needs
    the full 100: at a true 1-in-20 close rate, 20 sends produce
    zero closes ~36% of the time -- too noisy to kill a thesis on;
    100 sends make zero closes a real verdict, ~0.6%). stratify
    across creator segments (platform-burned right-coded, platform-
    burned left-coded, wildcard niches incl. paid-community
    sellers) -- segmentation happens at the PITCH and NODE level,
    one codebase, themed per node. never fork the app per
    audience; inc's rails stay neutral (D20).

M1 "import your life" (self-host launch / show hn):
    + media service, conventions doc, instagram exporter, drive/
    dropbox backup. deliverable: import your instagram, see it in
    an app you own, back it up to your own drive, reconfig it.

M2 "the first creator" (first revenue):
    + admin panel, analytics, ad records, custom domain, hosted-node
    offering. deliverable: one mid-size creator (100k-500k, recently
    platform-burned) running a branded node with real sponsors.

M3 "the network" (the protocol earns its name):
    + federation polish (cross-node follows + inbox delivery), e2e
    encryption tiers, sponsor marketplace rails, protocol spec +
    conformance suite public. deliverable: two unrelated nodes,
    friends across them, private content the operators can't read
    -- and a GRADUATION demo: an account moves nodes and its
    followers come along automatically (phase 10 graduation item).

KNOWN GAPS on the route (executional, tracked so they're not vibes):
  - the video gap: THE STORY's bar is kick/twitch but M0 ships
    posts/feed/dms -- phase 8.5 stages the route (direct-mp4
    playback + the vertical short-form lens are near-term lane-D
    work; the transcode pipeline lifts the caps later), streaming
    later still. consequence: founding creators #1-3 must be ones M0's
    format SERVES (photo/text/community-first, podcasters with
    clips), or the pitch scopes video honestly as roadmap. never
    pitch a pure video creator a product that can't hold their life.
  - memberships before creator #1: owned revenue in month one is
    what keeps a white-glove yes alive (the retention lever). the
    phase 4 tiers item + stripe rails must be live BEFORE the first
    founding creator onboards. hard M2 prerequisite.
  - hosted ops floor: white-glove hosting = inc runs production
    infra for a real community (uptime, backups, support). the
    proxmox homelab is staging, not that. pick the boring hosted
    floor (a $40 vps + the one-container node + caddy is enough)
    before the first yes.
  - t&s gate: the dmca/csam decisions (phase 12) are a PRE-CREATOR-
    #1 gate under white-glove hosting, not an M1 nicety. vendors +
    process decided before any hosted community posts media.
  - exporter battle-testing: D5's mappers are FIXTURE-tested (57
    tests), not mess-tested -- real takeouts are where parsers
    die, and meta churns formats constantly. week 3's real-takeout
    seeding is the first battle test (run the founder's own IG +
    YT exports; log every break). promote phase 9's llm-assisted
    mapping the FIRST time a real takeout breaks a mapper. white-
    glove absorbs the mess through M2 (founder-operated imports);
    consumer-grade robustness is an M1/show-hn bar, not M0.
    facts to keep pitches honest: youtube takeout DOES export
    videos + metadata but arrives as 50GB chunks from days-long
    jobs (the 24h onboarding clock starts when the takeout
    arrives); the AUDIENCE never ports from any platform (no
    export includes reachable subscriber identities) -- content
    ports, audiences are POINTED (story beat 4); imported video
    plays direct-mp4 off minio/R2 until the phase 8 transcode
    pipeline exists. imported-identity relinking is a TWO-TIER
    tradeoff, accepted: (1) now -- ghosts with provenance
    ("@sarah_k · from instagram", the origin field doing its
    job; archival comments are content, not live graph); (2)
    later, lazy, zero identity infra -- CLAIM-BY-MATCHING: when
    a commenter joins and imports their OWN takeout, their
    export contains the comments they made; match origin +
    timestamp + text against existing ghosts and link
    automatically (both parties' takeouts agreeing IS the
    proof; stakes don't justify more). a nice onboarding
    moment: your old words light up with your name.


PHASE 0 — fix up (modernize the toolchain)
------------------------------------------
current state: fastapi 0.68 + pydantic 1.x + PyJWT 1.7 (2019!), auth on
react 16 + react-scripts 2, auth2 on CRA 5 (CRA is dead), no typescript
anywhere. do this FIRST so every later phase builds on modern ground.

python (api):
[✓] switch to uv for package management (pyproject.toml, uv.lock,
    kill requirements.txt + Pipfile duplication)
[✓] python 3.12+, fastapi latest, pydantic v2
    breaking-change hotspots to watch:
      - pydantic v2: models.py (dotdict, .dict() -> .model_dump(), config)
      - PyJWT 1.7 -> 2.x: encode returns str not bytes, decode requires
        algorithms=, options handling changed (main.py decode_token)
      - passlib/bcrypt version pinning (known friction, test login flow)
[✓] ruff for lint+format while we're in there
[✓] prune dead deps (python-ldap? python-gnupg? systematic? "secrets" from
    pypi is NOT the stdlib secrets module -- remove it)

js (ui + sdk):
[✓] bun as package manager + runtime for all js packages
[✓] CRA -> vite (auth2/ui first; old auth dies in phase 2 anyway)
[✓] typescript everywhere, incrementally: allowJs on, new files .ts/.tsx,
    port components as they get touched
[✓] react 18 -> current, drop dead deps as found
[✓] rtc service too: it's old node/peerjs and phase 11 makes it
    load-bearing (key channel). modernize + typescript it with the rest

[✓] docker images rebuilt on the new toolchains (uv sync / bun install --
    both are dramatically faster builds, better layer caching)


PHASE 1 — switch to documentdb (open db backend)
------------------------------------------------
mongodb's SSPL is a problem for the node-operator model (nodes expose
mongo-ish query semantics as a service). documentdb (MIT, linux foundation,
postgres extension) + ferretdb (speaks the mongo wire protocol on top of it)
is the open path. pymongo connects to ferretdb unmodified -- this is mostly
a DB_URL change, not a rewrite.

[✓] ferretdb (documentdb-backed) in docker-compose as the default db
[✓] run the api against it, audit the CRUD paths (insert/find/update/delete,
    $set/$inc/$max/$currentDate/$pull/$unset, sort/skip/limit)
[✓] metering paths use admin commands -- verify or replace:
      - get_collection_size (collstats) and total_size (dbstats) are
        postgres-derived estimates on ferretdb. decided: accept estimates
[✓] keep atlas/real-mongo as a supported config (protocol is the standard,
    backend is the node operator's choice)
[ ] add a mongo -> ferretdb migration path (script lives in api/ or a
    dedicated tools/ dir, not in the deleted web10-deploy)
[✓] rename mongo.py -> documentdb.py (adapter is backend-agnostic; pymongo
    speaks to either real Mongo or FerretDB/DocumentDB)
[✓ 1.0.72] CONNECT THE REAL DATA (A7, added 19.07.2026): the ubuntu box
    already runs the ORIGINAL production MongoDB — ~208 real users,
    historical usage stats, and the registered apps from web10's
    live app-store era. it runs NATIVELY on the host (not a docker
    container — leave it that way), so the node's containers reach it
    via the host gateway / box LAN ip, not a compose service name.
    the new stacks ship an empty ferretdb; point the node at the real
    mongo instead of starting from zero. we already speak the wire
    protocol, so it's config + verification (shape audit for pre-
    convention records, star protection against real data), NOT a
    rewrite. gate: prove a real historical user can log in against a
    COPY in dev before pointing prod at it. deliverable also includes
    the total-users + total-apps counts surfaced like the original
    web10 (the numbers that made it feel alive). this is the data
    foundation for the auth revamp (B6) and the app-store
    restoration (D16) below.


PHASE 2 — ui (finish auth2, rename, migrate)
--------------------------------------------
auth2 is the future. it already has:
  - CredentialPage (login / signup / forgot)
  - Contracts (terms viewer/editor -- the user-owned ACL editor)
  - AppStore
  - Settings

steps:
[✓] port auth2 to vite + bun + ts first (phase 0), THEN finish features
[✓] parity audit: auth/src/components vs auth2/src/components, port
    anything missing
[✓] rename: auth2 -> ui. auth stays until cutover, then gets deleted.
[✓] update docker-compose: service "auth" -> "ui" (keep auth.localhost /
    auth.web10.app as aliases so existing links + CORS_SERVICE_MANAGERS
    don't break during migration)
[✓ 1.0.101] update api settings: CORS_SERVICE_MANAGERS to include the ui origin
[✓] delete auth/ after cutover. one UI, one truth.


PHASE 2.5 — the ui makeover (rectangles out, product in)
--------------------------------------------------------
rectangles-npm is our homemade layout framework. it got the project
here, but it's a dependency only one person on earth understands, and
uis built on it read as an engineering tool, not a product. this phase
retires it from every ui in the repo, replaces it with a boring,
very mainstream stack -- and uses the forced rewrite to give every
surface a real product design pass.

this is a mindset phase as much as a code phase. our uis have always
been built protocol-first: screens that expose records, tokens, terms.
the makeover flips them story-first: every screen answers "what does
this let ME do with MY data", and the protocol machinery recedes into
the walls. think product in a way we never have before.

the visual bar (THE STORY, beat 5): kick/twitch-grade slick. a
creator's node must read as an upgrade to their brand -- the cool
thing their audience brags about being early to -- never fediverse
jank (and the whole fediverse is jank: peertube, even mastodon).
if a screen would look at home in any of them, it isn't done.

sequencing: do this BEFORE the phase 3 wizard screens and phase 4
admin panel are built, so those get written on the new stack once
instead of on rectangles and then rewritten.

the swap:
[✓ 1.0.52] pick the mainstream stack + record it in decisions.md. default
    candidate: tailwind + shadcn/ui (native to our vite + react + ts
    toolchain, huge community, accessible radix primitives, themeable
    -- which the phase 4 white-label creators will need). the bar is
    "boring and mainstream", not clever.
[✓ 1.0.52] rip rectangles-npm out of ui/ (main.tsx startRectangles + css;
    AppStore, Contracts, CredentialPage, Settings, shared/*) --
    component by component, no feature regression
[✓ 1.0.51] rip rectangles-npm out of marketing/web10-social/ (index.js; Bio, Feed,
      Chat, Contacts, Login, shared/*)
[✓ 1.0.52] drop the dependency from ui/package.json. rectangles is
    retired from ui/, permanently -- no new ui may import it.
[✓ 1.0.51] drop the dependency from web10-social/package.json. rectangles is
    retired from web10-social, permanently -- no new ui may import it.

the makeover (story-first, not a screen-by-screen reskin):
[✓ 1.0.52] one small shared design language (tokens: type scale, spacing,
    color, dark mode) used by ui + social + (later) marketing-ui.
    themeable from day one -- a creator's node wears THEIR brand.
[✓ 1.0.52] narrative pass on ui/: login -> consent -> app store -> terms
    should read as a story ("this is your node. these are your
    contracts. this is what apps may touch.") -- not admin CRUD.
[✓ 1.0.51] narrative pass on web10-social: it has to feel like a place
    you'd live. first-run empty states that point at the exporters
    ("import your life"). this app IS the pitch; it has to look
    like one -- a plain good social app that stands on its own (D20).
[ ] the demo test: every M0-facing screen is judged by "would this
    look credible in front of a creator and their manager" (M0
    deliverable: THE STORY + demo video). a screen that can't
    appear in the pitch isn't done.

the level-up (added 19.07.2026 — design.md is the law):
the first makeover got the stack right and stripped rectangles, but
the surfaces still read prototype, not product — and the repo was
shipping the react atom and apple's app store glyph as its own logos.
design.md (repo root) now defines the brand (dark-first, violet, the
keys marks), the canonical token set, and the ui definition of done
(the screenshot test; pr screenshots at desktop + 375px). every ui
agent reads it before working, every time (CLAUDE.md/AGENTS.md gate).
the three items below are PARALLEL — disjoint directories:
[✓ 1.0.65] level-up ui/ to the design.md standard (lane B, item B5):
    tokens.css migrated to design.md §13 (dark-first violet), inline-
    style habit burned down, dead vendored bulma dir deleted,
    SideBar.tsx className/style bug fixed, components/ui primitives
    kit, mobile bottom-nav shell, hub.png (apple's app store logo —
    a trademark, replace it) + react-atom icons swapped for the real
    keys marks. the studio screen is the money shot — highest stakes.
    includes the urgent deployment unblock: backend origins become
    build-time env instead of the hardcodes in authAdapter.ts/
    config.ts (any deployed env ships a bundle calling
    api.web10.app — see ubuntu-deployment/AGENT-OPS.md §4.1).
[✓ 1.0.64] level-up web10-social to the design.md standard (lane D,
    item D12): wired @tailwindcss/vite, self-hosted Inter + Space
    Grotesk (@fontsource-variable), retired the FontAwesome CDN kit,
    media-forward feed (wired to real reaction/comment counts + a
    working like-toggle and inline comments), a composer that feels
    like publishing (drag-drop attach, error state), profile as a
    creator page (banner, tabular-nums stats), skeleton loading states,
    data-testid + 44px touch targets + a real focus-visible ring
    throughout. Also fixed a load-bearing tsconfig bug (`@/*` path
    alias was never declared — `tsc -b` had ~90 pre-existing errors)
    and the legacy Crm/Mail/Bio vitest excludes (files no longer
    exist). 193 tests green, tsc+build clean. See decisions.md D24 for
    the alternative.png brand-asset correction (shared with D13).
[✓ 1.0.87] web10-social vibrancy overhaul (lane D, D12 follow-up):
    the social flagship was muted — flat surfaces, no ambient light,
    zero interaction energy — compared to Kick's vibrant, alive feel.
    design.md §4 relaxed for social: glow tokens added, new animations
    (shimmer, heart-burst, glow-pulse, float), custom scrollbar,
    gradient brand buttons with glow-on-hover, badge glow/live variants,
    shimmer skeletons, sidebar gradient + active nav glow pill, card
    hover glow, heart-burst on like, vibrant tags, gradient sent DM
    bubbles, presence dots, vibrant profile banner, media hover zoom,
    animated login background with floating orbs. 195 tests green,
    tsc clean, build clean.
[✓ 1.0.63] rebuild marketing-ui on the design system (lane D, item D13):
    bulma OUT entirely (already rejected in D22), tailwind v4 +
    design.md tokens, dark cinematic landing (lockup hero, reach-gap
    proof section built as tested HTML/CSS bars — no real product
    screenshots exist yet, so that sub-item is deferred rather than
    faked), docs pages readable (prose measure, mono code), app store
    on house cards (first-party proof, not a catalog), education-era
    imagery + university logos deleted. brand asset debt PARTIALLY
    paid for all three apps: favicons/pwa icons + shared og-image
    generated from the keys mark (design.md §3) and dropped in
    .context/brand-assets/ for lanes B/D12 to apply; svg vectorization
    and dark-on-light variants still open (no tracing tool available —
    see .context/brand-assets/NOTE.md). also found: design.md §3's
    documented square-mark file (web10-social/public/alternative.png)
    does not contain the keys mark — flagged for a design.md fix, not
    corrected in this PR (out of lane).

now-that-it's-deployed round (added 19.07.2026, gated on A7's real
mongo — the box is live but pointed at an empty ferretdb; connecting
the original data changes what "done" looks like for these):
[✓ 1.0.72] AUTHENTICATOR REVAMP (lane B, item B6): the ui/ auth flow is
    broken in look AND function — the web10 logo renders broken, and
    login "straight up doesnt work" (operator, 19.07). B5 fixed the
    shell; this fixes the actual login/consent journey end to end
    against real data (A7), with a playwright test so it can't
    regress. this is the front door — nothing else matters if a
    creator can't log in.
[✓ 1.0.136] APP STORE, restored + curated (lane D, item D16): the original
    web10 had a LIVE app store with real registered apps; the
    current marketing-ui shows first-party "proof" cards only. read
    the real registered apps from the mongo (A7), keep web10-social
    promoted up top with third-party apps in a lower section, and
    add admin-approval curation (register freely, web10 Inc.
    approves what shows). surface the real historical stats (user/
    app counts). STRATEGIC NOTE: this reverses the earlier "keep the
    store minimal, an empty catalog reads as jank" call (D20 thread)
    — that assumed an empty catalog; with 208 real users and real
    apps reconnected, restoring the store IS the "real company"
    goal. reversal logged in decisions.md as D31 + the partial-vs-
    allowlist sub-reversal (operator 23.07: takedown-only →
    allowlist — "any app can register, but i should be able to
    approve"). 1.0.84 shipped the store stats (real apps from mongo)
    + web10-social hero; 1.0.136 ships the curation: pending flag on
    `/register_app`, `/apps/admin` + `/apps/approve` (admin-only,
    reusing check_admin), and the App Store Approvals card in the
    authenticator's Node Config panel.
[✓ 1.0.70] docs 404 fix (lane D, D16.1): /docs/protocol-spec 404'd
    live — marketing-ui's nginx /docs/ alias block shadowed the SPA
    fallback. try_files → index.html added.
[✓ 1.0.72] RESTORE THE DEV DOCS (lane D, item D17): the old sharp docs —
    two runnable demo apps (hello/, notes/) + sdk.md/sdk.pdf — are
    NOT lost; they live in git at `82667060^:auth/public/docs/`.
    bring them into marketing-ui's docs on the design.md standard,
    and restore the app store's "make your own web10 app with the
    web10 CLI" call-to-action (the CLI still exists at
    marketing/web10-cli/ but is invisible) + a CLI quickstart.
    don't teach legacy wapi as the future — align with C2.
[✓ 1.0.72, 1.0.100] SDK visibility + publish flow (lane D item D18 + lane E item
    E7): sdk/ documented in marketing-ui docs (new sdk.md page, wired into
    Docs.tsx sidebar), SDK link added to Home.tsx footer, npm badge + SDK
    link added to README.md. npm publish verified: web10-npm@1.0.8 is public
    on npmjs.com, cd.yml's `npm` job fires on v* tags with provenance.
    E7 verification (1.0.100): cd.yml end-to-end flow confirmed — fires on
    v* tag push, npm job gated on tag prefix, publishes with --provenance
    --access public. Package publicly accessible (versions 1.0.0–1.0.8).
    Publish flow decision reaffirmed: stays tag-gated (decisions.md D26) —
    auto-publish on merge rejected while C2's typed rewrite is in flight;
    a v* tag forces a deliberate release decision, preventing legacy wapi.js
    from drowning npm with versions nobody should install.
[ ] mobile encryptor → app stores (lane E item E8, parked): expo
    eas build/submit, listings, signing. post-M0, gated on the
    encryptor doing real e2e key management — on the board so it
    isn't lost.


PHASE 3 — setup wizard (the missing piece)
------------------------------------------
first-run experience: docker compose up -> open browser -> setup form ->
you have a sovereign node. like pocketbase / home assistant onboarding.

api side:
[✓] first-run detection: if no config exists in the data volume, api boots
    in "setup mode" and only answers setup endpoints
[✓] POST /setup: provider domain, admin username+password, optional stripe/
    twilio keys (all optional except domain + admin account)
[✓] api generates its own JWT signing key on setup (stop hand-managing
    PRIVATE_KEY in settings files)
[✓] config written to the data volume, settings.py becomes fallback/dev only

ui side (lives in the ui, phase 2 dependency):
[✓] /setup wizard screens: welcome -> node identity -> admin account ->
    (optional) payments/sms -> done, you're on your node
[ ] setup only reachable in setup mode, 404s forever after
[ ] the WORDPRESS-STYLE config panel (operator direction, 19.07;
    D25): first run is a panel where you CAN set everything —
    provider, DB, S3, policy, stripe/twilio, branding (NodeConfig
    already models all of it) — but sane DEFAULTS mean `docker
    compose up` just works for a dev out of the box (db_url ->
    containerized ferretdb, minio, open signup). the SetupRequest
    defaults already do this. config is stored IN THE DB
    (web10.config collection, wordpress-wp_options-style — already
    built, api/app/services/config.py), NOT a flat file. the one
    field that can't be panel-editable is db_url itself (chicken-and-
    egg: you need it to reach the db that stores config), so it stays
    a bootstrap/stack-env input — that's WHY ConfigUpdate omits it,
    correctly. everything else is db-backed + panel-editable. pairs
    with B6 (the panel/auth ui must actually work + look like
    design.md).

node policy (anti-abuse is the NODE'S call, not the protocol's):
[ ] phone-required signup becomes a policy toggle, not a hardcode.
    it's an anti-abuse measure -- extreme for a 50-person community
    node, reasonable for a 5m-user celebrity node. operator picks.
    (VERIFY_REQUIRED / BETA_REQUIRED flags exist; grow them into a
    real policy config, editable in the admin panel post-setup)
[ ] signup gate options: open / invite links / approval queue /
    beta code / email verify / captcha-turnstile / phone verify.
    stackable. default for self-host: open or invite. twilio only
    needed if the operator chooses phone gates.
[~] recovery must not assume sms: recovery codes generated at signup
    (download/print), email recovery, and later the phase 10/11
    escrow path. sms recovery stays as an option, not the only path.
    forgot-password must be SMOOTH (jacob, 19.07.2026): phone number
    AND email as first-class reset channels — enter either, get a
    code, set a new password, done. the phone flow exists; email
    needs an smtp/provider hookup in api/ plus the same code flow in
    the ui CredentialPage. losing a password must never feel like
    losing the account.
    RE-CONFIRMED + ESCALATED (operator rant, 30.07.2026): "really bad
    if people cant get into their web10, like really bad! we need to
    pester every user to set a recovery email pronto or number, if
    they havent! the recovery flow has to be tight so the user
    accounts aren't ephemeral, they are permanent!" — "probably more
    than half of users will forget their password." VERIFIED GAP at
    HEAD: `email` appears NOWHERE in api/app — the star record holds
    phone but no email field, and recovery is SMS-only (twilio
    /recovery_prompt + /recovery_bot). so today a user who loses
    both password and phone has NO way back in — the account IS
    ephemeral. filed as A20-recovery-email-channel (api) +
    B9-recovery-contact-nudge (ui: pester every user with no
    recovery contact set until they set one). accounts are
    PERMANENT is the product promise. bite a merged [✓ 1.0.282],
    bite b (email reset flow) still open.
[ ] passwordless phone login (operator rant, 30.07.2026): "even
    better, passwordless phone text verification code is the goat!
    also cool if we dont let people make an account if the phone
    number aint legit, so everyone is a very legit user on web10!"
    — login by phone + SMS code, no password at all (the twilio
    Verify send/check pair already exists for signup — the login
    path is the new part), and on web10 Inc.'s node phone
    verification is MANDATORY at signup (a legit verified number or
    no account — every user a very legit user). stays a node policy
    toggle per the item above (self-hosters pick open/invite); the
    web10.app policy flips to phone-required. filed as
    A21-passwordless-phone-login (api) + the ui half in B9.
    OPERATOR CLARIFICATIONS (30.07, same rant): (a) NO
    one-account-per-phone restriction — "we dont need to restrict
    one user account per phone number!" (the PHONE_NUMBER_TAKEN
    check in create_user goes away under the new policy); (b)
    VERIFIED-ONLY USER COUNT — "they need to verify to be added to
    the count of users!": get_user_count is currently
    len(list_collection_names()) — every collection, unverified
    signups and the web10 system collection included; the /stats
    users number must count verified users only. LIVE EVIDENCE
    (operator, 30.07, screenshot of the prod stats bar at "464
    USERS"): "problem is new users signing up verry frequently!
    and they arent verifying phone number" — unverified signups
    are inflating the headline number right now; bites b+c of A21
    are the priority, and the existing unverified collections need
    a read-only audit + a keep-vs-reap call recorded in
    decisions.md. KEEP-VS-REAP RESOLVED (operator, 30.07): REAP,
    with reminders first — "we should ask all the unverified users
    platform wide to verify their number over text with a link to
    verify! and if they dont, after three reminders, we shut the
    accounts down, set the account INACTIVE, and someone else
    could take the username it frees up the username." filed as
    A23-verification-enforcement: a daily pass texts every
    unverified user a verify link, three unheeded reminders ->
    status=INACTIVE + the collection is RENAMED (not deleted —
    the data survives, the permanence promise holds for the
    account owner; reactivation path is a follow-up) so the
    username frees up for re-registration; (c) FORGOT
    USERNAME — "if they say forgot username, they enter their phone
    number, and it shows them a list of users under that phone
    number AFTER verifying their phone number through text... used
    to have some logic like this in the old version of the project
    before this hardcore summer development push" — the seed of
    that old logic survives (the phone registry + twilio
    recovery_bot); filed as A22-forgot-username-by-phone (api) +
    B9 bite d (ui).
[ ] passkeys as a login option once phase 0 lands -- better than
    passwords, no phone number, phishing-proof. fits the vibe.
[ ] UTC-explicit timestamps everywhere (operator rant, 30.07.2026):
    "they were trending as newest, timestamp was -12000 seconds...
    do you see the bug? does it have to use UTC, or one time zone?"
    — the api writes NAIVE UTC timestamps (`datetime.utcnow()
    .isoformat()`, no offset marker); JS parses offset-less ISO as
    LOCAL time, so viewers see server-stamped posts up to their
    utc-offset hours in the FUTURE: negative relative-time ages and
    a broken newest sort. the rule: UTC everywhere, always with the
    explicit offset marker — the server emits `+00:00`/`Z`, the
    client clamps negative ages to "just now" as defense-in-depth.
    filed as A24-utc-timestamps (api sweep + web10-social clamp).

packaging:
[ ] multi-stage Dockerfile: bun build ui static -> serve from fastapi
    (web10 CRUD uses PATCH-for-read, so all GET routes are free for the ui.
    api + ui share one container, one port, zero route conflicts)
[ ] compose profiles: core = api(+ui) + db. optional = rtc, media.
[ ] target: docker run -p 80:80 -v web10:/data web10/node
[ ] custom domains: a node lives at the operator's own domain with
    auto-tls (caddy). rogan.social, not rogan.web10.app. this is the
    wordpress moment -- the brand is theirs, the address is theirs.


PHASE 4 — the STUDIO (creator console + analytics + monetization)
------------------------------------------------------------------
the node operator's console, inside the ui. this is where "how the creator
makes money" becomes visible product instead of thesis. this page is the
thing you demo to an influencer's manager -- make it GOOD.

the mental model is YOUTUBE STUDIO (call it the studio): one pane
where the creator posts, watches their audience numbers, sees
revenue, and sees the monetization MENU as an unlock ladder --
youtube's YPP screen trained every creator alive to read "X more
to unlock Y" progress bars, so rungs 0-4 render as exactly that,
except thresholds unlock MORE money, not permission to earn at
all. two personas share the pane: the CREATOR studio (post,
analytics, revenue, menu) and the OPERATOR console (policy,
signup gates, moderation queues) -- same person on a creator
node, but design them as separable tabs from day one (the
white-label future has managers in the operator seat).

access:
[✓] admin role: node operator account(s) flagged at setup
[✓] admin panel section in ui, only visible to admins

audience analytics (the numbers a sponsor asks for):
[ ] DAU / WAU / MAU, signups over time, retention curves
[ ] request volume + storage growth (node health at a glance)
[ ] per-app usage: which lenses/apps the node's users actually use
    (register_app + /stats exist in the api -- extend into real
    time-series, not just counters)
[ ] top services by activity (posts? dms? what's this community DO?)
[ ] implementation note: current metering is a mongo $inc on the star
    record -- fine for billing, useless for analytics. emit per-request
    events (user, app, service, action, ts) to a metering store. start
    with a capped collection; clickhouse sidecar when volume justifies
    it (see LATER). aggregate exhaust only -- individual record contents
    stay sovereign. that privacy line is a selling point of the ad
    inventory, keep it bright.

monetization controls:
[ ] membership tiers / ranked communities (the skool/whop model,
    natively): terms ACLs already express ranked access -- a
    member-only service, an inner-circle service, an officers
    service; a "rank" is just which services your token can read.
    the paid hierarchical community (hustlers-university shape:
    ranks, climbing, belonging -- THE RISE as a product) runs on
    existing permission machinery at ~97% payout vs skool/whop's
    cut. paid-community sellers are also a founding-creator
    segment: they already charge members, already live off-platform,
    and are the most ban-prone creator category alive (their
    deplatforming fear includes payment rails). serve the MECHANICS
    (tiers, ranks, paid inner circles), never the aesthetic -- inc
    stays neutral infrastructure (D20), and this segment imports
    the phase 12 t&s burden fastest. gamification layers (xp,
    leaderboards) are post-M2 product, not now.
[ ] ad/sponsorship slots: define sponsored placements that node-default
    lenses render. operator controls what's promoted.
[✓] ads are RECORDS the operator owns (creative, link, schedule,
    weight, price). curated by architecture: nothing renders unless
    the operator created or approved the record. no programmatic
    surprise garbage, ever.
[✓] works at an audience of 100: hand-entered direct deals, affiliate
    links (operator picks exact products), community/patron slots
    (a fan or local business buys a slot, operator approves).
[✓] the monetization MENU (the weakest point, researched 07.2026:
    easy instant deals for a self-hosted node, as a ladder that
    unlocks as the node grows -- the networks already gate by
    traffic/revenue, so the unlock ladder maps to reality):
      rung 0, day one, any size: memberships/tips (own rails, 3%),
        affiliate adapters (amazon associates; levanta-class --
        amazon/shopify/walmart unified, 10k-follower min, 24h
        review), operator-entered direct deals. the instant-money
        star: AUTO-AFFILIATE EVERYTHING (skimlinks/sovrn-commerce
        pattern) -- paste your associates tag / connect once, and
        every product link posted on the node rewrites to earn at
        render time. zero per-link work, first dollar in minutes.
        AMAZON ADAPTER v1 spec (researched 07.2026):
          - the card: one field, paste your associates tag.
            stored as a record in the creator's own collection.
          - render-time tagging: amazon links normalize to
            canonical /dp/ASIN form + ?tag= appended. NEVER
            cloaked/shortened (amazon prohibits hiding the
            destination); amazon.com stays visible.
          - whose tag wins (revenue routing, RISE-aligned): the
            POSTER's own tag if they set one -- members earn on
            their own links at any size -- else the node
            operator's tag as house fallback.
          - composer "attach product" v1 needs NO pa-api: paste
            an amazon url -> normalized product card. the rich
            search-based picker is a later rung: pa-api access
            is gated on an account with sales history.
          - compliance baked into the adapter, not left to the
            creator: auto "affiliate link" disclosure chip (ftc +
            amazon), NO tag rewriting in email digests (amazon
            prohibits affiliate links in email), no stale price
            display (prices only via api, else none), and the
            card tracks the 3-QUALIFYING-SALES-IN-180-DAYS
            countdown (miss it and amazon closes the account --
            surface it, don't let creators get silently banned).
          - upgrade rungs: levanta-class (10k followers, better
            rates, shopify/walmart too), amazon influencer
            program storefront where eligible.
        AI PRODUCT SUGGESTER (the first ai feature in the product
        is the MONEY assistant -- passes the D20 oracle where the
        feed chatbox didn't):
          - an llm under a READ-ONLY scoped token on posts (I5;
            never dms/contacts) profiles the account's niche and
            suggests relevant products: a suggestions feed in the
            studio + the killer version, COMPOSER-TIME chips
            ("this post mentions your pre-workout -- attach it as
            a product?"). one tap, tagged, disclosed, earning.
          - always human-approved, never auto-inserted: curation
            by architecture holds; no temu-slop, no amazon
            placement violations. suggestions resolve v1 as
            tagged search links / normalized ASINs (no pa-api);
            levanta's catalog becomes the better source at 10k+.
          - works from follower #1: every wannabe gets an ai
            revenue manager that platforms reserve for the big
            (THE RISE stage 2 gets guided to its first dollar).
          - the same profiling engine is the M3 marketplace's
            matching brain: brands book 1,000 relevant $20 nano
            promos without reading 1,000 profiles.
          - funding: hosted-tier feature; self-hosters byok
            (D19 pattern -- never a free api proxy).
      rung 1, ~1k sessions: contextual display FILL via privacy-
        safe adapters (ethicalads-class: zero cookies, pure
        contextual -- manifesto-compatible). mediavine journey now
        accepts from ~1k sessions (2026 -- the old 50k wall fell).
      rung 2, ~10k: sponsor-marketplace adapters (paved / kit /
        beehiiv shape). NOTE the takes: paved 30%, kit 23.5-30%,
        passionfroot $199/mo -- incumbents trained creators that
        marketplaces cost 20-30%. our marketplace at 3% undercuts
        by 10x in a market with proven willingness to pay.
      rung 3, ~25k+: premium programmatic (raptive-class, dropped
        to 25k in 2025) -- ONLY as clearly-disclosed operator
        opt-in, if ever: tracking-based programmatic breaks the
        manifesto ("nobody is mining you") and the ad-records
        rule (nothing renders unapproved). contextual/server-side
        fill is the default; tracking is not.
      rung 4, the endgame (M3): the web10 sponsor marketplace at
        3% -- cross-node inventory, curation by architecture. its
        killer segment is the NANO TIER nothing serves natively:
        $20 promos for 5k-follower members (brands love nano
        engagement rates; today this only exists as platform-
        locked ugc-gig sites). plugs into THE RISE: stage 3 gets
        a paycheck -- fame starts paying at 5k followers here,
        not 100k. every aspirant is future marketplace inventory
        a sponsor can book in bulk, operator-curated.
    third-party networks stay optional FILL, never the foundation.
    ZERO-FRICTION rule (the studio's bar for every rung): each
    option is a CARD with ONE BUTTON, and the adapter does the
    paperwork. the node owns its analytics, so the studio (a)
    shows only options the node already qualifies for, (b)
    prefills third-party applications with verified stats (an
    auto-generated media kit), (c) tracks application status in
    the pane. the metric is TIME-TO-FIRST-DOLLAR: memberships =
    minutes (stripe connect onboarding), auto-affiliate = one
    paste, direct deal = type it and publish. an option that
    needs a tutorial isn't done.
    AND IT ATTACHES TO CONTENT: the composer (phase 8 posting
    flow) carries one-tap monetization affordances on the post
    itself -- attach a product (auto-affiliated, above), mark as
    sponsored (renders disclosure + feeds sponsor reporting),
    gate to a member tier (a terms ACL on the record -- phase 4
    tiers), enable tips. the youtube-product-tags / ig-shopping
    pattern: earning is part of the posting habit, not a
    settings page you remember to visit.
[ ] sponsor reporting: impressions/interactions per placement, per
    campaign, exportable (this is what justifies podcast-grade CPMs)
[ ] traffic routing controls: which apps/nodes get promoted placement
    (the shoutout economy, formalized)
[ ] revenue dashboard: what came in, what web10's cut was (rails already
    exist: stripe connect transfer_data + amount_percent in stripe.py)


PHASE 4.5 — the OPERATOR CONSOLE (auth.web10.app/admin)
-------------------------------------------------------
the current admin surface is a single /config route in ui/ -- admins list,
app-store approvals, and one wall of credential fields (s3/stripe/twilio/
brand/policy). that is "operator can type secrets into a form", not
"operator can run their node." the operator wants a moderation console the
way youtube/twitch/mastodon admins have one: a dashboard you leave open in
a tab, glance at node health, triage abuse, look someone up, ship a ban.
this phase builds that at auth.web10.app/admin. /config becomes one tab
inside it (the credentials/identity half stays; the policy half moves into
the moderation tab where it belongs). the existing ConfigPage.tsx is the
seed -- it already loads /config, /apps/admin, /apps/approve -- and stays
github-pasteable for the secrets half; everything new lives behind it.

ROUTE + AUTH GATE:
[ ] new ui mode `admin` (case "admin" in App.tsx switch) served at the
    path /admin. App.tsx's effectiveMode already falls back to login when
    signed-out; the /admin route additionally no-ops to a 404-style gate if
    I.isAdmin is false (the same calm Lock card ConfigPage renders today,
    lifted into its own NotAnAdmin component so every admin sub-route can
    reuse it -- do not duplicate).
[ ] the existing /config mode stays mountable for deep-link compat (the
    side bar, plan.txt/CHANGELOG refs, the gauntlet step). it just becomes
    a redirect to /admin#config so the secrets form is one click away and
    nothing 404s. the ib `mode="config"` callers (sidebar, status) keep
    working.
[ ] the admin nav itself is a left rail (desktop) collapsing to a top
    segmented control (mobile, 375px) -- same AppShell the rest of ui/
    uses. tabs: Overview · Users · Moderation · App Store · Node Config ·
    Audit Log. mobile shows the active tab name as the page title and the
    segmented control below it; never a hamburger.
[ ] I.isAdmin already hydrates from check_admin (App.tsx's restored-
    session effect). extend the interface so /admin sub-routes can read
    I.isLoadingAdmin separately from I.isAdmin -- a route rendered before
    the /am_admin call resolves must show a skeleton, not the "admins
    only" card (which today flickers on reload).

USERS TAB (the missing moderation layer):
[ ] api: POST /users/admin -- admin-only (check_admin). paginated,
    searchable. query: { token, q, status, sort, skip, limit }.
    - q searches username (prefix/substring, mongo regex anchored) and
      phone (exact only -- PII). q="" lists everyone.
    - status filter: all | active | suspended | banned | unverified.
    - sort: newest | oldest | most_active (last_seen desc) | top_storage.
    - response: { users: [{ username, phone_masked, verified,
      created_at, last_seen, status, storage_bytes, request_count_30d,
      apps_count }], total, pending_reports }.
    - never return password hashes, full phone numbers, or tokens. phone
      is masked (+1...1234) except for admins of THIS node (still in the
      api only; the ui masks by default and reveals on an explicit click
      that hits an audit-logged /users/admin/reveal endpoint -- the reveal
      is the load-bearing anti-snooping piece, do not skip it).
[ ] api: PATCH /users/admin/{username} -- admin-only. body: { status:
    "active"|"suspended"|"banned", reason?, duration? }.
    - "suspended": the user can log in and read their own data, but every
      write (create/update/delete) is rejected at is_permitted with an
      exceptions.SUSPENDED. the token still certifies so they can graduate
      off the node (phase 10) -- suspension is a throttle, not a kill.
    - "banned": mint no new tokens for this username. existing tokens
      continue to work until expiry (we don't ship a global token-revoke
      yet -- that's a phase 12 invariant, see "audit log + revoke" below)
      but every write is rejected at is_permitted. signup with the same
      username is blocked.
    - "active": clears the flag.
    - reason + actor + ts land in the audit collection (see AUDIT LOG
      TAB). every state transition is append-only history, not an
      overwrite -- so a ban-then-unban-then-ban shows three rows, not one.
[ ] api: POST /users/admin/{username}/reset_password -- admin-only,
    admin-prints-or-emails-the-link flow (no, the admin does not see the
    new password). returns a one-shot reset token valid for 10 minutes;
    the user follows the existing /change_pass path. alternative: admin
    sets a temporary password that must be changed on next login (flag on
    the user record). pick one in implementation, not in the plan.
[ ] api: DELETE /users/admin/{username} -- admin-only, HARD delete.
    removes the user's auth record + cascades their {service:*} records
    (the same _author-scoped set the user owns, per A8). stage gated
    behind a typed confirm in the ui (the operator types the username)
    and a 24h grace entry in the audit log + a restorable tombstone
    collection (username, deleted_at, actor, reason, snapshot_oid). undo
    window means a "banned, deleted in anger, sorry" fat-finger doesn't
    nuke a creator's archive. (ties into phase 10 escape-pod; the
    tombstone is the same machinery, pointed at a different table.)
[ ] ui: Users tab = a toolbar (search input, status select, sort select)
    + a paginated table (username, status pill, created, last_seen,
    storage, 30d requests). row click opens a right-side drawer (mobile:
    full-screen sheet) with the user's full card: identity, masked phone
    + reveal button (audit-logged), quota (credits/space), apps they've
    registered, recent activity, and the action buttons (suspend / ban /
    unban / reset password / delete). every action opens a confirm modal
    that requires a typed username match for ban/delete. the drawer is
    the single source of truth -- the table is just navigation to it.

MODERATION TAB (the anti-spam half of phase 3's policy menu, now real):
[ ] api: GET/PATCH /policy (admin-only) -- a typed policy record in the
    web10.config collection, edit-visible in this tab (NOT in node config
    -- credentials and policy live different lives; an operator changes
    policy every week and credentials once). fields:
    - signup_gate: "open" | "invite" | "approval_queue" | "beta_code" |
      "phone_verify" | "email_verify" | "captcha_turnstile". stackable
      (ordered list, all must pass). default ["open"] for self-host;
      ["beta_code","phone_verify"] for the celebrity case.
    - beta_code (string), beta_required (bool) -- already modelled,
      migrate here.
    - phone_required, email_required, verify_required (bools).
    - captcha_site_key, captcha_secret (secret stays server-side; only
      the site key is returned to the ui).
    - signup_rate_limit: { per_ip_per_hour, per_phone_per_hour,
      per_email_per_hour } -- the actual numbers. enforced at /signup +
      /send_code (auth.py), keyed by ip (X-Forwarded-For last hop) +
      phone/email hash. the enforcement itself belongs to lane A in
      auth.py, the tunables live here.
    - write_rate_limit: { per_user_per_min, per_ip_per_min } -- enforced
      at is_permitted, throttle by credits (D21's quota path) rather than
      a hard 429; running out of credits is already the existing abuse
      shape, the policy record just sets the per-minute refill.
    - blocked_usernames: regex list ( RESERVED words, slurs, handle
      squatting). checked at /signup before create_user.
    - blocked_phones, blocked_emails: hash lists for known abusers.
    - invite_codes: { code, max_uses, used, expires_at, note }[] -- the
      operator's manual invite-link issuer/revoker. one field less than a
      whole invite system; the table lives in this tab.
[ ] api: POST /reports -- already exists as /feedback in marketing-api;
    bring it (or a sibling) to the authenticator so USERS can flag a
    post/user for the operator to review. body: { token, target:
    {username? , service?, record_id?}, reason, note }. writes to an
    admin_reports collection. anonymous-in-the-ui (the reporter is in
    the record for the operator; the reported user never sees who
    flagged them).
[ ] api: POST /reports/admin { token, status, skip, limit } -- the
    operator's queue. returns reports with the target's current status
    pre-joined so the operator doesn't have to second-click.
[ ] api: PATCH /reports/admin/{id} -- { status: "open"|"reviewing"|
    "actioned"|"dismissed", action_taken?: string }. dismiss + actioned
    are terminal; both write to the audit log.
[ ] ui: Moderation tab = three stacked sections:
    (a) Policy -- the typed form for the /policy record above. every
        numeric rate limit field has a sane default shown as a placeholder
        and a "reset to default" ghost button. the captcha + email gate
        fields only render when the matching signup_gate is selected
        (same conditional pattern as ConfigPage's beta_code reveal).
    (b) Reports queue -- a Kanban-ish table grouped by status, oldest
        first, with a quick "open user drawer" + "set status" action per
        row. clicking a report opens the user drawer from the Users tab
        so the operator sees history before deciding. dismiss requires
        a one-line reason.
    (c) Blocklists -- searchable lists of blocked usernames/phones/
        emails + invite-code CRUD. each entry shows who added it, when,
        and why (audit join) -- never an anonymous block.
[ ] the anti-spam ENFORCEMENT (signup/write rate limits, captcha,
    blocked-list checks) is lane A work in api/app/endpoints/auth.py +
    auth/services/auth.py + is_permitted; this phase only adds the
    tunables + the ui surface. they ship together so the policy form is
    never a no-op.

ANALYTICS TAB (the "how's my node doing" screen):
[ ] api: POST /analytics/admin { token, metric, range: {from,to},
    bucket: "hour"|"day"|"week" } -- built on A5's per-request event
    collection (already merged; if the event store isn't there yet, the
    ui builds against fixtures and the api follows). metrics:
    - signups_over_time: count of user records grouped by created_at
      bucket. (cheap; doesn't need the event store.)
    - dau/wau/mau: distinct _author values seen in the event store per
      day/week/month.
    - retention_cohort: 7×8 matrix of "joined in week N, seen in week N+k".
    - request_volume: event count per bucket, optionally split by service.
    - storage_growth: sum(media bytes) per bucket -- daily snapshot, not a
      scan (a daily cron writes a storage snapshot to a metric collection
      so the chart is constant-time).
    - per_app_usage: events grouped by app origin (the register_app url
      is on the token's site field -- map it).
    - top_services: events grouped by service={posts,dms,media,...}.
[ ] ui: Analytics tab = a date-range picker (default 30d) and a 2-col
    grid of cards, each card = one metric, sparkline + headline number +
    % delta vs previous range. the cards are not charts-as-decoration --
    delta > 0 is green for signups, red for storage-burn, etc. the kind
    of thing an operator screenshots for a sponsor (ties to phase 4
    sponsor reporting). charts use the existing charting lib in
    marketing-ui (look before adding one -- a chart dep is a lane-shared
    decision). aggregate exhaust only, never individual record contents,
    the same privacy line as the metering note in phase 4.
[ ] the analytics tab is what makes this phase DEMOABLE to a creator's
    MANAGER (the second audience for the studio screen). the operator
    shows "208 real users, 1.2k requests today, here's the signup
    chart"; that's the social proof the /config form never carried.

OVERVIEW TAB (the dashboard head):
[ ] one screen, no inputs. cards: total users (with +N this week),
  pending app approvals (links to App Store tab), open reports (links to
  Moderation tab), node health (db ping + storage last-snapshot + 24h
  error count from the event store), today's signup chart (reuse the
  analytics sparkline). it's a launchpad to the other tabs -- the
  operator's muscle memory is Overview -> click the red number.
  pulling the open-reports + pending-apps count is one new endpoint:
  POST /admin/counts { token } -> { users, pending_apps, open_reports,
  health: {db,storage_bytes,errors_24h} }.

APP STORE TAB (already shipped, just relocated):
[✓ 1.0.136] the existing /apps/admin + /apps/approve flow + the
    ConfigPage App Store Approvals card move here verbatim
    (ConfigPage.tsx's card and its handlers get lifted into a new
    components/Admin/AppStoreCard.tsx). no behavior change; just a new
    home under /admin#appstore.

NODE CONFIG TAB (the credentials half of today's /config, retained):
[✓ 1.0.40] the existing ConfigPage form (Node Identity, Database,
    Media S3, Twilio, Stripe, Free Tier) stays as-is under
    /admin#config. the Admins card + Access Policy card MIGRATE OUT:
    Admins -> Users tab's role assignment, Access Policy -> Moderation
    tab's policy form. so this tab is purely secrets + identity, and
    the Admins list is no longer a footgun sitting next to the stripe
    live key.

AUDIT LOG TAB (the trust layer under all of the above):
[ ] api: every admin mutation (ban/unban, delete, policy change,
    admin add/remove, report dismiss/action, invite-code issue,
    phone-reveal, manual quota override) writes one row to an audit
    collection: { ts, actor_username, action, target_username?,
    target_kind, before?, after?, reason? }. POST /audit/admin { token,
    skip, limit, actor?, action?, from?, to? } -- admin-only read,
    paginated + filterable. there is no PATCH -- audit rows are
    immutable.
[ ] ui: Audit Log tab = a reverse-chronological table, filtered by
  actor/action/date range, every row expandable to show the before/after
  diff (the same JSON-tree the Contracts page already renders). this is
  what makes "who banned whom and why" answerable in 5 seconds -- a
  multi-admin node needs this from day one (plan.txt I5 integrity), and
  the SPONSOR asks for it ("is this node well-run?").
[ ] the audit log + a future token-revoke endpoint are also what make
  "ban takes effect immediately, not at next token mint" possible (phase
  12 invariant work) -- the audit row's ts is the cutoff an is_permitted
  check can consult. NOT shipped in this phase, but the schema must leave
  room for it (a `revoked_at` column on the user record, nullable).

PRIORITY + GATES (honest call):
[ ] the GAUNTLET (priority one) precedes this phase. /admin is operator
    tooling -- it makes the node RUNNABLE, not LOVABLE, and the priority
    one bar is "the operator opens the social app on his phone, proud."
    a moderation console with no users to moderate is premature. BUT:
    the Users-search + ban/suspend + the policy record are the smallest
    slice that EARNS the operator's trust and unblocks safe growth --
    that slice can land right after the gauntlet passes, before phase 4's
    full monetization menu. sequence: gauntlet green -> /admin#users +
    /admin#moderation (policy form + reports) + audit log -> then
    analytics tab -> then the rest. sewing the audit log into the
    FIRST slice (not the last) is the load-bearing call -- every later
    admin action inherits it for free, and "we shipped moderation with
    no audit trail" is the one regression you can't undo.

LANE SPLIT (parallel-execution coordination):
[ ] lane A owns the api endpoints: /users/admin{,PATCH,DELETE,reset},
    /policy GET+PATCH, /reports + /reports/admin, /analytics/admin,
    /admin/counts, /audit/admin. permission-matrix tests must be
    extended: each admin endpoint rejects a non-admin token; every
    status transition writes exactly one audit row; the phone-reveal
    endpoint is itself audit-logged (recursive test).
[ ] lane B owns ui/ -- the new /admin route in App.tsx, the Admin/
    component tree (Overview, Users, Moderation, AppStore, NodeConfig,
    AuditLog components under components/Admin/), and the lift of
    ConfigPage into /admin#config. coordinate with lane A on the
    endpoint shapes (don't reach into api/) -- build against fixtures
    until the api lands, same pattern B4 used.
[ ] the NOT-AN-ADMIN gate, the drawer/sheet primitive, the audit diff
    tree, and the analytics chart card are all SHARED ui primitives
    that should land in components/Admin/_shared/ first so each tab
    composes them -- the worst outcome is six tabs each hand-rolling a
    confirm-modal and a phone-reveal.
[ ] e2e: extend the playwright gauntlet with an admin journey:
    sign in as the bootstrap admin -> open /admin -> search the user
    you signed up with in step 1 -> suspend them -> verify their next
    write is rejected -> unsuspend -> verify the audit log shows both
    transitions. this is the regression seatbelt for the whole phase.


PHASE 5 — media service (the s3 layer)
--------------------------------------
records store metadata + url. blobs live in object storage. s3 API is the
standard (minio self-host, or R2/B2/wasabi hosted) so the vendor is
swappable per node.

[✓] new service: media/. small fastapi that:
    - issues presigned upload urls, gated by web10 token permissions
      (same is_permitted logic as CRUD -- terms records stay the single
      source of permission truth)
    - issues short-lived signed read urls for private media: fresh
      url per read, 30-60s expiry, issuance logged. check-at-issue-
      time is an accepted gap -- see D14 in decisions.md.
    - writes a metadata record into the owner's collection on upload
      (ordinary {service:"media", body} record -- terms/ACLs apply
      with zero new concepts, see D13)
    - list and delete media metadata records
[✓] minio in compose under the "media" profile for self-hosters
[✓] config for hosted s3-compatibles (R2/B2) for bigger nodes
[ ] size accounting: media bytes count against the user's space plan
    NOTE (1.0.30): the standalone media/ service was merged into api/
    as a router — same routes and auth, one app on port 6000, no
    separate container. only minio remains in compose for blobs.


PHASE 5.5 — the public layer (discovery + schema registry + public ledger)
--------------------------------------------------------------------------
D27: posts are plaintext, discoverable by design. E2E encryption is DMs
only. D28: a schema-registry public ledger gives any app a flexible,
validated way to publish structured public interactions.

The social app splits posts into separate collections by visibility:
`public_posts` (anon whitelisted, discovery indexes it) and
`private_posts` (anon blocked, discovery ignores it). Each collection is
a separate service with its own terms record, so bulk control is instant
("make all posts private" = change 1 terms record) and per-app
permissions work (App A reads public, App B reads both).

API side (Lane A):
[✓ 1.0.93] discovery index: `web10.discovery_posts` system collection,
    write-protected (background task only). On post create/update in a
    service where anon is whitelisted, upsert a projection into the
    index. On post delete or terms change, remove backfill accordingly.
[✓ 1.0.93] schema registry: `web10.schemas` system collection.
    - POST /schemas/register — any user, returns provider.uuid6 ID
    - PATCH /schemas/{id} — fetch (anon OK)
    - PUT /schemas/{id} — update (author only, API enforces ownership)
    - DELETE /schemas/{id} — delete (author only)
    - Schemas store JSON Schema for payload validation
[✓ 1.0.93] public ledger: `web10.public` system collection.
    - POST /public/entries — create (any authenticated user, validates
      payload against schema)
    - PATCH /public/entries — query (anon OK, filter by schema_id,
      target, author)
    - PUT /public/entries/{id} — update (author only)
    - DELETE /public/entries/{id} — delete (author only)
[✓ 1.0.93] discovery endpoints (new `discover` router):
    - PATCH /discover/posts — for you feed (recent/trending sort)
    - PATCH /discover/users — suggested accounts
    - PATCH /discover/search — full-text search (Mongo $text index)
    - PATCH /discover/topics — trending hashtags
    - PATCH /discover/post/{user}/{service}/{id} — single post lookup
[✓ 1.0.93] engagement aggregation: derived live from public ledger at read
    time (no cached deltas). Engagement is schema-agnostic, keyed by
    payload.action, filtered by target.
[✓ 1.0.93] engagement score: formula for trending sort
    (likes * 1 + comments * 3 + reposts * 5)
    - POST /schemas/register — any user, returns provider.uuid6 ID
    - PATCH /schemas/{id} — fetch (anon OK)
    - PUT /schemas/{id} — update (author only, API enforces ownership)
    - DELETE /schemas/{id} — delete (author only)
    - Schemas store JSON Schema for payload validation
[ ] public ledger: `web10.public` system collection.
    - POST /public/entries — create (any authenticated user, validates
      payload against schema)
    - PATCH /public/entries — query (anon OK, filter by schema_id,
      target, author)
    - PUT /public/entries/{id} — update (author only)
    - DELETE /public/entries/{id} — delete (author only)
[ ] discovery endpoints (new `discover` router):
    - PATCH /discover/posts — for you feed (recent/trending sort)
    - PATCH /discover/users — suggested accounts
    - PATCH /discover/search — full-text search (Mongo $text index)
    - PATCH /discover/topics — trending hashtags
    - PATCH /discover/post/{user}/{service}/{id} — single post lookup
[ ] engagement aggregation: background task updates cached engagement
    counts on the discovery index when public ledger entries are created
    or deleted. Engagement is schema-agnostic (keyed by schema_id).
[ ] engagement score: formula for trending sort
    (likes * 1 + comments * 3 + reposts * 5? or just chronological)

Social app side (Lane D):
[✓ 1.0.92] split posts into public_posts / private_posts services
[✓ 1.0.92] create default terms for both services on user signup
[✓ 1.0.92] register default schemas (Reaction, etc.) on first boot
[✓ 1.0.92] cache schemas locally: schema_id → schema definition
[✓ 1.0.92] route createPost by visibility toggle
[✓ 1.0.92] wire reactions to /public/entries endpoint
[✓ 1.0.92] wire feed preview to /discover/posts endpoint (replace placeholder)

Marketing-ui side (Lane D):
[✓ 1.0.92] replace FeedPreview placeholder data with /discover/posts
[✓ 1.0.92] wire tab switching to sort params (recent, trending)
[✓ 1.0.92] wire reaction buttons to /public/entries
[✓ 1.0.92] fetch schema definitions for rendering interaction types
[✓ 1.0.130] D-trending-card: marketing-ui TrendingCard component — media-forward
    card with rank badge (#1 gold, #2-3 silver, #4+ brand), heat glow
    (engagement_score drives violet glow intensity), author row, content
    truncated to 3 lines, tags as chips, engagement bar. Hover: 2px lift
    + glow intensify. Skeleton shimmer matches card shape.
[✓ 1.0.130] D-trending-grid: marketing-ui /trending page layout — responsive grid
    (1/2/3 col), #1 featured card spans 2 cols, topic filter chips (sticky,
    horizontal scroll), hero statement ("What's actually trending. No
    algorithm."). No subtabs.
[✓ 1.0.130] D-trending-sidebar: marketing-ui /trending sidebar (desktop only, wide
    screens) — "🔥 Top 10" ranked list, compact entries (position + author
    + snippet + score), click scrolls to card in main grid.
[✓ 1.0.130] D-trending-polish: marketing-ui /trending loading + empty states — shimmer skeleton
    grid (matches layout, no shift), empty state story beat ("The network is quiet"
    + CTA to social app), infinite scroll feel (load 20, "Load more" brand pill).

[ ] D-trending-interact — make /trending feel alive (operator, 23.07: the like
    button "endlessly goes up not effecting the state of the app," the repost/
    share icons are dead, the comment icon doesn't expand, clicking a card goes
    nowhere). the D-trending-card cluster shipped read-only engagement buttons
    that are pure local theater — Trending.tsx handleReaction only
    setAllPosts-increments a counter, Share2 has no onClick, MessageCircle only
    fires a funnel event, and the card isn't a link. this cluster makes them
    real. the page is a PUBLIC, signed-out surface, so the work splits by what
    auth allows: anon-OK ships first (card->profile deep-link, share, view-
    comments inline via the anon-queryable PATCH /public/entries), then the
    auth-gated writes (like/repost toggle, compose-comment; POST /public/entries
    is "any authenticated user" and marketing-ui has no wapi/token today, so
    ship the funnel-to-social design first -- the button opens web10-social at
    the post and the reaction happens there, matching the existing
    trending_comment_attempt funnel event's intent -- with an inline auth bridge
    via AUTH_ORIGIN as a later upgrade). the endless fake local increment
    STOPS either way; that's the minimum honest fix. acceptance: design.md §12.

    PRIVACY MODEL (operator, 23.07, decided as D32): comments are NOT DMs —
    they are public discourse. so `comments` ships anon-readable (the same
    anon-read whitelist `public_posts` carries, serviceTerms.ts:71) and the
    comment mirror to `/public/entries` is UNCONDITIONAL — matching reactions
    (reactions.ts:35, already unconditional). any web visitor (the default anon
    user) reads comments via PATCH /public/entries (anon-OK, public.py:42); the
    trending comment count (currently always 0 — see D-comments-ledger) goes
    live. this is D30 applied to interactions: the collection IS the security
    boundary, NOT a per-record/per-post visibility flag — the earlier "gate the
    comment mirror on the parent post's visibility" draft was a per-record-
    inspection mistake D30 already rejects ("the permission layer never looks
    inside the record"). the panic button is ONE terms change in the
    authenticator: flip `comments` (or `reactions`) to owner-only and the
    whole interaction surface goes dark server-side for every post on the node
    at once. per-record permissions is a FUTURE decision, deferred. the
    layered model (operator): collection-level = authoritative but lenient
    (public by default, one place to lock down); individual POSTS configurable
    via the public_posts/private_posts/staging_posts collections (D30);
    comments/reactions/reposts ride their OWN collection's default and do NOT
    inherit the parent post's visibility.

[✓ 1.0.149] D-comments-ledger (social-side prerequisite, lane D; decision D32):
    (1) `createComment` (web10-social/src/data/comments.ts:36) mirrors to
    /public/entries (POST, "any authenticated user") UNCONDITIONALLY — exactly
    as `createReaction` does (reactions.ts:35). no per-post visibility gate;
    comments are public by default (D32). fires the registered Comment schema
    (feed.ts:33) with payload.action='comment' (what documentdb.py:828 counts).
    fixes the always-0 trending comment count + the test_discovery.py:645 mock
    that hid it.
    (2) the `comments` service gains an anon-read whitelist in serviceTerms.ts
    (matching `public_posts`:119) so any logged-in user reads comments on any
    post via CRUD, not just the post's author (today `comments`:120 is owner-
    only, which the social app's own feed-thread UI already works around by
    reading the author's own collection — but it breaks for cross-user reads).
    (3) mirror `deleteComment` to the ledger's DELETE /public/entries/{id}
    (author-only, public.py:82) so a removed comment drops out of the engagement
    count and the marketing-ui list; without this, deleted comments ghost-read.
    unblocks D-trending-comments. owns: marketing/web10-social/src/data/
    {comments,serviceTerms}.ts + the FeedScreen call site. gate: none — start
    now.
[✓ 1.0.149] D-trending-link: wrap TrendingCard (or its author row) in a deep-link to
    the author's profile on web10-social -- `${SOCIAL_ORIGIN}/u/<author>`
    (origins.ts exports SOCIAL_ORIGIN). GATE: web10-social has no per-user
    profile route yet -- ProfileScreen only renders the signed-in user's own
    profile (App.tsx mode==='my-bio'); the gauntlet report (1.0.142) flags "no
    user profiles." a social-side `/u/:username` public-profile route is a
    lane-D sibling that must land first; until it does, the card links to
    social's root (a real navigation, not a dead click). carry author_provider
    so cross-node profiles resolve. owns: FeedPreview.tsx + Trending.tsx.
[✓ 1.0.149] D-trending-share: the Share2 button has NO onClick handler today. wire it
    to navigator.share when available (title = post snippet, url = a canonical
    post url; until social has a per-post route, share the card's /trending
    anchor or social root), with a copy-link fallback (clipboard API + a "Link
    copied" toast). no auth needed. owns: FeedPreview.tsx.
[ ] D-trending-comments: the MessageCircle button only fires
    trackFunnel('trending_comment_attempt'); it expands nothing. add an inline
    comment panel under the card: fetch existing comments via PATCH
    /public/entries?target=<author>/<service>/<post_id>&schema_id=<Comment>
    (anon-OK, public.py:42) and render them with designed loading + empty states
    (design.md §1, not a spinner). GATE: D-comments-ledger (today nothing writes
    comments to the ledger, so the count is always 0 and the read returns [];
    the social-side mirror must land first). compose is auth-gated -> signed-out
    visitors get an "Open web10 social to comment" CTA (funnel-to-social); a
    signed-in inline compose box is the later auth-bridge upgrade. owns:
    FeedPreview.tsx + Trending.tsx.
[✓ 1.0.149] D-trending-reactions: like + repost call handleReaction (Trending.tsx:98)
    which only setAllPosts-increments a parsed count -- endless, fake, never
    toggles off. replace with a real toggle mirroring the social app's
    reactions.ts (read existing, find mine, delete or create via /public/entries)
    so the count reflects the ledger, not a local +1, and a second click
    removes it. signed-out -> funnel-to-social or a "sign in to react" gate.
    keep the heart-burst animation (D12 vibrancy). owns: FeedPreview.tsx +
    Trending.tsx.
[✓ 1.0.272] D-trending-views (operator, 30.07 rant: "the same way that messages has
    views, social media, mail, crm, i want the trending to be the same! the
    current trending is very twitter/instagram looking kind of a view! I
    would like youtube view where it only shows the videos and maybe images?
    but with a youtube style of layout!"): /trending gains a view toggle
    like the messages screen's social/mail/crm switcher — the ranked-card
    grid stays default, plus a YOUTUBE VIEW: media posts only (videos, maybe
    images), YouTube-style layout (big 16:9 thumbs, title + author under
    the thumb). the active view is in the URL (/trending?view=youtube) per
    the deep-link rule. bites: a = toggle + youtube view (ws-D/marketing);
    b = the same toggle on social's DiscoverScreen (ws-D/discover, gate:
    a) [✓ 1.0.286]. pairs with the YouTube-first import push.

[✓ 1.0.164] D-trending-knobs — THE ALGORITHM IS YOURS (operator, 24.07): /trending
    gains a synth-rack of knobs that re-rank the feed live. the hero already
    says "What's actually trending. No algorithm." — this makes the claim
    tactile: the ranking function is a WEIGHTED POWER MEAN over the
    engagement signals, and the user holds every parameter. twist a knob,
    the grid reshuffles instantly. never label it "power mean" on the
    surface (the joke is they're playing a synth, not doing math); an
    unobtrusive "how does this work?" popover can tell the truth for the
    curious.
    THE MATH (identical on client and server — see v1): normalize each
    signal to (0,1] with SET-INDEPENDENT saturating normalizers (never
    minmax-over-the-fetched-set — scores must not depend on what else
    was fetched, or v0 and v1 disagree and cached rankings drift):
    R = exp(-age/half_life) for recency, L = l/(l+K_l) with l =
    log1p(likes), same shape for comments (K_c) and reposts (K_p);
    pick K so a "good" count lands ~0.5 and record the choice.
    score = (Σ wᵢ·xᵢ^p / Σ wᵢ)^(1/p), the weighted power mean. p=0 is
    the weighted geometric mean (compute as exp of the weighted
    log-mean; epsilon-floor zero signals so one dead signal doesn't
    nuke a post at p<=0).
    THE KNOBS (rotary, synth-styled): RECENCY / LIKES / COMMENTS (weights
    w_r, w_l, w_c; add reposts if the bar fits), TIME HORIZON (half-life,
    log taper 1h → ∞ "all time"), and CHARACTER (p, -5..+5 — low = "every
    signal must be high" (min-like), high = "any one spike wins"
    (max-like), labeled by feel, not by p). one-tap PRESET chips: Newest
    (w_r only), Most loved · all time (w_l only, half-life ∞), Balanced
    (default ≈ the server's trending order so the page loads familiar).
    DETENTS (operator, 24.07): knobs are STEPPED, not continuous — 6
    detents each (0, .2, .4, .6, .8, 1; half-life and p over 6 named
    stops). this buys three things at once: (1) FEEL — snap-to-detent
    with a tick is how real synth hardware behaves; (2) SHAREABILITY —
    a full knob state serializes to a 5-char code in the URL hash
    (#mix=40213), so "here's MY algorithm" is a copyable link, every
    preset is just a code, and rankings are deterministic/reproducible
    (ships in v0, zero infra); (3) a FINITE CACHE KEYSPACE for v2 —
    6^5 = 7776 raw combos, far fewer effective ones (the power mean is
    scale-invariant in the weights, so (0,.2,.2) ≡ (0,.4,.4); all-zero
    weights is invalid; collapse to a canonical key). so when the server
    eventually computes rankings, one user's "power mean shit" is
    computed once per combo per TTL window and served to EVERYONE on
    that combo — nothing a user dials in is wasted work.
    ACCURACY MODEL (operator, 24.07: "without server calls it won't be
    accurate — it's just shuffling 100 posts, right?" then the fix:
    "it does a TRUE db search, saves it to the cache, and that is like
    100 posts"). right on both counts — a client-side re-rank is only
    exact over what was fetched, and the detent cache is what makes
    the exact answer affordable at scale. two stages, both EXACT:
    V0 (now): the discovery index is tiny (persona posts + the node's
    public posts), so fetch the WHOLE index once — client-side
    re-ranking is EXACT for every knob setting, zero network calls per
    twist, 60fps FLIP/spring reshuffle (prefers-reduced-motion → fade).
    log/flag when the fetch nears its cap so v1 isn't a surprise.
    V1 (when the corpus outgrows one fetch): TRUE-SEARCH-ON-MISS +
    DETENT CACHE (lane A). a knob twist still re-ranks the local
    window instantly for feel; a debounced (~300ms) request sends the
    CANONICAL DETENT CODE. server: cache hit → return the requested
    page of that combo's ranking; miss → run the TRUE db search
    (one-pass $addFields power-mean pipeline over the whole discovery
    index — the set-independent normalizers above make it single-pass:
    $exp/$ln/$pow over the cached engagement counts + created_at —
    then $sort + a DEEP $limit: cache the top ~1k-10k prefix per
    combo, not one page; the depth is what lets tag filtering compose
    downstream, see FUTURE below), save under the detent key with a
    SHORT TTL (~60s: recency decays continuously and engagement moves,
    so entries stale by construction), return the page. the grid
    settles on the exact answer. compute-on-miss means cost scales
    with DISTINCT COMBOS REQUESTED per TTL window, not the 7776-key
    keyspace, and real traffic pre-warms the presets by itself — one
    user's power-mean dialing is computed once and served to everyone
    on that combo. the keyspace stays small even at deep prefixes
    (requested combos only, ids + a few floats each) — redis fits,
    but so does a mongo TTL-index collection or an in-process LRU in
    the api container; DECIDE AT V1, and do NOT add a redis service
    before v1 exists (D29 — a cache tier for a page the client ranks
    for free today reads as infra company). VERIFY at v1 that
    ferretdb/documentdb supports the pipeline operators ($pow/$exp/
    $ln); if not, rank in-process in the api over a projected cursor —
    same cache, same contract. (a multi-anchor union of per-signal
    top-N windows remains the fallback sketch ONLY if both prove too
    slow; with the cache absorbing the cost, it shouldn't be needed.)
    FUTURE (operator, 24.07 — noted, NOT in scope): TAG-SCOPED KNOBS —
    rank within a topic ("show me the most-loved #guitar post of all
    time"). free at v0 (client-side filter before ranking, exact —
    the whole index is local). at v1 the operator's refinement makes
    it compose with ONE cache, no (tag, detent) key explosion: the
    cached entry is the WHOLE deep-ranked prefix per detent combo,
    and tag filtering happens DOWNSTREAM of it. this is exact, not
    approximate, because the set-independent normalizers make each
    post's score filter-invariant — the global ranking restricted to
    tag T IS the correct tag-T ranking. the deep prefix (top ~1k-10k)
    is what makes it useful: a niche tag with zero posts in a global
    top-100 would render empty, so filter depth = coverage; a tag too
    long-tail for even the deep prefix falls back to one scoped true
    search (rare by construction). file tag-knobs as its own item
    when v1 gets staged; don't widen this one.
    D20 GUARDRAIL: this is marketing-ui's public pitch surface — the
    SOCIAL APP's feed stays chronological + sort dropdown (D20;
    lens/customizability remain in later.md). if the knobs prove the
    concept, a social-app lens is a later.md graduation, not a lane item.
    UI BAR: the north star is a VST/synth plugin — the guitar-center
    feeling of walking in and being INVITED to touch the gear (operator,
    24.07: "users actually tuning their experience like musicians").
    it reads as an instrument you play, never a settings form: stepped
    knobs that tick into their detents, readouts that respond mid-drag,
    the rack visually one piece of hardware. ANTI-ANXIETY RULE
    (operator, 24.07: a few sliders is play; too many is anxiety):
    FIVE knobs max, ever — if a sixth wants in, something gets cut or
    merged; the preset chips do the work for anyone who doesn't want
    to twist (most people); there are NO WRONG SETTINGS — every combo
    is a valid feed, nothing errors, nothing empties (worst case the
    order just changes less than expected); and one tap on Balanced
    resets the rack. play is only fun when you can't break anything —
    that's also what makes a guitar center fun. design.md §13 tokens +
    glow only; knobs are draggable (vertical drag + wheel) AND
    accessible (role="slider", arrow keys, visible focus); tabular-nums
    readouts; mobile (375px): knobs in a horizontal scroll rack or
    bottom sheet, never crammed. acceptance:
    design.md §12 (PR screenshots desktop + 375px); twisting any knob
    reshuffles with zero network requests; presets match their promise
    ("Newest" is pure reverse-chron regardless of likes; "Most loved ·
    all time" ignores age). GATE: D-trending-comments merged first (same
    files — Trending.tsx/FeedPreview.tsx are in flight in ws-D/marketing).
    owns: marketing/marketing-ui/** (Trending.tsx + a new
    components/TrendingKnobs.tsx + a pure lib/powerMean.ts with unit
    tests pinning the math: p→±∞ limits, p=0 geometric, weight-zeroing).


Content lifecycle — staging + visibility management (Lane D, D19; decisions.md D30):
The visibility split shipped half-built and crossed at both ends — the
composer sets no visibility so native posts trap in `private_posts` (never
reach the wall or public), while imports write to the legacy anon-readable
`posts` service (importing auto-publishes your entire history — the opposite
of staging). Visibility/lifecycle is the COLLECTION a record lives in, never a
status field (decisions.md D30). This is on the gauntlet critical path (D29):
"import your life -> review -> publish" and "post -> it's public" both run
through here.

Phase A — repair the foundation (lands first; imports currently auto-publish):
[✓ 1.0.131] marketing-api import writes posts to `staging_posts`, not legacy `posts`
    (instagram/facebook/youtube parsers tag `service`; main.py writes it)
[✓ 1.0.131] register `staging_posts` terms (owner-only, no whitelist) in the social
    app's SMROnReady list; confirm node default-deny for absent terms holds
    (auth.py is_permitted returns False; owner's own token always passes)
[✓ 1.0.131] reconcile the wall: readMyPosts reads `public_posts` + `private_posts`
    (owner sees both of their own), not legacy `posts`
[✓ 1.0.131] fix the broken `/exporters` CTA -> `/import` in the 3 social screens
    (FeedScreen, ProfileScreen, DmsScreen) — note cross-app origin

Phase B — visibility on native posts:
[ ] composer gets a visibility selector (public / private; default public per
    the conventions-schema default). createPost already routes once set.

Phase C — the staging + management UI (the feature):
[✓ 1.0.164] Staging/Review screen: reads `staging_posts`, imported content grouped by
    origin, per-item + bulk actions Publish(public) / Keep private / Delete,
    each = a collection move. design.md-grade (§12, PR screenshots).
[ ] post-publish management on the wall: change an existing post's visibility
    (public <-> private) = a move; "make everything private" = 1 terms change.
[ ] PUBLISHING grants the target audience read access to the post's media —
    the media access gate is per-service and coarse (media is owner-only), so
    public media needs an explicit path (grant/tier or discovery-minted link).
    Private/staged media is already creator-only (media.py /read gate). NOT I4.

Deferred (this model absorbs each by adding a collection): `friends_posts`
(needs the friends graph) and `unlisted_posts` (anon-read but discovery-
excluded — the correct home for YouTube unlisted). Until then imported
friends/unlisted stages as private.

Federation (later):
[ ] cross-node discovery: /discover/posts aggregates from federated peers
[ ] schema resolution: fetch remote schemas by provider.uuid6
[ ] public ledger federation: /public/entries aggregates across nodes

Live testing — persona seeding (Cross-lane, persona-orchestration/):
[✓ 1.0.89] persona-orchestration/: 5 persona accounts with distinct
    voices (solar-flare-69, noodle-empress, void-walker, butterfly-
    mechanic, disco-donkey), seed scripts (bash + python), first-week
    content plans, cross-follows, posts, comments, DMs, reactions.
    PURPOSE: this is the test data that exercises the ENTIRE Phase 5.5
    public layer end to end. when the discovery index, schema registry,
    and public ledger land, these personas are the ones that populate
    /discover/posts, /discover/topics, /discover/users, and the
    marketing-ui FeedPreview. the seed script writes public posts
    (visibility: "public"), reactions (the engagement aggregation
    trigger), and comments (schema-validated ledger entries). the
    trending sort gets real data to rank. the homepage feed stops being
    placeholder. the personas also test the social app's core flows
    (post, feed, DM, comment, react, follow) with actual cross-user
    interaction — no more empty-state screenshots in the demo.
[✓ 1.0.145] the seed script is FULLY idempotent (ws-G2 fix): every
    create path reads-before-writes (stable origin_id on posts + DMs,
    natural-key dedup on contacts/follows/inbox/ledger), --cleanup
    removes dups from prior non-idempotent runs (296 removed on dev),
    --verify reports state without writing. re-running is a no-op
    (proven: second run = 0 new records). 7 posts/persona + reply DM
    threads added so the feed + trending demo alive.
    the action plans per persona define ongoing content so another
    agent can log in as a persona and keep the platform alive.

PHASE 6 — wapi.js revamp (the sdk)
----------------------------------
wapi.js is how frontend devs build web10 apps. it deserves the same
glow-up as everything else: right now it's untyped ES with axios and
peerjs hard-bundled, published as web10-npm.

[✓ 1.100] rewrite in typescript. full types for the protocol: records, queries
    ($sort/$skip/$limit), updates, terms/contracts, tokens. typed CRUD:
    read<T>(service, query): Promise<T[]>
[✓ 1.100] drop axios -> native fetch. zero required dependencies.
[✓ 1.100] peerjs/rtc becomes an optional subpath export (web10/rtc) so the core
    stays tiny and tree-shakeable
[✓ 1.100] modern packaging: bun build, ESM + types, publish as a clean package
    (decide: keep web10-npm name or claim something nicer)
[✓ 1.100] auth flow ergonomics: the popup/oauth dance in wapiAuth wrapped into
    one promise-based login()
[✓ 1.100] docs generated from the types (typedoc) into the docs site
[ ] react hooks package later (useRecords(service, query) with live
    refresh) -- separate small package on top, not in core

the 5th verb -- aggregate (kill the 4-crud bottleneck):
4 crud ops is too weak a surface to build real apps on. devs should
get (nearly) the full mongo query language. the original fear was
injection -- but mongo queries are structured json, not strings.
walk the pipeline, validate it, sandbox it. flexibility with a
violation check.

[✓] new api endpoint: aggregate on /{user}/{service}. read-only by
    construction. terms treat it as "read" (or its own action).
[✓] sandbox by structure, not by rewriting:
      server prepends: $match {service} -> $replaceRoot to body.
      dev's pipeline runs on clean user-space docs and cannot even
      NAME the service/star fields. scoping is unescapable.
      (star record also excluded via body.service != "*".)
[✓] operator allowlist: $match/$project/$group/$sort/$skip/$limit/
    $unwind/$addFields/$count/$facet/$bucket/$sample + full
    comparison/logical/array operators.
[✓] hard denylist: $where/$function/$accumulator (js execution),
    $lookup/$graphLookup/$unionWith (cross-collection read),
    $out/$merge (cross-collection write). enforced at any nesting
    depth ($facet sub-pipelines, $group accumulators, expressions).
[✓] resource caps: maxTimeMS, pipeline length cap, $limit ceiling,
    allowDiskUse off. regex allowed but bounded by maxTimeMS.
    metering: charge() scales per pipeline stage -- same
    credits_spent ledger and credit/space gate as the 4 verbs.
[ ] widen updates too with the same philosophy: $push/$addToSet/
    $pull/$mul/arrayFilters through the existing field-prefix
    transform (u_t already does the prefixing -- grow the allowlist)
[ ] sdk surface: wapi.aggregate<T>(service, pipeline) fully typed
    (untyped wapi.aggregate shipped in legacy wapi.js 1.0.38; the
    typed variant lands with the C2 rewrite)
[✓] add aggregate to the protocol spec + conformance suite

mcp server (promoted from LATER -- it ships with the sdk):
[ ] mcp server exposing token mint + crud + aggregate. same typed
    surface the sdk uses. "talk to your node from claude/any llm."
[ ] aggregate is what makes the llm story real: 4 crud verbs let an
    llm fetch your records; aggregate lets it ANALYZE your life
    ("what did i post most about in 2019?" is one pipeline, not a
    thousand context-stuffed reads)
[ ] scoped tokens per mcp connection: user picks which services the
    llm can touch, same terms machinery as any app
[ ] high configurability + mcp is the wordpress lesson applied to
    the llm era: operators config via policy/ads, devs via real
    queries, llms via mcp. every layer talks to the same records.


PHASE 6.5 — the dev on-ramp (create-web10)
------------------------------------------
"stateless frontends, no backend to run" is the dev pitch, but today
getting started means reading this repo. fix that with a scaffolder:
one command -> a working web10 app talking to a node. the acceptance
bar for the whole phase: time-to-first-record under five minutes.

how it ships (be realistic about how devs start projects now):
[ ] publish as `create-web10`, NOT a global cli. the npm create-*
    naming convention means every runner works for free from ONE
    package: `npm create web10@latest`, `pnpm create web10`,
    `yarn create web10`, `bun create web10`, `npx create-web10`.
    this is how vite/astro/next onboard devs -- zero install,
    always latest, nothing global to go stale. built + published
    with bun like the rest of phase 6. claim the npm name early
    (same errand as the phase 6 package-name decision).
[ ] the interactive flow (plus flags for the impatient and for CI:
    --template, --node, -y): app name -> template -> node to talk
    to. writes .env with node url + provider, prints the three next
    lines (cd / bun install / bun dev), and nothing else.
[ ] templates (few and excellent beats many and rotting):
      - vanilla-ts: vite + typescript + sdk only. the protocol with
        zero framework noise: login -> create a record -> read it.
      - react (the default): vite + react + the phase 6 hooks
        (useRecords live query).
      later, by demand: more templates as devs ask for them.
    every template: sdk login(), one service round-trip, and the
    consent screen appears on FIRST run -- terms are the product,
    the hello-world should show them.
[ ] the node story (a scaffold with nothing to talk to is dead on
    arrival). the node prompt offers:
      - an existing node url (your node, a creator's node)
      - local: docker run web10/node (phase 3's one-container node;
        until that exists, point at this repo's compose stack)
      - the web10 sandbox node: hosted by inc, throwaway accounts,
        wiped weekly. cheap to run, and it's an acquisition funnel.
    default: sandbox -- zero local deps for hello world. graduating
    to a real node is editing one env var.
[ ] templates cannot rot: ci scaffolds each template, installs,
    typechecks, builds, and runs the record round-trip against a
    throwaway node on every pr (same harness as the sdk tests).
    a failing template blocks merge like any other red test.
[ ] docs: the dev quickstart in marketing-ui IS this command. the
    dev "why" page ends with `npm create web10@latest`. (guides-
    fiction rule applies: write it when this ships.)
[ ] explicitly NOT building (yet): a persistent `web10` cli (dev
    servers, deploys, codegen). vite covers dev, the node is
    `docker run` (phase 3), apps deploy as static files anywhere.
    a real cli earns its way in later if recurring workflows
    demand it -- likely first candidates: minting scoped tokens
    for agents/mcp, and generating types from the conventions-doc
    schemas. scaffolder first: 90% of the value, 10% of the
    surface to maintain.

sequencing: after the phase 6 sdk rewrite -- templates are written
on the typed sdk, never on legacy wapi (a scaffold that teaches the
old way poisons the well). the sandbox + local-node options improve
as phase 3 lands, but --node <url> works from day one.


PHASE 7 — reorganize the apps (monorepo, but tidy)
--------------------------------------------------
revised thinking: the killer app STAYS IN THIS REPO. it's not "an app"
-- it's the default lens. it ships with every node, renders the
operator's ad slots, embodies the conventions doc, and during the
build it's what drives the protocol (aggregate, inbox, conventions
all get discovered by building against them). schema + api + sdk +
app need atomic commits. wordpress ships its default theme; mastodon
ships its web client. same logic.

[✓] killer app gets a first-party top-level dir (marketing/social/), built and
      versioned with the node. phase 8 + 9 live HERE.
[✓] crm/ + mail/: resolved differently than planned — integrated as
     sub-apps INSIDE web10-social (CRM Rolodex + Mail inbox, 1.0.30)
     and the standalone dirs deleted. no examples/ dir was created;
     the killer app itself is now the reference implementation.
[✓] home/ + docs/ -> marketing-ui/: web10 inc's website, ONE site.
    docs are a key part of a saas marketing site, so they live
    together: landing pages + dev docs, one dir, one build. not part
    of a node -- own vhost, never in the node compose. it STAYS in
    this monorepo by choice: one dev, one repo, atomic commits
    across product + site is lean. multi-repo is a later option,
    not a goal.
[✓] marketing-ui rebuild: replace home/'s static html (delete the
    cruft: index - Copy.html) with the same toolchain as ui
    (vite + react + bun, one stack to maintain).
[ ] the naming makes the repo read clean: api (the node),
    ui (the node's admin/consent surface, from auth2 -- phase 2),
    marketing-ui (inc's website + docs), web10-cli, web10-social.
[✓] node compose after the tidy: api(+ui), db, social, [rtc],
    [media]. examples run via an "examples" profile for devs.
[ ] third-party apps (the ecosystem) live in their own repos and
    always will -- that's the protocol working. first-party lives
    here.


PHASE 8 — the killer app (all-in-one social)
--------------------------------------------
an instagram-shaped social app -- photos, video, streaming, feed, dms --
that is ALSO the landing zone for everything the exporters pull in. this
is the thing a normal person opens and gets it. lives IN THIS REPO
(social/ -- it's the default lens, see phase 7),
built on the phase 6 sdk, media via phase 5, deployed as the default
lens on every node.

the schemas come first (this is the real interop work -- an application
profile on top of the protocol, not part of it):
[ ] web10 social conventions doc: standard service schemas that ALL
    social apps + exporters share. loose, documented, versioned:
      - posts     (text, media refs, ts, origin, visibility)
      - media     (metadata + object-store url, from phase 5)
      - contacts  (the friend graph: username, provider, labels)
      - follows   (who i follow, cross-node)
      - groups    (community spaces: group meta + membership records --
                   see the groups item below)
      - comments  (parent post ref, threading)
      - reactions (likes etc, target ref)
      - profile   (display name, avatar ref, bio)
      - inbox     (the feed. see below.)
    origin field everywhere: "instagram", "facebook", "web10", ... so
    imported life and native life coexist in the same services.

the app:
[✓ 1.0.48] profile + grid view (the instagram surface: media-first)
    DATA LAYER ONLY — profile CRUD, media upload via presigned URLs,
    grid view screens deferred to D2.5 post-B2.5 tokens
[✓ 1.0.48] posting: photo/video upload via media service, records into posts
    DATA LAYER ONLY — post CRUD + media upload via API presigned URLs
    per conventions schemas. composer screens deferred to D2.5.
[✓ 1.0.48] feed via the INBOX PATTERN — DATA LAYER ONLY: chronological
    + sort dropdown (newest / oldest / most-reacted via aggregate).
    screens deferred to D2.5 post-B2.5 tokens.
[ ] follows across nodes: follow alice@othernode -> terms handshake ->
    her posts land in your inbox. federation made visible + trivial.
[ ] GROUPS (post-M0; the fb-groups-shaped gap, spotted by the operator
    24.07 -- a social super app without groups isn't one): member-
    created community spaces inside a node, the scene's sub-scenes.
    shape: a group lives in its owner's collection -- one group record
    (name, description, privacy, avatar ref) + membership records,
    terms-gated; the group feed reuses the SAME inbox pattern as
    follows (fan-out on write to the member list instead of the
    follower list -- no new delivery machinery). private groups are
    terms-gated now, crypto-private later: phase 11 already names
    "a group" as an audience circle, so record shapes here must not
    preclude an audience key attaching to one. why this is plan.txt
    and not later.md (the D20 lens): paid-community sellers are a
    named M0 pitch segment, and memberships (phase 4 stripe rails) x
    private groups IS the paid-community product -- a members-only
    space on a node the creator owns (the skool/discord+patreon
    shape), which strengthens the creator P&L directly. milestone
    honesty: not M0 (M0 is post/feed/profile/dms); this is the
    natural M2-adjacent build, since paid communities are creator-#1
    monetization surface. schemas (groups, memberships) land in the
    conventions doc when built.
[✓ 1.0.48] comments/reactions on posts — DATA LAYER ONLY: threaded
    comments CRUD, reaction toggle + aggregate counts. screens
    deferred to D2.5.
[✓ 1.0.48] dms: records for async — DATA LAYER ONLY: deterministic
    conversation service names, send/read/delete DMs as records.
    screens deferred to D2.5. (rtc for live: phase 11)
    video: EXPANDED into phase 8.5 (photos v1/v2 polish, video v0
    direct playback, v1 vertical short-form lens, v2 transcode ->
    hls). the ticks live there now.
[ ] streaming (later milestone in this phase): live via rtc/webrtc small-
    scale first; hls fan-out for big rooms when a node needs it

the boring plumbing social apps die without:
[ ] url routing: the address bar holds your place. today navigation is
    one useState mode in App.tsx — refresh dumps you at the feed,
    back/forward are dead, nothing is linkable. real routes (/feed,
    /discover, /messages, /profile, /u/:username) so a refresh
    restores the screen you were on, and /u/:username unblocks the
    trending card -> author-profile deep-link (phase 5.5
    D-trending-link ships a root-link TODO until this exists).
    board item D-url-routing in parallel execution.txt.
[✓] real follower count (gauntlet step 6's own words: "a creator page
    without follower count is not a creator page — the primary social
    proof metric." 1.0.149 honestly removed the FAKE stat; this builds
    the real one): follows mirror to the public ledger
    (payload.action='follow', the D32 pattern — this makes the follow
    graph public; record the call as decision D34 when implementing),
    counts read from the ledger, /discover/users gains follower_count
    in the engagement aggregation, and the Followers tile returns to
    ProfileScreen with a real number — the M0 screenshot stat.
    board item D-follower-count in parallel execution.txt.
    HONESTY NOTE (25.07, found writing the ws-C gauntlet e2e pin): the
    tick is only half true. ProfileScreen's Followers tile DOES read a
    real number off the ledger (countFollowers(), self profile only) —
    but the WRITE side never fires: followUser()'s mirror is gated on
    getCachedSchema('Follow'), which is only ever populated by
    registerDefaultSchemas() (data/feed.ts) — a function nothing in the
    app calls. Even with that fixed, createPublicEntry() posts a body
    with no `token` field, so the mirror would 401 anyway (caught
    silently, returns a fake local stub). And /discover/users'
    suggested_users() (api/app/services/documentdb.py) never actually
    computes or returns a followers_count field — so UserProfileScreen's
    /u/:username page (viewing someone ELSE's profile, where the follow
    button lives) can never show a follower count at all, only your own
    /profile can. Full detail + suggested fix shape in
    .context/e2e-finding-public-ledger-mirror-dead.md. Not fixed here
    (e2e/** lane); re-open as a lane D + lane A item.
    RE-OPENED (26.07 audit #4, verified still dead at HEAD): the lane D
    half is [✓ 1.0.193] D-ledger-mirror-fix (registerDefaultSchemas never called +
    createPublicEntry sends the wrong body shape vs public.py — fixed:
    bootstrap call in App.tsx + correct {token, query} body shape), the
    lane A half is A14-followers-count (/discover/users returns
    followers_count).
    both in parallel execution.txt. until these merge, ALL real-account
    social proof (follower counts, trending engagement) is theater —
    persona-seeded numbers only.
[ ] user profile media resolution (gauntlet step 6): viewing another
    user's profile always shows the default initial-letter avatar and
    gradient banner, even when the user has uploaded images.
    `UserProfileScreen.tsx` never populates `mediaMap` with resolved
    avatar/banner refs. Also: post media in the profile grid is
    structurally broken — `media_refs` is dropped from the discovery
    post map and the discovery API doesn't return media refs.
    board items D-user-profile-media and D-feed-avatar-resolution in
    parallel execution.txt.
[ ] feed and discover avatar resolution: `FeedScreen.tsx` and
    `DiscoverScreen.tsx` try to find the author's avatar inside the
    post's media items — only works by accident if the avatar was also
    attached to the post. Fix: resolve profile avatar/banner refs
    separately from post media.
    board item D-feed-avatar-resolution in parallel execution.txt.
[ ] notifications: web push first (pwa), mobile push later. new post
    from a follow, comment on your post, dm, key request (phase 11).
    notification prefs are an ordinary settings record the user owns
[ ] realtime: db change streams -> websocket push, surfaced in the
    sdk as live queries (useRecords with live refresh, phase 6).
    feed/dms update without refreshing.
[ ] search: full-text over YOUR OWN collections first (my posts, my
    dms, my imported history -- the exporters make this instantly
    valuable: search your whole social media life for the first
    time ever). cross-node people lookup: alice@node resolves via
    the protocol; an opt-in directory service for discovery.
[ ] mobile story: pwa first (installable, push-capable). native
    wrapper only when the pwa hits a wall. the encryptor app
    (phase 11) stays native -- it guards keys.
[ ] ad slots: renders the node's sponsored placements (phase 4 admin
    controls) -- this app is where the node operator's monetization
    actually displays. default lens = monetized lens. users can swap.

the lens chatbox + feed customizability: CUT (D20, 18.07.2026):
moved to later.md in full (lens record, llm chatbox, preset lenses,
customization surface). the feed ships chronological + a sort
dropdown -- "no algorithm" is itself the feature, and the pitch is
ownership/no-shadowban (THE STORY), not configurability. the killer
app stands on its own or it isn't killer. D19's byok architecture
stands ready if the chatbox ever earns its way back; the promotion
bar is in later.md.

PHASE 8.5 — media polish + video (the tiktok-shaped gap)
--------------------------------------------------------
the honest state (23.07.2026 audit): photos upload and render, but
the composer is a bare file input with 80x80 hover-only square
previews (PostComposer.tsx) -- no reorder, no alt text, no aspect
ratio, remove button invisible on touch. the feed grid has no
lightbox, silently slices past 6 images, and reserves no space
from image dimensions. and video is a TRAP: the picker accepts
video/* but preview AND feed render it with <img>, so a picked
video just breaks. the api stores anything up to 500MB and derives
NOTHING server-side (no thumbnails, no dimensions, no transcode)
-- thumbnail_url / hls_manifest_url are pass-through fields nobody
populates, so the feed's `thumbnail_url || url` fallback always
ships the original. design.md sets the bar (media-forward,
kick/twitch-grade) but has no media spec: no canonical aspect
ratios, no player chrome, no grid numbers.

the principle that shapes all of it: THE CLIENT DOES THE WORK
FIRST. the node derives nothing today, and for private media it
never can -- a transcoder can't transcode what it can't decrypt
(I4). so resize, thumbnails, poster frames, and dimensions happen
client-side at attach time, and the server pipeline (when it
comes) is a PUBLIC-media upgrade, not the foundation. this also
keeps every early stage shippable against the api that exists
right now.

media v0 -- reads survive a private bucket (the dev-minio 403;
lanes C+D with one tiny lane-A touch):
the media record stores the BARE unsigned object url composed at
upload time (confirmUpload: `${uploadUrl}/${objectKey}`) and
every renderer (avatar, feed, composer) trusts record.url
verbatim -- readable only while the bucket allows anonymous GET.
on dev minio the bucket is private, so every avatar/photo 403s
(AccessDenied). the api's presigned-read endpoint
(request_read_url) exists with ZERO callers; the wapi wrapper
has getUploadUrl/confirmUpload but no read half; the typed sdk
has no media surface at all.
[✓] wapi gains the read half: getReadUrl(objectKey, user?,
    provider?) -> POST /media/{user}/read -> presigned GET, with
    an expiry-aware cache (READ_URL_EXPIRY) so a feed of N
    images doesn't cost N api round-trips per render. lands in
    BOTH the web10-social wrapper (lane D) and the typed sdk's
    new media surface alongside upload (lane C -- sdk/src has
    neither today). [LANE D half landed: web10-social wrapper
    + resolveMediaRefs presigning; C7 (typed sdk) still open]
[ ] confirm-upload persists object_key on the media record (tiny
    lane-A touch; today only the composed url is stored). legacy
    records: derive the key by stripping endpoint+bucket from
    the stored url.
[✓] resolveMediaRefs (and the avatar/feed/composer paths above
    it) exchange refs for fresh presigned urls; nothing renders
    record.url raw anymore. [web10-social lane D, D23]
[ ] the bucket-policy call is SEPARATE and stays open: public-
    read for public media (avatars, public posts) may come later
    for cacheability/cdn -- decisions.md entry when decided.
    presigned reads must work regardless: they are the ONLY path
    for private/encrypted media (I4).

photos v1 -- the composer stops being ugly (lane D; gauntlet step
2 territory, "post a photo -> it appears in the feed"):
[✓ 1.0.147] media tray: previews at real aspect ratio in the same grid the
     feed will render (composer preview == post render, no
     surprises), always-visible remove button (hover-only fails
     touch, and the gauntlet runs on a phone), drag to reorder
[✓ 1.0.147] client-side processing at attach: downscale to max edge 2048 +
     recompress (canvas -> webp/jpeg), extract width/height,
     generate + upload a real thumbnail -- populate the media-record
     fields (width, height, thumbnail_url) the confirm endpoint
     already accepts but nobody fills
[✓ 1.0.147] alt text per image in the tray (design.md requires alt; the
     schema has the field; the composer never asks)
[✓ 1.0.147] validation with a voice: wrong type / over the size cap gets
     rejected at attach time with words, not a failed upload spinner

photos v2 -- the feed renders like it means it (lane D):
[ ] lightbox: tap any image -> full-screen viewer, swipe between a
    post's images, pinch/double-tap zoom, swipe-down/esc to close
[ ] "+N" overflow tile on the sixth image instead of silently
    slicing the rest
[ ] no-layout-shift for real: reserve space from stored
    width/height (the design.md rule); dominant-color placeholder
    while the image loads
[ ] design.md gains a MEDIA SPEC: canonical aspect ratios (4:5
    portrait cap, 1.91:1 landscape, 9:16 vertical video), grid
    rules, player chrome. the screenshot test needs numbers.

video v0 -- it plays at all (lane D; works against today's api):
[ ] the composer treats video as video: <video> preview (not
    <img>), poster frame captured client-side (seek + canvas),
    poster uploaded as the thumbnail, duration_seconds filled
[ ] the feed plays mp4: <video> with poster, preload="metadata",
    tap to play with sound. h.264/aac mp4 (+ webm) accepted;
    anything else rejected at attach with "convert to mp4" words
    -- no transcoder yet, honesty over a spinner. phones record
    h.264 mp4 natively, so the mainline path just works.
[ ] caps while originals ship untranscoded: duration + size gate
    at attach (e.g. 3 min / 100MB -- video v2 lifts them)
[ ] one player, both sources: this is also how IMPORTED video
    already plays (the phase 9 direct-mp4-off-minio/R2 note)

video v1 -- the vertical feed (lane D; the tiktok answer):
[ ] a short-form lens IN web10-social: 9:16 full-bleed vertical
    feed, snap scroll, muted autoplay + tap for sound, preload
    next. reads the SAME posts/media services -- short-form is a
    LENS over the conventions schemas, not a new silo. rides v0's
    direct mp4 (phone-recorded h.264 plays everywhere), so it
    does NOT wait for the transcode pipeline; it inherits hls
    later for free.
[ ] the composer knows: 9:16 detection -> "this will show in the
    vertical feed", cover-frame picker for the poster
[ ] the pitch lands here: own-your-audience pointed at short-form
    creators -- 100% delivery on a surface tiktok-shaped enough
    to demo next to tiktok

video v2 -- the transcode pipeline (lane A; the old phase 8 line):
[ ] transcode worker: ffmpeg in a compose profile ("transcode"),
    queued off upload-confirm for PUBLIC video only: h.264 ladder
    -> hls segments + manifest -> poster + thumbnail server-side
    -> patch the media record (hls_manifest_url, duration,
    dimensions). boring presets, no invented pipelines.
[ ] playback upgrades itself: hls.js where hls_manifest_url
    exists (safari plays it natively), mp4 fallback otherwise --
    the v0 player grows a source-selection step, nothing else
    changes
[ ] private video never touches the transcoder (I4): encrypted
    blobs stay direct playback of what the client uploaded;
    phase 11's encrypted-hls item covers the rest
[ ] size accounting (the phase 5 straggler) lands with this:
    video makes it real -- media bytes count against the space
    plan before a node fills its disk
[ ] then lift the v0 caps

streaming/live is NOT this phase: it stays phase 8's later-
milestone line (rtc small-scale, then hls fan-out). video v2's
hls plumbing is deliberately the same tech so live inherits the
player when it comes.

PHASE 8.6 — social product surface (operator requests, 26.07.2026)
------------------------------------------------------------------
captured from a live operator pass on the deployed social app. the
coming-soon sidebar entries are STUBS ONLY -- non-interactive "soon"
chips so the roadmap is visible in-product (shipped 1.0.170); the real
builds are the [ ] lines below.

navigation / surfaces (coming-soon stubs shipped; builds pending):
[ ] flares -- ephemeral 24h posts (the "stories" shape). provisional,
    non-infringing name; rename pending operator sign-off. stub live.
[ ] takes -- short vertical video (the "reels" shape). provisional
    name. stub live.
[ ] livestream -- rtc small-scale then hls fan-out (already phase 8's
    later-milestone; the stub just surfaces it). stub live.
[ ] games -- placeholder surface, scope undefined. stub live.
[ ] groups -- multi-user spaces. stub live.
[ ] marketplace -- creator commerce. stub live.

messages -- three views, a toggle on the messages screen (operator idea):
[✓ 1.0.182] view 1: current chat/DM list (shipped).
[✓ 1.0.182] view 2: MAIL view -- the whole inbox rendered gmail-style (threads,
    read/unread, preview) as a toggle.
[✓ 1.0.182] view 3: CRM view -- notes + contact history per person, in-app, so
    influencers remember who they reached out to. the web10-social CRM
    sub-app (here since 1.0.30) is the reference; this is a messages-only
    lens over the same contact/DM data.

messages v2 -- the operator's 26.07 night pass on 1.0.182 (with
screenshots; "these are the things that can give a platform flare" —
the mail/crm lenses ARE the all-in-one-lens pitch made visible:
"showing the robustness of web10 — you can make a mail app on the
messages service, or a crm. it is a clinic on interoperable data."
PITCH NOTE: the one-click chat→mail→crm flip over the SAME records is
a demo-video beat — sovereignty made screenshot-able; no incumbent's
data model can do it. generalizing lenses to feed/profile is parked
in later.md "view lenses everywhere"):
[✓ 1.0.203 #352] MAIL must actually be email: "that doesn't look like email! where
    is the inbox, outbox, spam" — folders (Inbox / Sent / Spam), and
    the thread view gets real email anatomy: Subject, Reply / Reply-all
    / Forward, To/Cc/Bcc on compose, From/To header per message. new
    fields are BODY fields on existing message records (conventions
    addition, no api change); legacy DMs render fine without them.
    lane item D-mail-experience (ws-D/messages).
[✓] CRM parity with the ORIGINAL crm sub-app ("the crm is actually
     better, more on target, but missing the status colors from the old
     crm! should have all the features of that original crm — bin and
     sort people"): color-coded contact status (the old green/yellow/red
     priority + color filters — components/Crm/Crm.tsx, deleted #120,
     reference in git; crm-contacts/crm-notes services still registered),
     sort/filter by status, richer contact fields (email/phone/company/
     links + free-form), contact CRUD from the view. lane item
     D-crm-upgrade (ws-D/messages).

shell:
[ ] DEEP LINKS EVERYWHERE (operator, 26.07: "everything should be a
    deep hyperlink on web10 social… every screen in the social app is
    shareable, or saveable/bookmarkable if it is a private screen like
    dms"): D-url-routing (1.0.155) gave the five top-level routes;
    the STATE inside them still lives in useState. sweep every screen
    so the URL holds the state: /messages/:conversationKey +
    ?view=chat|mail|crm, /discover?tag=&q=, /profile?tab=posts|media,
    /staging, post permalinks (/u/:username/p/:postId — shared with
    D-feed-lightbox), and provider disambiguation on user routes
    (identity is (username, provider) — decide the canonical shape at
    implementation, keep /u/:username back-compat). the standing rule
    for all FUTURE pages is now in AGENTS.md ("UI screens: the URL
    holds the state") — new screens that don't encode state in the
    URL are a review rejection. THE RATIONALE (operator): the funnel —
    marketing /trending click-throughs redirect to those advanced
    urls: click a post → the exact post on social; click a COMMENT →
    the post opened AT that comment (comment anchor:
    /u/:username/p/:postId?comment=<id>, scroll + highlight). "that is
    the rationale, and makes it polished, aka killer." marketing-ui's
    TrendingCard/comment-panel links upgrade as the routes land
    (D-marketing-search + D-trending-link get the deep targets).
    PRINCIPLE (operator, 26.07 — ten years on social media): shareable
    links are how people get pulled in, and they build USER TRUST —
    the incumbents degraded exactly this (instagram's login wall,
    twitter's logged-out lockdown; the link opens, then a door slams).
    web10's public layer is anon-readable by architecture, so the rule
    is: A SHARED LINK TO PUBLIC CONTENT NEVER HITS A LOGIN WALL — it
    opens, full quality, with joining as an invitation, not a ransom.
    the link IS the demo. any agent adding an auth gate to a public
    surface is breaking the funnel; private screens auth as they
    always did.
    lane item D-deep-links (ws-D/shell; cross-screen seam — stage
    after the current fleet drains). BITES MERGED: messages [✓ 1.0.266],
    discover ?tag=&q= [✓ 1.0.276], comment anchor [✓ 1.0.271]; open:
    profile ?tab=, marketing click-through retarget [✓ 1.0.287].
[✓ 1.0.211 #365] SETTINGS TAB (operator, 26.07): "web10 social doesn't have a
    settings tab, should be a settings tab, facebook style." v0 =
    account section, posting defaults (default visibility), data/
    ownership explainer + terms-editor link, about/version/report-bug;
    full facebook-style tree proposed in .context for sign-off first.
    settings persist as an ordinary owner-only `settings` record.
    lane item D-settings-tab (ws-D/settings).

feed / lightbox (operator, 26.07 night pass, screenshots):
[✓ 1.0.239] FEED POSTS AREN'T CLICKABLE — "can't click the posts in the feed to
    make them big, only on my profile." clicking any feed post should
    open the same lightbox the profile grid got in 1.0.170/1.0.180.
    on someone ELSE's post the owner actions (edit/delete) are hidden —
    "but should be share post! kind of a function": a Share action for
    every post (navigator.share + copy-link fallback, the
    D-trending-share pattern) copying a deep link that opens the post.
    lane item D-feed-lightbox (ws-D/feed).
[ ] LIGHTBOX MEDIA SIZING — "maybe they can be bigger? or some kind of
    proper resizing to make it look right": the lightbox renders media
    small with dead space around it. media-first sizing: scale the
    modal to the media (up to ~90vh/vw), aspect-aware, no letterbox
    voids. lane item D-lightbox-media-sizing (ws-D/profile,
    PostLightbox seam).
[ ] TEXT-ONLY POST TREATMENT — "something more creative and artistic
    with the background with text only posts" (screenshot: a text post
    renders as a huge empty dark cell with tiny top-left text). give
    text-only posts the text-on-generated-background pattern: centered,
    scaled type on a deterministic gradient/texture derived from the
    post id (stable per post, picked from design.md token-based
    palettes — never hardcoded colors), applied in the profile grid
    cell, the feed card, and the lightbox. one shared component so all
    three surfaces agree. PLUS author choice (operator follow-up):
    "maybe some choice of background styles or formatting — facebook
    has a markdownesque formatter for text only posts, center, left
    align, that kind of stuff" — the composer gets a background-style
    picker (the deterministic one stays as the default) and light
    formatting controls (align center/left, maybe bold/emphasis),
    persisted as body fields on the post record so every surface
    renders the author's choice. lane item D-text-post-cards
    (ws-D/profile, shared-component seam).

posts / profile -- controls the operator found missing:
[✓ 1.0.178] delete a post (owner action, on the wall + in the lightbox).
[ ] change a post's visibility after publish (public <-> private is a
    collection move, D30; mirror the composer's selector post-hoc).
[ ] profile avatar + banner PLACEMENT editing -- reposition/crop, fix
    centering (today it's object-cover with no offset control).
[ ] follower + following LIST UI -- the profile shows only counts today;
    tapping Followers/Following should open the actual list of accounts
    (avatar + handle, link to /u/:username). data exists in follows.ts
    (D-follow-lists in parallel execution.txt, ws-D/profile).
    OPERATOR RE-CONFIRMED 30.07 (rant): "when i click my followers or
    following count, i dont see a page with my list of followers /
    following, it should link me there when i click the numbers!" —
    per the address-bar priority the lists are ROUTES
    (/u/:username/followers + /following), not a modal.

messages -- controls the operator found missing:
[ ] delete a message / conversation.
[ ] edit a sent message.

engagement:
[ ] an "advanced poke" -- a playful, lightweight nudge primitive (the
    facebook-poke feeling) reimagined as web10's own, NOT a clone.
    design TBD, keep it non-infringing; could ride the public-ledger
    reaction plumbing.

posts -- rich content (operator, 26.07):
[✓ 1.0.251] LINK EMBEDS -- paste a url into a post (or dm) and it renders as an
    embed, the table-stakes feature every platform has: a youtube link
    becomes a playable player in the feed; other links become a preview
    chip. v0 is client-side only (D-link-embeds in parallel
    execution.txt, ws-D/feed): allowlisted iframe embeds (youtube +
    vimeo, privacy-enhanced src, click-to-load thumbnail so feeds never
    eat N iframes), every other url a favicon+domain external-link chip.
    NO server-side og-scraping in v0 -- a generic link-preview fetcher
    is a lane-A/marketing-api item with SSRF surface; it graduates
    later, behind the same allowlist thinking, if plain chips prove too
    thin. embeds render everywhere post text renders: feed, profile
    grid/lightbox, dm bubbles.

search (operator, 26.07: "the one thing we are really missing now is
search! searching by tag, even on the marketing page — youtube-esque,
an entry point into web10 social, if you could search from the
marketing ui!"):
[ ] PUBLIC SEARCH on marketing-ui — a search bar on /trending (+ the
    site header) over the public corpus: posts by text, #tag, and
    users, backed by the discovery API that already ships (A5.5:
    PATCH /discover/search, /discover/topics, /discover/users — verify
    the endpoints' current shapes before building). results render in
    the trending grid's card language; every result deep-links INTO
    web10-social (/u/:username, post links once D-feed-lightbox's
    permalink route exists) — search IS the funnel. lane item
    D-marketing-search (ws-D/marketing).
[✓ 1.0.230] SEARCH ONLY SEARCHES TAGS (operator, 27.07: "searching only
    searches tags also. it doesnt search the other things") — a word
    that appears in a post's BODY (not its tags) returns nothing, and
    author handles don't hit either. /discover/search is a $text query
    over (body_text, tags) in theory — suspects: the text index
    missing/broken on the deployed DB (query errors → the client
    swallows to []), body_text empty on indexed posts (field drift),
    or $text whole-word semantics failing substrings. fix so body
    words + author handles + tags ALL return results. lane item
    D-search-fulltext (ws-D/marketing, small lane-A seam inside
    /discover/search only).
[ ] /trending CONTROLS ALIGNMENT BRICKED (operator, 27.07, screenshot):
    the topic-chip rail starts with a half-clipped chip at the left
    edge, and the presets row + Advanced toggle are centered under a
    left-aligned search bar — two axes colliding. lane item
    D-trending-controls-alignment (ws-D/marketing).
[✓ 1.0.210] NO IMAGES ON /trending POSTS (operator, 27.07, screenshot: "cant
    see images on posts either!"): posts with photos render as
    text-only cards — the discovery index is text-only (media_refs
    never projected), so nothing downstream CAN show media. two-part
    fix: A17-discovery-media-projection (lane A — index carries
    media_refs + has_media + first-attachment mime; backfill re-run
    needed per env) then D-trending-card-media (ws-D/marketing —
    TrendingCard renders the real first image via the public_media
    presign path, design.md MEDIA SPEC). the D-user-profile-media
    grid half is unblocked by the same A17.
[ ] FOLLOW DOESN'T PERSIST FOR REAL ACCOUNTS (operator, 27.07,
    screenshot: "tried to follow my friend coolguydavid, and it gives
    a question mark, says following i refresh and i am not following
    him"): /u/:username follow → optimistic "Following" → lost on
    refresh; the Following count renders "?" (a swallowed throw); the
    profile shows "No posts yet" for an author who HAS public posts.
    the likely root is the SMR-only terms class A13 fixed for
    public_posts: the app's `follows` SIR is registered only while
    the auth portal is open, so real accounts that never got the SMR
    have no follows terms and the write 403s into a catch. lane item
    D-follow-persistence (ws-D/follow, PRIORITY) — repro first, quote
    the failing request, provision the app's core service set at
    signup + migrate, kill every fake-success state. NOTE the "No
    posts yet" half likely needs E-run-discovery-migration RUN on
    prod (still operator-gated) — the item names it instead of
    hacking around it.
[✓ 1.0.210] MESSAGE BUTTON ON USER PROFILES (operator, 27.07, screenshot:
     "there should be a button to message my friend on his profile"):
     /u/:username has only Follow. add a Message button that deep-links
     to the messages screen with that thread open (existing thread by
     key, else prefilled compose via D-dm-compose's flow) —
     /messages?to=<username> per the URL-holds-state rule. lane item
     D-profile-message-button (ws-D/follow, after D-follow-persistence
     — same UserProfileScreen seam).
[ ] in-app search parity note: DiscoverScreen's existing search (if
    any) gets audited against the same corpus in the same pass —
    "search your own stuff" stays a separate low-hanging-fruit item
    (gauntlet follow-on list, top of this file).

showcase app (operator, 26.07: "showing the web10 social network as a
web, like an app outside of web10 social — graph.web10.app, something
wooshy — is great marketing, because it is an app interoperating on
web10 data… one outside analytical app on top of your social data"):
[PARKED 29.07 — operator: "the graph isnt aligned enough with the
minimum necessities for the business… save this graph stuff for much
later after we make some money!" the anon /graph route shipped as
bite a (#362) and was REMOVED in 1.0.213 — it never worked on prod
(GET on the PATCH-only endpoints; mocked tests hid it). both items
below park to later.md with unpark notes (use PATCH; a top-N-by-
followers aggregation over the public ledger is the right shape for
a legible graph; add a real-data smoke assertion). original text
kept for history:]
[ ] THE GRAPH APP — a standalone, anon-first visualization of the
    node's social graph: users as nodes, follows as edges, force-
    directed and wooshy (drag, zoom, watch it breathe), click a node →
    that user's /u/:username deep link. THE POINT is the pitch: a
    separate app interoperating on web10 data — the existence proof
    for "apps are just lenses" from OUTSIDE the flagship (the
    wordpress-flywheel claim made visible; pairs with the messages
    lens toggle as demo-video beats). two modes (operator: "it could
    render your personal graph, not public necessarily only!"):
    (1) anon node view — the PUBLIC follow ledger only (D34, already
    anon-queryable) + /discover/users for avatars/names; never
    private data here. (2) MY GRAPH, signed-in — the app walks the
    normal consent flow, takes a SCOPED token to your own follows
    (+ contacts if consented) and renders your ego graph client-side,
    nothing published: the third-party-app contract (consent → scoped
    read → lens, I5) demonstrated live, which is the deeper interop
    proof. FRAMING (operator): the global graph is the entry point;
    "SEE YOUR GRAPH" is the hero CTA — "more than facebook ever let
    you in on." explain the interop story in-product (the truth-
    telling popover pattern) + one docs paragraph, manifesto voice,
    written once and reused.
[ ] SOCIAL ANALYTICS, chess.com-style (operator, 26.07): the second
    outside-app beat — ranked analytics on your social data: your
    followers/engagement/growth, you vs your friends, you vs the top
    creators, percentiles and leaderboards, computed client-side from
    public data under your own scoped token. "your number is real
    here" as a dashboard. lane item D-social-analytics
    (ws-D/marketing, after D-graph-app).
    hosting: v0 ships as a marketing-ui route (/graph) to avoid new
    vhost/compose churn; graph.web10.app becomes its own vhost when it
    earns it (lane E touch then, not now). gate: D-ledger-mirror-fix —
    until real follows mirror, the graph renders seeded personas only
    (fine for the demo, say so honestly in the empty state). lane item
    D-graph-app (ws-D/marketing, after D-marketing-search).

join page — the steps grow (operator, 26.07, screenshot of the
1.0.178 two-step strip: "i really like this dora the explorer what
steps to complete! it should be more!"):
[✓ 1.0.203] extend the numbered steps to the full onboarding path — the Rise
    arc as a checklist: get the app → create your account → set up
    your monetization (deep link to the Studio money screen in the
    authenticator) → post to the feed. each step links where it
    actually happens. lane item D-join-steps-extend (ws-D/marketing).
[✓ 1.0.247] step VISUALS (operator, 29.07, screenshot of the shipped strip:
     "i like this join but, i feel like the story doesnt have good
     icons / visuals… this doesnt feel a16z like a palantir or
     something, but i like the clarity actually on the other hand!"):
     the copy and clarity STAY (explicitly liked); the step cards
     level up from bare numbered circles to real visual anchors —
     step 1 shows the actual web10 social PWA icon, the keys-mark
     motif for account, a money/Studio motif for monetization, a
     composer motif for posting — with designed connectors and
     hover states. a16z/palantir-grade storytelling with the
     straightforwardness intact. lane item D-join-steps-visuals
     (ws-D/marketing(2)).

app store revamp (operator, 26.07, screenshot: "would be nice if it
was all just apps… i actually liked the old app store, where they
really really really looked like apps. web10 social is the flagship,
and the authenticator — we don't need to plug them so hard, they will
naturally get landed on… this is like the apple app store,
PREMMIUMMM!!!!!"):
[✓] EVERYTHING IS AN APP — kill the FLAGSHIP hero card and the
     abstract "On web10" feature cards; first-party apps (social,
     authenticator/node console, importer) get the SAME icon-forward
     card as every registered app: real app icon, name, one-liner,
     visit count, Open. prominence comes from the visit-count sort,
     not editorial banners (the store doesn't rig itself — same trust
     story as the no-algorithm feed). premium bar: apple-app-store
     feel — big rounded icons, generous spacing, crisp type, hover
     lift, all design.md tokens. first-party apps need REAL icons
     (design.md §3 brand assets, not lucide glyphs). lane item
     D-appstore-revamp (ws-D/marketing). ✓ 1.0.211
[✓] plugs (operator, 29.07 — partial reversal of the no-hero call):
     "i actually liked the social app had a bigger card in the top in
     the old app store layout! but the logo was bad, i like that it
     has its progressive web app logo!" the big top card was good in
     SHAPE, bad in ASSET. the uniform grid stays; above it, curated
     plug slots as one bigger-card component: FLAGSHIP (web10 social,
     real PWA icon — the one editorial slot), MOST POPULAR (#1 by
     visits — earned), NEWEST (latest approved registration —
     freshness). slots degrade gracefully (no eligible app → slot
     hides; flagship==most-popular dedupes). CATEGORIES (top games
     etc. — "there are so few apps that can be down the line!"):
     filed, not built — stage when the catalog is big enough that a
     rail never renders empty; the v2 registration-shape spec leaves
     room for a category field. lane item D-appstore-plugs
     (ws-D/marketing(2)). ✓ 1.0.230
[ ] V2, the product page (UI only, no comments/ratings yet): click an app → big logo, description, visits, open link, screenshot carousel, 404 fallback. AppCard `appId` prop → internal nav. AppStore enriched data from API. ✓ 1.0.253
     REMAINING: COMMENTS/reviews (D32 public-ledger entries targeting the app id
     — the machinery post comments already use; federated review
     aggregation explicitly deferred to M3). operator, 29.07 rant:
     reviews are 1-5 STAR RATINGS + comments, and every app surfaces
     on a #web10apps thread on web10 social — "would be cool if these
     all get on the #web10apps thread on web10 social, or something
     like that! and people can comment on them and give them a review
     from 1 to 5 stars!" the product page and the social thread read
     the SAME ledger entries — one conversation, two lenses. the
     BROWSE grid (operator, same rant): uniform small cards — big
     icon, name, visits, rating, NO descriptions (they're why the
     cards are uneven) — click → the full product page; plus a
     browse search bar (client-side filter, the catalog is small). ✓ 1.0.251
    lane items D-appstore-browse + D-appstore-revamp v2. AND the
    registration
    rewire (operator): app registrations move onto the same public
    data plane as everything else (an `App` schema in the registry,
    discovery-indexed), with listing metadata as NODE-HOSTED records
    — approval attaches to a metadata VERSION, any listing change
    (a new pwa thumbnail, say) re-enters review; the store never
    auto-pulls assets from the app's origin. moderation by
architecture; lane A spec note before v2 builds (see the
     D-appstore-revamp lane item). ✓ 1.0.239 (D37 spec in
     .context/appstore-v2-registration-spec.md)

engagement-count accuracy (operator, 26.07, screenshot: a post's badge
says 3 comments while the expanded panel renders more than that):
[✓ 1.0.210] the discovery index's CACHED like/comment counts disagree with
    what the comment panel actually renders — the badge reads the
    index's engagement aggregation (documentdb.py, cached at index/
    aggregation time), the panel reads live ledger entries. audit what
    each path counts (action values, dedup, edits/deletes) and when
    the cache refreshes; make them agree or refresh-on-read. related
    but distinct: real-account interactions don't mirror AT ALL
    (D-ledger-mirror-fix) — this item is the seeded/ledger data
    agreeing with itself. lane item A15-engagement-count-accuracy.
[✓ 1.0.230/1.0.231] REGRESSION (operator, 29.07, screenshot: every /trending card at
    0 likes / 0 comments): A15's fix made it worse — the prod ledger
    has THREE target formats (seed script writes
    `{author}/public_posts/{post_id}`, the app client writes
    `posts:{post_id}`, A15's aggregation matches `public_posts:
    {post_id}`) and A15's matches NONE of the real data. DECISION:
    the canonical ledger target is `{author}/{service}/{post_id}`
    (engine-native, seed-written, globally unambiguous). fix = A18
    (lane A: revert to the raw post_key $match) +
    D-engagement-target-client (ws-D/feed: social comments/reactions
    + the marketing trending panel write/read the canonical target).
    both PRIORITY 1. operator follow-up in the same pass: "even
    better if i clicked them and it went to the hyperlink to the
    web10 social post" — needs social's post permalink route
    (D-feed-lightbox bite b / D-deep-links; the URL-holds-state rule
    is already law), then a small marketing bite points TrendingCard
    clicks at the permalink.

bug reports -> the operator (operator idea):
[✓ 1.0.181] route "Report a bug" submissions to the operator instead of dying in
     memory. TODAY: ReportBug (web10-social) POSTs marketing-api /feedback,
     which appends to an in-memory `feedback_store: list` (main.py:394) --
     lost on restart, only readable via GET /feedback. WANT: persist first
     (done — JSON file on disk, survives restart), then publish each report
     as a public post in the `web10` system account's `public_posts`
     collection tagged `#web10-bugs` — bug reports are discoverable,
     searchable, and engageable on web10-social. The post delivery is
     opt-in via `NODE_API_URL` + `NODE_API_TOKEN` env vars (fire-and-forget,
     logged on skip/failure). When unset, the report still persists to disk.
     HONESTY + PRIVACY NOTE (26.07 audit #4): what shipped is option (c)
     — public posts — which was NOBODY's named option (the item said DM
     to the operator, the fallback was an operator-only panel); operator
     to confirm or redirect. And the public post INCLUDES THE REPORTER'S
     CONTACT INFO (_format_bug_post appends `Contact: ...` into an
     anon-readable, discovery-indexed post) — a PII leak on the platform
     whose manifesto says "nobody is mining you." fix filed as
      [✓ 1.0.192] D-feedback-contact-privacy (parallel execution.txt, ws-D/feedback):
      contact/user_agent never enter the public body; they stay in the
      disk store the operator reads.

known gaps confirmed this session (see CHANGELOG 1.0.169-1.0.174):
[✓ 1.0.178] PRIORITY — public discoverability is still broken for real accounts.
    RUN-IT NOTE (26.07 late, operator screenshot: his real public post
    absent from /trending Newest on the deployed app): 1.0.178 ships
    the fix as code + ADMIN ENDPOINTS (POST /admin/discovery/
    migrate_terms + /admin/discovery/backfill) — merging does NOT run
    them. Existing accounts (the operator's included) stay
    undiscoverable on dev/prod until the migration + backfill are
    EXECUTED per environment after deploy. filed as lane item
    E-run-discovery-migration (ops, needs an admin token).
     (A13-discovery-terms in parallel execution.txt, LANE A). 1.0.171
    fixed HALF: service_allows_anon now matches the app's `.*` anon-read
    whitelist by regex (was an exact "anon" compare). The remaining half:
    the public_posts anon-read TERM isn't provisioned for a real account.
    records.services_record() ships an empty whitelist, and the only
    thing that writes the term is SMROnReady's SIR consent (sdk
    compat.ts:77) which posts the SMR ONLY while the auth portal window
    is open — an already-signed-in user is never re-prompted, and a
    server-only change triggers no SMR (the CLIENT contract didn't
    change). So the operator "gets no SMR" and the term never lands →
    not indexed AND anon GET 403s. Fix: provision the canonical public
    terms by default + migrate existing accounts + backfill existing
    public_posts into discovery (LANE A item has the full spec + a D30
    "collection is the boundary" option c to decide first).
[ ] follow does not backfill / there is no pull feed: following someone
    surfaces nothing until THEY post again (fan-out is push-only, at
    post time, to current followers). "followed X, don't see them in my
    feed" is exactly this. needs on-follow backfill OR a pull over
    followees' public_posts -- filed 26.07 as D-follow-backfill
    (parallel execution.txt, ws-D/feed): on follow, deliver the
    followee's recent public posts into the follower's own inbox,
    capped, deduped on post_id.
[✓ 1.0.230] in-app Discover borrows HEAVILY from /trending (operator, 29.07,
     screenshot of the knob rack: "the trending page on the marketing
     site is amazing! the discover should borrow much more heavily
     from it, the knobs to tune your algorithm should be on
     discover"): the D20 revisit is SIGNED (decisions.md D36) —
     Discover is already a ranked surface, so knobs there don't touch
     the chronological feed's delivery pitch (the feed stays
     chronological, full stop). port the TrendingCard language +
     presets + KnobRack/RotaryKnob/powerMean (copied, not shared) to
     DiscoverScreen; client-side re-rank, zero new api surface. lane
item D-inapp-discover-knobs (ws-D/discover, bites: [✓ 1.0.230] cards+controls,
      [✓ 1.0.239] knobs).
[✓ 1.0.245] trending multimedia + AUTOPLAY (operator, 29.07: "trending lacks
    videos and images, how are we going to compete with insta without
    multimedia on this kind of a page! also videos should auto play
    with no volume, otherwise how are we going to catch attention!"):
    the card-media code is live since 1.0.210 — the board renders
    text-only because A17's media fields reach existing index docs
    only via the backfill RE-RUN (E-run-discovery-migration,
    operator-gated P1 — run it). the new build: videos autoplay
    muted/loop/playsInline on cards, pause offscreen, sound on tap
    (insta pattern; respects prefers-reduced-motion). lane item
    D-video-autoplay-muted (ws-D/marketing).
[✓] DM header links to profile (operator, 30.07 rant: "when i click my
    friends bubble / name on the top of the chat, it doesnt bring me
    to their profile"): the DM conversation header's avatar + name are
    dead text — make them a Link to /u/:username. lane item
D-dm-header-profile-link (ws-D/messages, tiny). merged 1.0.251.
[✓ 1.0.252] follow button toggles to unfollow (operator, 30.07 rant: "when i
    hit follow, it doesnt toggle and let me unfollow!"): the toggle
    handlers exist on both UserProfileScreen and DiscoverScreen — this
    is a runtime failure in the unfollow path (suspects: ledger
    cleanup throwing, stale/duplicate follow records read without a
    status filter). repro first, capture the failing request. lane
    item D-follow-toggle (ws-D/profile).
[✓ 1.0.262] home stats bar styling (operator, 30.07 rant, screenshot: "the
    border isnt necessary on those, and can just say data liberated
    under the data part to be consistent with the other two! they
    could just be on the background, or they can each be in their own
    sleek card, or use your best judjgemen"): kill the heavy bordered
    container on the 1.0.240 stats bar; "data liberated" becomes the
    third stat's label (449 USERS · 9 APPS · 27.6 MB DATA LIBERATED);
    bare-on-background vs sleek individual cards is the builder's
    taste call against design.md §12. lane item
    D-home-stats-bar-styling (ws-D/marketing(2), small).
[✓ 1.0.263] data liberated counts S3 bytes too (operator, 30.07 rant: "the
    data liberated tab isnt counting s3 data! should also add up all
    s3 data in the count!"): /stats' `storage` is mongo dbstats
    storageSize only (documentdb.py:867) — media blobs live in the
    object store, so every uploaded photo/video is missing from the
    landing-page number. fix: add the bucket bytes (MinIO usage API
    or summed size_bytes over media metadata records, cached),
    back-compat response shape. lane item A19-stats-s3-bytes (lane A).
[✓ 1.0.265] delete post button broken (operator, 30.07 rant: "the delete post
    button isnt working for me!"): root cause visible in the code —
    deletePost (posts.ts:141) hardcodes the `posts` service, but posts
    live in public_posts/private_posts since the D30 collection split;
    the delete matches nothing and the error is swallowed. fix:
    delete from the post's actual collection, remove the discovery
    ghost on public deletes, re-read the grid after delete. lane item
    D-delete-post-broken (ws-D/profile).
[✓ 1.0.272] another user's profile shows HIS stats + posts (operator, 30.07
    rant, screenshot of /u/coolguydavid: "i dont see how many hes
    following and how many followers! nor do i see his posts but i
    know he has a post"): three confirmed UserProfileScreen bugs —
    the "Following" count is the VIEWER's own countFollows() (and is
    first set from posts_count, a mixup); the Followers tile vanishes
    when the user is outside the client-side-filtered
    /discover/users?limit=100; the post grid filters
    /discover/posts?limit=50 client-side so his indexed post is cut.
    fix: per-user counts from the public ledger (never the viewer's),
    posts via a per-author discovery query. lane item
    D-user-profile-stats (ws-D/profile).
[✓ 1.0.267] self-engagement counts (operator, 30.07 rant: "when i like my own
    posts it gives 0 likes, it should be one like! self likes count!
    same for comments, shares"): PRODUCT DECISION — self-engagement
    counts. the server doesn't exclude it, so the 0 is a write/read
    bug: prime suspect is an own-post call site that doesn't thread
    postAuthor/postService and falls back to the orphaned legacy
    `posts:{id}` ledger target the aggregation never matches; also
    the mirror's non-fatal catch swallowing the write, the
    discovery-vs-own-collection count source split, and shares having
    NO ledger write path at all (add payload.action='repost' on
    share). lane item D-self-engagement-counts (ws-D/feed).
[✓ 1.0.279] profile posts clickable + videos visible again (operator, 30.07
    rant: "not able to click on my posts anymore, and cant see the
    videos, maybe it is that play feature that plays them?" — fixed
    directly on operator call, severe enough to skip the filing queue):
    regression from D-own-profile-canonical (1.0.266) — /u/<you>'s
    UserProfileScreen grids were plain divs (no lightbox) and rendered
    videos with <img>. grids now open PostLightbox and render video
    posters + play badges, matching the retired ProfileScreen.
[✓ 1.0.284] visibility toggle duplicates the post (operator, 30.07 rant: "i
    tried publicing a post, privating the post, publicing it again,
    there is now two posts one in public one in private of the same
    post"): movePostVisibility is create-then-delete; a repeated
    toggle leaves a copy in each collection. prime suspect: the
    lightbox runs the second toggle against the STALE pre-toggle
    record (the move mints a new _id in the target collection), so
    the delete misses. also check the discovery index for a ghost
    public copy after privatizing. lane item
    D-visibility-toggle-duplicate (ws-D/profile).
[ ] profile photos on someone else's profile (operator, 30.07 rant:
    "when i go to someones profile, i dont see their profile photos"):
    the public-media read path (A12 + D-public-media-client, D35) is
    fully merged but was never verified end-to-end — and the operator
    confirms it's still broken on prod. the fix queue: FIRST
    D-public-media-verify (ws-D/profile, tiny — two real accounts on
    the local stack, capture every cross-user media request's status,
    fix or name the exact blocker), which clears the gate on
    D-feed-avatar-resolution + the D-user-profile-media grid half.
[✓] own profile at the canonical /u URL (operator, 30.07 rant: "when i
    click my username, it goes to my profile page instead of just
    /profile it goes to profile/u/jacoby149 — lets just have it always
    be profile/u/jacoby149!"): your own profile always lives at
    /u/<you>; /profile becomes a redirect (back-compat, never 404 a
    shipped route). OPERATOR DECISION (30.07 rant #2, screenshot of
    /u/jacoby149): "ultimately, /u/{your web10 username} should be the
    same as /profile for the signed in user… should get rid of
    /profile so you can share with friends!" — /profile is RETIRED as
    a screen; /u/<you> IS the profile (owner experience: edit
    affordances, own-collection post read), and it's the link you hand
    a friend. the same screenshot shows the /u/ owner path today hits
    the D-user-profile-stats bugs ("0 Posts", no Followers tile, "No
    posts yet" with posts existing) — the owner path must read the
    owner's own collection. lane item D-own-profile-canonical
    (ws-D/shell, tiny). [✓ 1.0.266]
[ ] ADDRESS BAR EVERYWHERE — operator priority call (30.07 rant: "i
    guess when we make every screen in the address bar have a link to
    it, that will happen. maybe lets have that address bar for every
    screen be a priority for the next web10web10!"): D-deep-links
    (above, the 26.07 item) is the PRIORITY of the next kickoff batch
    — stage its per-screen bites first.
[ ] how-it-works middle card is jargon (operator, 30.07 rant: "the
    marketing page says this thing 'it fans out on write' it doesnt
    make so much sense, dont know what that means. the first and the
    third card were good though! lets think of a way on how to better
    refactor that story"): the homepage How-it-works trio's card 2
    title "It fans out on write" is engineer-speak — re-tell the
    middle beat in reader language (the description "every follower's
    inbox gets the post the instant you publish" is the meaning;
    candidate titles: "It lands in every inbox" / "Every follower
    gets it" — builder's taste call, keep cards 1+3). sweep
    overview.md:50, which uses the same phrase as its step-2 title.
    lane item D-how-it-works-story-copy (ws-D/marketing(2), tiny).

PHASE 9 — the exporters (fill the collections)
----------------------------------------------
apps that pull your social media life into your web10 services. paired
with phase 8: exporters fill, the killer app renders. day one on a node
= import your old life, see it in the app, in your collections, yours.

design principle: client-side first. the export never touches anyone's
server -- parse in the browser, write to YOUR node via the sdk. the
sovereignty story stays intact end to end.

v1 (takeout-file based, works today, no api permission needed):
[✓] instagram exporter: user requests official data download (json zip),
    drags it into the exporter app -> parsed in browser -> media to the
    media service, records into posts/contacts/comments per the
    conventions doc, origin="instagram"
[✓] facebook exporter: same shape (their takeout is messier -- start
    with posts, photos, friends list)
[✓] youtube exporter: google takeout -> videos + metadata
[ ] twitter/x, tiktok: same pattern, priority by demand
[✓] shared exporter core: one package that handles zip parsing, schema
    mapping, batch record writes, media upload, resume on failure.
    each platform = a mapping module on top.
[ ] llm-assisted mapping (optional, later): unknown/changed takeout
    formats get normalized by an llm instead of a hand-written parser
    breaking. this is what makes exporters durable.

IMPORT YOUR LIFE, the revamp (operator, 29.07 rant: "the import your
life feature lets be real, this is bricked right now… the format for
the join tab would work great for the import your life!"): rebuild
/import as a /join-style numbered-step journey:
  1. export the zip from legacy media — platform logo buttons deep-
     linking to each platform's own data-export page (Facebook,
     YouTube, X, Instagram, TikTok). pure links, pure value today.
     PLUS web10 export (operator, 29.07: "and web10 export! and make
     a note, yes you can export from your current web10 node, and
     import somewhere else!") — download your own node's collections
     as a zip, import on another node; the page says it proudly
     (the anti-lock-in proof is the pitch).
  2. log into the web10 authenticator → the import tab.
  3. upload the zip — a WeTransfer-like flow onto the hosted S3.
  4. we do the rest — a background queue in marketing-api (operator
     says fastapi + celery; a lighter runner is allowed if argued)
     processes it onto web10 social, then DELETES the originals from
     the bucket (a load-bearing privacy promise, stated in the UI).
  5. posts enter staging — review privacy in the Staging screen
     (already built, 1.0.164), click done, they're on web10 social.
  reuses the 1.0.31 marketing-api ZIP pipeline + 1.0.131 staging_
   posts writes. lane item D-import-revamp (bites: a = the step
   strip + export links, ws-D/marketing(2); b = the WeTransfer
   pipeline, ws-D/marketing-api; c = the authenticator import tab,
   ws-B; d = web10 node export import path, ws-D/marketing-api
   [✓ 1.0.272]). follow-ups filed 30.07: D-import-export-links-dead
  (operator-verified working export URLs + our own /docs export-
  guidance article — operator supplies the content; NOT llm
  redirects) and D-export-links-health-e2e (a scheduled link-
   health workflow that catches rotted/"shit page" export links
   before the operator does). NOTE: this revises the "client-side first" principle above
   for the hosted path — the server-side pipeline is what the
   operator wants (the browser parse is the bricked part); the
   sovereignty story is preserved by the delete-after-processing
   promise + the data landing in the user's own collection.

[✓ 1.0.277] YOUTUBE-FIRST IMPORT (operator, 30.07 rant: "my business outreach
    efforts are ONLY to youtubers for the first go! therefore i want
    to on the import feature efforts to make it youtube explicitly,
    and the other platforms TBD! and get that suuuppper sharp!!!!!!
    maybe make it clear it is Youtube first, rolling out to export
    from the others soon after! because google takeout seems the most
    transparent!"): the import surface leads with YouTube (Google
    Takeout) as THE primary path — the outreach targets YouTubers
    only for the first go, so /import must read YouTube-first at a
    glance, with the other platforms honestly marked "rolling out
    soon after" / TBD. lane item D-import-youtube-first
    (ws-D/marketing(2)); the bite-b/c pipeline bites build the
    YouTube mapper path first too.

v2 (continuous):
[ ] dma / official portability apis where they exist -> scheduled sync
    instead of one-shot import
[ ] agent-driven export for platforms with no api (user's own agent,
    user's own credentials, writes to user's own node)


PHASE 10 — user backups (the escape pod)
----------------------------------------
node backups (operator-side) live in cross-cutting. this is the USER'S
backup: my whole life on this node, in MY google drive / dropbox, next
to my camera roll. exit rights made tangible -- the backup IS the
ownership story, in a folder a normal person can see.

[ ] export format: one archive = all my services as json records +
    media files, versioned against the social conventions doc (so a
    backup is also a valid import -- backup and migration are the
    same feature)
[ ] google drive + dropbox oauth integrations in the ui: connect once,
    scheduled auto-backup (daily/weekly), backup-now button
[ ] restore/import flow: point a fresh node (or a new account on
    another node) at your backup -> your life comes back. this is
    node migration, demoable.
[ ] GRADUATION (account migration with your audience): a signed
    "moved" record on the old node + followers' nodes auto-re-follow
    at the new address (the mastodon Move-activity pattern -- adopt,
    don't invent). data comes via the restore flow above; this item
    is the FOLLOWERS coming too. needs federation (M3) to be fully
    live. this is the ladder's top rung in THE STORY: grow on a
    creator's node, graduate to your own, audience intact -- the
    ownership story proven from the inside.
[ ] backups of encrypted content stay encrypted (phase 11) -- drive/
    dropbox hold ciphertext, never plaintext private data


PHASE 11 — e2e encryption (your phone is your keychain)
-------------------------------------------------------
the node stores ciphertext for private content. keys live on your
phone. this resolves the deepest tension in the whole model: the
influencer/operator hosts your data but CANNOT read it. landlord
holds the building, not your diary.

seeds already in the repo: mobile/encryptor (expo app), sdk/src/
wapiencrypt.js (secp256k1 experiments), rtc service (the key
channel -- note: rtc's current check is jwt.decode-without-verify
plus trust-a-remote-200, the I1 bug in miniature. it gets the same
JWKS fix before it carries keys).

the mental model (whatsapp, not web3):
  the phone is the WALLET: it holds the root keys for your whole
  life -- one identity key plus per-service keys for all your apps.
  laptops and other devices are LINKED COMPANIONS (the whatsapp
  desktop move): the phone provisions them ONCE over p2p webrtc,
  then they encrypt/decrypt on their own. day-to-day traffic never
  routes through the phone -- the phone is the root of trust, not a
  proxy. two balanced goals, stated plainly: (a) the operator never
  sees private content, (b) sharing is real but REVOCABLE. every
  primitive here is boring standardized crypto -- x25519/ed25519,
  hpke (rfc 9180), xchacha20-poly1305, argon2id, the signal/mls
  playbook. no chains, no coins, no invented crypto. (D15-D18)

the key hierarchy (what's in the wallet):
[ ] one master seed (random 32 bytes). hkdf-derive from it: the
    identity keypair (ed25519 -- signs everything), the exchange
    keypair (x25519), and per-service app keys. deterministic
    derivation means backup is ONE secret, and "a bunch of elliptic
    curve keys for all your different apps" costs nothing to mint.
[ ] keys friends send you (grants, below) are a cache, not
    precious: re-fetchable as records from their nodes.
[ ] key manifest: a public self-signed record ({service:"keys"}) in
    your collection listing your current public keys, device certs,
    and audience epoch numbers. friends fetch it to encrypt TO you
    -- a jwks-for-people / signal prekey bundle. trust-on-first-use,
    with optional qr safety-number verification for the careful.
[ ] storage: hardware-backed where the platform allows; enclave-
    wrapped at rest otherwise (ios secure enclave won't do x25519 --
    wrap the sodium keys under an enclave key, don't fight it).
[ ] the wallet guards opaque secrets too, not just derived keys: the
    byok llm api key (D19; chatbox parked in later.md) is an e2e-encrypted record
    the phone beams only to the apps the user picks -- the node
    holds ciphertext, the operator can never read or exfiltrate it,
    and it moves nodes with you like any record. (D19)

the keyring api (design it like the record api -- generic or dead):
the record model survived because it never hardcoded a schema:
{service, body}, any name, four verbs. the keyring gets the same
discipline. if audiences were an enum ("friends"/"close") or grants
only went to humans, the first unanticipated use case kills it.
[ ] named keys, user-chosen names, exactly like services. an
    audience is just a key name: "friends", "gym-circle",
    "post:8f3a" (one record), "agent:lens-llm" (an llm's key),
    "hls:live" (a stream). no built-in circle list, no specials.
[ ] minting is ONE call and free: mint(name) hkdf-derives from the
    master seed. a key per app, per post, per mood -- as cheap as
    naming a service is today.
[ ] two key types only: keypair (x25519+ed25519) and secret
    (symmetric). every named key is (name, type, epoch). epochs
    exist on ALL keys uniformly -- rotation is never a special
    feature of one kind of key.
[ ] principals are PUBLIC KEYS, not usernames. a grantee can be a
    friend's identity, a linked device, an llm agent's session
    key, a future thing we haven't met. human identity binds ON
    TOP via the key manifest + signatures -- never baked into the
    primitive.
[ ] wrap targets are pubkeys OR other named keys. this one rule
    is the composability engine:
      content key wrapped under an audience key    (sharing)
      audience key wrapped to a member's pubkey    (membership)
      audience key wrapped under another audience  (nested circles)
      master seed wrapped under a passphrase key   (backup -- not
      a special subsystem, the same verb)
[ ] the verb set (small, closed, composes -- the 4-crud lesson):
      mint(name)               create/derive a named key
      rotate(name)             bump epoch, return the new one
      wrap(name, to, meta)     issue a grant (meta: expires, epochs)
      unwrap(blob)             consume a grant
      encrypt / decrypt (name, epoch, data)
      sign / verify (name, data)
      list(filter)             who holds what, until when
      handout(name, session)   live-mode release, never persisted
    revoke(name, grantee) = terms-drop + rotate + rewrap: a
    COMPOSITION of the verbs above, not a new primitive.
[ ] everything the keyring persists is ordinary records
    ({service:"keys", body}): manifests, grants, revocations.
    terms gate who fetches a grant; crud moves them; phase 10
    backups and node migration carry them untouched. the node
    grows ZERO key-specific endpoints -- it stays a dumb
    ciphertext + records host. (D18)
[ ] crypto agility or screwed-later: every wrapped blob, grant,
    and manifest carries {v, suite} ids. D17 pins today's suite;
    the format must survive the day it rotates (a broken
    primitive, post-quantum kex). version fields are free now, a
    migration crisis later.
[ ] scope guard: the keyring does keys, not policy. no roles, no
    acl language inside grants -- authorization stays terms (node
    layer) + possession (crypto layer). the moment keys grow a
    policy dsl we've rebuilt kerberos badly.
[ ] the futureproof checklist (design review gate -- ALL of these
    must be expressible with the verbs above, no new primitives):
    one person; a circle; a circle-of-circles; a single record; a
    whole service; an llm agent on a 1-hour grant; a second
    device; an hls stream with rotating segment keys; a
    multi-admin group (mls later -- record shapes must not
    preclude it); a full node migration with keys intact.
[ ] seed in repo: encryptor.js's listen() switch {mint, public,
    sign, decrypt} is this verb set, embryonic. grow that surface,
    keep it closed. the wallet exposes it locally + over the
    linking channel; wapi mirrors it so apps never touch raw keys.

device linking (whatsapp desktop -- the webrtc payoff #1):
[ ] laptop generates its own device keypair and shows a qr: its
    pubkey + rtc peer id + a short pairing secret. phone scans,
    dials it p2p through the rtc service, runs a handshake bound to
    the qr secret so the signaling server can't mitm.
[ ] phone signs a DEVICE CERT {device pubkey, device id, expires}
    with the identity key and syncs current audience keys + grants
    over the channel. from then on the laptop encrypts and decrypts
    ALONE -- viewing on your laptop never touches your phone.
[ ] revoke a device: signed revocation in the key manifest + epoch
    bump (below). lost laptop != lost account; phone stays root.
[ ] the rtc server stays untrusted by construction: it brokers
    dials, never sees key material (payloads sealed to the qr-
    exchanged keys on top of dtls).

content encryption (envelope, per record/blob):
[ ] every private record/blob gets a fresh random content key
    (xchacha20-poly1305). ciphertext is stored as an ordinary
    record, or in s3 via the media service -- node and bucket only
    ever hold ciphertext. D13/D14 machinery unchanged.
[ ] the content key is wrapped under an AUDIENCE KEY, not per-
    friend-per-post. audiences are the user's circles: friends,
    close-friends, a group, a service.

audience keys, epochs, revocation (the "flavor" on shared keys):
[ ] an audience key is (audience id, epoch n): a symmetric key per
    circle per epoch. membership = that key hpke-wrapped to the
    member's public key, stored as a GRANT record in YOUR
    collection, terms-gated so only that member can fetch it. this
    is the diffie-hellman friends-exchange-keys story, done with a
    standard (hpke) instead of hand-rolled ecdh.
[ ] grants are SIGNED by the owner's identity key; readers verify
    grant -> device cert -> identity. a malicious node can withhold
    data but can never silently add a reader (itself included).
[ ] revoking bob is LAYERED, and honest about physics:
      1. node layer, instant: terms drop bob -> he can no longer
         fetch the ciphertext records or get presigned media urls.
         enforced now, logged, works with existing machinery.
      2. crypto layer, forward: bump the epoch -> new audience key
         wrapped to everyone-but-bob. everything posted after the
         revoke is unreadable to him even if he obtains ciphertext
         (node compromise, leaked backup).
      3. optional deep-clean: lazy background re-encryption of old
         content under the new epoch, for the paranoid tier.
    what no system can do: make bob un-know a key he already has or
    unsee content he already saw. signal/whatsapp/mls all rotate
    forward rather than pretend otherwise; so do we.
[ ] epochs are independent random keys, NOT a hash chain -- a
    derivable chain would let an old epoch compute new ones and
    break revocation. granting history to a new member = wrapping
    the specific old epochs you choose; "sees history" vs "new
    content only" membership falls out for free.
[ ] scale check: thousands of friends x 32-byte wraps = kilobytes
    per epoch bump. pairwise hpke is fine for a human graph. mls
    (rfc 9420) tree groups are the graduation path if huge groups /
    heavy churn demand log(n) rotation -- adopt, don't invent.

timed access (the api's clock, not magic keys):
[ ] grant records carry expires; the node stops serving both the
    grant and the ciphertext past it (same is_permitted machinery
    -- terms gain a time dimension). presigned media urls already
    expire in 30-60s (D14). net: "alice can see this for 48 hours"
    is one field on the grant.
[ ] explicitly rejected: cryptographically self-expiring keys.
    without trusted hardware on every reader they don't exist;
    pretending is how you reinvent drm badly. node-enforced expiry
    + epoch rotation covers the real threat; the sensitive tier
    covers the rest.
[ ] live-handout mode (sensitive/ephemeral tier -- webrtc payoff
    #2): key never stored anywhere, handed out p2p per-read from
    your phone, exactly the original vision. strongest control
    (revoke = stop answering; expiry = your phone enforces it),
    costs availability (phone offline = content dark). user picks
    the tier per service or per record.

media + s3 (the double door):
[ ] private blobs are encrypted client-side BEFORE upload; media
    service and bucket never see plaintext. a reader needs BOTH
    doors: a fresh presigned url (node acl -- instant revoke,
    timed, logged) AND the key (crypto -- epoch-revocable). D14's
    check-at-issue window now leaks at most ciphertext.
[ ] hls video: encrypt segments; key delivery rides grants (or the
    live-handout channel for the sensitive tier).

backup + recovery (or this kills people's data):
[ ] wallet backup = master seed + key manifest snapshot, encrypted
    under argon2id(passphrase), pushed over the phase 10 rails to
    drive/dropbox/icloud/usb/a friend's node. owned keys re-derive
    from the seed; received grants re-fetch.
[ ] TRUST SPLITTING, hard invariant: key backups NEVER live on the
    node that holds the ciphertext. node has ciphertext, no keys.
    escrow party has passphrase-wrapped keys, no ciphertext. no
    single party can read you; collusion + your passphrase is the
    bar. "joe rogan can't leak your shit" is structural, not policy.
[ ] multi-device is also recovery: any linked device can bless a
    new phone (device cert chain), so lost phone != lost life.

what the operator still sees (say it, don't oversell it):
    ciphertext sizes, timing, service names, and the social graph
    implied by grants. e2e hides CONTENT, not existence. metadata
    minimization (padding, private grant delivery) is later work.

app + sdk surface:
[ ] mobile/encryptor grows into the real keychain app: the wallet,
    approve/deny linking + key requests, and the grants map -- who
    can see what, until when, with revoke buttons that actually run
    the layered dance above. a live map of your privacy.
[ ] sdk: encryption is a property of the record, not the app --
    wapi handles tiering on create/read so lens + killer app render
    all three tiers seamlessly.
[ ] tiers end to end: public (plaintext), friends (wrapped keys),
    sensitive (live handout).


PHASE 12 — trust & safety (the unglamorous load-bearing wall)
-------------------------------------------------------------
the moment a node hosts user media, some of this is LEGALLY REQUIRED,
not optional. and the moment nodes federate, spam arrives (wordpress's
pingback system died of exactly this). the white-label business sells
compliance as part of the product -- an operator's manager asks about
this before they ask about features.

operator moderation tools (in the admin panel, phase 4):
[ ] report flow: users flag content/accounts -> operator queue
[ ] takedown, account suspend/ban, appeal trail
[ ] dmca flow: registered-agent info, takedown + counter-notice
    process, repeat-infringer policy (required for safe harbor)
[ ] csam scanning on hosted media (photodna / safer api class
    tooling) + ncmec reporting path. non-negotiable for the hosted
    business. self-hosters get it on by default too.

federation abuse (the pingback lesson):
[ ] inbox delivery is terms-gated already -- keep default-closed
[ ] node-level blocklists: drop deliveries from known-bad nodes
[ ] rate limits on cross-node delivery + per-sender caps
[ ] new-node reputation: throttle until proven (age, volume shape)

adult content (decide BEFORE the first adult creator emails -- they
will be among the most motivated early knockers, being the most
deplatformed + payment-rail-burned segment alive):
[ ] the wordpress answer, forced by the rails anyway: stripe
    prohibits adult content and inc's rails are stripe connect, so
    INC-HOSTED nodes + inc rails are clean-only (survival, not
    morality). the software stays neutral: a SELF-HOSTED node with
    its own processor (ccbill-class) is the operator's business
    under D10 node policy. onlyfans context for the pitch, not the
    positioning: OF proved fans pay creators directly at ~$6B+/yr
    scale, takes 20% (we take ~3% + hosting), and in aug 2021 tried
    to evict its own creators -- the ultimate "you were renting"
    story. never market as "sfw onlyfans" (fanhouse/oftv died
    fighting that brand); market ownership, let the phase 4
    membership mechanics quietly BE the onlyfans economics.

consent-contract abuse (operator, 26.07 — the bait-and-switch class,
generalized from the app-store listing loophole: "new contracts asking
for everything… security by flashing red danger in the auth app,
telling the user don't accept — even blocking them until they hit
proceed anyways, protecting them"). ROADMAP ONLY — operator: "this
doesn't need to be soon, this is down the line, just talking about
it." filed so the thinking isn't lost; NOT staged on any board, no
lane item — stage it when the app ecosystem is real (M2+). lane B
(the authenticator consent surface) when it comes. RISK-TIERED,
explicitly NOT for every app or every contract (operator) — the
ordinary consent flow stays frictionless:
[ ] tier 1, unknown app: the requesting app isn't approved by THIS
    node's admin in the app-store registry, AND no federated node the
    operator trusts has approved it either ("this app isn't approved
    by the admin on the app store, or another node federated hasn't
    approved it" — approval travels the trust graph, an M3-flavored
    web-of-trust detail; single-node v0 checks only the local
    registry) → a modest caution treatment on the consent screen
    ("a little flashing") — informative, not blocking.
[ ] tier 2, scope escalation: the app HAS prior granted terms and the
    new SMR asks for meaningfully more (new services, read→write/all,
    wildcard identities) → show the DIFF prominently ("this app had
    X, now wants Y") in danger styling.
[ ] tier 3, hallmark-malicious shapes: a contract asking for
    everything (broad wildcards across many services, all-permissions,
    star-adjacent reach) → full red-flashing danger interstitial that
    BLOCKS until an explicit "proceed anyway" — the user is protected
    by default and sovereign on override. UX reference (operator): the
    macOS GATEKEEPER pattern — the sketchy-download dialog has no
    bypass button; "Open Anyway" lives in Settings → Privacy &
    Security. same here: the danger screen itself offers only
    decline; the override lives in the authenticator's own
    settings/security page ("allow this app anyway"). the RELOCATION
    is the security — a user following a phisher's script has to
    break momentum and navigate elsewhere on purpose.
    design note: the risk heuristics are simple shape checks on the
    SIR list (count of services × permission breadth × wildcard use ×
    diff vs existing terms), not an ML thing. pairs with the
    D-appstore-revamp v2 registration rewire (versioned, node-hosted
    listings — the store registry is what tier 1 checks against).

operator legal kit (part of the white-label product):
[ ] tos + privacy policy templates per node
[ ] gdpr story -- and say it loud: right-to-erasure and data export
    are NATIVE here (delete your collection, take your backup).
    compliance as a feature, not a burden.
[ ] jurisdiction notes per deployment region (eu nodes, us nodes)


CROSS-CUTTING — quality, testing, security
------------------------------------------
not a phase. starts in phase 0 and never stops. a node holds people's
lives and (eventually) a creator's money -- it has to be boring-reliable
and hard to pop.

testing:
[✓] api test suite (pytest): auth flows (signup/login/mint/certify),
    the full permissions matrix (terms, whitelist/blacklist, cross-
    origin, anon, remote provider), star protection, metering/billing
    paths. this is the highest-value test surface in the repo -- the
    permission logic IS the product.
    DONE (1.0.21): unit layer -- mongo.py transformations (q_t/u_t/
      to_gui/to_db, star_found), ACL logic (get_approved,
      is_in_cross_origins), auth functions (kosher, can_mint, certify,
      decode_token, is_permitted), pydantic models, record factories.
      mocked DB, no HTTP.
    DONE (1.0.40): endpoint-level tests through the FastAPI app
      (TestClient, 45 new tests) -- signup/login flows, CRUD routes
      end-to-end, star protection via routes (star_selected,
      star_found), metering/billing (check/charge, out-of-credits,
      out-of-space), forged-token rejection (I1), scoped-token
      enforcement (I5), cross-collection access impossible (I3),
      aggregate pipeline validation, certify endpoint, system
      endpoints. 280 total api tests green.
[ ] run the api suite against BOTH backends in CI (ferretdb + real
    mongo) -- that's the phase 1 compatibility guarantee, permanently
[ ] protocol conformance suite: a black-box test any node must pass
    (token mint, CRUD, terms enforcement, certify). matters the moment
    a second implementation or fork exists. federation depends on it.
    this suite also MECHANICALLY enforces the security invariants
    (I1-I5 below): forged tokens rejected, unsigned decode never
    authorizes, cross-collection access impossible, scoped tokens
    can't exceed scope. a node that fails these is not a web10 node.
[ ] write the protocol SPEC as a versioned doc (token format, mint
    rules, certify, terms semantics, CRUD, inbox delivery). the
    conformance suite tests the spec, not the reference impl's
    accidents. api/web10.0.md is the seed.
[ ] sdk tests (bun test) against a throwaway test node
[✓] browser e2e suite: playwright, in a container, against the real
    compose stack, covering ALL THREE uis (ui, web10-social,
    marketing-ui). the vitest/jsdom suites can't catch routing
    breaks, white-screens, vhost/CORS misconfig, or "the api changed
    and the app dies on load" -- only a real browser against the
    assembled node does. playwright over selenium: ts-native
    (matches the vite+bun stack), auto-waiting (kills the flake
    class selenium is famous for), official docker image with
    browsers baked in (no grid/driver-version drift), traces on
    failure. shape: e2e/ top-level dir + one path-filtered ci job --
    compose up the node (api + db + ui + social + marketing-ui
    behind the nginx-proxy *.localhost vhosts), wait for health,
    run playwright from the mcr.microsoft.com/playwright container,
    upload traces on red. keep it to ~6-10 critical journeys: e2e
    is the smoke layer proving the assembled system boots and the
    money paths work; the component suites stay the workhorse.
    flows:
      - ui: signup -> login -> consent/terms grant screen
      - web10-social: post -> appears in feed (the 100%-delivery
        pitch, mechanically checked); grant terms -> app reads;
        revoke -> app can't (the auth model end to end in a browser)
      - marketing-ui: landing + docs + app store routes render
        (smoke level -- catches white-screens and router breaks,
        which is most of what a static-ish site can break)
    sequencing: marketing-ui smoke + ui auth flows are landable
    today; the social post->feed flow firms up as D4's M0 slice
    lands. visual-regression screenshots are explicitly LATER --
    real value, but baselines churn while B2.5/D2.5 restyle the
    world; revisit after the makeovers settle.
[✓ 1.0.74] e2e deep sweep (added 19.07.2026 — C5's harness makes this
    possible; expect it to hurt): the 10 smoke journeys prove the
    stack boots; now actually EXERCISE the ecosystem locally
    (e2e/docker-compose.yml) end to end and find out what doesn't
    work — the working assumption is that plenty doesn't. expand
    playwright coverage across the real money paths and the seams
    between lanes: full signup -> consent -> grant on ui/; social
    post with media -> feed -> comment/react -> dm round trip;
    profile edit; terms revoke actually cutting an app off;
    marketing-ui exporter upload -> marketing-api job -> records
    in the node; app store -> launch app -> token handoff; studio
    screens load with real metering data (A5). test accounts are
    part of the harness: account CREATION is itself a test (every
    run signs up fresh users), and a seed script provides persona
    fixtures (a creator, fans, granted + revoked app terms, posted
    content) for flows that need existing state — the same seed
    powers the dev env's wipe+reseed. per-env test credentials via
    env files, never in git. every failure found
    gets triaged: small fixes land with the suite (tick the fix in
    the changelog), lane-owned breakage gets filed as a .context/
    note + lane queue item, never a silent skip. deliverable is
    the enlarged suite AND the honest bug list. visual-regression
    baselines still wait for the design level-up to settle
    (B5/D12/D13), but functional coverage doesn't wait on pretty.
    and its test script is `vitest --passWithNoTests`, so a green
    ci check there certifies nothing. test the surfaces with logic
    (exporter/import ui, funnel events), then drop the
    --passWithNoTests flag so an empty suite can never look green
    again.
[✓] report-a-bug loop -- the explicit counterweight to keeping the
    e2e suite small: ~10 smoke journeys guarantee the money paths,
    so REAL USERS are the coverage for everything else, and that
    only works if telling us something broke costs them almost
    nothing. two pieces:
      - a "report a bug" affordance in all three uis (ui, social,
        marketing-ui): one button, a text box, POST to a marketing-
        api /feedback endpoint (it already ingests analytics --
        same shape). the report auto-attaches context a human
        would forget: app + route, version/commit, user agent,
        recent console errors. optional contact field, never
        required.
      - react error boundaries on each app so a crash never dies
        as a silent white-screen: the boundary catches it, shows
        "something broke -- send report?", and the report carries
        the stack trace. white-screens are the exact bug class the
        thin e2e layer can miss, so every one that reaches a user
        should convert into a report, not a bounce.
    acceptance: a fan or creator who hits a bug can tell us in <=2
    clicks without leaving the app, and the report alone is enough
    to reproduce. storage starts dumb (marketing-api's store,
    surfaced however is cheapest); dogfooding it as web10 records
    on the operator's node is a nice later, not the blocker.
[✓] ux telemetry -- seeing users silently fail. the third leg of
     the stool: e2e catches what we predicted, bug reports catch
     what users SAY, this catches what they don't (rage clicks,
     abandoned funnels, dead ends nobody bothers to report). the
     privacy line splits it by surface:
       - marketing-ui (web10 inc's own site, pre-signup visitors):
         FULLY DONE (1.0.281): Hotjar session replay + heatmaps live
         (D38: Hotjar SaaS override, operator 30.07). marketing-api
         already ingests pageview + funnel events; Hotjar adds the
         session-replay/heatmap leg. self-hosted PostHog deferred
         but not blocked (decisions.md D38).
         OPERATOR ESCALATION (rant, 30.07.2026): "if we havent set
         up hotjar on the marketing page, we NEED to!" — SHIPPED.
         filed as D-marketing-session-analytics.
      - ui + web10-social (the platform, where users' lives
        live): third-party session-recording js (hotjar /
        fullstory / logrocket-class) is RADIOACTIVE. "nobody is
        mining you" (manifesto) cannot coexist with recording
        fans' DOM and keystrokes, and phase 11 e2e encryption
        makes content replay contradictory anyway. allowed
        exhaust: anonymous, aggregate, CONTENT-FREE ux events
        (feature used, funnel step reached, client error fired)
        into the same metering store the phase-4 studio
        analytics use -- same "aggregate exhaust only" line,
        keep it bright. plus a js error beacon (window.onerror /
        unhandledrejection -> endpoint) on all three uis: that
        is "is the ui doing it" with zero content cost, and it
        catches the errors users never report.
- replay-grade insight on the platform, when genuinely
         needed: OPT-IN recorded sessions from dogfooding and
         design-partner creators ("help us improve -- record this
         session"), self-hosted, staging-first. watch real usage
         without surveilling fans; the consent is the feature,
         and it's a pitch line ("they record everyone; we asked").
- Google Analytics 4 (operator, 30.07): aggregate pageview +
        event analytics across ALL surfaces (marketing-ui, web10-social,
        ui/auth). GA4 is NOT session recording — no DOM capture, no
        keystroke logging, no replay. It is anonymous, aggregate,
        CONTENT-FREE: fits squarely within the standing "aggregate
        exhaust only" allowance. One measurement ID shared across all
        three apps (or per-app if the operator wants property separation).
        File: D-ga4-marketing (ws-D/marketing(2)), [✓ 1.0.289] D-ga4-social (ws-D/
        social-analytics, merged), D-ga4-auth
       (ws-B). Each is SMALL, no gate, parallel-safe (different
       directories). Acceptance: GA4 dashboard shows real pageviews
       from the corresponding surface; no content leaves the app.
[ ] ci from phase 0: github actions on every pr -- lint, typecheck,
    tests, docker builds. nothing merges red. (specced in full in
    CROSS-CUTTING — ci/cd below; landable today, don't wait.)

security invariants (must hold END TO END, every phase, forever):
these are the load-bearing truths. every phase, every pr, every new
endpoint is checked against them. the conformance suite (above) is
where they get MECHANICALLY enforced so they can't silently rot.
  I1. a provider can cryptographically verify who issued ANY token,
      including other providers' tokens, WITHOUT trusting the token's
      own claims. (this is the one that's broken today -- see below.)
  I2. authorization decisions are made only on VERIFIED token data,
      never on an unsigned decode.
  I3. a request can only ever touch the addressed user's collection.
      cross-collection access is impossible by construction.
  I4. private content is unreadable by the node operator (phase 11).
I5. every actor (app, agent, llm) acts under a scoped, expiring,
       revocable token -- least privilege, always.
   I6. every record carries server-injected, immutable metadata:
       `_author` (token's username), `_source_node` (the provider
       that minted the token — NOT the destination node), and
       `_created_at` (server time at the node that stored the record).
       the client cannot set or forge these fields. a cross-node
       request (foreign provider token certified via remote /certify)
       still gets stamped with the originating provider in
       `_source_node` and the local node's time in `_created_at`.
       this stops sender impersonation on DMs, posts, comments,
       reactions — any record where the client currently claims
       to be someone else.

*** CONFIRMED BUG -- federation provider validation (invariant I1) ***
today providers do NOT validate each other. the chain:
  - tokens are signed HS256 (symmetric secret). a provider can only
    verify tokens IT signed -- it cannot verify another provider's
    token cryptographically.
  - so certify_with_remote_provider() phones the other provider's
    /certify endpoint and trusts a bare HTTP 200.
  - it picks that endpoint's URL from decoded.provider -- a field in
    the token that was decoded WITHOUT signature verification.
  - net: a token's own unverified claim chooses who vouches for it,
    and a bare 200 from that (attacker-choosable) URL is trusted.
    nothing proves the token was really minted by the named provider.
    this is both a spoofing hole and an SSRF vector.
[ ] FIX: asymmetric signing (RS256 / EdDSA). each provider signs with
    its private key, publishes its public key at a well-known JWKS URL
    on its domain. any provider verifies any token OFFLINE by fetching
    the claimed provider's public key -- a valid signature proves the
    token came from whoever controls that domain's key. kills the
    callback, the SSRF, and the spoof at once. (this is how OIDC
    federation works -- don't invent it.)
[ ] migration: dual-verify during rollout (accept HS256 legacy +
    RS256), rotate keys, then drop HS256. PRIVATE_KEY becomes a
    per-node keypair generated at setup (phase 3).
[ ] then decode_token's unsigned path (I2) can go away -- verify
    signature everywhere, using the right key per provider.

other known issues, fix early (phase 0/1, before any real users):
      [✓] remote certify SSRF: even with I1's fix, validate provider
        urls (https, public host, sane shape) before any fetch
      - terms matching: exact-match by default, regex opt-in (redos +
        accidental-wildcard footgun)
      [✓] CORS: allow_origins=["*"] + allow_credentials=True has to go
      [✓] bare except in certify() swallows everything -- tighten error
        handling, no silent auth failures
      - rate limiting on auth endpoints (login, signup, recovery,
        verify codes) -- recovery-by-sms especially
      - print() -> structured logging. never log tokens/secrets.
[ ] dependabot debt: 913 alerts on the default branch (48 critical).
    phase 0's upgrades clear most of it; keep dependabot/renovate on
    and green after.
[ ] secrets hygiene: signing key generated at setup (phase 3), stored
    in the data volume, never in git. same for stripe/twilio keys.
[ ] token revocation story: jwts expire but can't be revoked today.
    decide: short expiry + refresh, or a jti denylist. matters more
    once agents/llms hold tokens (mcp, future agents).
[ ] backups that restore: automated node backup + a scheduled restore
    drill. a backup you've never restored is a rumor.
[ ] before the first creator-node pitch: external security review /
    pen test of the node stack + a responsible disclosure policy.
    "we've been audited" is a sales document in this business.
[ ] dependabot hackathon (operator, 30.07: "i am seeing alot of
    stuff in here, we should do a hackathon and kill a bunch of
    these, can be done later though"): the dependabot backlog
    (161 alerts — 13 critical, 67 high at filing) gets burned
    down in one dedicated batch session, criticals first, not a
    steady drip. parked behind the complaint-driven product work
    until the operator calls it. lane item E-dependabot-hackathon.

quality:
[ ] frontend cache headers: index.html must ship no-cache on all
    three static frontends (ui, social, marketing-ui) -- hashed
    assets can stay immutable. today static-web-server defaults
    html to max-age=86400, so every deploy leaves browsers on dead
    bundles for up to a day (operator hit this 19.07: the shipped
    B6 login fix looked still-broken because the browser held the
    old bundle). fix in each Dockerfile's server config; verify
    with curl -I after deploy.
[ ] republish web10-npm to npm with the wapiAuth defaultAPIProtocol
    fix (>=1.0.9), then delete the sed patch in ui/Dockerfile --
    every consumer of the published sdk still gets the broken login
    until this ships.
[ ] ruff + mypy on the api (type the permission logic first),
    typescript strict on ui/sdk, pre-commit hooks
[ ] health/readiness endpoints + basic metrics on every service
    (fleet ops in the white-label future depends on these)
[ ] error responses: consistent shapes, no stack traces to clients


CROSS-CUTTING — ci/cd (github actions: the referee)
---------------------------------------------------
"nothing merges red" has been a rule without a referee. this is the
referee -- and to start it SHOWS the score rather than stopping the
game: checks report pass/fail on every pr, merging stays a human
call. it is landable TODAY against the tests that already exist
(205 api unit tests, vitest suites in ui/sdk/web10-social/exporters)
-- do NOT wait for the endpoint permission suite to start reporting.
ci first makes every later branch safer; the suites plug into an
existing pipeline the day they land. this is the top of wave 0.

ci — runs on every pr + push to dev/main (.github/workflows/):
[✓] monorepo path filtering: each job triggers only when its dirs
    changed (api job on api/**, ui job on ui/**, ...). a docs-only
    pr costs seconds, not a full matrix. one workflow file per lane
    so workflow ownership follows directory ownership -- a lane
    edits its own yaml, never another lane's.
[✓] api job: uv sync --frozen, ruff check + ruff format --check,
    pytest. uv cache keyed on uv.lock. (mypy joins the gate when
    the quality item types the permission logic.)
[✓] js jobs (ui, sdk, web10-social, exporters, rtc): bun install
    --frozen-lockfile, tsc --noEmit, vitest run, vite build. write
    ONE reusable workflow and call it per package -- five diverging
    copies of the same yaml is how ci rots.
[✓] close the js job escape hatches: typecheck and vite build run
    with continue-on-error in the shared js workflow today -- a ui
    that doesn't compile still shows a green check, which breaks
    the visibility-first premise (the referee must at least SHOW
    red). make both steps report honestly; merging stays a human
    call as before. the browser e2e suite (quality/testing section)
    joins as its own path-filtered job the day it lands.
[✓] the api job has never actually gone green (found 19.07.2026 via
    #117): uv sync --frozen installs neither ruff (not a dependency
    anywhere in pyproject) nor pytest (lives in the test extra), so
    "uv run ruff check" dies on spawn before anything lints or
    tests. fix in ONE lane-A branch so the check lands green, not
    red: add ruff to a dev dependency group + sync with --extra
    test in api.yml, then pay the debt the spawn error was hiding
    -- 104 ruff errors and 26 files ruff-format would rewrite
    across api/ (measured with ruff 0.15.22).
[✓] docker job: buildx build for every Dockerfile whose context
    changed, with gha layer caching. images that only build on
    laptops are how "works on my machine" comes back.
[ ] the endpoint permission-matrix suite + conformance suite join
    the pipeline the day they exist (wave 0 finishes here -- forged
    tokens, star protection, I1-I5 checked on every pr). phase 1
    then adds the db matrix: same suite against BOTH a mongo
    service container and ferretdb.
[✓] visibility first, NOT gatekeeping: all checks report pass/fail
    on the pr (green tick / red x) but nothing blocks merging yet.
    seeing what broke, per lane, per pr, is the immediate value.
    LATER, by explicit choice: flip on branch protection (required
    status checks, no direct pushes) once the suites have earned
    trust -- it's a github setting, not a code change, and that
    flip is the moment "nothing merges red" becomes literal. the
    security-invariant checks (I1-I5) should be first to graduate
    to required.
[✓] changelog check: a tiny job flags any pr that touches code but
    not CHANGELOG.md (an explicit skip label for the rare genuine
    exception). the board stays true MECHANICALLY -- agents forget,
    ci doesn't. same job can warn on plan.txt/lane-tick drift later.
[✓] hygiene: concurrency groups with cancel-in-progress per branch
    (parallel agents push often -- don't queue dead runs), timeout
    on every job, actions pinned to major versions. tests need NO
    repo secrets (stripe/twilio are mocked -- keep it that way so
    prs from forks stay safe to run).

ci optimization (reduce github actions minutes -- free tier is
2,040 min/month, current usage ~$3/day = ~$90/month overage):
[✓ 1.0.99] add bun caching to js-ci.yml: bun install is uncached so every
    job downloads the full dependency tree. use actions/cache keyed
    on bun.lock + the bun cache directory. cuts each js job ~50%.
[✓ 1.0.99] remove push triggers from all CI check workflows: js-ci, api,
    docker, e2e, marketing-api all fire on every push to dev — with
    parallel agents pushing frequently, that's ~60+ minutes wasted
    per merge (code already passed PR checks). deploy.yml keeps push
    (it actually deploys). saves ~80% of total CI minutes.
[✓ 1.0.99] per-package path filtering in js-ci.yml: touching ui/** currently
    runs all 6 packages (ui, sdk, web10-social, marketing-ui,
    encryptor, rtc). each job should only trigger when its own
    package's paths change. use the reusable workflow's `if`
    condition with `github.event.pull_request.changed_files` or
    separate path filters per job.
[✓ 1.0.99] drop linux/arm64 from cd.yml image builds: multi-arch doubles
    build time per image. if no self-hosters need ARM (Raspberry Pi,
    Mac Mini), build linux/amd64 only. saves ~50% on CD minutes.
[✓ 1.0.99] use ubuntu-latest-small runners where possible: smaller, cheaper
    runners for jobs that don't need heavy resources (changelog
    check). docker/e2e builds stay on ubuntu-latest.

cd — runs on merge to main + version tags (starts small, grows
with the phases that give it something to ship):
[✓] ghcr.io image publishing: on merge to main, build + push every
    service image (api, ui, rtc, media, social) tagged {sha, latest}.
    phase 3's one-container web10/node image publishes from the same
    workflow on version tags -- `docker run web10/node` needs a
    registry to pull from, and this is it. multi-arch (amd64+arm64)
    because self-hosters live on raspberry pis and mac minis.
[✓] npm publishing on version tags: the sdk (phase 6) and
    create-web10 (phase 6.5), built with bun IN ci, published with
    provenance. releases come from the pipeline, never a laptop.
[✓] github release notes generated from the CHANGELOG.md entries
    since the last tag (the changelog is the board -- it's also
    the release notes, free).
[ ] hosted-node deploy (web10.app) stays manual / compose-pull for
    now. automate it when M2's hosted offering exists -- a deploy
    job with no real target is fiction (same rule as guides).

sequencing: one branch, ws1-sized. skeleton (path filters + api +
js + docker jobs) first and merged fast; the changelog check and
cd jobs are safe follow-ups; branch protection is a deliberate
later flip, not part of the build. every lane benefits from the
day the skeleton lands.


CROSS-CUTTING — docs
--------------------
sounds like a lot; it's one writing task (the D1 spec), one coding
habit (annotate as you go), and one website that waits. split by
AUDIENCE, not by tool. generated wherever possible -- generated
docs can't rot.

site architecture (D20 -- settles the two-sites question): there is
ONE marketing site (marketing-ui, web10.app) and it is CREATOR-FIRST:
hero = THE STORY (the subs-vs-views gap, own your audience), primary
cta = "become a founding creator" (demo video + call). fans are NOT
a tab there -- fans never google web10; they click a link in their
creator's bio and land on the CREATOR'S NODE, where the manifesto
join page (manifesto.md) is the entire user-facing pitch. this is
the wordpress/shopify pattern: wordpress.org sells to site owners,
readers never visit it; shopify sells to merchants, shoppers never
see shopify.com. the only fan-facing corporate page is a thin
"powered by web10 -- what is this?" reassurance explainer linked
from node footers (trust, not acquisition). devs get the docs
section (below); self-hosters get the setup story. no users tab.

for everyone -- explanation (lives in marketing-ui, phase 7):
the missing quadrant: guides say HOW, reference says WHAT, nothing
says WHY. the architecture (records, tokens, terms, federation)
already exists, so unlike guides these are writable NOW -- they
don't wait on milestones. they are the marketing site's core copy.
[ ] "what is web10" concept pages: the mental model -- node, record
    ({service, body}), one collection per user, tokens + terms,
    lens, provider federation. source from GLOSSARY.md/CLAUDE.md/
    pitch.txt, rewritten public-facing.
[ ] per-audience "why" pages: as a user (own your data, portable
    across apps, private/temporary unlike blockchain), as a
    creator/influencer (run your node, own the audience
    relationship, monetize -- the wordpress analogy), as a dev
    (stateless frontends, no backend to run), as a node operator.
    productize pitch.txt's vs-crypto / vs-cloud arguments here.
[ ] how-it-works diagrams as code, versioned in-repo (generated
    docs can't rot applies to diagrams too): token mint/certify
    flow, request lifecycle, data model, federation between
    providers, the e2e encryption story. mermaid over plantuml --
    starlight/docusaurus render it natively at build time, no java
    toolchain.

for users + creators (lives in marketing-ui, phase 7):
[ ] guides ARE the marketing content for a saas: get an account,
    own your audience, import your instagram, run a node, monetize
    as a creator. write each guide when its milestone (M0-M3)
    ships, not before -- docs for unshipped features are fiction.
    (the fiction rule gates GUIDES only -- explanation pages above
    describe what already exists and don't wait.)

for devs:
[✓] protocol spec + conventions doc (D1): versioned markdown + JSON
    Schema, in-repo, framework-less. two layers: the conformance
    suite tests the protocol spec; the schemas are an application
    profile (not protocol) that validates the exporters (P9) and the
    killer app (P8) in their own test suites. the source of truth.
    lives in docs/protocol-spec.md + docs/conventions.md + docs/schemas/*.json
[ ] api reference: already generated -- fastapi emits openapi at
    /docs on every node, grows with the api for free. the habit
    that keeps it good: route tags (auth/crud/terms/billing),
    summaries, response_model on every route, examples on the
    pydantic models. (optional cosmetic: swagger ui -> scalar.)
[ ] sdk reference: typedoc from the types (phase 6 item)
[ ] dev docs pages in marketing-ui render the in-repo spec verbatim
    + embed the generated references. js-native framework (starlight
    or docusaurus) since marketing-ui is js. build this when there's
    a killer app to document, not before.
[ ] no hosted saas docs (mintlify/readme): self-hostable project,
    self-hosted docs.


CROSS-CUTTING — deployment (proxmox homelab)
----------------------------------------------
proxmox host at 192.168.8.20:8006, accessible only over VPN.
target: host the marketing sites and a web10 node here for
dev soak, demos, and public-facing use.

what goes in:
[✓ 1.0.60] web10 node (api + ui + db + rtc + minio): Portainer + Nginx
    Proxy Manager approach (prep-vm.sh, self-contained staging compose,
    Cloudflare DNS plan). ubuntu-deploy.sh replaced — stacks managed
    through Portainer UI, reverse proxy + TLS through NPM.
    README.md documents the operational procedures.
[ ] marketing-ui: separate container or LXC. serves the public
    marketing site (landing, docs, app store, exporter UI).
    own vhost, never inside the node compose.
[ ] marketing-api: separate container. handles the ZIP import
    pipeline and analytics for marketing-ui.

environments (added 19.07.2026): TWO full stacks on the linux box
that ubuntu-deployment/ preps, both managed as Portainer stacks on
the same host, one compose file parameterized by env (domain prefix,
volumes, env file) — never two divergent composes. (a public staging
env was considered and CUT the same day — overkill on one lean box;
the short-lived legacy *.staging deploy gets decommissioned during
the box migration, AGENT-OPS.md §4):
[✓ 1.0.68] PROD env — the whole ecosystem public: marketing-ui +
    marketing-api + the node (api, ui, social, rtc, minio, db).
    public Cloudflare DNS -> router-forwarded 80/443 -> NPM with
    real TLS certs (DNS-01 via cloudflare). this is E3 executed:
    prod marketing ui, prod ui, prod social ui.
    (repo side merged 1.0.66: docker-compose.ecosystem.yml +
    env.prod.example + social prod Dockerfile. remaining: box
    execution per README.md — lane E queue has detail.)
[✓ 1.0.68] DEV env — the SAME whole ecosystem, VPN-only: cloudflare DNS
    records (dev.web10.app / *.dev.web10.app or similar) pointing
    at the box's INTERNAL LAN ip. the records resolve publicly but
    route nowhere unless you're on the VPN — zero exposure, no
    port-forwards for dev vhosts, and still real domain names so
    TLS (DNS-01 challenge needs no inbound traffic), CORS, and
    vhost routing behave exactly like prod. dev is where merged
    work soaks before prod promotion; wipe+reseed procedures per
    README.md. follows the E1 security model: infra
    panels stay LAN/VPN-only regardless of env.
    (repo side merged 1.0.66: same compose + env.dev.example +
    DNS-01 cert procedure. remaining: box execution.)
[✓ 1.0.66] promotion flow documented in README.md: merge
    to dev branch -> dev env redeploy (Portainer re-pull),
    tag/release -> prod redeploy. keep it manual-button simple
    until E4.
[ ] push-to-deploy automation (E6, gated on the stacks existing):
    dev push -> web10-dev redeploy, main release -> web10-prod.
    repo is public => no self-hosted runner; use Portainer GitOps
    polling first, Cloudflare-Tunnel'd stack webhooks for instant
    deploys later. lane E queue has the full design.

networking:
[ ] VPN gateway must be up for any management access. document
    the VPN client config so every dev can reach the host.
[ ] reverse proxy (caddy or nginx) on the proxmox host or a
    dedicated LXC: terminates TLS, routes to backend containers.
    auto-TLS via caddy for any public-facing domain later.
[ ] internal DNS: *.web10.internal or similar for the node box,
    so services can reach each other by name.

provisioning:
[ ] terraform or proxmox-ssh-api script to stand up VMs/LXCs
    from a template, not manual GUI clicks. idempotent,
    repeatable. starts with a minimal Ubuntu/Debian container
    with docker + docker compose pre-installed.
[ ] ansible (or simple shell + ssh) to configure the provisioned
    machines: docker compose pull, env vars, reverse proxy config.
    keep it simple — this is a homelab, not AWS.
[ ] secrets: vault or at minimum encrypted env files pushed via
    ansible vault. no secrets in git, ever.

backup:
[ ] PRODUCTION MONGO DUMPS (do this one first -- the data is 11MB,
    there is no excuse): nightly cron `mongodump --db deploy | gzip`
    on the box, rotated 30 days, written to a spare drive that is
    NOT the data disk (operator has spare drives in the machine --
    mount one for this). the one-off dump from 19.07 lives on the
    same disk as the data and protects against nothing physical.
    monthly restore drill: mongorestore the latest dump into dev's
    ferretdb -- this same restore is the A7/B6 "dev login against a
    COPY" gate, so the first drill is already owed.
[ ] proxmox scheduled VM/LXC backups (daily incremental, weekly
    full). off-box if possible (another machine on the LAN).
[ ] docker volume backups: periodic tar of data volumes pushed
    to off-box storage.

phasing:
- step 1: get the node running behind VPN, accessible by team.
  prove docker compose works on the target hardware.
- step 2: add marketing-ui + marketing-api, wire up the reverse
  proxy. internal staging is complete.
- step 3: public domain + auto-TLS (when ready for external traffic).
- step 4: automated provisioning (terraform/ansible). don't rush
  this — manual is fine until step 2 works.
----------------------
- perf hardening on the api hot path: cache token certs, cache collstats,
  add per-collection index on "service"
- provider url validation on remote certify (ssrf) + anchor exact-match
  terms by default, regex opt-in
- clickhouse sidecar: graduate phase 4's metering events into clickhouse
  when volume justifies it. one OLAP store, two products: node ops
  console + sponsor analytics.
  SCOPE EXTENSION (operator, 24.07): the same sidecar is the natural
  BACKBONE FOR THE PUBLIC LAYER at scale — a read-optimized, append-only
  replica of PUBLIC data only (discovery index projections + public
  ledger events), serving /discover, tag search, and D-trending-knobs'
  v1 true-search-on-miss. the layering already points here: user
  collections are the source of truth, web10.discovery_posts is the
  derived projection (the "duplicate for reads" pattern in miniature),
  and CH is that projection's OLAP graduation — rebuildable from the
  layer below, ingesting append-only engagement events (aggregation at
  query time is CH's home turf, no updates needed). the privacy story
  survives by construction: only public data ever enters it (I4
  untouched). honest gate: mongo/ferretdb + indexes + the detent cache
  are nowhere near their ceiling at single-node M2 scale, and every
  stateful service added to the node compose taxes the one-container
  self-host story and the $40-vps hosted floor (D29) — so CH earns in
  at inc-side fleet scale (cross-node trending, sponsor analytics) or
  a genuinely large single node, measured not vibes. the knobs v1
  contract is deliberately engine-agnostic (detent cache + deep prefix
  + tag-filter-after) so swapping the true-search engine from a mongo
  pipeline to CH later changes zero interfaces.
  FAR-OUT TIER on the same backbone (operator, 24.07 — his own label:
  "this is farrrr out"; parked, not tasked): PUBLIC-CORPUS SEMANTIC
  SEARCH — CH's 2026 text-search machinery (operator cites a strong
  march release; VERIFY capabilities at build time, don't spec from
  memory) + sparse embeddings (SPLADE-style / sparse-autoencoder-
  derived, e.g. off a qwen-class model; sparse vectors are inverted-
  index-shaped, which is exactly what CH-style indexes eat) over
  public posts AND photos → "find me that guitar post" actually finds
  it. the sharp edge (operator, 24.07): a tag/search query matches
  what is IN the image, not just what it was labeled — an untagged
  photo of someone playing guitar surfaces under #guitar because the
  embedding bridges media content to the query. this matters because
  sparse-or-no tags is the NORM, and it compounds with the exporter
  story: a creator's 10-year imported back catalog arrives basically
  untagged, and semantic media search makes that whole archive
  findable on day one — the "import your life" pitch gains "and
  actually FIND your life." the principle that makes this ON-thesis rather than algorithm
  creep, worth keeping verbatim (operator: "ai relevancy to your text
  search, rather than ai fucking with your brain and the algo — that
  is the good use of ai, not the toxic use"): AI RELEVANCE BELONGS IN
  THE USER'S SEARCH, NEVER IN THE FEED — ai answering a query the
  user typed is the user holding the tool (pull); ai deciding what
  the feed shows is the platform holding the user (push, the thing
  the manifesto swears off). same family as the later.md lens/
  chatbox: user-held ai, byok-friendly, and it only ever touches the
  public corpus (I4). promotion bar: later.md's rule — M0 video
  shipped AND a creator conversation asked for it.
- schema contracts: opt-in, per-service schema enforcement. a user (or
  their service manager app) attaches a JSON Schema to a service's
  terms record (a "schema" field next to whitelist/cross_origins); the
  node validates create/update bodies against it and rejects
  violations. the node stays generic -- it runs whatever schema it's
  handed, exactly like it runs whatever ACL it's handed -- but a
  collection gains protection from buggy/hostile apps and agents that
  hold a create-scoped token. NOT protocol-mandatory: no contract, no
  validation, wild west by default. design questions to settle before
  building: partial updates ($set/$inc/$unset can't be fully validated
  without read-modify-validate), pre-existing records are grandfathered
  (validate writes going forward only), schema evolution (additive-only,
  same rule as the conventions doc). the social schemas in docs/schemas/
  become drop-in contracts for free. candidate to promote into phase 6
  alongside the update-widening work (same write path, same philosophy:
  flexibility with a violation check).

PHASE 13 — night_owl (the AI team that moonlights so you don't have to)
----------------------------------------------------------------------
the 10x operator leverage: you stop orchestrating. you're in the back,
giving two points on SMS. the fleet runs itself.

the supervisor loop runs every 40 minutes on your Mac (Hammerspoon +
AI vision). it screenshots Conductor, an AI agent sees the UI and
drives it — creates workspaces, sends prompts, reads results, kicks
off K3 bites. on crucial moments (web10web10, gather up, brick), it
texts you like your top engineer.

architecture: night_owl/ lives in the repo. Hammerspoon init.lua is the
entry point. conductor_agent.lua handles screenshot → AI vision →
click/keystroke pipeline. texter.lua fires SMS on crucial events.
config.json holds API keys, phone numbers, schedule.

the AI is the eyes — it adapts to UI changes. no hardcoded coordinates.
each click is ~5-10s (screenshot → AI → response → click). a 5-step
workflow takes a minute. fine for a 40min cycle.

the long game: from manual conductor.build → Fable supervisor →
night_owl autonomous fleet. you wake up to PRs merged, prod shipped,
and a text: "12 PRs landed overnight. all green. ready for next round."

[ ] night_owl scaffold: Hammerspoon config, AI vision pipeline, texter
      (scaffolded in night_owl/ — init.lua, conductor_agent.lua,
      texter.lua, config.json, README.md). acceptance: Cmd+Opt+Ctrl+C
      triggers a manual cycle that screenshots Conductor and returns
      valid actions from the AI.
[ ] night_owl conductor pilot: the AI can navigate Conductor — find
      the new workspace button, select a model, type a prompt, hit
      enter, read the result. acceptance: one full cycle: screenshot →
      AI creates a test workspace → AI types a prompt → AI reads the
      response → text confirms.
[ ] night_owl supervisor loop: the full "should we do it?" pipeline —
      Fable runs the audit, K3 workspaces are created for each bite,
      progress is tracked across cycles. acceptance: one end-to-end
      cycle completes without human intervention.
[ ] night_owl texting: crucial events fire SMS. acceptance: a simulated
      web10web10 event texts the operator in the Steve Jobs top engineer
      style.
[ ] night_owl 24/7: scheduler enabled, fleet runs overnight. acceptance:
      wake up to merged PRs and a summary text.
