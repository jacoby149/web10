apiVersion: apps/v1
kind: Deployment
metadata:
  name: api-dev
spec:
  replicas: 1
  selector:
    matchLabels:
      app: api-dev
  template:
    metadata:
      labels:
        app: api-dev
    spec:
      containers:
        - name: api-dev
          image: gcr.io/pure-phalanx-344719/api-dev
          ports:
            - containerPort: 80
          resources:
