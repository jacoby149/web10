parallel execution
==================

higher-level companion to plan.txt. that file says WHAT and WHY; this
file says what can happen AT THE SAME TIME. target: conductor.build
running 4 workspaces/branches in parallel without merge hell.

the two rules that make 4-wide parallelism safe:
1. LANE OWNERSHIP: each lane owns its directories. api/app/** belongs
   to lane A. auth2(ui)/** belongs to lane B. new top-level dirs
   (media/, sdk rewrite, mobile/, docs) can't conflict with anything.
   docker-compose.yml + settings are lane A territory -- other lanes
   that need a compose change queue it as a note for A, don't edit.
2. MERGE SMALL, MERGE OFTEN: branches live days, not weeks. the
   permission test suite (wave 0) is the seatbelt that makes frequent
   merging safe.
3. EVERY BRANCH UPDATES THE CHANGELOG: add a CHANGELOG.md line for what
   you changed (newest at top), tick the plan.txt item you completed,
   tick your lane item in THIS file's queues below, and keep
   CLAUDE.md/GLOSSARY.md/decisions.md true if you moved them.
   CHANGELOG.md is lane-neutral (append at top) so it rarely conflicts;
   if it does, it's a trivial resolve.
4. NAMING (decisions.md D33): decisions.md owns the bare "D-nn"
   numbers (D1-D32, referenced throughout plan.txt). lane/board items
   use slugs ("D-url-routing", "D-comments-ledger"). the legacy
   numbered lane items (D21-D25, ticked) stay as history — never mint
   a NEW bare D-nn lane item; the collision already skipped one number
   and made "D21" mean three different things.

STATUS KEY (check before starting anything — don't redo merged work):
  [✓ x.y.z] = merged, see that CHANGELOG.md entry.  [~] = in flight in
  another workspace.  [ ] = open. this file is ticked on merge, so also
  scan the top of CHANGELOG.md for anything newer than the ticks here.


WAVE 0 — the seatbelt (do this alone, merge fast, ~single branch)
-----------------------------------------------------------------
STATUS: PARTIAL — the unit layer landed (1.0.21, expanded to 205
tests in 1.0.31) and the github actions ci skeleton landed (1.0.32:
api/js/docker/changelog/cd workflows, visibility-first). 1.0.36
repaired the pipeline so EVERY existing suite is actually wired and
reporting red on failure (api 205, ui 43, sdk 48, social 172,
encryptor 55, marketing-api smoke). 1.0.40 landed the endpoint-level
permission-matrix suite (45 new tests through FastAPI routes: auth
flows, CRUD, aggregate, star protection, forged-token rejection,
scoped-token enforcement, metering/billing, certify, system — 280
total api tests green). 1.0.49 landed the small security fixes (CORS
tightened, bare excepts removed, provider URL SSRF guard). still OPEN:
pytest ports of the old 57-test exporter mapper suite (marketing-api).
The js workflow's typecheck/build escape hatches are closed (1.0.103).

before running 4-wide, land the thing that protects all 4 lanes:

  [seatbelt] github actions ci + the pytest permission-matrix suite
  written against the CURRENT api (cross-cutting section of plan).
  the ci/cd pipeline is now specced in full in plan.txt
  ("CROSS-CUTTING — ci/cd"): the skeleton (path-filtered api/js/
  docker jobs) is landable TODAY against the 1.0.21 unit tests --
  it does not wait for the endpoint suite. checks REPORT pass/fail
  only; merging stays a human call (branch protection is a
  deliberate later flip once the suites earn trust). workflow
  ownership follows lane ownership: one workflow file per lane
  under .github/workflows/, a lane edits only its own yaml (the
  shared reusable js workflow is wave-0/ws1 territory).
  every later branch -- especially the P0 dependency upgrades -- gets
  judged by these tests. writing tests before upgrading deps is what
  turns "did pydantic v2 break auth?" from a mystery into a red X.

  also fine to slip in here: the tiny security fixes (CORS, bare
  except, provider url validation) -- small diffs, huge value, and
  they want to exist BEFORE the refactors bury the context.

  the permission suite must include the security-invariant tests
  (plan.txt I1-I5): forged-token rejection, no-authz-on-unsigned-
  decode, cross-collection-access-impossible. these are the seatbelt's
  seatbelt -- they catch the confirmed federation bug (HS256 -> RS256)
  and stop any lane from regressing it. the RS256 federation fix
  itself is a LANE A item (A1/early A), gated by these tests.


THE 4 LANES (each is an ordered queue; top of each = in flight now)
-------------------------------------------------------------------

LANE A — api / backend        (owns: api/**, docker-compose, settings)
  [✓ 1.0.11] A1. P0 python: uv + fastapi/pydantic/pyjwt upgrades
      (landed BEFORE the wave-0 endpoint suite — that referee is
      still owed; see wave 0 status above)
  [✓ 1.0.39] A2. P1 ferretdb/documentdb switch + compose profiles + rename mongo.py -> documentdb.py
  [✓ 1.0.40] A3. P3 setup wizard api side (setup mode, /setup, key generation,
      node policy config) + one-container packaging (multi-stage
      dockerfile -- needs B2's build output, coordinate the handoff)
  [✓ 1.0.38] A4. P6 aggregate endpoint (the 5th verb, sandbox validator;
      per-stage metering through charge(); wapi.aggregate in legacy sdk)
[✓ 1.0.58] A5. P4 metering events (per-request event records for analytics)
  [✓ 1.0.56] A6. D21 strip user billing → quotas: remove user-facing
      plans/subscriptions/per-account stripe; repurpose credits/
      space + charge() as OPERATOR-SET per-user quotas (credits =
      rate/abuse throttle, space = storage caps incl. imports).
      stripe stays for the creator economy only (memberships,
      rails). keep the permission-matrix tests green through it.
  [✓ 1.0.72] A7. connect the node to the EXISTING production MongoDB on the
      ubuntu box (~208 real users + historical data + registered
      apps from the original web10) instead of the empty ferretdb
      the stacks ship with. IMPORTANT (operator, 19.07): this mongo
      runs NATIVELY on the host — it is NOT a docker container and
      that's fine, leave it as-is. so the node's containers reach it
      NOT by a compose service name but via the host: DB_URL points
      at the host gateway (add `host.docker.internal:host-gateway`
      to the api service's extra_hosts, or use the box LAN ip
      192.168.8.25) : mongo's port (27017), assuming it binds a
      reachable interface — confirm the bind + any auth creds, keep
      them in the box .env, coordinate the compose var/extra_hosts
      with lane E. we already speak the mongo wire protocol
      (documentdb.py), so this is mainly config + verification, not
      a rewrite: confirm the {service,body} + star-record shape
      matches what our code expects (older records may predate
      to_gui/to_db conventions — write a read-only audit first,
      then a migration/adapter if the shape drifted), and make sure
      star protection + scoped queries behave against real data.
      gate: do NOT point PROD at the real mongo until a login works
      against a COPY in dev (dump/restore into web10-dev's db, or a
      read-only connection). deliverable: dev logs in as a real
      historical user; total-users + total-apps counts computed from
      the real data and surfaced like the original web10 (feeds the
      D16 app-store stats); and the honest report of any shape drift.
      unblocks B6 (real login to test against) and D16 (real apps).
    [✓ 1.0.92] A8. P1 server-side record metadata — invariant I6: every record
       gets `_author` (token's username), `_source_node` (the provider
       that minted the token), and `_created_at` (server time at the
       destination node) injected by the API on create, immutable on
       update. the client cannot set or forge these fields. on cross-node
       replication, the receiving node stamps its own `_created_at`
       (arrival time) while preserving the original `_author` and
       `_source_node`. this stops sender impersonation on DMs, posts,
       comments, reactions. implementation: in documentdb.py `to_db()`,
       inject `_author`, `_source_node`, `_created_at` from the
       authenticated token + server clock. add to `q_t()` / `u_t()` to
       block client from overwriting via update. expose in `to_gui()` on
       read. audit the whole cross-node path (certify, token handoff,
       remote CRUD) to verify it works correctly with multi-node traffic.
       gate: permission-matrix tests must cover forged `_author`
       rejection. unblocks: safe DMs, safe feed attribution, audit trail.
  [ ] A9. video transcode pipeline (plan.txt phase 8.5, video v2).
      ffmpeg worker in a compose profile ("transcode"), queued off
      upload-confirm for PUBLIC video only: h.264 ladder -> hls
      segments + manifest + server-side poster/thumbnail -> patch the
      media record (hls_manifest_url, duration, dimensions).
      private/encrypted blobs never touch it (I4). includes the
      phase 5 size-accounting straggler (media bytes vs the space
      plan) and lifting D21's video-v0 caps. gate: PARKED until the
      gauntlet passes (D29 — a transcoder reads as infra) AND D21's
      video v0 is merged; the app proves mp4 playback before the
      node earns a transcoder.
  [ ] A10. P4.5 OPERATOR CONSOLE api (plan.txt phase 4.5 — the
      auth.web10.app/admin backend). the current admin surface is the
      /config form (admins list, app-store approvals, credentials) —
      operator-editable secrets, NOT operator-runnable node. this lane
      adds the moderation + analytics + audit endpoints under it:
      - POST /users/admin (search/paginate), PATCH /users/admin/{u}
        (status: active/suspended/banned, append-only history),
        DELETE /users/admin/{u} (hard delete + tombstone, 24h undo),
        POST /users/admin/{u}/reset_password, POST
        /users/admin/{u}/reveal_phone (audit-logged).
      - GET/PATCH /policy (typed policy record: signup_gate stack,
        beta code, phone/email/captcha toggles, signup + write rate
        limits, blocked_usernames/phones/emails, invite_codes CRUD).
        enforcement moves INTO auth.py + is_permitted (signup rate
        limits, captcha gate, blocked-list checks, write throttle via
        credits) in the SAME PR — the ui form must never be a no-op.
      - POST /reports (user-facing flag), POST /reports/admin
        (queue), PATCH /reports/admin/{id} (status). reporter is in
        the record for the operator, hidden from the reported user.
      - POST /analytics/admin (signups_over_time, dau/wau/mau,
        retention_cohort, request_volume, storage_growth — daily
        snapshot cron, not a runtime scan — per_app_usage,
        top_services). built on A5's event store; if the store
        isn't queryable yet, ship the cheap ones (signups_over_time
        from user records, storage_growth from the snapshot cron)
        and stub the rest behind a TODO + fixtures for lane B.
      - POST /admin/counts (the Overview tab's one-shot:
        users/pending_apps/open_reports/health).
      - POST /audit/admin (paginated, filterable read). writes are
        not an endpoint — every admin mutation in the items above
        appends one immutable row (ts, actor, action, target, before,
        after, reason). the audit append is the load-bearing call of
        this lane: ship it in the FIRST slice, not the last, so every
        later admin action inherits it.
      gate: PARKED behind the gauntlet (priority one — D29: operator
      tooling reads as infra until the social app is lovable). start
      the slice immediately after the gauntlet goes green: users +
      policy + reports + audit first (the safe-growth triangle),
      analytics second. ugh-level arm is the phone-reveal endpoint
      being itself audit-logged — recursive but mandatory.
      permission-matrix suite MUST extend: each admin endpoint rejects
      a non-admin token; every status transition writes exactly one
      audit row; forge-`_author` rejection (A8) still holds through
      the new endpoints. coordinate endpoint shapes with lane B's B8
      BEFORE coding — B8 builds against fixtures, hand off the JSON
      contract first. unblocks: B8, phase 12 trust&safety (the audit
      log + a future token-revoke are what make "ban takes effect now"
      possible).
  [✓ 1.0.155] A11. USERNAME CHARSET — premise corrected 24.07 (code audit,
      1.0.154): the api does NOT reject hyphens. kosher()
      (api/app/endpoints/auth.py:127) has allowed them since #89 —
      `c.isalnum() or c in ["-"]` — so the hyphenated personas
      (solar-flare-69, noodle-empress) ARE signup-able names and the
      original "demo shows names a real signup can't have" concern is
      moot. the REAL gap is the opposite: kosher() is too LOOSE for a
      string that names a mongo collection (I3 boundary) and rides in
      urls/media paths. python isalnum() is unicode-aware, so CJK/
      greek/etc usernames pass; uppercase passes (case-sensitive
      collections + any case-insensitive lookup elsewhere = twin-
      account risk); no length cap; bare "-" and leading/trailing
      hyphens pass. task: pin the charset explicitly — lowercase
      ascii + digits + interior hyphens, bounded length (suggested
      ^[a-z0-9](?:[a-z0-9-]{1,28}[a-z0-9])?$; decide exact bounds and
      record why) — audit every place a username is parsed/regexed
      (auth.py, documentdb.py q_t/u_t + collection addressing, media
      paths, endpoints/*) while tightening, extend the permission-
      matrix suite with BAD_USERNAME cases (unicode, uppercase,
      edge-hyphens, over-length, reserved), and make the error
      message state the actual rule. SIGNUP-TIME validation only:
      existing users (the 208 real + personas, any legacy uppercase)
      are grandfathered — login/lookup must keep working, never a
      migration. the signup form's inline charset message is a small
      lane B seam (leave a .context/ note). small item; gate: none.

LANE B — ui / frontend        (owns: auth2->ui/**, auth/**)
  [✓ 1.0.12-1.0.14] B1. P0 js: auth2 -> vite + bun + typescript
      (react 19, vitest, multi-stage dockerfiles, rtc to bun too)
  [✓ 1.0.26] B2. P2 finish auth2 features, parity audit, rename to ui
  [✓ 1.0.52] B2.5. P2.5 ui makeover in ui/: rectangles-npm -> mainstream
      stack (tailwind + shadcn/ui candidate, record in decisions.md),
      shared design tokens, story-first narrative pass. do BEFORE
      B3/B4 so wizard + admin panel are born on the new stack.
  [✓ 1.0.40] B3. P3 setup wizard screens
  [✓ 1.0.40] B4. P4 admin panel: policy config, ad records crud, analytics
      views (analytics needs A5 merged; build ui against fixtures
      until then)
  [✓ 1.0.55] B4.5. P4 the STUDIO respec (post-B2.5, on the new stack):
      youtube-studio frame, monetization menu as one-button cards
      with the YPP-style unlock ladder, amazon adapter v1 (tag
      card, render-time tagging, compliance chips, 3-sales
      countdown), memberships/tiers wiring to stripe rails.
      composer monetization affordances land in D4's composer
      (coordinate, don't reach into lane D files). this screen is
      the M2 demo -- highest-stakes ui in the repo.
      M0 SLICE OF THIS (D21): the menu SCREEN with rung-0 cards
      (memberships, amazon tag, direct deals) ships inside the M0
      window so the demo video shows the money screen -- visually
      complete + rung-0 functional; deeper wiring (unlock ladder,
      adapters) follows in weeks 5-10.
  [✓ 1.0.65] B5. level-up ui/ to the design.md standard (READ design.md
      FIRST — repo root; it is the law for all ui work). tokens.css
      migrated to design.md §13 (dark-first violet), inline styles
      out, dead vendored bulma dir deleted, SideBar.tsx className/
      style bug fixed, components/ui primitives kit, mobile bottom-
      nav shell, hub.png (apple's app store logo — trademark,
      replace) + react-atom icons swapped for the real keys marks
      (design.md §3). studio = the money screen, highest stakes.
      ALSO (urgent, the dev/prod deploys are gated on this):
      parameterize the ui's backend origins —
      ui/src/interfaces/authAdapter.ts + ui/src/config.ts hardcode
      api.web10.app/auth.web10.app for prod builds, so any deployed
      env ships a bundle calling the wrong api (see
      ubuntu-deployment/AGENT-OPS.md §4.1).
      build-time env (REACT_APP_*/VITE_* both allowed by
      vite.config.js) + Dockerfile ARG. LANDED with the names
      REACT_APP_API_ORIGIN / REACT_APP_AUTH_ORIGIN /
      REACT_APP_RTC_ORIGIN (full origins) + REACT_APP_DEFAULT_API
      (host); docker-compose.ecosystem.yml passes exactly these.
      acceptance: design.md §12 (screenshot test, pr screenshots
      desktop + 375px, tokens-only, all states designed) + an env
      rebuild can point at dev/prod domains without a code edit.
[✓ 1.0.72] B6. AUTHENTICATOR REVAMP — the ui/ auth flow is broken in
       both look AND function (operator report, 19.07: "looks
       retarded and straight up doesnt work", web10 logo on auth is
       "hella broken"). B5 restyled the shell but the actual
       login/consent flow is still wrong. this is a from-the-user's-
       seat fix, not just tokens: (1) the web10 logo renders broken —
       audit the asset path/import (design.md §3 names the real
       marks; a deployed build serves from the app root, so a
       dev-only path breaks in prod), (2) drive the REAL login flow
       end to end against dev once A7's mongo is connected — signup,
       login, the auth portal popup/redirect, token handoff to a
       consuming app — and fix what's actually broken (the c5/c6
       playwright harness + data-testid hooks exist; write the auth
       journey as a test so it can't regress), (3) make it look like
       design.md §12. gate: A7 (need real users to log in as) helps
       but the logo + obvious-broken bits can start now. acceptance:
       a human can sign up and log in on auth.dev.web10.app without
       hitting a broken screen; playwright covers the journey;
       screenshots desktop + 375px.
       NOTE: the critical functional fixes landed (wapiAuth.js
       defaultAPIProtocol->APIProtocol, token restoration on load,
       SignupForm phone arg, ForgotForm cancel, authAdapter hostname
       check, Branding restyled, logo.svg deleted). The playwright
       auth journey test against real users still waits on A7.
  [✓ 1.0.76] B7. AUTH UI: FROM-THE-PHONE QUALITY PASS — operator report
       19.07 (screenshots in the ops session): (1) a restored-but-
       invalid/expired token lands the user on an EMPTY "Your
       contracts" page with no login button anywhere — restored
       tokens must be validated (certify) before showing the
       authenticated view, and a dead token must bounce to the login
       form; (2) the header logo renders as a broken <img> on prod —
       asset missing from the deployed bundle (design.md §3 marks);
       (3) auth.web10.app does not render usably on a phone —
       B6's 375px acceptance was never verified on device. bar:
       design.md §12 (screenshot test, desktop + 375px in the PR),
       "operator would demo it to an influencer without apologizing."
       gate: none — start anytime. compare against social/marketing,
       which the operator calls the quality reference.
  [ ] B8. P4.5 OPERATOR CONSOLE ui (plan.txt phase 4.5 —
      auth.web10.app/admin). lift the existing ConfigPage.tsx into a
      real moderation console. mental model: twitch/mastodon admin
      dashboard you leave open in a tab, not a secrets form.
      - new ui mode `admin` in App.tsx's switch, served at path /admin.
        the existing `config` mode redirects to /admin#config (deep-
        link compat, sidebar/status refs still resolve); nothing 404s.
        a NotAnAdmin component (lift ConfigPage's Lock card) gates
        every admin sub-route so the gate isn't hand-rolled per tab.
        extend the interface with I.isLoadingAdmin (separate from
        I.isAdmin) so a route rendered before /am_admin resolves
        shows a skeleton, not a flicker of the "admins only" card.
      - tabs, left rail (desktop) collapsing to a top segmented control
        (mobile, 375px, never a hamburger): Overview · Users ·
        Moderation · App Store · Node Config · Audit Log.
      - Users: toolbar (search + status + sort) + paginated table; row
        click opens a right-drawer (mobile: full sheet) with identity,
        masked phone + audit-logged reveal button, quotas, registered
        apps, recent activity, action buttons (suspend/ban/unban/reset
        password/delete). every destructive action opens a confirm
        modal that requires a TYPED username match.
      - Moderation: three stacked sections — (a) the typed /policy form
        (signup_gate stack with conditional captcha/email fields, rate
        limits with "reset to default" ghosts), (b) the reports Kanban
        table (oldest first, dismiss requires a reason, opens user
        drawer from the Users tab), (c) blocklists + invite-code CRUD
        (each row shows the actor + ts + reason — never an anon block).
      - App Store: ship verbatim — lift ConfigPage's App Store Approvals
        card into components/Admin/AppStoreCard.tsx. no behavior change.
      - Node Config: ConfigPage's secrets + identity half stays as-is
        under /admin#config. the Admins card MIGRATES OUT to the Users
        tab's role assignment; the Access Policy card MIGRATES OUT to
        the Moderation tab's policy form. so secrets and who-can-touch-
        them are no longer on the same screen — a real footgun fix.
      - Audit Log: reverse-chronological table, filter by actor/action/
        date, every row expandable to the before/after JSON tree (reuse
        Contracts' renderer). immutable; no PATCH.
      - Overview: no-input dashboard — total users (+N this week),
        pending apps, open reports, node health (db ping + storage
        snapshot + 24h errors), today's signup chart. the operator's
        muscle memory is Overview -> click the red number.
      - shared primitives land FIRST in components/Admin/_shared/:
        NotAnAdmin gate, drawer/sheet, audit diff tree, analytics
        sparkline card, typed-confirm modal. six tabs must NOT each
        hand-roll their own confirm-modal — that's the worst outcome.
      gate: PARKED behind the gauntlet (priority one — D29) AND behind
      A10's endpoint-shape handoff (build against fixtures until then,
      same pattern B4 used — do NOT reach into api/ files for shapes;
      get the JSON contract from lane A first). ship in two waves:
      Wave 1 (gauntlet green) = the /admin route + Users tab +
      Moderation tab + Audit Log + the lift of ConfigPage into
      /admin#config + App Store tab. Wave 2 = Overview + Analytics
      tab (a chart dep is a lane-shared decision — check what
      marketing-ui uses before adding one).
      acceptance: design.md §12 screenshot test (desktop + 375px per
      tab in the PR), all-token-colors from the design system, and a
      playwright admin journey added to the gauntlet suite: sign in
      as bootstrap admin -> /admin -> search the user from gauntlet
      step 1 -> suspend -> write rejected -> unsuspend -> audit log
      shows BOTH transitions. that test is the regression seatbelt for
      the whole phase — it's how "we shipped moderation with no test"
      doesn't happen.

LANE C — greenfield services  (owns: sdk/, mcp/, new dirs)
  [✓ 1.0.25] C1. P5 media service (presigned urls, minio profile,
      metadata records) — landed standalone, then merged into api/
      as a router in 1.0.30/1.0.31 (standalone media/ dir is gone;
      media code now lives in api/, which is lane A territory)
  [✓ 1.100] C2. P6 sdk rewrite (typescript, fetch, typed crud; aggregate
      types land after A4 merges -- everything else doesn't wait)
  [ ] C3. P6 mcp server (thin layer over the C2 surface)
  [ ] C3.5. P6.5 create-web10 scaffolder (needs C2's sdk -- templates
      are written on the typed sdk, never legacy wapi; node story
      improves when A3's one-container node + sandbox exist, but
      --node <url> works from day one)
  [ ] C4. P10 backup integrations (drive/dropbox oauth, export/import
      archive -- format comes from D1's conventions doc)
  [✓ 1.0.59] C5. browser e2e harness (new top-level e2e/ dir + its ci
      workflow): playwright-in-docker against the compose stack,
      ~6-10 smoke journeys across ui + web10-social + marketing-ui
      (specced in plan.txt CROSS-CUTTING quality/testing). touches
      other lanes' apps via read-only flows only (data-testid hooks
      by coordination, never reach-in edits). marketing-ui smoke +
      ui auth flows are landable now; social post->feed firms up
      with D4.
  [ ] C6. e2e deep sweep + bug hunt (specced in plan.txt CROSS-
      CUTTING quality/testing, "e2e deep sweep"): C5's harness
      exists — now really exercise the ecosystem locally and find
      what doesn't work (assume plenty doesn't). expand playwright
      across the money paths + lane seams: signup -> consent ->
      grant; social post w/ media -> feed -> comment -> dm; terms
      revoke actually cuts the app off; exporter upload ->
      marketing-api job -> records; app store -> launch -> token
      handoff; studio w/ real metering. TEST ACCOUNTS are part of
      the harness: signup IS a test (fresh account per run), plus
      a seed script of persona fixtures (creator + fans + granted/
      revoked terms) for flows needing existing state — same seed
      powers dev-env wipe+reseed. no secrets in git; per-env creds
      via env files. every failure triaged: small fixes land with
      the suite, lane-owned breakage -> .context/ note + lane
      queue item, never a silent skip. deliverable = the enlarged
      suite AND the honest bug list.
  [✓ 1.0.141] C7. sdk media surface (plan.txt phase 8.5, media v0): the
      typed sdk (sdk/src) gains typed media methods — requestUploadUrl,
      confirmUpload, upload (presigned POST + confirm), and getReadUrl
      (presigned GET exchange + expiry-aware cache) matching the
      contract the web10-social wrapper lands in D23. small; no gate.

LANE D — docs / apps / mobile (owns: mobile/**, marketing/, examples/
                                *.md/txt)

  LANE D SUB-LANES (23.07.2026): the killer-app push made every open
  demo item lane D, which collapsed 4-wide parallelism into one
  directory — and 1.0.148 (two branches independently shipping
  DiscoverScreen.tsx, hand-reconciled) is what that costs. so the
  same two rules apply one directory deeper. one sub-lane per
  workspace, named in the PR:
    D/feed      — web10-social/src/components/Feed/** + data/posts.ts,
                  feed.ts, comments.ts, reactions.ts
    D/profile   — web10-social/src/components/Bio/** + data/profile.ts
    D/dms       — web10-social/src/components/Chat/** + data/dms.ts,
                  contacts.ts
    D/discover  — web10-social/src/components/Discover/** +
                  data/follows.ts
    D/shell     — web10-social/src/App.tsx, components/Social/
                  Layout.tsx, main.tsx, index.css, routing
    D/marketing — marketing/marketing-ui/** (as before)
  SHARED SEAMS (all sub-lanes touch, none owns): data/types.ts,
  data/wapi.ts, data/serviceTerms.ts, data/index.ts,
  components/shared/**, components/ui/**. seam rule: touch a seam
  file only to add your own exports, keep the diff single-purpose,
  first-merged wins and the others rebase fast. needing another
  sub-lane's file works like needing another lane's: leave a
  .context/ note, don't reach in.
  [✓ 1.0.18, 1.0.20] D1. conventions doc (posts/media/contacts/inbox
      schemas) + protocol spec draft. docs/protocol-spec.md,
      docs/conventions.md, docs/schemas/*.json — C4/D4/D5 now unblocked.
  [✓ 1.0.30] D2. P7 tidy: crm/mail integrated as sub-apps inside
      web10-social (CRM Rolodex + Mail inbox), old crm/ and mail/
      folders removed. 35 new tests, shared mock factory.
[✓ 1.0.51] D2.5. P2.5 ui makeover in web10-social: rectangles-npm ->
       mainstream stack (tailwind v4 + Radix UI primitives + Lucide),
       story-first pass (empty states point at exporters; kick/twitch-grade
       slick is the visual bar -- THE STORY beat 5, D20). pairs with
       D4 -- credible-in-a-creator-pitch is the acceptance bar.
  [✓ 1.0.27] D3. P11 mobile encryptor: complete rebuild landed —
      expo 52, crypto core (hkdf, ed25519/x25519, xchacha20), wallet
      persistence, tabbed UI, 55 tests. the rtc key channel + sdk
      crypto integration still waits for A/C (phase 11 proper).
  [✓ 1.0.50] D4. P8 killer app M0 slice (data layer): post + feed
      (chronological + sort dropdown ONLY -- lens/chatbox is CUT to
      later.md, D20) + profile + dms. full conventions-schema data
      layer in src/data/ (posts, feed, profile, contacts, dms,
      comments, reactions, media upload via presigned URLs). 55 new
      tests, 227 total green. screens deferred to D2.5 post-B2.5 tokens.
  [✓ 1.0.27] D5. P9 exporter core + instagram mapping (needs D1 conventions;
       client-side, so no api deps) — also includes Facebook + YouTube
       mapping, multi-platform UI with takeout checklists, 57 tests
   [✓ 1.0.28] D6. web10-social: full TS + Vite + Bun revamp + 137 vitest tests
[✓ 1.0.30] D7. P7 marketing-ui: consolidated home/ + docs/ + App Store
        into marketing-ui/ (Vite + React 19 + TS + Bun + react-router).
        App Store moved out of ui/ — public-facing discovery belongs on
        the marketing site, not the node's auth UI.
    [✓ 1.0.31] D8. P7 marketing-api + exporter migration: new FastAPI
        service for ZIP import pipeline (server-side streaming parse,
        validate, dedup, batch write) + analytics (pageview, funnel).
        Exporter UI moved from exporters/ into marketing-ui/ (now polls
        marketing-api jobs instead of browser-side ZIP parsing). Legacy
        home/ and exporters/ dirs deleted.
    [✓ 1.0.33] D9. P7 final tidy: rtc/ -> api/rtc/, docs/ ->
        marketing-ui/public/docs/. all references updated (compose,
        CI, CLAUDE.md). top-level tree clean.
  [✓ 1.0.58] D10. report-a-bug loop: feedback endpoint in marketing-api +
      "report a bug" affordance and error boundaries in social +
      marketing-ui (specced in plan.txt CROSS-CUTTING quality/
      testing). the ui/ app's button is lane B territory --
      coordinate, don't reach in. the counterweight to the thin
      e2e layer: users become the coverage, so reporting must cost
      them <=2 clicks.
  [ ] D11. ux telemetry (specced in plan.txt CROSS-CUTTING quality/
      testing -- read the surface split there before touching):
      marketing-ui funnel analytics + js error beacon via
      marketing-api; platform uis get content-free aggregate
      events + the error beacon ONLY -- no session-recording js
      on ui/ or social, ever (manifesto line). self-hosted
      posthog/openreplay deploy, if adopted, is lane E territory.
  [✓ 1.0.64] D12. level-up web10-social to the design.md standard (READ
      design.md FIRST — repo root). owns marketing/web10-social/**
      only — runs parallel to D13, disjoint dirs. this is the deck
      screenshot next to kick/twitch: wire the missing
      @tailwindcss/vite plugin, actually load inter, media-forward
      feed cards, composer that feels like publishing, profile as
      creator page, story-first empty states (point at the
      importer), finish evicting excluded rectangles/@chatscope
      legacy components, fontawesome CDN kit out for lucide.
      acceptance: design.md §12. also fixed tsconfig's missing `@/*`
      path alias (tsc -b had ~90 pre-existing errors) and the
      alternative.png-isn't-the-keys-mark bug (decisions.md D24,
      converged with D13).
  [✓ 1.0.87] D12 vibrancy overhaul (follow-up): the social flagship was
      muted — flat surfaces, no ambient light, zero interaction energy.
      design.md §4 relaxed for social: glow tokens, shimmer skeletons,
      heart-burst on like, card hover glow, gradient buttons, presence
      dots, vibrant banner, media hover zoom, animated login. 195 tests
      green, tsc clean, build clean.
  [✓ 1.0.63] D13. rebuild marketing-ui on the design system. bulma OUT
      entirely (D22), tailwind v4 + design.md §13 tokens, dark
      cinematic landing (lockup hero, tested reach-gap proof section —
      "real product screenshots" deferred, none exist yet to be
      honest about), docs readable (65-75ch prose, mono code), app
      store on house cards (first-party proof, not a catalog),
      education-era imagery + university logos deleted. brand asset
      debt for all three apps PARTIALLY paid: favicons/pwa icons +
      shared og-image from the keys mark dropped in
      .context/brand-assets/ for lanes B/D12 to apply (D13 doesn't
      reach into ui/ or web10-social/ code); svg vectorization + dark
      variants still open, no tracing tool in-environment. flagged:
      design.md §3's alternative.png does not contain the keys mark —
      needs a design.md correction (not done here, out of lane).
  [✓ 1.0.67] D14. parameterize web10-social's backend origins:
      src/lib/origins.ts reads VITE_API_ORIGIN / VITE_AUTH_ORIGIN /
      VITE_RTC_ORIGIN with the prod values as fallbacks; adapter +
      wapi wrapper + bare-username default provider use it, and the
      15 repeated cross_origins lists collapsed to one that also
      includes the serving hostname (a deploy authorizes itself).
      verified: env-arg build bakes dev origins into the bundle,
      195 tests green.
  [✓ 1.0.136] D16. APP STORE — restore it as a real, curated marketplace
      (gated on A7's mongo; the original web10 had live registered
      apps + real usage stats, so this is RESTORATION of real
      assets, not the speculative empty-catalog the D20 thread
      warned against — see decisions.md, reconciled below). the
      current marketing-ui AppStore is first-party-only "proof"
      cards; make it the real thing:
      (1) read the registered apps from the connected mongo (A7),
          not hardcoded cards.
      (2) LAYOUT: the killer app (web10-social) stays promoted up
          top — do NOT bury it; registered third-party apps go in a
          lower section. one hero, then the catalog.
      (3) CURATION: web10 Inc. controls the store. anyone can
          register an app, but it needs ADMIN APPROVAL to appear.
          needs an api/admin side: an approval flag on app records +
          an admin-only approve action (coordinate with lane A/B —
          the admin panel is B4; the record shape + gate is lane A
          territory). unapproved = registered but hidden.
      (4) surface the historical stats that made it feel alive
          (user count, app count) — real numbers from the mongo,
          never faked.
      acceptance: design.md §12; store shows web10-social first +
      real approved apps below; registering an app is possible but
      admin-gated; no hardcoded catalog.
      NOTE — reconciles with the D20/app-store decision (which said
      "don't build a speculative ecosystem, keep it as proof"): that
      call assumed an EMPTY catalog would read as fediverse-jank.
      with 208 real users and real registered apps from the original
      web10 now reconnected (A7), the catalog is genuinely alive, so
      restoring it IS the "looks like a real company" goal, not a
      distraction. record this reversal in decisions.md when D16
      lands.
      STATUS: (1)+(2)+(4) landed in 1.0.84 (marketing-ui AppStore
      POSTs the node /stats); (3) CURATION landed in 1.0.136 —
      operator reversed the earlier "takedown, not allowlisting"
      call (decisions.md D31) and now wants allowlist ("any app can
      register, but i should be able to approve"). `register_app`
      adds new entries as approved:false (pending, via $setOnInsert
      so repeat visits don't reset state); `get_apps` filters
      approved=True so /stats hides unapproved; `POST /apps/admin`
      lists all + a pending count, `POST /apps/approve` toggles
      (both admin-only via check_admin, reuses the 1.0.77 admin
      model). The authenticator's Node Config panel gained an "App
      Store Approvals" card with per-row Approve/Unapprove buttons
      and pending-badge side-by-side with the rest of the node's
      variables. Backward-compat: historical pre-A7 apps without an
      `approved` field show as pending in the admin panel — the
      operator curates them once on first launch.
  [✓ 1.0.70] D16.1. fix the /docs/* 404 — marketing-ui nginx served
      /docs/ via an alias block that shadowed the SPA fallback, so
      /docs/protocol-spec 404'd (it's a client-side route that
      fetches /docs/<slug>.md). added try_files ... /index.html to
      the /docs/ location. (small live-prod fix done alongside the
      planning.)
  [✓ 1.0.72] D17. RESTORE THE DEV DOCS (operator, 19.07 — "docs are lacking,
      were sharp before with demo apps"). NOT LOST — recoverable from
      git: the old rich docs lived in auth/public/docs/ and were
      removed in 82667060 ("moving of docs to separate endpoint").
      recover from `82667060^:auth/public/docs/`:
      - hello/ and notes/ — the two live DEMO APPS (index.html +
        script.js each) that made the docs sharp.
      - sdk.md + sdk.pdf — the SDK guide.
      bring these into marketing-ui's docs (the new home,
      marketing/marketing-ui/public/docs + src/pages/Docs.tsx), on
      the design.md standard, as runnable demo apps + an SDK page.
      also: the old app store had a "make your own web10 app with the
      web10 CLI" call-to-action (old AppStore.js, commit cc548b68) —
      the CLI still exists (marketing/web10-cli/) but is invisible;
      restore that CTA (ties to D16's "Build on web10" card) and
      write a CLI quickstart doc. acceptance: /docs shows demo apps +
      SDK + CLI quickstart; the app store links devs to "build your
      own". gate: none (docs), but the SDK doc should reflect C2's
      typed sdk once it lands (don't teach legacy wapi as the future
      — see C2/C3.5 parking note).
[✓ 1.0.72] D18. DOCUMENT + LINK THE WEB10 SDK (operator, 19.07: GitHub
       shows 4 built packages, none is the SDK; docs never mention it
       or link to it). DONE: sdk.md docs page created and wired into
       Docs.tsx sidebar; SDK link added to Home.tsx footer; npm badge
       + SDK link in README.md. npm publish confirmed: web10-npm@1.0.8
       public on npmjs.com. Publish flow decision documented in
       decisions.md D26: stays tag-gated (no auto-publish on merge
       while C2 typed rewrite is in flight).
[✓ 1.0.131] D19. CONTENT LIFECYCLE — staging + visibility management (operator,
        23.07; on the D29 gauntlet critical path). the visibility split
        shipped half-built and crossed: composer sets no visibility (native
        posts trap in private_posts, never public), imports write to legacy
        anon-readable `posts` (import auto-publishes your whole history).
        model: visibility = the COLLECTION a record lives in, never a status
        field (decisions.md D30). staging_posts (owner-only, awaiting triage)
        / private_posts / public_posts; publish = move between collections.
        PHASE A (foundation repair, PID 1.0.131 — the live exposure fix):
        imports -> staging_posts (marketing-api parsers + main.py);
        register staging_posts owner-only terms in SMROnReady; wall reads
        public_posts+private_posts not legacy posts; fix broken /exporters ->
        /import CTA (3 social screens). PHASE B: composer visibility selector
        (default public). PHASE C: Staging/Review screen (grouped by origin,
        publish/keep-private/delete = collection moves), wall visibility
        toggle, and PUBLISHING grants audience media read (per-service gate is
        coarse; public media needs an explicit path). owns marketing/web10-
        social/** + the post-write path in marketing/marketing-api/**. gate:
        none — Phase A is a live exposure fix, start now. acceptance: import
        -> review in staging -> publish public -> appears in feed/discovery;
        native post with visibility choice; nothing auto-exposed; design.md
        §12 (PR screenshots). deferred: friends_posts (needs graph),
        unlisted_posts (anon-read, discovery-excluded).
        STATUS: Phase A LANDED (1.0.131). Phases B + C remain open.
[✓ 1.0.149] D24. PROFILE FOLLOWER/FOLLOWING COUNTS BUG — `ProfileScreen.tsx` loads
        `countFollows()` and `readFollows()` in parallel and assigns both
        `followingCount` AND `followerCount` from the same data source
        (people YOU follow). There is no reverse lookup for who follows
        YOU, so the "Followers" stat is a duplicate of "Following."
        Fix: either wire a real follower-count endpoint (e.g. a public
        ledger query or a discovery API call) or hide the follower stat
        until the backend supports it. Owns marketing/web10-social/src/
        components/Bio/ProfileScreen.tsx. Gate: none.
[✓ 1.0.149] D25. USER-PROFILE BACK NAVIGATION BUG — `handleBackFromProfile` in
        `App.tsx` hardcodes `setMode('discover')` so leaving any user
        profile always returns to Discover, even when the user came from
        Feed, Chat, or Profile. The `prevModeRef` useEffect (lines
        130-137) tracks mode changes but its conditional branches are
        empty — it never stores the pre-profile mode. Fix: capture the
        mode before navigating to `user-profile` and restore it on back.
        Owns marketing/web10-social/src/App.tsx. Gate: none.
[✓ 1.0.157] D-profile-media-refresh (sub-lane D/profile; was "D26" for a day —
        renamed per rule 4/D33). PROFILE AVATAR/BANNER VANISH ON
        REFRESH — (operator, 23.07:
        uploading a profile pic + banner now works and they render —
        1.0.149's presign fix — but after a page refresh they're gone
        and the browser makes NO request to load the images.) the load
        path LOOKS right — `ProfileScreen.loadData` pushes `avatar_ref`/
        `banner_ref` into `resolveMediaRefs` (ProfileScreen.tsx:63-66) —
        so the break is in what a fresh read returns. suspects, in
        order: (1) `saveProfile` (data/profile.ts) is called from
        `handleUpload` with the whole in-memory profile spread into the
        `$set` payload, `_id` included — verify the update actually
        persists `avatar_ref`/`banner_ref` onto the record `readProfile`
        returns; (2) `readProfile` returns `records[0]` unsorted —
        accounts with multiple `profile` records (the legacy-identity
        adapt path creates one; pre-1.0.145 seed runs left dups) can
        read a stale record while the save updated a different one;
        (3) `resolveMediaRefs` failing silently and returning [] for
        the refs. fix + a vitest regression: upload → save → fresh
        `readProfile` round-trip carries the refs → media request
        fires. verify on dev with a hard refresh. TEST-DEPTH NOTE:
        this bug got past the step-6 playwright journey (it asserts
        profile save+read, never refresh-with-media-refs) — the
        regression also gets encoded into gauntlet.spec.ts step 6.
        e2e/** is lane C: leave a .context/ note with the repro, or a
        tiny cross-lane PR by agreement. journeys must assert the
        BAR, not the happy path. owns marketing/
        web10-social/src/components/Bio/ProfileScreen.tsx + src/data/
        profile.ts. gate: none — gauntlet step 6 ("your profile reads
        as a creator page you'd screenshot") doesn't survive F5 today.
[ ] D-url-routing (sub-lane D/shell; was "D27" for a day — renamed per
        rule 4/D33). THE ADDRESS BAR HOLDS YOUR PLACE — (operator,
        23.07: refreshing the social app should put you back on the
        screen you were on.) navigation is one `useState<Mode>` in
        App.tsx:95 — the URL never changes, so refresh dumps you at
        the feed, browser back/forward do nothing, and no screen is
        linkable. adopt real routes (react-router, already the
        marketing-ui stack): /feed, /discover, /messages, /profile,
        and /u/:username for public profiles — the route
        D-trending-link is explicitly waiting on (plan.txt PHASE 5.5:
        marketing-ui's trending author link deep-links to
        SOCIAL_ORIGIN root today with a TODO for /u/<author>).
        refresh restores the screen INCLUDING which user profile you
        were viewing; browser back replaces D25's hand-rolled
        preProfileModeRef back-stack; the login flow preserves the
        intended destination and redirects post-auth; unknown paths
        fall back to /feed. owns marketing/web10-social/src/**
        (App.tsx, Layout nav); the marketing-ui author-link upgrade
        to /u/<author> is a small same-lane follow-up touch. gate:
        none. acceptance: refresh anywhere → same screen; back/
        forward work; /u/<persona> loads while signed in.
[✓ 1.0.155] D-follower-count (sub-lane D/discover + a D/profile touch). THE
        REAL FOLLOWER COUNT — gauntlet step 6's own words: "a creator
        page without follower count is not a creator page — that's
        the primary social proof metric." 1.0.149 (legacy item D24)
        honestly REMOVED the fake stat; nothing was queued to build
        the real one — this is it. follower count of X lives in every
        follower's own collection, and I3 forbids cross-collection
        reads, so the count comes from the public layer: `followUser`
        (follows.ts) mirrors to /public/entries with
        payload.action='follow' targeting the followed user — the
        exact D32 pattern reactions/comments already use
        (unconditional; collection-level terms is the lock; unfollow
        deletes the entry like deleteComment). NOTE this makes the
        follow graph public — record the call as decision D34 when
        implementing (D32's logic extends: follows are public
        discourse, the panic button is one terms flip). count reads
        from the ledger; /discover/users gains follower_count in the
        engagement aggregation (small lane A touch in documentdb.py —
        coordinate via .context/, don't reach in). backfill: the
        persona seed script mirrors the existing cross-follows
        (persona-orchestration/, idempotent like 1.0.145). then the
        Followers tile returns to ProfileScreen with a real number
        (tabular-nums — this is the M0 screenshot stat). gate: none.
        acceptance: personas show real cross-follow counts; following
        someone increments their count; unfollow decrements.
[✓ 1.0.155] D-dm-compose (sub-lane D/dms). START A CONVERSATION — gauntlet
        step 5: "no way to start a new conversation with someone you
        don't already have a thread with" (listConversations only
        returns existing threads; the step-5 playwright journey is a
        test.fixme scaffold waiting on this). add a "New message"
        button → picker over contacts + follows (+ compose-to-
        username fallback); dms.ts already has deterministic
        conversation service names, so composing to a fresh contact
        is just writing the first record. persona reply seeding
        (1.0.145) means the thread won't read as a monologue.
        owns web10-social/src/components/Chat/** + data/dms.ts,
        contacts.ts. gate: none. acceptance: DM a persona you've
        never messaged, entirely from the UI; lane C note to flip the
        step-5 fixme into a real journey.
[ ] D-login-cta (sub-lane D/shell). THE LOGIN SCREEN NEVER SAYS YOU
        CAN SIGN UP — gauntlet step 1: LoginScreen is a single "Log
        in" button; account creation exists only inside the auth
        popup (ConsentView → SignupForm, 1.0.135) and nothing tells a
        first-time user that. smallest honest fix: the button copy
        and a line under it ("Log in or create your account — one
        step") so the affordance is stated, not discovered. if the
        auth portal supports a signup-mode hint param, pass it;
        don't build a second signup flow. owns web10-social/src/
        App.tsx (LoginScreen). gate: none. design.md §12 (it's the
        first screen of every demo).
[ ] D-join-page (sub-lane D/marketing). THE MANIFESTO JOIN PAGE —
        timeline.md week-2 item, the only one not started (flagged
        23.07). marketing-ui gains /join: the fan-facing pitch page
        built from THE STORY's user side (plan.txt) — the first
        sentence is the fan's ("never miss a post from [creator]
        again — you see 100% of what they make, always"), the
        aspirant is honored at the door ("you're not joining the
        crowd — you get your own page here too"), THE RISE arc as the
        page's skeleton, quiet safety line second (delete actually
        deletes). CTA → signup on the flagship node. voice:
        manifesto.md's register — declarative, no gush, never claim
        what isn't built. owns marketing/marketing-ui/src/pages/
        Join.tsx + route + nav link. gate: none. design.md §12
        (screenshot-grade; this page IS the pitch).
[✓ 1.0.147] D21. MEDIA POLISH — photos + video v0 (plan.txt phase 8.5; item
        number D20 skipped — it reads as decisions.md's D20
        everywhere). the composer's 80x80 hover-square tray, the
        no-lightbox feed, and the video trap (a picked video renders
        via <img> and just breaks) fixed in one lane-D arc, staged:
        PHOTOS V1 composer (real-aspect tray matching the feed grid,
        touch-visible remove, drag reorder, client-side downscale +
        thumbnail + width/height into the confirm fields nobody
        fills, per-image alt text, attach-time validation with
        words); PHOTOS V2 feed (lightbox with swipe/zoom, "+N"
        overflow tile past six, reserve-space-from-dimensions, and
        the design.md MEDIA SPEC: canonical aspect ratios, grid
        rules, player chrome); VIDEO V0 (video previews + plays as
        <video> with client-captured poster + duration, h.264 mp4
        only with honest attach-time rejection otherwise, duration/
        size caps until A9 lifts them). owns marketing/web10-
        social/** + the design.md media-spec section. gate: none —
        photos v1 is gauntlet step 2 territory ("post a photo -> it
        appears in the feed"), start now. acceptance: attach photos
        on a phone and the tray looks intentional; a picked video
        plays in the feed with a real poster; design.md §12 (PR
        screenshots).
[ ] D22. THE VERTICAL FEED — short-form lens (plan.txt phase 8.5,
        video v1; the tiktok answer). 9:16 full-bleed snap-scroll
        feed in web10-social: muted autoplay + tap for sound,
        preload next; reads the SAME posts/media services (a lens
        over the conventions schemas, not a new silo); composer
        detects 9:16 -> "this will show in the vertical feed" +
        cover-frame picker. rides D21's direct-mp4 playback — does
        NOT wait for A9, inherits hls when A9 lands. owns
        marketing/web10-social/** only. gate: D21's video v0
        merged. acceptance: a phone-recorded vertical video posts
        and swipes like short-form, demoable next to tiktok.
[✓ 1.0.143] D23. MEDIA READS SURVIVE A PRIVATE BUCKET (plan.txt phase 8.5,
        media v0; the dev-minio 403). media records store the bare
        unsigned object url and every renderer trusts record.url
        verbatim, so a private bucket 403s every avatar/photo; the
        api's request_read_url endpoint has ZERO callers. the wapi
        wrapper gains getReadUrl (POST /media/{user}/read ->
        presigned GET, expiry-aware cache so a feed of N images
        isn't N round-trips per render); resolveMediaRefs + the
        avatar/feed/composer paths render fresh presigned urls,
        never record.url raw. cross-lane bits: the typed-sdk media
        surface is C7; confirm-upload persisting object_key is a
        tiny lane-A touch (coordinate, don't reach in — legacy
        records derive the key from the stored url meanwhile).
        owns marketing/web10-social/** only. gate: none — and
        D21's photos-v1 acceptance ("post a photo -> it appears
        in the feed") is BLIND on dev until this lands, so it
        goes first. acceptance: avatars + feed photos render on
        dev (private bucket) with zero 403s in the console;
        presigned urls refresh when they expire.


LANE E — infrastructure / deployment (owns: proxmox provisioning,
    reverse proxy, CI/CD deploy targets)
[✓ 1.0.60] E1. proxmox staging node: Portainer + Nginx Proxy Manager
       infra (prep-vm.sh, docker-compose.staging.yml self-contained stack,
       Cloudflare DNS plan). ubuntu-deploy.sh replaced by Portainer UI
       approach. README.md updated. DEPLOYED 19.07.2026:
       api live at staging.web10.app, ui serves at
       auth.staging.web10.app but is broken by hardcoded origins
       (lane B fix queued in B5); rtc/minio DNS records missing;
       marketing/social not in the stack yet. live triage +
       ops procedures: ubuntu-deployment/AGENT-OPS.md + OPS-LOG.md.
[✓ 1.0.38] E2. marketing deploy: marketing-ui + marketing-api on proxmox,
       caddy reverse proxy with auto-TLS. internal staging complete.
  [✓ 1.0.68] E3. PROD env — the whole ecosystem public (specced in plan.txt
      CROSS-CUTTING deployment "environments"): prod marketing-ui +
      marketing-api + node (api, ui, social, rtc, minio, db) behind
      NPM with public cloudflare DNS + router-forwarded 80/443 +
      real TLS. infra panels stay LAN/VPN-only (E1 security model).
      REPO SIDE DONE (1.0.66): docker-compose.ecosystem.yml (one
      parameterized compose, dev + prod — the staging env was cut),
      env.prod.example, social prod Dockerfile, runbook. REMAINING:
      box execution — create the web10-prod Portainer stack + NPM
      hosts + DNS per README.md and AGENT-OPS.md §4.2 migration
      order (needs SSH/.env + CF creds; watch the
      api.web10.app/auth.web10.app cutover caution).
  [✓ 1.0.68] E5. DEV env — the SAME whole ecosystem, VPN-only, on the same
      linux box ubuntu-deployment/ preps (parallel to E3, and to
      the B5/D12/D13 ui revamp): cloudflare DNS for dev vhosts
      (*.dev.web10.app or similar) pointing at the box's INTERNAL
      LAN ip — resolves publicly, unreachable off-VPN, zero
      port-forwards, TLS still works (DNS-01 needs no inbound).
      second Portainer stack from the same parameterized compose,
      own volumes + env file. promotion flow (dev soak -> prod
      redeploy) documented in README.md.
      REPO SIDE DONE (1.0.66): compose + env.dev.example + DNS-01
      procedure + promotion flow all in README.md.
      REMAINING: box execution — web10-dev stack, *.dev DNS records
      pointing at the LAN ip, DNS-01 certs. ui/social bundles wire
      to dev origins only after B5/D12's env fixes merge (compose
      already passes the build args).
  [ ] E4. automated provisioning: terraform or ssh-api script +
      ansible for idempotent VM/LXC setup. don't rush — manual is
      fine until E2 works.
[~] E6. SSH-DEPLOY CI/CD (replaces Portainer GitOps as the deploy
       trigger): the current Portainer GitOps loop (5-min poll, silent
       retries on failure, zero signal back to GitHub) is brittle —
       when a deploy fails, Portainer retries forever in a loop and
       GitHub has no idea. the replacement: GitHub Actions SSHes into
       the Ubuntu server, runs `docker compose up` directly, captures
       exit codes, and runs the smoke test. GitHub now knows instantly
       if a deploy succeeded or failed. no self-hosted runner needed
       (avoids the public-repo security issue — no runner in the
       docker group on the box).

       AUTH: deploy key (SSH key-pair, not PAT). the private key
       lives as a GitHub Actions secret (`DEPLOY_SSH_KEY`), the public
       key is installed on the box at `~/.ssh/authorized_keys` with a
       command restriction scoped to the deploy script only — the key
       can't be used for general shell access.

       WORKFLOW: new `.github/workflows/deploy.yml`:
         - triggers: push to `dev` → deploy `web10-dev`; push to
           `main` or `v*` tag → deploy `web10-prod`
         - runs on `ubuntu-latest` (ephemeral GA runner)
         - steps:
           1. checkout
           2. SSH→box (`ssh -o StrictHostKeyChecking=accept-new`)
           3. `cd /opt/web10 && git pull origin <branch>`
           4. `docker compose -p <stack> --env-file env.<env> -f
              ubuntu-deployment/docker-compose.ecosystem.yml
              up -d --build --remove-orphans`
           5. wait for health (poll `docker ps` for stable state,
              max 5 min)
           6. `bash ubuntu-deployment/scripts/smoke.sh` (exits
              non-zero on failure → GitHub marks the job failed)
           7. on success: Portainer API call to register/update the
              stack metadata so the Portainer UI reflects the deploy
              (see below)
         - concurrency: `deploy-<env>` group, cancel-in-progress

       PORTAINER INTEGRATION (post-deploy register): Portainer is
       just a UI on top of Docker — it watches the Docker socket.
       when you `docker compose up` on the box, Portainer SEES the
       containers automatically (they show up in the "Containers"
       view). to also have the stack appear in the "Stacks" view
       (for UI management: restart, logs, env-var editing), call the
       Portainer API AFTER a successful deploy to create/update the
       stack entry. PREFERRED approach: FILE-BACKED stack via
       Portainer API — point at the compose file on disk
       (`/opt/web10/ubuntu-deployment/docker-compose.ecosystem.yml`).
       this gives Portainer full stack management (restart, rebuild,
       logs, env-var editor) WITHOUT GitOps polling. the deploy
       script calls `POST /stacks/create/standalone/file` (or PUT to
       update) after docker compose succeeds. keeps Portainer as the
       management UI without making it the deploy trigger.

       DISABLE GITOPS: remove the `autoUpdate` field from existing
       stacks (or delete and recreate as file-backed). the GitOps
       polling is the source of the "retrying" loop — it must be
       killed. `deploy-stacks.py` currently sets `"autoUpdate":
       {"interval": "5m"}` on create; the new deploy script must
       create file-backed stacks (no autoUpdate) or update existing
       stacks to disable autoUpdate via `PUT /stacks/{id}`.

       SECURITY: the SSH deploy key is scoped. even if compromised,
       it can only run the deploy script (command restriction in
       authorized_keys). the deploy script runs as the non-root SSH
       user (jacob), who is in the docker group. no sudo, no root.
       the key is rotated by replacing the GitHub secret and the
       authorized_keys entry.

       ROLLBACK: if smoke test fails, the workflow can automatically
       rollback: `docker compose -p <stack> down` then `docker compose
       -p <stack> up -d` to restore the previous image (Docker keeps
       the old image until the next build). or more reliably: tag
       images at deploy time (`<stack>:<sha>` and `<stack>:previous`)
       and rollback is `docker compose up -d --no-build` with the
       previous tag. the smoke test failure is visible in GitHub
       Actions with full logs — the operator can decide rollback vs.
       fix.

       RATIONALE: this is the standard pattern for teams that want
       CI visibility without self-hosted runners. the SSH-from-
       ephemeral-runner pattern is used by Railway, Render, and
       Fly.io internally. the advantage over Portainer GitOps:
       instant feedback (not 5-min poll), failure is visible in
       GitHub (not a silent retry loop), the smoke test is part
       of the deploy (not a separate manual step), and concurrency
       control prevents concurrent deploys from fighting.

       BLOCKS: none — can start immediately. depends on the operator
       generating an SSH key pair and adding the private key as a
       GitHub Actions secret. the operator must also install the
       public key on the box with the command restriction.
  [✓ 1.0.100] E7. SDK npm publish flow (with C2/D18): cd.yml publishes sdk/
      to npm on a v* tag today — confirm it fires + the package is
      public, and decide whether a version bump on merge to dev/main
      should auto-publish so the sdk stays fresh (operator, 19.07:
      "publish the web10 sdk to npm if it updates"). small.
  [ ] E8. MOBILE ENCRYPTOR → app stores (future, operator 19.07 —
      "just dont want to lose track"): mobile/encryptor is an Expo
      app (the phone-as-keychain seed, D3/D6). when it's ready,
      deploying to the Apple App Store + Google Play becomes real
      work: expo eas build + submit pipeline, store listings,
      signing certs/provisioning, review. NOT now (post-M0, gated on
      the encryptor actually doing e2e key management) — parked here
      so it's on the board, not lost.
  [✓ 1.0.71] E9. DEPLOYMENT STATUS PAGE (operator, 19.07 — iterated from
      "portainer should alert github" down to what's actually
      wanted: "all i need is an easy way to know we are deployed, a
      url i can visit ... the version, the commit, the date, the
      PR"). one URL per env — status.web10.app / status.dev.
      web10.app — showing the juicy info about what's live:
        - VERSION: the top entry of CHANGELOG.md at the deployed
          commit (the repo's release number, e.g. 1.0.71)
        - COMMIT: sha + the squash title (which carries the PR #,
          e.g. "... (#130)") + author date
        - DEPLOYED AT: when the stack last updated
        - per-service health dots (reuse scripts/smoke.sh checks)
      preferred design — ZERO new machinery: the stacks already
      rebuild from git on every change (GitOps), so add a tiny
      static status page BAKED AT BUILD TIME (a build step reads
      git sha/date/title + CHANGELOG top and emits status.html/
      status.json; serve via a minimal nginx container or an
      existing frontend's public dir) + one NPM vhost + DNS record
      per env. every auto-redeploy refreshes it by construction —
      no poller, no github token, works for VPN-only dev too.
      (portainer's API does track the deployed sha — verified — so
      a box-side poller remains the fallback design if the
      build-time stamp proves awkward.) json endpoint doubles as
      the version marker future tooling can check. below PRIORITY
      ZERO — do after A7/B6.

WHAT MUST STAY SEQUENTIAL (don't try to parallelize these)
----------------------------------------------------------
- A1 -> A2: both churn api internals; ferretdb testing against a
  moving pydantic target is pain for nothing
- B1 -> B2 -> B3 -> B4: same codebase, one lane, in order
- wave 0 before A1: tests exist before the upgrade they referee
- C2 -> C3.5: the scaffolder's templates are built on the new sdk;
  scaffolding legacy-wapi apps would poison the dev on-ramp
- D1 before C4/D4-schemas/D5: everyone imports the conventions
- P12 trust & safety tooling needs B4's admin panel; but the csam/
  dmca DECISIONS (vendors, process) are d1-style writing -- can
  happen any time, and must before M1 ships hosted media


INTEGRATION MOMENTS (when lanes must sync)
------------------------------------------
M0 "stands on its own"   = A3 + B3 + D4 converge. first real
                           cross-lane assembly; expect a dedicated
                           integration branch + a day of glue.
M1 "import your life"    = C1 + C4 + D5 + M0 node.
M2 "the first creator"   = B4 + A5 + custom domains (A3 packaging).
M3 "the network"         = encryption integration (A rtc + C2 sdk
                           crypto + D3 keychain app) + federation
                           polish. the last big merge.


CURRENT CONDUCTOR BOARD (as of 23.07.2026 — keep this section fresh)
--------------------------------------------------------------------
PRIORITY ONE (plan.txt, top — supersedes 19.07's priority zero,
whose baseline chain A7/B6/D16.1/D17 is merged): THE KILLER APP,
PROUD AND WORKING, before anything else. the only question a task
must answer: "does this make web10-social something the operator
opens on his phone, in front of someone, and is proud of?" (D29)
[✓ 1.0.140] ws-G1: run THE GAUNTLET (plan.txt top, 8 steps) against dev;
          write the honest fail list (docs/gauntlet-23.07.2026.md);
          fix in order of demo impact. lane D owns app fixes, lane A
          owns api fixes, lane B owns the auth/consent seam.
          HONESTY NOTE (23.07): the 1.0.140 run was a DESK gauntlet —
          api probes + code audit; its own method line says "NOT a
          real phone." the operator's actual phone passes (22-23.07)
          found what the desk run missed (legacy D24/D25,
          D-profile-media-refresh, the trending-card theater). the
          tick stands for the fail list, not for the bar.
  STANDING until the M0 gate: operator phone pass, weekly, a calendar
          event not an intention (timeline.md standing rules). every
          finding is filed as a lane item SAME-DAY — the 22-23.07
          loop (find → file → fix within a day) is the referee that
          works; keep it running.
  [✓ 1.0.145] ws-G2: keep the personas alive (persona-orchestration/) so feed +
         trending never demo empty. Seed script is now idempotent
         (read-before-write on every create; stable origin_id on posts +
         DMs; --cleanup removes dups from prior buggy runs; --verify reports
         state without writing). 2 new posts/persona + 4 reply DMs added.
  [✓ 1.0.145] ws-G3: encode each passing gauntlet step as a playwright journey
         in e2e/ (lane C — this is C6 continued, NOT C2).
GAUNTLET RESIDUE (in flight now — one workspace per SUB-LANE, see
the lane D sub-lane map; all gates none, run all of these 4-wide+):
  ws-D/profile:   D-profile-media-refresh (bug in shipped work — first)
  ws-D/shell:     D-url-routing, then D-login-cta (small)
  ws-D/dms:       D-dm-compose
  ws-D/discover:  D-follower-count (its lane-A aggregation touch goes
                  through .context/, not a reach-in)
  ws-D/marketing: D-trending-comments + D-join-page
  ws-A:           A11 username charset (small)
  ws-C:           harden the gauntlet journeys — encode the
                  media-refresh regression into step 6, flip step 5's
                  fixme when D-dm-compose lands. journeys assert the
                  BAR, not the happy path (D-profile-media-refresh got
                  past step 6's save+read assertion).
                  GATE NOTE (24.07): both named tasks are gated on
                  UNMERGED work — a step-6 refresh assertion merged
                  before the D-profile-media-refresh fix turns the
                  e2e job red on dev ("nothing merges red"), and the
                  step-5 flip waits on D-dm-compose by its own text.
                  stage ws-C when ws-D/profile merges, not 4-wide now.
WEEK-3 STAGING (timeline.md aug 3-10 — staged NOW so no lane idles
when the residue drains):
  ws-E:           BOX EXECUTION remainder (AGENT-OPS.md §4.2) + the
                  off-box backup + restore drill (plan.txt KNOWN GAPS)
  ws-C/cross:     demo-node theming + seed via a REAL takeout import
                  (the founder's own IG/YT exports — first exporter
                  battle test; log every parser break, promote phase
                  9's llm-assisted mapping on the first one)
  then: low-hanging fruit by demo impact — realtime feed refresh,
          notifications, search-your-own-stuff, D-trending-knobs (the
          user-held algorithm knobs on /trending — gated on
          D-trending-comments, same files).
ON DECK — M2 PREREQUISITES (stage when the gauntlet closes; these are
plan.txt KNOWN GAPS with no lane items yet, and all three gate
creator #1): memberships wired to stripe rails (B4.5 rung-0 →
functional — owned revenue in month one is the retention lever);
hosted ops floor decision (a $40 vps + one-container node + caddy is
enough — pick it before the first yes); t&s gate (dmca/csam vendors +
process decided before any hosted community posts media).
PARKED until the gauntlet passes (do NOT pick up — they read as an
infra company, not a social platform): C2 sdk rewrite, C3 mcp,
C3.5 create-web10, D11 ux telemetry, A9 transcode pipeline, E4
provisioning, E8 mobile store submission, docs/knowledge prose
beyond fixes. (D21 media polish + D22 vertical feed are NOT
parked — they are gauntlet-step-2 / demo-impact work.)

previous board (19.07, for history — all merged):
  ws-A: A7 connect the real host mongo — [✓ 1.0.72]
  ws-B: B6 authenticator revamp — [✓ 1.0.72] (+ B7 [✓ 1.0.76])
  ws-C: C6 e2e deep sweep — [✓ 1.0.74]
  ws-D: D17 dev docs [✓ 1.0.72] + D16 app store [✓ 1.0.136 —
        curation landed 23.07]

  ws1: [✓ 1.0.65] B5 level-up ui/ to design.md (lane B — ui/**) — merged
  ws2: [✓ 1.0.64] D12 level-up web10-social to design.md (lane D —
       marketing/web10-social/** only) — merged, see decisions.md D24
       for the alternative.png asset correction (shared with D13).
  ws3: [✓ 1.0.63] D13 rebuild marketing-ui on the design system, bulma
       out (lane D — marketing/marketing-ui/** only) + partially pays
       the shared brand asset debt (design.md §3) — icons + og-image
       done and dropped in .context/brand-assets/ for B5/D12 to apply;
       svg vectorization still open (no tracing tool available).
  ws4: E5/E3 REPO side merged in 1.0.66 (one parameterized
       docker-compose.ecosystem.yml for dev/prod incl. social +
       marketing, env.{dev,prod}.example, edge NPM stack, runbook
       in README.md). staging as an env was CUT 19.07 (dev + prod
       only). what's left is BOX EXECUTION, the full ordered
       migration in AGENT-OPS.md §4.2: stop legacy caddy → edge NPM
       stack → dev/prod DNS (dev → LAN ip, DNS-01 certs) →
       web10-dev + web10-prod stacks + NPM hosts → smoke tests →
       decommission the legacy *.staging stack/records/caddy.
       B5's ui origins fix is MERGED (1.0.65) — a fresh stack build
       serves per-env origins; only social still needs D14. operator
       prerequisites: docker group for the ssh user, rotate the CF
       token exposed in the world-readable Caddyfile.
ws5: C6 e2e deep sweep + bug hunt (lane C — e2e/** only; local
        stack, no deploy gate). signup-as-a-test + persona seed
        fixtures; the honest bug list is the deliverable.
   ws-new: [✓ 1.0.86] D-homepage-feed. Social-media-style tabbed feed
        preview on the marketing-ui homepage. Placeholder content with
        For You / Following / Trending tabs, post cards with avatars,
        media placeholders, engagement counts. Sits between hero and
        reach-gap section. Ready for live backend content.
    [✓ 1.0.130] D-trending-card: TrendingCard component for marketing-ui — media-forward
         card with rank badge (#1 gold, #2-3 silver, #4+ brand), heat glow
         (engagement_score → violet glow intensity), author row, content
         truncated to 3 lines, tags as chips, engagement bar. Hover: 2px lift
         + glow intensify. Skeleton shimmer matches card shape.
         Gate: none. Owns: marketing/marketing-ui/src/components/FeedPreview.tsx
         (new TrendingCard, TrendingSkeleton exports).
    [✓ 1.0.130] D-trending-grid: /trending page layout — responsive grid (1/2/3 col),
         #1 featured card spans 2 cols, topic filter chips (sticky, horizontal
         scroll), hero statement ("What's actually trending. No algorithm.").
         Gate: D-trending-card. Owns: marketing/marketing-ui/src/pages/Trending.tsx
    [✓ 1.0.130] D-trending-sidebar: /trending sidebar (desktop only, wide screens) —
         "🔥 Top 10" ranked list, compact entries, click scrolls to card.
         Gate: D-trending-grid. Owns: new TrendingSidebar.tsx + Trending.tsx
    [✓ 1.0.130] D-trending-polish: /trending loading + empty states — shimmer skeleton
         grid (matches layout), empty state story beat, "Load more" button.
         Gate: D-trending-sidebar. Owns: Trending.tsx + FeedPreview.tsx
     [✓ 1.0.149] D-comments-ledger (social-side prerequisite; decision D32):
         `createComment` (web10-social/src/data/comments.ts:36) mirrors to
         /public/entries UNCONDITIONALLY (like createReaction at
         reactions.ts:35) — no per-post visibility gate; comments are public
         by default (D32). fires the Comment schema (feed.ts:33) with
         payload.action='comment' (what documentdb.py:828 counts). ALSO: the
         `comments` service gains anon-read whitelist in serviceTerms.ts
         (matching public_posts) so any logged-in user reads comments on any
         post via CRUD, not just the post author; AND deleteComment mirrors
         to DELETE /public/entries/{id} (author-only) so removed comments
         drop out of the count. fixes the always-0 trending comment count +
         the test_discovery.py:645 mock that hid it. Gate: none. Owns:
         web10-social/src/data/{comments,serviceTerms}.ts + the FeedScreen
         call site.
     [✓ 1.0.149] D-trending-link: TrendingCard → deep-link to the author's profile on
         web10-social (`${SOCIAL_ORIGIN}/u/<author>`); until social gains a
         per-user profile route (lane-D sibling — App.tsx only has my-bio,
         flagged in gauntlet 1.0.142), link to social's root so the click still
         navigates instead of dead-clicking. Gate: none (degrades to root
         link). Owns: FeedPreview.tsx + Trending.tsx.
     [✓ 1.0.149] D-trending-share: Share2 button has no onClick → navigator.share +
         copy-link fallback (no auth). Gate: none. Owns: FeedPreview.tsx.
    [ ] D-trending-comments: MessageCircle only fires a funnel event → expand
         an inline comment panel (anon read via PATCH /public/entries, which
         is anon-OK per public.py:42; compose auth-gated → funnel-to-social).
         Gate: D-comments-ledger (nothing writes comments to the ledger today,
         so the read returns [] until the social-side mirror lands). Owns:
         FeedPreview.tsx + Trending.tsx.
    [ ] D-trending-knobs (sub-lane D/marketing): THE ALGORITHM IS YOURS —
         /trending gains synth-styled rotary knobs (recency / likes /
         comments weights, time-horizon half-life, power-mean "character"
         p) that re-rank the grid LIVE, client-side, zero network calls
         per twist. knobs are STEPPED (6 detents) → a knob state
         serializes to a shareable URL mix code (#mix=40213) and gives
         v1 a finite cache keyspace. full spec incl. the math (set-
         independent saturating normalizers so client and server score
         identically), presets (Newest / Most loved · all time /
         Balanced), the accuracy staging (v0 fetch-whole-index = exact
         today; v1 when the corpus outgrows one fetch: true db search
         on cache miss → top-~100 per detent combo cached w/ short TTL,
         debounced local-then-settle — lane-A item then, cache tech
         decided then, NO redis before v1), the tag-scoped-knobs future
         note, and the D20 guardrail (the SOCIAL feed stays
         chronological — this is pitch theater on the marketing surface)
         in plan.txt PHASE 5.5. new lib/powerMean.ts is pure + unit-
         tested (p→±∞ limits, p=0 geometric mean, weight-zeroing).
         GATE: D-trending-comments merged (same files: Trending.tsx /
         FeedPreview.tsx — in flight in ws-D/marketing now). owns:
         marketing/marketing-ui/**. acceptance: design.md §12
         (screenshots desktop + 375px); knob twist reshuffles with zero
         network requests; "Newest" is pure reverse-chron regardless of
         likes, "Most loved · all time" ignores age.
     [✓ 1.0.149] D-trending-reactions: like + repost are fake local increments
         (Trending.tsx:98) → real toggle via /public/entries mirroring
         reactions.ts; signed-out funnels to social. Gate: none. Owns:
         FeedPreview.tsx + Trending.tsx.
[✓ 1.0.93] A5.5. DISCOVERY API — the public layer (Lane A, api/**):
         discovery index (web10.discovery_posts, write-protected,
         background task), schema registry (web10.schemas, CRUD with
         author enforcement, provider.uuid6 IDs), public ledger
         (web10.public, validated entries), discovery endpoints
         (/discover/posts, /users, /search, /topics, /post/{user}/{service}/{id}),
         engagement aggregation (cached counts on index, schema-agnostic).
         See PHASE 5.5 in plan.txt and docs/discovery.md.
    [✓ 1.0.92] D5.5. SOCIAL APP PUBLIC LAYER — split posts into public_posts /
         private_posts, default terms on signup, register default schemas,
         wire reactions to /public/entries, wire feed to /discover/posts.
         See PHASE 5.5 in plan.txt.
    [✓ 1.0.89] persona-orchestration/ — 5 live-testing personas with seed scripts,
         first-week content plans, cross-follows, posts, comments, DMs, and
         reactions. powers C6's e2e persona fixtures and makes the platform
         look alive for dev testing + demos.

  merge-order notes: B5/D12/D13 don't gate each other; merge each as
  it passes design.md §12. D13 owns the cross-app asset generation
  (svg marks, favicons) but replacing assets INSIDE ui/ is B5's to
  apply — D13 leaves the files + a .context note, doesn't reach in.
  C2/C3/C3.5 still parked until after M0.
  (A1/B1/B2/B3/B4/B4.5/C1/C5/D1/D2/D2.5/D3/D4/D5–D10/E1-infra/E2
  are DONE — don't re-pick.)

LANE CI — github actions optimization (owns: .github/workflows/**, cross-cutting)
   goal: reduce from ~$90/month overage to within the 2,040 min free tier.
   each item is a small, independent branch. no lane conflicts.
   [✓ 1.0.99] CI-bun-cache: add bun install caching to js-ci.yml (actions/cache
       keyed on bun.lock + bun cache dir). cuts each js job ~50%.
   [✓ 1.0.99] CI-push-triggers: removed push triggers from all CI check
       workflows (js-ci, api, docker, e2e, marketing-api). code already
       passes PR checks — re-running after merge wastes ~60+ min/merge.
       deploy.yml keeps push (it actually deploys). saves ~80% of CI.
   [✓ 1.0.99] CI-path-filter: per-package path filtering in js-ci.yml so
       touching ui/** only runs the ui job, not all 6 packages.
   [✓ 1.0.99] CI-single-arch: drop linux/arm64 from cd.yml image builds.
       saves ~50% on CD minutes.
   [✓ 1.0.99] CI-small-runners: use ubuntu-latest-small for changelog check.
[✓ 1.0.128] CI-e2e-push-only: skip e2e.yml on pull_request, run only on
        push to dev/main. saves ~15 min per PR.
   [✓ 1.0.99, audit 1.0.140] CI-single-arch: drop linux/arm64 from cd.yml
       image builds if no self-hosters need ARM. saves ~50% on CD minutes.
       AUDIT (23.07, jacoby149/ci-cost-trims): cd.yml + docker.yml on
       origin/dev publish `platforms: linux/amd64` only — arm64 was dropped
       in 1.0.99 (commit 8b2e5aca) and never re-added. README.md +
       ubuntu-deployment/README.md + AGENT-OPS.md document NO arm64/
       aarch64/Raspberry-Pi operator (only "self-hostable" generically; the
       documented deploy target is Ubuntu-on-Proxmox x86_64). An operator
       needing arm64 runs `docker compose up --build` locally — the ghcr.io
       prebuilt images don't gate them. No workflow code change required;
       the trim is in target state on dev.
   [ ] CI-small-runners: use ubuntu-latest-small for lightweight jobs
       (changelog check, js typecheck). docker/e2e builds stay on
       ubuntu-latest.
       BLOCKED (23.07, audit 1.0.140): this org's GitHub plan does not
       provision the `ubuntu-latest-small` runner label. commit 196d908e
       reverted ubuntu-latest-small because it "queued indefinitely (empty
       runner_name, cancelled after ~20min) and showed 'pending' forever
       on every PR"; changelog-check workflow was then removed entirely in
       a3712077 ("a dedicated Actions VM per PR just to run a diff+grep
       isn't worth the wall-clock"). Applying ubuntu-latest-small to any
       PR-gated check (the js.yml typecheck is the only remaining
       candidate) would re-regress the same queue-forever → every JS PR
       red, which violates the "both trims land green" acceptance at the
       top of this lane. Re-open when the operator moves the github org to
       a paid plan that provisions the `ubuntu-latest-small` label (or
       replaces it with a self-hosted small runner). NOT TICKED: the trim
       is infeasible in the current plan, not done.

   from here on: pull the top unticked item of whichever lane's queue
   is unblocked. lanes A and B are long marches; C and D are a stream
   of short branches that keep two slots busy without touching A/B
   files.
