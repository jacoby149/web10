parallel execution
==================

higher-level companion to plan.txt. that file says WHAT and WHY; this
file says what can happen AT THE SAME TIME. target: conductor.build
running 4 workspaces/branches in parallel without merge hell.

the two rules that make 4-wide parallelism safe:
1. LANE OWNERSHIP: each lane owns its directories. api/app/** belongs
   to lane A. auth2(ui)/** belongs to lane B. new top-level dirs
   (media/, sdk rewrite, mobile/, docs) can't conflict with anything.
   docker-compose.yml + settings are lane A territory -- other lanes
   that need a compose change queue it as a note for A, don't edit.
2. MERGE SMALL, MERGE OFTEN: branches live days, not weeks. the
   permission test suite (wave 0) is the seatbelt that makes frequent
   merging safe.
3. EVERY BRANCH UPDATES THE CHANGELOG: add a CHANGELOG.md line for what
   you changed (newest at top), tick the plan.txt item you completed,
   tick your lane item in THIS file's queues below, and keep
   CLAUDE.md/GLOSSARY.md/decisions.md true if you moved them.
   CHANGELOG.md is lane-neutral (append at top) so it rarely conflicts;
   if it does, it's a trivial resolve.
4. NAMING (decisions.md D33): decisions.md owns the bare "D-nn"
   numbers (D1-D32, referenced throughout plan.txt). lane/board items
   use slugs ("D-url-routing", "D-comments-ledger"). the legacy
   numbered lane items (D21-D25, ticked) stay as history — never mint
   a NEW bare D-nn lane item; the collision already skipped one number
   and made "D21" mean three different things.
5. BITE SIZING (operator, 27.07 — the workhorse agents are Qwen-class
   27B: sharp SWEs, small windows): ONE BITE = ONE PR ≈ 20-40 focused
   minutes — a couple of files, one seam, one acceptance check an
   agent can hold in its head. if an item's description needs an
   "AND", it is probably two bites: split it with a BITES: breakdown
   inside the item and hand agents ONE bite per kickoff. chains
   (ws-D/profile etc.) are QUEUES of bites — an agent takes the next
   bite, merges, then takes the next; never "do the whole chain" in
   one branch. big items keep their slug as the umbrella; bites get
   -a/-b suffixes or their own slugs. the digestibility IS the
   throughput: a bite that merges beats a branch that balloons.

STATUS KEY (check before starting anything — don't redo merged work):
  [✓ x.y.z] = merged, see that CHANGELOG.md entry.  [~] = in flight in
  another workspace.  [ ] = open. this file is ticked on merge, so also
  scan the top of CHANGELOG.md for anything newer than the ticks here.


WAVE 0 — the seatbelt (do this alone, merge fast, ~single branch)
-----------------------------------------------------------------
STATUS: PARTIAL — the unit layer landed (1.0.21, expanded to 205
tests in 1.0.31) and the github actions ci skeleton landed (1.0.32:
api/js/docker/changelog/cd workflows, visibility-first). 1.0.36
repaired the pipeline so EVERY existing suite is actually wired and
reporting red on failure (api 205, ui 43, sdk 48, social 172,
encryptor 55, marketing-api smoke). 1.0.40 landed the endpoint-level
permission-matrix suite (45 new tests through FastAPI routes: auth
flows, CRUD, aggregate, star protection, forged-token rejection,
scoped-token enforcement, metering/billing, certify, system — 280
total api tests green). 1.0.49 landed the small security fixes (CORS
tightened, bare excepts removed, provider URL SSRF guard). still OPEN:
pytest ports of the old 57-test exporter mapper suite (marketing-api).
The js workflow's typecheck/build escape hatches are closed (1.0.103).

before running 4-wide, land the thing that protects all 4 lanes:

  [seatbelt] github actions ci + the pytest permission-matrix suite
  written against the CURRENT api (cross-cutting section of plan).
  the ci/cd pipeline is now specced in full in plan.txt
  ("CROSS-CUTTING — ci/cd"): the skeleton (path-filtered api/js/
  docker jobs) is landable TODAY against the 1.0.21 unit tests --
  it does not wait for the endpoint suite. checks REPORT pass/fail
  only; merging stays a human call (branch protection is a
  deliberate later flip once the suites earn trust). workflow
  ownership follows lane ownership: one workflow file per lane
  under .github/workflows/, a lane edits only its own yaml (the
  shared reusable js workflow is wave-0/ws1 territory).
  every later branch -- especially the P0 dependency upgrades -- gets
  judged by these tests. writing tests before upgrading deps is what
  turns "did pydantic v2 break auth?" from a mystery into a red X.

  also fine to slip in here: the tiny security fixes (CORS, bare
  except, provider url validation) -- small diffs, huge value, and
  they want to exist BEFORE the refactors bury the context.

  the permission suite must include the security-invariant tests
  (plan.txt I1-I5): forged-token rejection, no-authz-on-unsigned-
  decode, cross-collection-access-impossible. these are the seatbelt's
  seatbelt -- they catch the confirmed federation bug (HS256 -> RS256)
  and stop any lane from regressing it. the RS256 federation fix
  itself is a LANE A item (A1/early A), gated by these tests.


THE 4 LANES (each is an ordered queue; top of each = in flight now)
-------------------------------------------------------------------

LANE A — api / backend        (owns: api/**, docker-compose, settings)
  [✓ 1.0.11] A1. P0 python: uv + fastapi/pydantic/pyjwt upgrades
      (landed BEFORE the wave-0 endpoint suite — that referee is
      still owed; see wave 0 status above)
  [✓ 1.0.39] A2. P1 ferretdb/documentdb switch + compose profiles + rename mongo.py -> documentdb.py
  [✓ 1.0.40] A3. P3 setup wizard api side (setup mode, /setup, key generation,
      node policy config) + one-container packaging (multi-stage
      dockerfile -- needs B2's build output, coordinate the handoff)
  [✓ 1.0.38] A4. P6 aggregate endpoint (the 5th verb, sandbox validator;
      per-stage metering through charge(); wapi.aggregate in legacy sdk)
[✓ 1.0.58] A5. P4 metering events (per-request event records for analytics)
  [✓ 1.0.56] A6. D21 strip user billing → quotas: remove user-facing
      plans/subscriptions/per-account stripe; repurpose credits/
      space + charge() as OPERATOR-SET per-user quotas (credits =
      rate/abuse throttle, space = storage caps incl. imports).
      stripe stays for the creator economy only (memberships,
      rails). keep the permission-matrix tests green through it.
  [✓ 1.0.72] A7. connect the node to the EXISTING production MongoDB on the
      ubuntu box (~208 real users + historical data + registered
      apps from the original web10) instead of the empty ferretdb
      the stacks ship with. IMPORTANT (operator, 19.07): this mongo
      runs NATIVELY on the host — it is NOT a docker container and
      that's fine, leave it as-is. so the node's containers reach it
      NOT by a compose service name but via the host: DB_URL points
      at the host gateway (add `host.docker.internal:host-gateway`
      to the api service's extra_hosts, or use the box LAN ip
      192.168.8.25) : mongo's port (27017), assuming it binds a
      reachable interface — confirm the bind + any auth creds, keep
      them in the box .env, coordinate the compose var/extra_hosts
      with lane E. we already speak the mongo wire protocol
      (documentdb.py), so this is mainly config + verification, not
      a rewrite: confirm the {service,body} + star-record shape
      matches what our code expects (older records may predate
      to_gui/to_db conventions — write a read-only audit first,
      then a migration/adapter if the shape drifted), and make sure
      star protection + scoped queries behave against real data.
      gate: do NOT point PROD at the real mongo until a login works
      against a COPY in dev (dump/restore into web10-dev's db, or a
      read-only connection). deliverable: dev logs in as a real
      historical user; total-users + total-apps counts computed from
      the real data and surfaced like the original web10 (feeds the
      D16 app-store stats); and the honest report of any shape drift.
      unblocks B6 (real login to test against) and D16 (real apps).
    [✓ 1.0.92] A8. P1 server-side record metadata — invariant I6: every record
       gets `_author` (token's username), `_source_node` (the provider
       that minted the token), and `_created_at` (server time at the
       destination node) injected by the API on create, immutable on
       update. the client cannot set or forge these fields. on cross-node
       replication, the receiving node stamps its own `_created_at`
       (arrival time) while preserving the original `_author` and
       `_source_node`. this stops sender impersonation on DMs, posts,
       comments, reactions. implementation: in documentdb.py `to_db()`,
       inject `_author`, `_source_node`, `_created_at` from the
       authenticated token + server clock. add to `q_t()` / `u_t()` to
       block client from overwriting via update. expose in `to_gui()` on
       read. audit the whole cross-node path (certify, token handoff,
       remote CRUD) to verify it works correctly with multi-node traffic.
       gate: permission-matrix tests must cover forged `_author`
       rejection. unblocks: safe DMs, safe feed attribution, audit trail.
  [ ] A9. video transcode pipeline (plan.txt phase 8.5, video v2).
      ffmpeg worker in a compose profile ("transcode"), queued off
      upload-confirm for PUBLIC video only: h.264 ladder -> hls
      segments + manifest + server-side poster/thumbnail -> patch the
      media record (hls_manifest_url, duration, dimensions).
      private/encrypted blobs never touch it (I4). includes the
      phase 5 size-accounting straggler (media bytes vs the space
      plan) and lifting D21's video-v0 caps. gate: PARKED until the
      gauntlet passes (D29 — a transcoder reads as infra) AND D21's
      video v0 is merged; the app proves mp4 playback before the
      node earns a transcoder.
  [ ] A10. P4.5 OPERATOR CONSOLE api (plan.txt phase 4.5 — the
      auth.web10.app/admin backend). the current admin surface is the
      /config form (admins list, app-store approvals, credentials) —
      operator-editable secrets, NOT operator-runnable node. this lane
      adds the moderation + analytics + audit endpoints under it:
      - POST /users/admin (search/paginate), PATCH /users/admin/{u}
        (status: active/suspended/banned, append-only history),
        DELETE /users/admin/{u} (hard delete + tombstone, 24h undo),
        POST /users/admin/{u}/reset_password, POST
        /users/admin/{u}/reveal_phone (audit-logged).
      - GET/PATCH /policy (typed policy record: signup_gate stack,
        beta code, phone/email/captcha toggles, signup + write rate
        limits, blocked_usernames/phones/emails, invite_codes CRUD).
        enforcement moves INTO auth.py + is_permitted (signup rate
        limits, captcha gate, blocked-list checks, write throttle via
        credits) in the SAME PR — the ui form must never be a no-op.
      - POST /reports (user-facing flag), POST /reports/admin
        (queue), PATCH /reports/admin/{id} (status). reporter is in
        the record for the operator, hidden from the reported user.
      - POST /analytics/admin (signups_over_time, dau/wau/mau,
        retention_cohort, request_volume, storage_growth — daily
        snapshot cron, not a runtime scan — per_app_usage,
        top_services). built on A5's event store; if the store
        isn't queryable yet, ship the cheap ones (signups_over_time
        from user records, storage_growth from the snapshot cron)
        and stub the rest behind a TODO + fixtures for lane B.
      - POST /admin/counts (the Overview tab's one-shot:
        users/pending_apps/open_reports/health).
      - POST /audit/admin (paginated, filterable read). writes are
        not an endpoint — every admin mutation in the items above
        appends one immutable row (ts, actor, action, target, before,
        after, reason). the audit append is the load-bearing call of
        this lane: ship it in the FIRST slice, not the last, so every
        later admin action inherits it.
      gate: PARKED behind the gauntlet (priority one — D29: operator
      tooling reads as infra until the social app is lovable). start
      the slice immediately after the gauntlet goes green: users +
      policy + reports + audit first (the safe-growth triangle),
      analytics second. ugh-level arm is the phone-reveal endpoint
      being itself audit-logged — recursive but mandatory.
      permission-matrix suite MUST extend: each admin endpoint rejects
      a non-admin token; every status transition writes exactly one
      audit row; forge-`_author` rejection (A8) still holds through
      the new endpoints. coordinate endpoint shapes with lane B's B8
      BEFORE coding — B8 builds against fixtures, hand off the JSON
      contract first. unblocks: B8, phase 12 trust&safety (the audit
      log + a future token-revoke are what make "ban takes effect now"
      possible).
  [✓ 1.0.155] A11. USERNAME CHARSET — premise corrected 24.07 (code audit,
      1.0.154): the api does NOT reject hyphens. kosher()
      (api/app/endpoints/auth.py:127) has allowed them since #89 —
      `c.isalnum() or c in ["-"]` — so the hyphenated personas
      (solar-flare-69, noodle-empress) ARE signup-able names and the
      original "demo shows names a real signup can't have" concern is
      moot. the REAL gap is the opposite: kosher() is too LOOSE for a
      string that names a mongo collection (I3 boundary) and rides in
      urls/media paths. python isalnum() is unicode-aware, so CJK/
      greek/etc usernames pass; uppercase passes (case-sensitive
      collections + any case-insensitive lookup elsewhere = twin-
      account risk); no length cap; bare "-" and leading/trailing
      hyphens pass. task: pin the charset explicitly — lowercase
      ascii + digits + interior hyphens, bounded length (suggested
      ^[a-z0-9](?:[a-z0-9-]{1,28}[a-z0-9])?$; decide exact bounds and
      record why) — audit every place a username is parsed/regexed
      (auth.py, documentdb.py q_t/u_t + collection addressing, media
      paths, endpoints/*) while tightening, extend the permission-
      matrix suite with BAD_USERNAME cases (unicode, uppercase,
      edge-hyphens, over-length, reserved), and make the error
      message state the actual rule. SIGNUP-TIME validation only:
      existing users (the 208 real + personas, any legacy uppercase)
      are grandfathered — login/lookup must keep working, never a
      migration. the signup form's inline charset message is a small
      lane B seam (leave a .context/ note). small item; gate: none.
  [✓ 1.0.164] A12. PUBLIC MEDIA READ PATH — api side of decisions.md D35 (the
      shape is DECIDED there; don't re-litigate). cross-user media is
      dead today: the `media` service registers no read whitelist
      (web10-social serviceTerms.ts — owner-only), and both
      `POST /{user}/read` (media.py:83) and the media-records list
      (media.py:101) gate on is_permitted(token, user, "media",
      "read") — a follower can never presign an author's photo, and
      another user's avatar dies at issue time; on dev's private
      bucket (D23) every cross-user image 403s. this blocks
      D-feed-avatar-resolution, the D-user-profile-media grid half,
      and follower-side image rendering for D-post-delivery. build:
      (1) MetadataCreate/upload-confirm accepts optional `service`
          ("media" default | "public_media" ONLY — validate against
          exactly that allowlist; the body must never name an
          arbitrary collection) and writes the metadata record there.
      (2) ReadRequest + the records-list route accept the same
          optional `service`; is_permitted checks the REQUESTED
          service. quota/space accounting unchanged.
      api/** + tests only. permission-matrix additions: non-owner
      presign on public_media allowed once terms grant it, still
      denied on `media`; service allowlist rejects any other value;
      star protection untouched. the client half (serviceTerms
      public_media anon-read entry, uploadMedia targeting,
      resolveMediaRefs passing service) is D-public-media-client,
      lane D, gated on this — leave the JSON contract as a .context/
      note. gate: none.

[✓ 1.0.178] A13-discovery-terms — PRIORITY (26.07 operator pass): real users'
      public posts still don't reach the discover board (marketing
      /trending AND the social-app Discover, even sorted newest).
      1.0.171 fixed HALF of it: service_allows_anon (documentdb.py) now
      matches the app's `.*` anon-read whitelist via regex instead of an
      exact "anon" string. The REMAINING half is that the anon-read term
      for `public_posts` often DOESN'T EXIST for a real account, so
      there's nothing to match:
        - records.services_record() (api/app/services/records.py) ships
          an EMPTY whitelist on signup. `public_posts` is not a default
          service.
        - the ONLY thing that creates the public_posts anon-read term is
          the app's SMROnReady (sdk compat.ts:77) → SIR consent in the
          auth portal — which only posts the SMR WHILE the portal child
          window is open (login/signup). An already-signed-in user is
          never re-prompted, and a server-only change (like 1.0.171)
          triggers no SMR because the CLIENT contract (serviceTerms.ts)
          didn't change. So the operator "gets no SMR" and the term
          never lands → not indexed AND anon GET of the post 403s.
      Fix (recommend a + b together; c is the deeper D30 option):
        (a) provision the canonical PUBLIC service terms at account
            setup — add `public_posts` (anon read: {username:'.*',
            provider:'.*', read:true}) to the default account services
            in records.services_record()/config.py, so every new account
            is discoverable without an interactive SMR. + a one-shot
            migration/backfill that writes the term for EXISTING accounts
            that lack it.
        (b) a discovery BACKFILL: iterate user collections, read
            public_posts, upsert each into the discovery collection
            (reuse upsert_discovery_post) so posts made BEFORE the fix
            appear. 1.0.171 only indexes on CREATE going forward.
        (c) [design, decide first] treat `public_posts` as anon-read +
            discovery-indexed BY COLLECTION DEFINITION (D30: the
            collection is the boundary), removing the per-user-term
            dependency entirely. Bigger security-model call — do NOT
            ship unilaterally; write it up in decisions.md and get
            operator sign-off before (c).
      Files: api/app/services/records.py, config.py, documentdb.py
      (service_allows_anon done; add backfill + provisioning), + a
      migration entry + tests. owns api/** only. Do NOT weaken
      private_posts (owner-only) or star protection (I3). Acceptance:
      a FRESH real account posts with visibility=public → the post
      appears in PATCH /discover/posts?sort=recent within one request
      AND an anon GET of that post succeeds (no 403); an EXISTING
      account's pre-fix public posts appear after the backfill runs;
      permission-matrix + discovery tests green, ruff clean. gate: none.

[ ] A14-followers-count (api half of the dead-social-proof finding,
      plan.txt "real follower count" HONESTY NOTE 25.07 — the lane D
      half is D-ledger-mirror-fix): /discover/users' suggested_users()
      (api/app/services/documentdb.py) never computes or returns a
      followers_count field, so UserProfileScreen (/u/:username — the
      page with the follow button, the page a FAN sees) can never show
      a follower count; only your own /profile can (client-side ledger
      read). Fix: the discovery-users aggregation counts follow entries
      in the public ledger (web10.public, payload.action='follow',
      target=that user — the D34 shape countFollowers() already queries
      client-side) and returns followers_count per user; expose it in
      /discover/users' response model. Keep it aggregation-cached like
      the existing engagement counts if cost demands. owns api/** only.
      acceptance: /discover/users returns followers_count matching the
      ledger for seeded personas; a fresh follow increments it (allow
      cache lag if cached — document the TTL); discovery tests extended,
      ruff clean. gate: none (works today for persona follows seeded by
      script; real-account follows start mirroring when
      D-ledger-mirror-fix lands — the two merge independently).

[ ] A15-engagement-count-accuracy (operator, 26.07, screenshot: badge
      says 3 comments, the expanded panel renders more): the discovery
      index's cached engagement counts disagree with the live comment
      read. the badge comes from the index's engagement aggregation
      (api/app/services/documentdb.py — the cached counts around lines
      ~820-830: likes sum 'like'+'reaction', comments count
      payload.action='comment'); the panel reads live ledger entries
      (PATCH /public/entries). Audit the two paths — what each counts
      (action values, edit/delete cleanup, dedup) and when the cached
      count refreshes — then make them agree: either the aggregation
      recomputes on read/short-TTL, or the count and the panel query
      the SAME predicate. add a test that writes N comments via the
      ledger and asserts the discovery post's comment_count == N after
      refresh, and that a deleted comment decrements. related-not-
      duplicate: D-ledger-mirror-fix is about real accounts writing
      NOTHING to the ledger; this is the ledger data disagreeing with
      its own cached aggregate. owns api/** only. acceptance: the
      badge number equals the panel's comment list length for seeded
      personas on the local stack; discovery tests extended, ruff
      clean. gate: none.

LANE B — ui / frontend        (owns: auth2->ui/**, auth/**)
  [✓ 1.0.12-1.0.14] B1. P0 js: auth2 -> vite + bun + typescript
      (react 19, vitest, multi-stage dockerfiles, rtc to bun too)
  [✓ 1.0.26] B2. P2 finish auth2 features, parity audit, rename to ui
  [✓ 1.0.52] B2.5. P2.5 ui makeover in ui/: rectangles-npm -> mainstream
      stack (tailwind + shadcn/ui candidate, record in decisions.md),
      shared design tokens, story-first narrative pass. do BEFORE
      B3/B4 so wizard + admin panel are born on the new stack.
  [✓ 1.0.40] B3. P3 setup wizard screens
  [✓ 1.0.40] B4. P4 admin panel: policy config, ad records crud, analytics
      views (analytics needs A5 merged; build ui against fixtures
      until then)
  [✓ 1.0.55] B4.5. P4 the STUDIO respec (post-B2.5, on the new stack):
      youtube-studio frame, monetization menu as one-button cards
      with the YPP-style unlock ladder, amazon adapter v1 (tag
      card, render-time tagging, compliance chips, 3-sales
      countdown), memberships/tiers wiring to stripe rails.
      composer monetization affordances land in D4's composer
      (coordinate, don't reach into lane D files). this screen is
      the M2 demo -- highest-stakes ui in the repo.
      M0 SLICE OF THIS (D21): the menu SCREEN with rung-0 cards
      (memberships, amazon tag, direct deals) ships inside the M0
      window so the demo video shows the money screen -- visually
      complete + rung-0 functional; deeper wiring (unlock ladder,
      adapters) follows in weeks 5-10.
  [✓ 1.0.65] B5. level-up ui/ to the design.md standard (READ design.md
      FIRST — repo root; it is the law for all ui work). tokens.css
      migrated to design.md §13 (dark-first violet), inline styles
      out, dead vendored bulma dir deleted, SideBar.tsx className/
      style bug fixed, components/ui primitives kit, mobile bottom-
      nav shell, hub.png (apple's app store logo — trademark,
      replace) + react-atom icons swapped for the real keys marks
      (design.md §3). studio = the money screen, highest stakes.
      ALSO (urgent, the dev/prod deploys are gated on this):
      parameterize the ui's backend origins —
      ui/src/interfaces/authAdapter.ts + ui/src/config.ts hardcode
      api.web10.app/auth.web10.app for prod builds, so any deployed
      env ships a bundle calling the wrong api (see
      ubuntu-deployment/AGENT-OPS.md §4.1).
      build-time env (REACT_APP_*/VITE_* both allowed by
      vite.config.js) + Dockerfile ARG. LANDED with the names
      REACT_APP_API_ORIGIN / REACT_APP_AUTH_ORIGIN /
      REACT_APP_RTC_ORIGIN (full origins) + REACT_APP_DEFAULT_API
      (host); docker-compose.ecosystem.yml passes exactly these.
      acceptance: design.md §12 (screenshot test, pr screenshots
      desktop + 375px, tokens-only, all states designed) + an env
      rebuild can point at dev/prod domains without a code edit.
[✓ 1.0.72] B6. AUTHENTICATOR REVAMP — the ui/ auth flow is broken in
       both look AND function (operator report, 19.07: "looks
       retarded and straight up doesnt work", web10 logo on auth is
       "hella broken"). B5 restyled the shell but the actual
       login/consent flow is still wrong. this is a from-the-user's-
       seat fix, not just tokens: (1) the web10 logo renders broken —
       audit the asset path/import (design.md §3 names the real
       marks; a deployed build serves from the app root, so a
       dev-only path breaks in prod), (2) drive the REAL login flow
       end to end against dev once A7's mongo is connected — signup,
       login, the auth portal popup/redirect, token handoff to a
       consuming app — and fix what's actually broken (the c5/c6
       playwright harness + data-testid hooks exist; write the auth
       journey as a test so it can't regress), (3) make it look like
       design.md §12. gate: A7 (need real users to log in as) helps
       but the logo + obvious-broken bits can start now. acceptance:
       a human can sign up and log in on auth.dev.web10.app without
       hitting a broken screen; playwright covers the journey;
       screenshots desktop + 375px.
       NOTE: the critical functional fixes landed (wapiAuth.js
       defaultAPIProtocol->APIProtocol, token restoration on load,
       SignupForm phone arg, ForgotForm cancel, authAdapter hostname
       check, Branding restyled, logo.svg deleted). The playwright
       auth journey test against real users still waits on A7.
  [✓ 1.0.76] B7. AUTH UI: FROM-THE-PHONE QUALITY PASS — operator report
       19.07 (screenshots in the ops session): (1) a restored-but-
       invalid/expired token lands the user on an EMPTY "Your
       contracts" page with no login button anywhere — restored
       tokens must be validated (certify) before showing the
       authenticated view, and a dead token must bounce to the login
       form; (2) the header logo renders as a broken <img> on prod —
       asset missing from the deployed bundle (design.md §3 marks);
       (3) auth.web10.app does not render usably on a phone —
       B6's 375px acceptance was never verified on device. bar:
       design.md §12 (screenshot test, desktop + 375px in the PR),
       "operator would demo it to an influencer without apologizing."
       gate: none — start anytime. compare against social/marketing,
       which the operator calls the quality reference.
  [ ] B8. P4.5 OPERATOR CONSOLE ui (plan.txt phase 4.5 —
      auth.web10.app/admin). lift the existing ConfigPage.tsx into a
      real moderation console. mental model: twitch/mastodon admin
      dashboard you leave open in a tab, not a secrets form.
      - new ui mode `admin` in App.tsx's switch, served at path /admin.
        the existing `config` mode redirects to /admin#config (deep-
        link compat, sidebar/status refs still resolve); nothing 404s.
        a NotAnAdmin component (lift ConfigPage's Lock card) gates
        every admin sub-route so the gate isn't hand-rolled per tab.
        extend the interface with I.isLoadingAdmin (separate from
        I.isAdmin) so a route rendered before /am_admin resolves
        shows a skeleton, not a flicker of the "admins only" card.
      - tabs, left rail (desktop) collapsing to a top segmented control
        (mobile, 375px, never a hamburger): Overview · Users ·
        Moderation · App Store · Node Config · Audit Log.
      - Users: toolbar (search + status + sort) + paginated table; row
        click opens a right-drawer (mobile: full sheet) with identity,
        masked phone + audit-logged reveal button, quotas, registered
        apps, recent activity, action buttons (suspend/ban/unban/reset
        password/delete). every destructive action opens a confirm
        modal that requires a TYPED username match.
      - Moderation: three stacked sections — (a) the typed /policy form
        (signup_gate stack with conditional captcha/email fields, rate
        limits with "reset to default" ghosts), (b) the reports Kanban
        table (oldest first, dismiss requires a reason, opens user
        drawer from the Users tab), (c) blocklists + invite-code CRUD
        (each row shows the actor + ts + reason — never an anon block).
      - App Store: ship verbatim — lift ConfigPage's App Store Approvals
        card into components/Admin/AppStoreCard.tsx. no behavior change.
      - Node Config: ConfigPage's secrets + identity half stays as-is
        under /admin#config. the Admins card MIGRATES OUT to the Users
        tab's role assignment; the Access Policy card MIGRATES OUT to
        the Moderation tab's policy form. so secrets and who-can-touch-
        them are no longer on the same screen — a real footgun fix.
      - Audit Log: reverse-chronological table, filter by actor/action/
        date, every row expandable to the before/after JSON tree (reuse
        Contracts' renderer). immutable; no PATCH.
      - Overview: no-input dashboard — total users (+N this week),
        pending apps, open reports, node health (db ping + storage
        snapshot + 24h errors), today's signup chart. the operator's
        muscle memory is Overview -> click the red number.
      - shared primitives land FIRST in components/Admin/_shared/:
        NotAnAdmin gate, drawer/sheet, audit diff tree, analytics
        sparkline card, typed-confirm modal. six tabs must NOT each
        hand-roll their own confirm-modal — that's the worst outcome.
      gate: PARKED behind the gauntlet (priority one — D29) AND behind
      A10's endpoint-shape handoff (build against fixtures until then,
      same pattern B4 used — do NOT reach into api/ files for shapes;
      get the JSON contract from lane A first). ship in two waves:
      Wave 1 (gauntlet green) = the /admin route + Users tab +
      Moderation tab + Audit Log + the lift of ConfigPage into
      /admin#config + App Store tab. Wave 2 = Overview + Analytics
      tab (a chart dep is a lane-shared decision — check what
      marketing-ui uses before adding one).
      acceptance: design.md §12 screenshot test (desktop + 375px per
      tab in the PR), all-token-colors from the design system, and a
      playwright admin journey added to the gauntlet suite: sign in
      as bootstrap admin -> /admin -> search the user from gauntlet
      step 1 -> suspend -> write rejected -> unsuspend -> audit log
      shows BOTH transitions. that test is the regression seatbelt for
      the whole phase — it's how "we shipped moderation with no test"
      doesn't happen.

LANE C — greenfield services  (owns: sdk/, mcp/, new dirs)
  [✓ 1.0.25] C1. P5 media service (presigned urls, minio profile,
      metadata records) — landed standalone, then merged into api/
      as a router in 1.0.30/1.0.31 (standalone media/ dir is gone;
      media code now lives in api/, which is lane A territory)
  [✓ 1.100] C2. P6 sdk rewrite (typescript, fetch, typed crud; aggregate
      types land after A4 merges -- everything else doesn't wait)
  [ ] C3. P6 mcp server (thin layer over the C2 surface)
  [ ] C3.5. P6.5 create-web10 scaffolder (needs C2's sdk -- templates
      are written on the typed sdk, never legacy wapi; node story
      improves when A3's one-container node + sandbox exist, but
      --node <url> works from day one)
  [ ] C4. P10 backup integrations (drive/dropbox oauth, export/import
      archive -- format comes from D1's conventions doc)
  [✓ 1.0.59] C5. browser e2e harness (new top-level e2e/ dir + its ci
      workflow): playwright-in-docker against the compose stack,
      ~6-10 smoke journeys across ui + web10-social + marketing-ui
      (specced in plan.txt CROSS-CUTTING quality/testing). touches
      other lanes' apps via read-only flows only (data-testid hooks
      by coordination, never reach-in edits). marketing-ui smoke +
      ui auth flows are landable now; social post->feed firms up
      with D4.
  [ ] C6. e2e deep sweep + bug hunt (specced in plan.txt CROSS-
      CUTTING quality/testing, "e2e deep sweep"): C5's harness
      exists — now really exercise the ecosystem locally and find
      what doesn't work (assume plenty doesn't). expand playwright
      across the money paths + lane seams: signup -> consent ->
      grant; social post w/ media -> feed -> comment -> dm; terms
      revoke actually cuts the app off; exporter upload ->
      marketing-api job -> records; app store -> launch -> token
      handoff; studio w/ real metering. TEST ACCOUNTS are part of
      the harness: signup IS a test (fresh account per run), plus
      a seed script of persona fixtures (creator + fans + granted/
      revoked terms) for flows needing existing state — same seed
      powers dev-env wipe+reseed. no secrets in git; per-env creds
      via env files. every failure triaged: small fixes land with
      the suite, lane-owned breakage -> .context/ note + lane
      queue item, never a silent skip. deliverable = the enlarged
      suite AND the honest bug list.
  [✓ 1.0.141] C7. sdk media surface (plan.txt phase 8.5, media v0): the
      typed sdk (sdk/src) gains typed media methods — requestUploadUrl,
      confirmUpload, upload (presigned POST + confirm), and getReadUrl
      (presigned GET exchange + expiry-aware cache) matching the
      contract the web10-social wrapper lands in D23. small; no gate.
  [ ] C8-stripe-twilio-test-keys-e2e (lane C, e2e/** + compose; operator,
      27.07: "we aren't testing with stripe test api keys / twilio test
      api keys in any kind of e2e tests — we could use test keys!").
      VERIFIED GAP at HEAD: api/tests/test_stripe.py + test_twilio.py
      are pure-logic unit tests (helpers + TwiML strings, everything
      mocked — zero real API calls), and e2e/ has no stripe/twilio
      presence at all, so the MONEY path (dev pay, the memberships
      rails the M2 gate depends on) and the SMS path (signup verify,
      password recovery) have no end-to-end referee. the groundwork
      exists: settings.py already has STRIPE_STATUS + STRIPE_TEST_KEY
      — nothing ever flips it in a test env. build:
      (1) STRIPE TEST-MODE E2E: the e2e compose stack runs the api
          with STRIPE_STATUS=test + a sk_test key from CI secrets
          (GitHub Actions secret, never in git — same pattern as
          DEPLOY_SSH_KEY). playwright journey: a fan subscribes to a
          creator's membership (stripe test card 4242...), the sub
          lands, the connect transfer carries amount_percent (the 3%
          rail — assert the split, that's the business model), and
          cancel stops access. webhook signature verification gets a
          real test (stripe-cli forward or a signed test payload),
          because that's the production failure class mocking hides.
      (2) TWILIO HONEST HALF: twilio test credentials only validate
          auth + request shape (magic numbers +15005550006 valid /
          +15005550001 invalid) — they never deliver SMS. e2e asserts
          the api's verify/recovery endpoints call twilio with the
          right shape against test creds; the actual SMS delivery
          stays a documented manual pre-launch check (write it in the
          test file header so nobody thinks the green check means
          delivered texts).
      files: e2e/** (new payments spec + sms spec), e2e/docker-
      compose.yml (env wiring), .github/workflows/e2e.yml (secrets —
      workflow file = wave-0/ws1 territory, fine), api/ untouched
      (STRIPE_STATUS toggle already exists; if a code gap appears,
      .context/ note for lane A, don't reach in).
      acceptance: the payments journey runs green in CI against REAL
      stripe test mode (no mocks); the 3% split is asserted; twilio
      endpoints validated against test creds; zero secrets in the
      repo; e2e suite stays green. gate: stage with the M2-prereq
      block (memberships before creator #1 — ON DECK in the board) —
      not part of the current gauntlet fleet, but the compose/CI
      wiring (bite a) can land anytime.
      BITES (rule 5): bite a, WIRING — e2e compose flips STRIPE_STATUS
      + secrets plumbing + a trivial sk_test smoke assertion (api
      reaches stripe test mode). bite b, PAYMENTS JOURNEY — subscribe
      + 3% split + cancel (gate: bite a). bite c, TWILIO HALF +
      webhook signature test (gate: bite a; independent of b).

LANE D — docs / apps / mobile (owns: mobile/**, marketing/, examples/
                                *.md/txt)

  LANE D SUB-LANES (23.07.2026): the killer-app push made every open
  demo item lane D, which collapsed 4-wide parallelism into one
  directory — and 1.0.148 (two branches independently shipping
  DiscoverScreen.tsx, hand-reconciled) is what that costs. so the
  same two rules apply one directory deeper. one sub-lane per
  workspace, named in the PR:
    D/feed      — web10-social/src/components/Feed/** + data/posts.ts,
                  feed.ts, comments.ts, reactions.ts
    D/profile   — web10-social/src/components/Bio/** + data/profile.ts
    D/dms       — web10-social/src/components/Chat/** + data/dms.ts,
                  contacts.ts
    D/discover  — web10-social/src/components/Discover/** +
                  data/follows.ts
    D/shell     — web10-social/src/App.tsx, components/Social/
                  Layout.tsx, main.tsx, index.css, routing
    D/marketing — marketing/marketing-ui/** (as before)
  SHARED SEAMS (all sub-lanes touch, none owns): data/types.ts,
  data/wapi.ts, data/serviceTerms.ts, data/index.ts,
  components/shared/**, components/ui/**. seam rule: touch a seam
  file only to add your own exports, keep the diff single-purpose,
  first-merged wins and the others rebase fast. needing another
  sub-lane's file works like needing another lane's: leave a
  .context/ note, don't reach in.
  [✓ 1.0.18, 1.0.20] D1. conventions doc (posts/media/contacts/inbox
      schemas) + protocol spec draft. docs/protocol-spec.md,
      docs/conventions.md, docs/schemas/*.json — C4/D4/D5 now unblocked.
  [✓ 1.0.30] D2. P7 tidy: crm/mail integrated as sub-apps inside
      web10-social (CRM Rolodex + Mail inbox), old crm/ and mail/
      folders removed. 35 new tests, shared mock factory.
[✓ 1.0.51] D2.5. P2.5 ui makeover in web10-social: rectangles-npm ->
       mainstream stack (tailwind v4 + Radix UI primitives + Lucide),
       story-first pass (empty states point at exporters; kick/twitch-grade
       slick is the visual bar -- THE STORY beat 5, D20). pairs with
       D4 -- credible-in-a-creator-pitch is the acceptance bar.
  [✓ 1.0.27] D3. P11 mobile encryptor: complete rebuild landed —
      expo 52, crypto core (hkdf, ed25519/x25519, xchacha20), wallet
      persistence, tabbed UI, 55 tests. the rtc key channel + sdk
      crypto integration still waits for A/C (phase 11 proper).
  [✓ 1.0.50] D4. P8 killer app M0 slice (data layer): post + feed
      (chronological + sort dropdown ONLY -- lens/chatbox is CUT to
      later.md, D20) + profile + dms. full conventions-schema data
      layer in src/data/ (posts, feed, profile, contacts, dms,
      comments, reactions, media upload via presigned URLs). 55 new
      tests, 227 total green. screens deferred to D2.5 post-B2.5 tokens.
  [✓ 1.0.27] D5. P9 exporter core + instagram mapping (needs D1 conventions;
       client-side, so no api deps) — also includes Facebook + YouTube
       mapping, multi-platform UI with takeout checklists, 57 tests
   [✓ 1.0.28] D6. web10-social: full TS + Vite + Bun revamp + 137 vitest tests
[✓ 1.0.30] D7. P7 marketing-ui: consolidated home/ + docs/ + App Store
        into marketing-ui/ (Vite + React 19 + TS + Bun + react-router).
        App Store moved out of ui/ — public-facing discovery belongs on
        the marketing site, not the node's auth UI.
    [✓ 1.0.31] D8. P7 marketing-api + exporter migration: new FastAPI
        service for ZIP import pipeline (server-side streaming parse,
        validate, dedup, batch write) + analytics (pageview, funnel).
        Exporter UI moved from exporters/ into marketing-ui/ (now polls
        marketing-api jobs instead of browser-side ZIP parsing). Legacy
        home/ and exporters/ dirs deleted.
    [✓ 1.0.33] D9. P7 final tidy: rtc/ -> api/rtc/, docs/ ->
        marketing-ui/public/docs/. all references updated (compose,
        CI, CLAUDE.md). top-level tree clean.
  [✓ 1.0.58] D10. report-a-bug loop: feedback endpoint in marketing-api +
      "report a bug" affordance and error boundaries in social +
      marketing-ui (specced in plan.txt CROSS-CUTTING quality/
      testing). the ui/ app's button is lane B territory --
      coordinate, don't reach in. the counterweight to the thin
      e2e layer: users become the coverage, so reporting must cost
      them <=2 clicks.
  [ ] D11. ux telemetry (specced in plan.txt CROSS-CUTTING quality/
      testing -- read the surface split there before touching):
      marketing-ui funnel analytics + js error beacon via
      marketing-api; platform uis get content-free aggregate
      events + the error beacon ONLY -- no session-recording js
      on ui/ or social, ever (manifesto line). self-hosted
      posthog/openreplay deploy, if adopted, is lane E territory.
  [✓ 1.0.64] D12. level-up web10-social to the design.md standard (READ
      design.md FIRST — repo root). owns marketing/web10-social/**
      only — runs parallel to D13, disjoint dirs. this is the deck
      screenshot next to kick/twitch: wire the missing
      @tailwindcss/vite plugin, actually load inter, media-forward
      feed cards, composer that feels like publishing, profile as
      creator page, story-first empty states (point at the
      importer), finish evicting excluded rectangles/@chatscope
      legacy components, fontawesome CDN kit out for lucide.
      acceptance: design.md §12. also fixed tsconfig's missing `@/*`
      path alias (tsc -b had ~90 pre-existing errors) and the
      alternative.png-isn't-the-keys-mark bug (decisions.md D24,
      converged with D13).
  [✓ 1.0.87] D12 vibrancy overhaul (follow-up): the social flagship was
      muted — flat surfaces, no ambient light, zero interaction energy.
      design.md §4 relaxed for social: glow tokens, shimmer skeletons,
      heart-burst on like, card hover glow, gradient buttons, presence
      dots, vibrant banner, media hover zoom, animated login. 195 tests
      green, tsc clean, build clean.
  [✓ 1.0.63] D13. rebuild marketing-ui on the design system. bulma OUT
      entirely (D22), tailwind v4 + design.md §13 tokens, dark
      cinematic landing (lockup hero, tested reach-gap proof section —
      "real product screenshots" deferred, none exist yet to be
      honest about), docs readable (65-75ch prose, mono code), app
      store on house cards (first-party proof, not a catalog),
      education-era imagery + university logos deleted. brand asset
      debt for all three apps PARTIALLY paid: favicons/pwa icons +
      shared og-image from the keys mark dropped in
      .context/brand-assets/ for lanes B/D12 to apply (D13 doesn't
      reach into ui/ or web10-social/ code); svg vectorization + dark
      variants still open, no tracing tool in-environment. flagged:
      design.md §3's alternative.png does not contain the keys mark —
      needs a design.md correction (not done here, out of lane).
  [✓ 1.0.67] D14. parameterize web10-social's backend origins:
      src/lib/origins.ts reads VITE_API_ORIGIN / VITE_AUTH_ORIGIN /
      VITE_RTC_ORIGIN with the prod values as fallbacks; adapter +
      wapi wrapper + bare-username default provider use it, and the
      15 repeated cross_origins lists collapsed to one that also
      includes the serving hostname (a deploy authorizes itself).
      verified: env-arg build bakes dev origins into the bundle,
      195 tests green.
  [✓ 1.0.136] D16. APP STORE — restore it as a real, curated marketplace
      (gated on A7's mongo; the original web10 had live registered
      apps + real usage stats, so this is RESTORATION of real
      assets, not the speculative empty-catalog the D20 thread
      warned against — see decisions.md, reconciled below). the
      current marketing-ui AppStore is first-party-only "proof"
      cards; make it the real thing:
      (1) read the registered apps from the connected mongo (A7),
          not hardcoded cards.
      (2) LAYOUT: the killer app (web10-social) stays promoted up
          top — do NOT bury it; registered third-party apps go in a
          lower section. one hero, then the catalog.
      (3) CURATION: web10 Inc. controls the store. anyone can
          register an app, but it needs ADMIN APPROVAL to appear.
          needs an api/admin side: an approval flag on app records +
          an admin-only approve action (coordinate with lane A/B —
          the admin panel is B4; the record shape + gate is lane A
          territory). unapproved = registered but hidden.
      (4) surface the historical stats that made it feel alive
          (user count, app count) — real numbers from the mongo,
          never faked.
      acceptance: design.md §12; store shows web10-social first +
      real approved apps below; registering an app is possible but
      admin-gated; no hardcoded catalog.
      NOTE — reconciles with the D20/app-store decision (which said
      "don't build a speculative ecosystem, keep it as proof"): that
      call assumed an EMPTY catalog would read as fediverse-jank.
      with 208 real users and real registered apps from the original
      web10 now reconnected (A7), the catalog is genuinely alive, so
      restoring it IS the "looks like a real company" goal, not a
      distraction. record this reversal in decisions.md when D16
      lands.
      STATUS: (1)+(2)+(4) landed in 1.0.84 (marketing-ui AppStore
      POSTs the node /stats); (3) CURATION landed in 1.0.136 —
      operator reversed the earlier "takedown, not allowlisting"
      call (decisions.md D31) and now wants allowlist ("any app can
      register, but i should be able to approve"). `register_app`
      adds new entries as approved:false (pending, via $setOnInsert
      so repeat visits don't reset state); `get_apps` filters
      approved=True so /stats hides unapproved; `POST /apps/admin`
      lists all + a pending count, `POST /apps/approve` toggles
      (both admin-only via check_admin, reuses the 1.0.77 admin
      model). The authenticator's Node Config panel gained an "App
      Store Approvals" card with per-row Approve/Unapprove buttons
      and pending-badge side-by-side with the rest of the node's
      variables. Backward-compat: historical pre-A7 apps without an
      `approved` field show as pending in the admin panel — the
      operator curates them once on first launch.
  [✓ 1.0.70] D16.1. fix the /docs/* 404 — marketing-ui nginx served
      /docs/ via an alias block that shadowed the SPA fallback, so
      /docs/protocol-spec 404'd (it's a client-side route that
      fetches /docs/<slug>.md). added try_files ... /index.html to
      the /docs/ location. (small live-prod fix done alongside the
      planning.)
  [✓ 1.0.72] D17. RESTORE THE DEV DOCS (operator, 19.07 — "docs are lacking,
      were sharp before with demo apps"). NOT LOST — recoverable from
      git: the old rich docs lived in auth/public/docs/ and were
      removed in 82667060 ("moving of docs to separate endpoint").
      recover from `82667060^:auth/public/docs/`:
      - hello/ and notes/ — the two live DEMO APPS (index.html +
        script.js each) that made the docs sharp.
      - sdk.md + sdk.pdf — the SDK guide.
      bring these into marketing-ui's docs (the new home,
      marketing/marketing-ui/public/docs + src/pages/Docs.tsx), on
      the design.md standard, as runnable demo apps + an SDK page.
      also: the old app store had a "make your own web10 app with the
      web10 CLI" call-to-action (old AppStore.js, commit cc548b68) —
      the CLI still exists (marketing/web10-cli/) but is invisible;
      restore that CTA (ties to D16's "Build on web10" card) and
      write a CLI quickstart doc. acceptance: /docs shows demo apps +
      SDK + CLI quickstart; the app store links devs to "build your
      own". gate: none (docs), but the SDK doc should reflect C2's
      typed sdk once it lands (don't teach legacy wapi as the future
      — see C2/C3.5 parking note).
[✓ 1.0.72] D18. DOCUMENT + LINK THE WEB10 SDK (operator, 19.07: GitHub
       shows 4 built packages, none is the SDK; docs never mention it
       or link to it). DONE: sdk.md docs page created and wired into
       Docs.tsx sidebar; SDK link added to Home.tsx footer; npm badge
       + SDK link in README.md. npm publish confirmed: web10-npm@1.0.8
       public on npmjs.com. Publish flow decision documented in
       decisions.md D26: stays tag-gated (no auto-publish on merge
       while C2 typed rewrite is in flight).
[✓ 1.0.131] D19. CONTENT LIFECYCLE — staging + visibility management (operator,
        23.07; on the D29 gauntlet critical path). the visibility split
        shipped half-built and crossed: composer sets no visibility (native
        posts trap in private_posts, never public), imports write to legacy
        anon-readable `posts` (import auto-publishes your whole history).
        model: visibility = the COLLECTION a record lives in, never a status
        field (decisions.md D30). staging_posts (owner-only, awaiting triage)
        / private_posts / public_posts; publish = move between collections.
        PHASE A (foundation repair, PID 1.0.131 — the live exposure fix):
        imports -> staging_posts (marketing-api parsers + main.py);
        register staging_posts owner-only terms in SMROnReady; wall reads
        public_posts+private_posts not legacy posts; fix broken /exporters ->
        /import CTA (3 social screens). PHASE B: composer visibility selector
        (default public). PHASE C: Staging/Review screen (grouped by origin,
        publish/keep-private/delete = collection moves), wall visibility
        toggle, and PUBLISHING grants audience media read (per-service gate is
        coarse; public media needs an explicit path). owns marketing/web10-
        social/** + the post-write path in marketing/marketing-api/**. gate:
        none — Phase A is a live exposure fix, start now. acceptance: import
        -> review in staging -> publish public -> appears in feed/discovery;
        native post with visibility choice; nothing auto-exposed; design.md
        §12 (PR screenshots). deferred: friends_posts (needs graph),
        unlisted_posts (anon-read, discovery-excluded).
        STATUS: Phase A LANDED (1.0.131). Phase B is now part of
        D-post-delivery; Phase C's screen half is D-staging-review and
        its media-grant half is A12 + D-public-media-client (D35) —
        see those items below; nothing left to pick up under this tick.
[✓ 1.0.149] D24. PROFILE FOLLOWER/FOLLOWING COUNTS BUG — `ProfileScreen.tsx` loads
        `countFollows()` and `readFollows()` in parallel and assigns both
        `followingCount` AND `followerCount` from the same data source
        (people YOU follow). There is no reverse lookup for who follows
        YOU, so the "Followers" stat is a duplicate of "Following."
        Fix: either wire a real follower-count endpoint (e.g. a public
        ledger query or a discovery API call) or hide the follower stat
        until the backend supports it. Owns marketing/web10-social/src/
        components/Bio/ProfileScreen.tsx. Gate: none.
[✓ 1.0.149] D25. USER-PROFILE BACK NAVIGATION BUG — `handleBackFromProfile` in
        `App.tsx` hardcodes `setMode('discover')` so leaving any user
        profile always returns to Discover, even when the user came from
        Feed, Chat, or Profile. The `prevModeRef` useEffect (lines
        130-137) tracks mode changes but its conditional branches are
        empty — it never stores the pre-profile mode. Fix: capture the
        mode before navigating to `user-profile` and restore it on back.
        Owns marketing/web10-social/src/App.tsx. Gate: none.
[✓ 1.0.157] D-profile-media-refresh (sub-lane D/profile; was "D26" for a day —
        renamed per rule 4/D33). PROFILE AVATAR/BANNER VANISH ON
        REFRESH — (operator, 23.07:
        uploading a profile pic + banner now works and they render —
        1.0.149's presign fix — but after a page refresh they're gone
        and the browser makes NO request to load the images.) the load
        path LOOKS right — `ProfileScreen.loadData` pushes `avatar_ref`/
        `banner_ref` into `resolveMediaRefs` (ProfileScreen.tsx:63-66) —
        so the break is in what a fresh read returns. suspects, in
        order: (1) `saveProfile` (data/profile.ts) is called from
        `handleUpload` with the whole in-memory profile spread into the
        `$set` payload, `_id` included — verify the update actually
        persists `avatar_ref`/`banner_ref` onto the record `readProfile`
        returns; (2) `readProfile` returns `records[0]` unsorted —
        accounts with multiple `profile` records (the legacy-identity
        adapt path creates one; pre-1.0.145 seed runs left dups) can
        read a stale record while the save updated a different one;
        (3) `resolveMediaRefs` failing silently and returning [] for
        the refs. fix + a vitest regression: upload → save → fresh
        `readProfile` round-trip carries the refs → media request
        fires. verify on dev with a hard refresh. TEST-DEPTH NOTE:
        this bug got past the step-6 playwright journey (it asserts
        profile save+read, never refresh-with-media-refs) — the
        regression also gets encoded into gauntlet.spec.ts step 6.
        e2e/** is lane C: leave a .context/ note with the repro, or a
        tiny cross-lane PR by agreement. journeys must assert the
        BAR, not the happy path. owns marketing/
        web10-social/src/components/Bio/ProfileScreen.tsx + src/data/
        profile.ts. gate: none — gauntlet step 6 ("your profile reads
        as a creator page you'd screenshot") doesn't survive F5 today.
[~ half: ✓ 1.0.162] D-user-profile-media (sub-lane D/profile). USER PROFILE AVATAR/BANNER BROKEN —
        STATUS (24.07): the avatar/banner half LANDED in 1.0.162 (#284 —
        loadData now resolves refs and populates mediaMap). REMAINING:
        the post-media-in-grid half below — and it is GATED on the
        public-media read path (A12 + D-public-media-client, decisions.md
        D35): even with refs wired, a non-owner's presign is denied
        today, so don't pick this up before that path merges.
        viewing another user's profile always shows the default initial-letter
        avatar and gradient banner, even when the user has uploaded images.
        `UserProfileScreen.tsx` declares `mediaMap` state but never populates it.
        `loadData()` fetches the profile (carrying `avatar_ref`/`banner_ref`) but
        never calls `resolveMediaRefs()` — the lookups at lines 170-171 always
        return `undefined`. Fix: same pattern as `ProfileScreen.tsx` lines 67-77
        — collect refs after profile fetch, resolve, populate mediaMap.
        Also fixes post media in the user profile grid: `media_refs` is dropped
        during the discovery-post map (lines 98-106), so even with a populated
        mediaMap, post images can't render. Deeper issue: the discovery API
        (`DiscoveryPost`/`RawDiscoveryPost` types) does not return `media_refs` —
        the index is lightweight metadata only. Full fix requires either adding
        `media_refs` to the discovery API response (lane A touch in documentdb.py
        discovery aggregation) OR reading actual post records from the user's
        collection. owns marketing/web10-social/src/components/Bio/UserProfileScreen.tsx.
        gate: none. acceptance: viewing any user profile with an uploaded avatar/
        banner shows the images; post media renders in the profile grid.
[ ] D-feed-avatar-resolution (sub-lane D/feed). FEED AVATAR FALLS BACK TO INITIAL —
        `FeedScreen.tsx` tries to find the author's avatar inside the post's
        media items: `mediaMap[item.post_id]?.find(m => m._id === profile.avatar_ref)`
        (line 598). The avatar lives in the user's media collection independently,
        not in the post's `media_refs`. This only works by accident if the avatar
        image was also attached to the same post. Fix: collect all unique
        `avatar_ref`/`banner_ref` from the profile map and resolve them separately
        via `resolveMediaRefs(profileRefs, { username, provider })`. Same bug in
        `DiscoverScreen.tsx` line 798 (`mediaItems.find(m => m._id === profile.avatar_ref)`).
        owns marketing/web10-social/src/components/Feed/FeedScreen.tsx and
        Discover/DiscoverScreen.tsx. gate (updated 24.07): the public-media
        read path (A12 + D-public-media-client, decisions.md D35) — resolving
        another user's avatar requires a non-owner presign, which is denied
        today, so the fix can't be verified before that path merges.
        acceptance: feed and discover show real avatars for authors who have
        uploaded profile pictures.
[✓ 1.0.165] D-post-delivery (sub-lane D/feed). A REAL USER'S POST REACHES NO ONE —
        the 24.07 audit behind the pitch: PostComposer.tsx:379 calls
        createPost with NO visibility field, and posts.ts:37 routes
        visibility-less posts to private_posts (owner-only, never
        discovery-indexed); AND nothing fans out on write anywhere — the
        app never writes a follower's `inbox` (grep posts.ts + api/app:
        zero); only persona-orchestration/seed_personas.py:652
        deliver_to_inbox does, out-of-band, which is why gauntlet steps
        2-3 pass on seeded personas and are THEATER for real accounts.
        post once, reach ZERO followers — in the app whose pitch is 100%
        delivery by architecture. two fixes, one arc (same files):
        (1) D19 PHASE B — composer visibility selector, default PUBLIC
            (public → public_posts, discovery indexes it; private →
            private_posts).
        (2) FAN-OUT ON WRITE — after a PUBLIC post lands, deliver to
            every follower's inbox + your own: the follower list is the
            D34 public ledger (payload.action='follow' entries targeting
            you — countFollowers in follows.ts already queries it); the
            `inbox` service already whitelists create for everyone
            (serviceTerms.ts), so one POST /{follower}/inbox per follower
            with {post_id, post_body, author_username, author_provider,
            delivered_at} (the exact shape seed_personas.py:658 writes)
            is permitted today. client-side O(followers) is the honest v0
            at demo scale (server-side fan-out is a later lane-A item —
            D29). private posts do NOT fan out.
        owns PostComposer.tsx + data/posts.ts (+ a listFollowers export
        in follows.ts — seam rule: own exports only). NOTE: follower-side
        IMAGE rendering still hits the cross-user media wall (A12/D35) —
        text delivery is this item's bar; don't chase media 403s here.
        gate: none. acceptance: user X posts public → it appears in
        follower Y's Feed tab (two real accounts, dev or local stack);
        a private post doesn't; your own post shows in your own feed;
        the visibility control is design.md-grade at 375px (screenshots).
[✓ 1.0.164] D-staging-review (sub-lane D/staging — new screen, new sub-lane:
        owns components/Staging/** + data/staging.ts). D19 PHASE C, the
        screen half: imports land in staging_posts (owner-only, Phase A
        1.0.131) and there is NO ui to triage them (grep components/ for
        staging: zero). build the Staging/Review screen: list
        staging_posts grouped by origin (instagram / facebook / youtube /
        native draft), preview text + media (owner-only reads — no media
        wall here), per-item + bulk actions publish-public /
        keep-private / delete implemented as COLLECTION MOVES
        (create-in-target + delete-from-source, D30 — same body, new
        home; the move helper lives in data/staging.ts). route /staging
        + an entry point (Profile screen "Review imports (N)" affordance
        when N>0). the "publishing grants audience media read" bit is
        NOT this item — that's D-public-media-client after A12. seam
        touches: one Route line in App.tsx, one export line in
        data/index.ts (seam rule). gate: none. acceptance: seed a few
        staging records (or run an import), triage them entirely from
        the ui, published ones appear on the wall/feed; design.md §12,
        screenshots desktop + 375px.
[ ] D-public-media-client (sub-lane D/feed + data seam). client half of
        decisions.md D35, against A12's contract: serviceTerms.ts gains a
        `public_media` service with the anon-read whitelist public_posts
        already carries; uploadMedia targets public_media when the
        content is public (a public post's attachments; avatar/banner —
        the profile record is already anon-readable); resolveMediaRefs /
        getReadUrl pass the service through so non-owner reads presign
        against public_media. existing records stay in `media` (legacy
        fallback keeps rendering for the owner). gate: A12 merged.
        acceptance: a second account sees the author's public-post
        photos + avatar on dev (private bucket, zero 403s in console);
        DM/private-post media still 403s for non-owners.
        STATUS 26.07: the gate (A12) merged 1.0.164 — this is UNBLOCKED
        and staged on the 26.07 fleet (ws-D/feed). it is the single
        biggest visible hole after A13: no follower can render ANY
        image an author uploads.
        STATUS 26.07 (late, audit #4): PARTIALLY landed as PR #311
        (merged WITHOUT a CHANGELOG entry — logged retroactively in
        1.0.183): the data layer now threads `service` end to end
        (serviceTerms.ts public_media anon-read entry, uploadMedia /
        resolveMediaRefs / refreshMediaUrls / wapi getReadUrl +
        presign-cache key). But NO CALL SITE passes it — grep
        components/ for public_media: zero. Uploads still confirm into
        owner-only `media` (PostComposer.tsx:353/366, ProfileScreen
        .tsx:114) and no reader passes service='public_media', so
        cross-user images still 403. REMAINING WORK: (1) PostComposer
        uploads target public_media when visibility=public (private/DM
        stays `media`); (2) ProfileScreen avatar/banner uploads target
        public_media; (3) cross-user read paths (UserProfileScreen,
        FeedScreen/DiscoverScreen via resolveMediaRefs, PostLightbox)
        pass service='public_media' for other users' public content
        (owner's own legacy records keep the `media` fallback); (4)
        verify the original acceptance on dev. seam note: touches
        ProfileScreen.tsx — coordinate with the ws-D/profile chain
        (run this FIRST; ws-D/profile's D-profile-image-placement
        rebases after).
        BITES (rule 5 — one PR each):
        bite a, UPLOADS: PostComposer public-post uploads +
          ProfileScreen avatar/banner uploads pass
          service='public_media'; tests pin the confirm-body service
          field. acceptance: a fresh public post's media record lands
          in public_media (local stack API check).
        bite b, READERS + VERIFY: cross-user read paths
          (UserProfileScreen, FeedScreen/DiscoverScreen resolve,
          PostLightbox) pass service='public_media' for other users'
          content, owner legacy fallback intact; then verify the
          original acceptance on dev (second account sees images,
          zero 403s). gate: bite a merged.
[ ] D-follow-backfill (sub-lane ws-D/feed, AFTER D-public-media-client
        — same workspace chain, data-layer seam): "followed X, don't
        see them in my feed" (operator, 26.07 — plan.txt PHASE 8.6
        known gap, previously narrative-only). fan-out is push-only at
        POST time to CURRENT followers (D-post-delivery 1.0.165), so
        following someone surfaces NOTHING until they next post. fix:
        on follow, BACKFILL — followUser (data/follows.ts) fetches the
        followee's recent public posts (the pattern UserProfileScreen
        .tsx already uses to render another user's post grid — the
        discovery API filtered by author; direct public_posts CRUD
        read works too once A13's terms land) and writes each into the
        follower's OWN inbox, capped at the ~20 most recent, using the
        exact inbox shape D-post-delivery fans out ({post_id,
        post_body, author_username, author_provider, delivered_at}) —
        dedupe on post_id so re-follow doesn't duplicate. unfollow
        does NOT retract already-delivered inbox records (email
        semantics); it only stops future delivery (already true).
        files: data/follows.ts (+ own exports only; if a read helper
        is needed, add it, don't repurpose another sub-lane's).
        acceptance: a fresh account follows a persona → the persona's
        existing posts appear in the Feed tab immediately, no waiting
        for a new post; re-follow doesn't duplicate; text is the bar
        (image 403s are D-public-media-client's). gate: none hard —
        A13-discovery-terms makes the backfill source reliable on dev;
        verify against the local stack if dev's terms aren't migrated
        yet.
[✓ 1.0.155] D-url-routing (sub-lane D/shell; was "D27" for a day — renamed per
        rule 4/D33). THE ADDRESS BAR HOLDS YOUR PLACE — (operator,
        23.07: refreshing the social app should put you back on the
        screen you were on.) navigation is one `useState<Mode>` in
        App.tsx:95 — the URL never changes, so refresh dumps you at
        the feed, browser back/forward do nothing, and no screen is
        linkable. adopt real routes (react-router, already the
        marketing-ui stack): /feed, /discover, /messages, /profile,
        and /u/:username for public profiles — the route
        D-trending-link is explicitly waiting on (plan.txt PHASE 5.5:
        marketing-ui's trending author link deep-links to
        SOCIAL_ORIGIN root today with a TODO for /u/<author>).
        refresh restores the screen INCLUDING which user profile you
        were viewing; browser back replaces D25's hand-rolled
        preProfileModeRef back-stack; the login flow preserves the
        intended destination and redirects post-auth; unknown paths
        fall back to /feed. owns marketing/web10-social/src/**
        (App.tsx, Layout nav); the marketing-ui author-link upgrade
        to /u/<author> is a small same-lane follow-up touch. gate:
        none. acceptance: refresh anywhere → same screen; back/
        forward work; /u/<persona> loads while signed in.
[✓ 1.0.158] D-follower-count (sub-lane D/discover + a D/profile touch). THE
        REAL FOLLOWER COUNT — gauntlet step 6's own words: "a creator
        page without follower count is not a creator page — that's
        the primary social proof metric." 1.0.149 (legacy item D24)
        honestly REMOVED the fake stat; nothing was queued to build
        the real one — this is it. follower count of X lives in every
        follower's own collection, and I3 forbids cross-collection
        reads, so the count comes from the public layer: `followUser`
        (follows.ts) mirrors to /public/entries with
        payload.action='follow' targeting the followed user — the
        exact D32 pattern reactions/comments already use
        (unconditional; collection-level terms is the lock; unfollow
        deletes the entry like deleteComment). NOTE this makes the
        follow graph public — record the call as decision D34 when
        implementing (D32's logic extends: follows are public
        discourse, the panic button is one terms flip). count reads
        from the ledger; /discover/users gains follower_count in the
        engagement aggregation (small lane A touch in documentdb.py —
        coordinate via .context/, don't reach in). backfill: the
        persona seed script mirrors the existing cross-follows
        (persona-orchestration/, idempotent like 1.0.145). then the
        Followers tile returns to ProfileScreen with a real number
        (tabular-nums — this is the M0 screenshot stat). gate: none.
        acceptance: personas show real cross-follow counts; following
        someone increments their count; unfollow decrements.
[✓ 1.0.155] D-dm-compose (sub-lane D/dms). START A CONVERSATION — gauntlet
        step 5: "no way to start a new conversation with someone you
        don't already have a thread with" (listConversations only
        returns existing threads; the step-5 playwright journey is a
        test.fixme scaffold waiting on this). add a "New message"
        button → picker over contacts + follows (+ compose-to-
        username fallback); dms.ts already has deterministic
        conversation service names, so composing to a fresh contact
        is just writing the first record. persona reply seeding
        (1.0.145) means the thread won't read as a monologue.
        owns web10-social/src/components/Chat/** + data/dms.ts,
        contacts.ts. gate: none. acceptance: DM a persona you've
        never messaged, entirely from the UI; lane C note to flip the
        step-5 fixme into a real journey.
[✓ 1.0.155] D-login-cta (sub-lane D/shell). THE LOGIN SCREEN NEVER SAYS YOU
        CAN SIGN UP — gauntlet step 1: LoginScreen is a single "Log
        in" button; account creation exists only inside the auth
        popup (ConsentView → SignupForm, 1.0.135) and nothing tells a
        first-time user that. smallest honest fix: the button copy
        and a line under it ("Log in or create your account — one
        step") so the affordance is stated, not discovered. if the
        auth portal supports a signup-mode hint param, pass it;
        don't build a second signup flow. owns web10-social/src/
        App.tsx (LoginScreen). gate: none. design.md §12 (it's the
        first screen of every demo).
[✓ 1.0.155] D-join-page (sub-lane D/marketing). THE MANIFESTO JOIN PAGE —
        timeline.md week-2 item, the only one not started (flagged
        23.07). marketing-ui gains /join: the fan-facing pitch page
        built from THE STORY's user side (plan.txt) — the first
        sentence is the fan's ("never miss a post from [creator]
        again — you see 100% of what they make, always"), the
        aspirant is honored at the door ("you're not joining the
        crowd — you get your own page here too"), THE RISE arc as the
        page's skeleton, quiet safety line second (delete actually
        deletes). CTA → signup on the flagship node. voice:
        manifesto.md's register — declarative, no gush, never claim
        what isn't built. owns marketing/marketing-ui/src/pages/
        Join.tsx + route + nav link. gate: none. design.md §12
        (screenshot-grade; this page IS the pitch).
[✓ 1.0.147] D21. MEDIA POLISH — photos + video v0 (plan.txt phase 8.5; item
        number D20 skipped — it reads as decisions.md's D20
        everywhere). the composer's 80x80 hover-square tray, the
        no-lightbox feed, and the video trap (a picked video renders
        via <img> and just breaks) fixed in one lane-D arc, staged:
        PHOTOS V1 composer (real-aspect tray matching the feed grid,
        touch-visible remove, drag reorder, client-side downscale +
        thumbnail + width/height into the confirm fields nobody
        fills, per-image alt text, attach-time validation with
        words); PHOTOS V2 feed (lightbox with swipe/zoom, "+N"
        overflow tile past six, reserve-space-from-dimensions, and
        the design.md MEDIA SPEC: canonical aspect ratios, grid
        rules, player chrome); VIDEO V0 (video previews + plays as
        <video> with client-captured poster + duration, h.264 mp4
        only with honest attach-time rejection otherwise, duration/
        size caps until A9 lifts them). owns marketing/web10-
        social/** + the design.md media-spec section. gate: none —
        photos v1 is gauntlet step 2 territory ("post a photo -> it
        appears in the feed"), start now. acceptance: attach photos
        on a phone and the tray looks intentional; a picked video
        plays in the feed with a real poster; design.md §12 (PR
        screenshots).
[ ] D22. THE VERTICAL FEED — short-form lens (plan.txt phase 8.5,
        video v1; the tiktok answer). 9:16 full-bleed snap-scroll
        feed in web10-social: muted autoplay + tap for sound,
        preload next; reads the SAME posts/media services (a lens
        over the conventions schemas, not a new silo); composer
        detects 9:16 -> "this will show in the vertical feed" +
        cover-frame picker. rides D21's direct-mp4 playback — does
        NOT wait for A9, inherits hls when A9 lands. owns
        marketing/web10-social/** only. gate: D21's video v0
        merged. acceptance: a phone-recorded vertical video posts
        and swipes like short-form, demoable next to tiktok.
[✓ 1.0.143] D23. MEDIA READS SURVIVE A PRIVATE BUCKET (plan.txt phase 8.5,
        media v0; the dev-minio 403). media records store the bare
        unsigned object url and every renderer trusts record.url
        verbatim, so a private bucket 403s every avatar/photo; the
        api's request_read_url endpoint has ZERO callers. the wapi
        wrapper gains getReadUrl (POST /media/{user}/read ->
        presigned GET, expiry-aware cache so a feed of N images
        isn't N round-trips per render); resolveMediaRefs + the
        avatar/feed/composer paths render fresh presigned urls,
        never record.url raw. cross-lane bits: the typed-sdk media
        surface is C7; confirm-upload persisting object_key is a
        tiny lane-A touch (coordinate, don't reach in — legacy
        records derive the key from the stored url meanwhile).
        owns marketing/web10-social/** only. gate: none — and
        D21's photos-v1 acceptance ("post a photo -> it appears
        in the feed") is BLIND on dev until this lands, so it
        goes first. acceptance: avatars + feed photos render on
        dev (private bucket) with zero 403s in the console;
        presigned urls refresh when they expire.

── 26.07 OPERATOR PASS — web10-social product surface (plan.txt PHASE
   8.6 has the narrative; these are the executable lane items). Each is
   self-contained; the SEAM note names the file two items must not both
   edit — run those in the SAME workspace sequentially, not parallel.

[✓ 1.0.178] D-profile-post-experience (sub-lane ws-D/profile; SEAM:
      ProfileScreen.tsx + PostLightbox.tsx — this item owns both):
      make a profile post fully viewable + editable. (1) VIDEO THUMBS:
      the Posts/Media grid renders <img src=firstMedia.url> which shows
      a BROKEN-IMAGE icon for videos (mime_type startsWith 'video/');
      render media.thumbnail_url (or a <video poster preload=metadata>
      first-frame) + a play badge instead. (2) FULL VIEWER/EDITOR in
      PostLightbox.tsx (shipped 1.0.170, currently media+caption only):
      extract CommentThread from FeedScreen.tsx into
      components/Feed/CommentThread.tsx (re-import in FeedScreen — pure
      move, keep testids) and reuse it; add a like button
      (toggleReaction('posts', postId, 'like', ...) + countReactions +
      liked state from readReactions), the comment thread (see other
      comments + add), and — for the owner — EDIT text (updatePost) +
      DELETE (deletePost) with a confirm. Works for text / photo /
      video posts. Video already gets a native seek bar + timer via
      <video controls> (added 1.0.170) — keep it. On delete/edit, call
      a prop so ProfileScreen reloads. Files: components/Bio/
      PostLightbox.tsx, ProfileScreen.tsx, components/Feed/
      CommentThread.tsx (new), FeedScreen.tsx (import only). data fns
      all exist (posts.ts updatePost/deletePost, reactions.ts,
      comments.ts). acceptance: click a video post → thumbnail in grid,
      opens with a working scrubber; like toggles + persists; comments
      load + post; owner can edit text + delete; screenshot desktop +
      375px. gate: none.

[ ] D-post-visibility-toggle (sub-lane ws-D/profile; GATE CLEARED
      27.07 — D-profile-post-experience merged 1.0.180 — pick up now;
      same SEAM ProfileScreen/PostLightbox):
      change a post's visibility after publish. public<->private is a
      COLLECTION MOVE (D30): create in the other of public_posts/
      private_posts, delete from the current (mirror staging.ts's
      movePostToPublic pattern). Surface it as a control in the lightbox
      owner menu. Files: data/posts.ts (add movePostVisibility),
      PostLightbox.tsx. acceptance: flip a post public→private, it
      leaves discovery + the public wall; flip back, it returns. gate:
      D-profile-post-experience merged.

[✓ 1.0.182] D-messages-views (sub-lane ws-D/messages; SEAM: components/Chat/**
      + DmsScreen): three views behind a toggle on the messages screen.
      (1) current chat/DM list (exists). (2) MAIL view — the whole
      inbox rendered gmail-style (thread list, read/unread, preview
      line, timestamp column). (3) CRM view — notes + contact history
      per person, in-app, for influencers to remember who they reached
      out to (the web10-social CRM sub-app, here since 1.0.30, is the
      reference; this is a messages-only lens over the same contact/DM
      data — data/dms.ts + data/contacts.ts). Files: components/Chat/**,
      data/dms.ts, data/contacts.ts. acceptance: toggle cycles all three;
      mail view lists threads gmail-style; crm view shows per-contact
      notes you can edit + a message history; screenshot each. gate:
      none. (message delete/edit is D-message-controls, same sub-lane,
      AFTER this.)

[ ] D-message-controls (sub-lane ws-D/messages; GATE CLEARED 27.07 —
      D-messages-views merged 1.0.182 — pick up now): delete a message/conversation + edit a sent message.
      Files: data/dms.ts (update/delete), components/Chat/**. acceptance:
      edit a sent DM in place; delete a message + a whole conversation.
      gate: D-messages-views merged.

[ ] D-ledger-mirror-fix (sub-lane ws-D/ledger — data/feed.ts + one
      bootstrap line; PRIORITY with A13-class weight): THE APP'S PUBLIC
      LEDGER MIRROR IS DEAD FOR REAL ACCOUNTS — the social-proof
      numbers (follower counts, trending like/comment counts) only work
      for script-seeded personas. Found by ws-C 25.07 (plan.txt "real
      follower count" HONESTY NOTE, which said "re-open as a lane D +
      lane A item" — this is the lane D half; A14-followers-count is
      the A half). Two dead links, both in data/feed.ts, both verified
      live at HEAD 26.07:
        (1) registerDefaultSchemas() (feed.ts:70) is NEVER CALLED from
            app bootstrap — grep src/ outside feed.ts + tests: zero. So
            getCachedSchema('Follow'/'Reaction'/'Comment') is always
            undefined and every schema-gated mirror silently no-ops.
            Fix: call it once at app startup after auth (main.tsx or
            App.tsx auth-ready path), idempotent, non-blocking.
        (2) createPublicEntry() (feed.ts:186) POSTs the raw entry as
            the JSON body with a Bearer header — but POST
            /public/entries (api/app/endpoints/public.py:12) expects
            the wapi Token BODY shape: {token: <site-token>, query:
            {schema_id, target, payload, ...}} and reads token.token /
            token.query. The request 422s, the catch swallows it and
            returns a fake local stub — so even with schemas cached,
            nothing ever lands in the ledger. Fix: send
            {token: readToken()?.site, query: entry} (match public.py;
            deletePublicEntry/queries already work — mirror their
            shape).
      files: data/feed.ts + ONE bootstrap call line (seam note: App.tsx
      is touched by other sub-lanes for route lines — keep it to one
      line, first-merged-wins, rebase). callers (reactions.ts,
      comments.ts, follows.ts) should need no changes — verify, don't
      refactor them. acceptance: on a real (non-persona) account on the
      local stack: follow someone → a payload.action='follow' entry
      exists in the ledger (API query, not the stub) and the followee's
      own /profile Followers count increments; like + comment → the
      trending counts for that post increment. add tests that pin the
      REQUEST BODY SHAPE against public.py's contract so this can't
      silently regress to a stub again. tsc -b + tests green. gate:
      none.

[ ] D-mail-experience (sub-lane ws-D/messages, AFTER D-message-controls
      — same Chat/** seam; operator, 26.07 night pass, with
      screenshots): the Mail view (1.0.182) reads as a themed thread
      list, NOT email — "that doesn't look like email! where is the
      inbox, outbox, spam" and the thread detail has no email anatomy.
      the operator's vision: a REAL email client lens over messages —
      "it really elevates messages on web10 social… kickass feature of
      a killer platform." build it in two layers:
        (1) FOLDERS: a left rail (or top tabs at 375px) with Inbox /
            Sent / Spam (+ Archive if cheap). Inbox = threads whose
            latest message is inbound; Sent = your outbound; Spam =
            senders you've flagged (a flag record on the contact/thread
            — data/dms.ts or contacts.ts own export; no server change).
        (2) EMAIL ANATOMY in the thread view: Subject line (a `subject`
            field on the message record — optional, DM-compat: absent
            subject renders as the first-line preview like today),
            Reply / Reply-all / Forward actions, To/Cc/Bcc on compose
            (multi-recipient send = one DM record per recipient +
            cc/bcc metadata on the record body; bcc recipients never
            leak in other recipients' copies), From/To header block on
            each message, timestamp full-format.
      keep DM/Chat compat: mail-composed messages still render in Chat
      as plain messages (subject shown as a bold first line); nothing
      breaks for records without the new fields. record-shape note: new
      fields (subject, cc, bcc, spam-flag) are BODY fields on existing
      message/contact records — conventions-doc addition, no api
      change. files: components/Chat/MailView.tsx + DmsScreen.tsx,
      data/dms.ts + contacts.ts (own exports). acceptance: folders
      switch and hold their meaning; compose with subject + cc sends
      and threads correctly; reply quotes into the same thread; forward
      opens compose prefilled; a legacy DM thread renders fine in Mail;
      screenshots desktop + 375px, design.md §12. gate:
      D-message-controls merged (same files).
      BITES (rule 5 — one PR each, in order):
      bite a, FOLDERS: the Inbox/Sent/Spam rail (tabs at 375px) +
        the spam-flag record + filtering. no compose changes.
      bite b, THREAD ANATOMY: subject field (render + compose input),
        From/To header block, Reply + Forward actions (quote into
        thread / prefill compose). single-recipient only.
      bite c, CC/BCC COMPOSE: multi-recipient send (one record per
        recipient, cc/bcc metadata, bcc never leaks in others'
        copies) + Reply-all. gate: bite b merged.

[ ] D-crm-upgrade (sub-lane ws-D/messages, AFTER D-mail-experience —
      same Chat/** seam; operator, 26.07 night pass): the CRM view
      (1.0.182) is "more on target" but MISSING THE ORIGINAL CRM'S
      FEATURES — parity bar is the old CRM sub-app (components/Crm/
      Crm.tsx, integrated #90, UI deleted in #120 "dead rectangles" —
      recover the reference from git; its state machinery still lives
      in interfaces/Interface.ts:35-45 and the crm-contacts/crm-notes
      services are still in serviceTerms.ts:92-96): (1) COLOR-CODED
      STATUS on each contact (the old green/yellow/red priority — "so
      you can bin and sort people"), settable from the contact row +
      detail, persisted on the contact record; (2) color/status FILTER
      chips + sort (by status, by last-message, by name — the old
      crmColorFilter); (3) richer CONTACT FIELDS on the detail card:
      email, phone, company/role, links/socials, free-form fields
      ("or who knows" — keep it a flexible key/value section, records
      are schemaless); (4) contact CRUD from the CRM view (add a
      contact who isn't a DM thread yet — the old Rolodex could).
      notes + message history (1.0.182) stay. files: components/Chat/
      CrmView.tsx, data/contacts.ts (extend the contact record shape —
      body fields only). acceptance: set a contact red/yellow/green →
      persists, filterable, sortable; add/edit contact fields; add a
      fresh contact; screenshots desktop + 375px. gate:
      D-mail-experience merged (same seam; can swap order with it if
      staged first — they're one chain).
      BITES (rule 5 — one PR each):
      bite a, STATUS COLORS: green/yellow/red status on the contact
        record, settable from row + detail, filter chips + sort.
      bite b, CONTACT FIELDS + CRUD: richer detail fields (email/
        phone/company/links + free-form key/value) and add/edit/
        delete a contact from the CRM view. gate: bite a merged.

[ ] D-settings-tab (sub-lane ws-D/settings — new components/Settings/**
      + one App.tsx route line + one Layout.tsx nav entry; operator,
      26.07: "web10 social doesn't have a settings tab, should be a
      settings tab, facebook style, TBD"): a real Settings surface.
      v0 ships the uncontroversial core; the full facebook-style tree
      is proposed, not built: (1) ACCOUNT section — username/provider
      (read-only), link to /profile for avatar/banner, log out (move
      the sidebar's orphan actions home; keep the sidebar shortcuts).
      (2) POSTING DEFAULTS — default post visibility (public/private,
      feeds PostComposer's initial state via a settings record).
      (3) DATA — "your data lives in your collections" explainer +
      link to the authenticator's terms editor (the panic-button
      story, D32/D34) + export pointer. (4) ABOUT — version, report a
      bug (reuse ReportBug), the manifesto link. settings persist as
      an ordinary `settings` service record (owner-only, body fields —
      conventions addition, no api change). ALSO: write the full
      proposed tree (notifications prefs, blocked users, appearance,
      language, sessions) as a .context/settings-tab-proposal.md for
      operator sign-off — build only the four sections above now.
      files: new components/Settings/**, data/settings.ts (new, own
      file), App.tsx (one route line), Layout.tsx (one nav entry —
      seam note: first-merged-wins, rebase). acceptance: /settings
      route + nav entry; default-visibility setting actually changes
      the composer's initial selector on next load; log out works from
      settings; screenshots desktop + 375px, design.md §12. gate: none
      (the .context proposal covers the TBD part — v0 scope above is
      operator-safe).
      BITES (rule 5 — one PR each):
      bite a, SHELL: /settings route + nav entry + account section +
        data/about sections + log out + the .context proposal doc.
        static content only, no settings record yet.
      bite b, POSTING DEFAULTS: data/settings.ts record + the
        default-visibility control + PostComposer reads it for its
        initial state (one-line composer touch — coordinate with
        ws-D/feed via .context/). gate: bite a merged.

[✓ 1.0.192] D-feedback-contact-privacy (sub-lane ws-D/feedback; SEAM:
      marketing-api main.py — small, do promptly): 1.0.181 publishes
      each bug report as a PUBLIC post tagged #web10-bugs — including
      the reporter's CONTACT INFO: _format_bug_post() (marketing-api
      main.py) appends `Contact: {email/phone}` into an anon-readable,
      discovery-indexed public post. That leaks PII on the platform
      whose manifesto line is "nobody is mining you" — and the reporter
      never consented to public contact info (the form implies the
      operator reads it). Fix: (1) NEVER include `contact` (or
      user_agent) in the public post body — keep them in the disk store
      + GET /feedback only; consider capping stack traces harder (they
      can embed tokens/urls). (2) fix the stale docstring + PR-title
      drift: submit_feedback's docstring says "delivers as a DM to the
      operator" but the code publishes a public post — make the docs
      tell the truth. (3) FLAG FOR OPERATOR (don't decide): the lane
      item + plan.txt offered (a) DM-to-operator or (b) operator panel;
      what shipped is (c) public posts. It's defensible (dogfooding,
      transparency) but it was nobody's named option — operator
      confirms or redirects; if he wants DMs, that's a follow-up item,
      not this one. files: marketing/marketing-api/app/main.py + tests
      (pin: contact never appears in the delivered post body).
      acceptance: marketing-api tests green incl. the new pin, ruff
      clean. gate: none.
      STATUS 27.07 (audit #6): STILL LEAKING at HEAD — main.py:487
      appends `Contact: {...}` into the public post body. NOTE the
      squash title of PR #312 ("deliver bug reports as DMs to
      operator") does NOT match the merged code (public posts) — a
      title/code drift to correct in passing; the actual PII strip
      above is unbuilt and this item is wide open.

[ ] D-profile-image-placement (sub-lane ws-D/profile, AFTER
      D-profile-post-experience — SEAM ProfileScreen): editing avatar +
      banner PLACEMENT — reposition/crop, fix centering. Today the
      avatar/banner are object-cover with no offset control, so a
      non-centered source looks wrong. Add a drag-to-reposition (store a
      focal point / object-position on the profile record). SCOPE BOUND
      (rule 5): focal-point drag ONLY — no cropper library, no crop
      step, no image manipulation; one draggable offset persisted and
      applied via object-position. that is the whole bite. Files:
      ProfileScreen.tsx, data/profile.ts (persist the focal point).
      acceptance: upload an off-center avatar/banner, drag to reframe,
      it persists + renders framed on reload; screenshot. gate:
      D-profile-post-experience merged (same file).

[✓ 1.0.181] D-bug-reports-delivery (sub-lane ws-D/feedback; SEAM: marketing-api
      main.py + web10-social ReportBug.tsx): route "Report a bug" to the
      operator instead of an in-memory list. marketing-api /feedback
      appends to `feedback_store: list` (main.py:394) — lost on restart.
      PERSIST it (a collection / the operator's own store), and DELIVER
      each report as a DM to jacoby149@web10.app (the on-thesis choice:
      bugs become normal message records via the DM/inbox path). Files:
      marketing/marketing-api/app/main.py (+ models), web10-social
      ReportBug.tsx if the shape changes. acceptance: submit a bug →
      it persists across restart AND lands as a DM in the operator's
      inbox. gate: none. (option b, an operator-only Bugs panel in
      web10-social, is the fallback if DM delivery is heavier — see
      plan.txt PHASE 8.6.)

[ ] D-follow-lists (sub-lane ws-D/profile, AFTER D-profile-post-
      experience — small SEAM: ProfileScreen stat tiles become links):
      a UI to SEE the follower + following LISTS (today the profile only
      shows COUNTS — followerCount/followingCount stat tiles). Make the
      "Followers" and "Following" tiles open a list (a modal or new
      routes /followers + /following) of the actual accounts, each row
      an avatar + name + handle linking to /u/:username, with a
      follow/unfollow action where it applies. Data exists: follows.ts
      (readFollows / listFollowers / countFollows / countFollowers).
      Files: new components/Bio/FollowList.tsx (or a modal), ProfileScreen
      .tsx (link the tiles), maybe App.tsx routes. Also applies to the
      OTHER-user profile (UserProfileScreen). acceptance: tap Followers /
      Following → a scrollable list of real accounts, each linking to
      that profile; screenshot desktop + 375px. gate: none (independent
      of the post-viewer work beyond the shared ProfileScreen file — run
      after it in the same ws-D/profile chain).

[✓ 1.0.178] D-join-two-step (sub-lane ws-D/marketing; SEAM: marketing-ui
      Join.tsx): the /join flow is confusing — it conflates two steps
      (visit the web10-social app, THEN sign up for the account). Make
      the two steps explicit and sequential on the page (numbered, or a
      "1. get the app → 2. create your account" flow). Files:
      marketing/marketing-ui/src/pages/Join.tsx. read design.md first.
      acceptance: a first-time reader understands it's two steps;
      screenshot desktop + 375px. gate: none.

[ ] D-join-steps-extend (sub-lane ws-D/marketing; SEAM: Join.tsx —
      independent of the search/graph/store chain's files; operator,
      26.07, screenshot of the shipped two-step strip: "i really like
      this dora the explorer what steps to complete! i think that it
      should be more! have the link to set up your monetization,
      since that is in the web10 authenticator, and then to post! to
      the feed"): grow the two-step strip into the full onboarding
      path — the RISE arc as a checklist. steps (operator's order;
      final order is a taste call at build, keep it one row that
      wraps at 375px): 1. Get the app → 2. Create your account →
      3. Set up your monetization → 4. Post to the feed. each step
      DEEP-LINKS to where it actually happens (the deep-link
      philosophy): step 3 → the Studio monetization screen in the
      authenticator (the D21 money screen — auth origin, new tab);
      step 4 → the social feed/composer (deep param like
      /feed?compose=1 if cheap — coordinate one query param with the
      D-deep-links item via .context/ note — else plain /feed).
      keep the numbered-card visual language the 1.0.178 strip
      established; both hero and bottom sections stay in sync.
      acceptance: four numbered steps, each opening the right
      surface in a new tab; reads as "what to do next" to a
      first-timer; screenshots desktop + 375px. gate: none.
      NOTE (operator follow-up): "really like how this is almost a
      tutorial for web10 social!" — the steps ARE the product
      tutorial. future echo (not this item): the same checklist
      surfacing IN web10-social for fresh accounts (the empty-feed
      first-run state walks you through post/follow/monetize) —
      capture-only, stage when the onboarding pass comes.

[ ] D-link-embeds (sub-lane ws-D/feed, AFTER D-follow-backfill — same
      workspace chain; operator, 26.07): paste a link into a post and it
      renders as an EMBED, the table-stakes feature every platform has
      (a youtube link becomes a playable player, other links become a
      preview card). v0, client-side only, no new api: (1) composer +
      post renderers detect URLs in post text; (2) an allowlisted set of
      KNOWN embeds renders a real player via the provider's public
      iframe embed (youtube + vimeo to start — youtube-nocookie.com
      privacy-enhanced src; lazy-load: render a thumbnail + play badge,
      swap in the iframe on click so feeds never eat N iframes); (3) all
      other links render as a plain external-link chip (favicon + domain
      + open-in-new-tab, rel="noopener noreferrer") — NO server-side
      og-scrape in v0 (that's a marketing-api/lane-A fetcher with SSRF
      surface — the A-side og-preview fetcher is a LATER item, don't
      build it here). embeds must render in FeedScreen, the profile
      grid/PostLightbox, and DM bubbles. keep it design.md-grade: the
      embed card uses tokens, never raw provider chrome beyond the
      iframe itself. files: a new components/Feed/LinkEmbed.tsx + a pure
      lib/linkEmbeds.ts (URL detect + provider parse, unit-tested:
      youtu.be, youtube.com/watch, /shorts, vimeo, bare domains, no
      false-positives inside words), wired into FeedScreen.tsx,
      PostLightbox.tsx, Chat message renderer. SEAM: FeedScreen.tsx +
      PostLightbox.tsx are the ws-D/profile chain's files — do NOT run
      this in parallel with D-profile-post-experience; it runs in
      ws-D/feed AFTER that chain merges (or coordinate via .context/).
      acceptance: a post whose text contains a youtube URL shows a
      playable embed in feed + lightbox (click-to-load), a random URL
      shows a link chip, a DM with a link gets the same treatment;
      no layout shift (reserve 16:9 space); screenshots desktop + 375px.
      gate: D-profile-post-experience merged (file seam).

[ ] D-feed-lightbox (sub-lane ws-D/feed; operator, 26.07 night pass,
      screenshots): FEED POSTS AREN'T CLICKABLE — "can't click the
      posts in the feed to make them big, only on my profile."
      clicking any feed post (media or text) opens the same
      PostLightbox the profile grid got (1.0.170/1.0.180): media
      pager, like toggle, comment thread. differences from the
      profile: (1) on someone ELSE's post the owner actions
      (edit/delete) are HIDDEN (isOwner prop off the post's author vs
      readToken()); (2) EVERY post gets a SHARE action ("should be
      share post! kind of a function") — navigator.share + copy-link
      fallback (the D-trending-share pattern, FeedPreview.tsx in
      marketing-ui is the reference), sharing a deep link that opens
      the post (add a light route that opens the lightbox over
      /u/:username — e.g. /u/:username/p/:postId — so the copied link
      lands somewhere real; unknown post id falls back to the
      profile). files: components/Feed/FeedScreen.tsx (open the
      lightbox), components/Bio/PostLightbox.tsx (isOwner prop +
      Share action — IMPORT it, don't fork it), App.tsx (one route
      line). SEAM: PostLightbox.tsx belongs to the ws-D/profile chain
      (D-post-visibility-toggle, D-lightbox-media-sizing edit it too)
      — do NOT run concurrently with those; chain order decided at
      staging. acceptance: click a feed post → lightbox opens; a
      persona's post shows no edit/delete but does show Share; Share
      copies a link that reopens that post; screenshots desktop +
      375px. gate: file-seam with ws-D/profile (see above).
      BITES (rule 5 — one PR each):
      bite a, OPEN FROM FEED: feed posts clickable → PostLightbox
        opens with an isOwner prop (edit/delete hidden on others'
        posts). no new routes.
      bite b, SHARE + PERMALINK: the /u/:username/p/:postId route
        (opens the lightbox over the profile), Share action
        (navigator.share + copy-link) on every post. gate: bite a
        merged. (D-deep-links' ?comment= anchor builds on this
        route later — leave a .context/ note when the route lands.)

[ ] D-lightbox-media-sizing (sub-lane ws-D/profile — PostLightbox
      seam; operator, 26.07 night pass, screenshot): the lightbox
      renders media SMALL with dead space — "maybe they can be bigger?
      or some kind of proper resizing to make it look right." media-
      first sizing: the modal scales to the media's aspect (up to
      ~90vh × ~90vw), the media pane sizes to the intrinsic
      aspect-ratio (the media record carries width/height since D21 —
      use them; fall back to on-load measurement), object-contain with
      no letterbox voids, the caption/actions rail keeps a fixed
      readable width beside (desktop) or below (mobile) the media.
      files: components/Bio/PostLightbox.tsx. acceptance: a landscape
      video fills most of the viewport width; a portrait photo fills
      most of the height; no big empty bands; screenshots desktop +
      375px with both orientations. gate: runs in the ws-D/profile
      chain (same file as D-post-visibility-toggle — sequential).

[ ] D-text-post-cards (sub-lane ws-D/profile chain, shared-component
      seam; operator, 26.07 night pass, screenshot): TEXT-ONLY POSTS
      ARE A VOID — a text post renders as a huge empty dark cell with
      tiny top-left text. "we can do something more creative and
      artistic with the background with text only posts." build ONE
      shared component (components/shared/TextPostCard.tsx — the
      shared/ dir is a named seam: own exports only) that renders
      text-on-generated-background: centered type scaled to length
      (short = display-size, long = comfortable paragraph), on a
      deterministic gradient/texture picked from the post id hash out
      of a small set of design.md token-derived palettes (stable per
      post, never random per render, never hardcoded hex). use it in:
      the profile Posts/Media grid cell, the feed card, and the
      lightbox. PLUS AUTHOR CHOICE (operator follow-up, same pass):
      "maybe some choice of background styles or formatting — facebook
      has a markdownesque formatter for text only posts, center, left
      align, that kind of stuff": the composer gets a background-style
      picker for text-only posts (deterministic palette stays the
      default when none chosen) and light formatting controls (align
      center/left; keep the formatter minimal — this is not a rich-
      text editor), persisted as body fields on the post record
      (e.g. text_style: {bg, align}) so every surface renders the
      author's choice; unknown/absent style falls back cleanly.
      composer touch = PostComposer.tsx (ws-D/feed's file — coordinate
      or stage after that chain's items merge). acceptance: the same
      post gets the same background everywhere and across reloads;
      an author-picked style + alignment persists and renders on grid,
      feed, and lightbox; short vs long text both look intentional; no
      hardcoded colors (review bar); screenshots desktop + 375px.
      gate: after D-feed-lightbox + the ws-D/profile chain items merge
      (it touches the same three surfaces — stage LAST of the lightbox
      cluster).
      BITES (rule 5 — one PR each):
      bite a, THE BACKGROUND: shared TextPostCard component with the
        deterministic post-id-hash palette, wired into grid + feed +
        lightbox. no composer changes.
      bite b, AUTHOR STYLE: composer background picker + align
        controls, text_style body fields, all surfaces render the
        choice. gate: bite a merged.

[ ] D-deep-links (sub-lane ws-D/shell; operator, 26.07: "everything
      should be a deep hyperlink on web10 social… every screen
      shareable, or bookmarkable if it's a private screen like dms" —
      and the rationale: marketing /trending click-throughs land on
      the EXACT thing: "when you click the comment [it] can bring up
      the post to that exact comment… makes it polished, aka
      killer"): D-url-routing (1.0.155) covered the five top-level
      screens; the state INSIDE them is still useState-only. sweep:
      (1) /messages/:conversationKey + ?view=chat|mail|crm (open
      thread + view toggle restore on refresh; private = bookmarkable
      by the owner, never renderable by others — auth already
      guarantees that, just don't leak content into the url);
      (2) /discover?tag=&q=; (3) /profile?tab=posts|media;
      (4) /staging; (5) post permalink /u/:username/p/:postId
      (COORDINATE: D-feed-lightbox files the same route — whichever
      merges first owns it, the other rebases) + ?comment=<id> anchor
      that scrolls to + highlights that comment in the thread;
      (6) provider disambiguation on user routes — identity is
      (username, provider), /u/:username assumes the home node;
      decide the canonical shape at implementation (path seg or query
      param), keep /u/:username back-compat. THEN upgrade marketing-ui
      click-throughs (TrendingCard, comment panel, search results) to
      the deep targets — funnel clicks land on the exact post/comment.
      the standing rule for future screens is in AGENTS.md ("UI
      screens: the URL holds the state"). files: App.tsx routes +
      each screen's state lift (DmsScreen, DiscoverScreen,
      ProfileScreen, PostLightbox) + marketing-ui FeedPreview/
      Trending link targets — a CROSS-SUB-LANE seam sweep: run it as
      ONE workspace AFTER the current fleet drains; do not run
      concurrently with any ws-D chain. acceptance: refresh restores
      every screen incl. open thread/view/tab/lightbox; a trending
      comment click lands on that comment highlighted in social;
      back/forward walk the history; screenshots desktop + 375px.
      gate: current 26.07-late fleet merged (it touches most of their
      files).
      BITES (rule 5 — one bite PER SCREEN when staged, each its own
      PR): messages (thread key + view param), discover (tag/q
      params), profile (tab param), comment anchor (?comment= on the
      permalink route — gate: D-feed-lightbox bite b), marketing
      retarget (TrendingCard/comment/search links point at the deep
      urls — gate: the relevant routes merged). never one sweeping
      branch.

[ ] D-marketing-search (sub-lane ws-D/marketing; operator, 26.07: "the
      one thing we are really missing now is search! searching by tag,
      even on the marketing page… youtube-esque and be an entry point
      into web10 social, if you could search from the marketing ui!"):
      PUBLIC SEARCH on marketing-ui as the funnel's front door. (1) a
      search bar on /trending (prominent, youtube-placement: header
      row) + a compact search affordance in the site nav; (2) queries
      hit the discovery API that already exists — A5.5 shipped PATCH
      /discover/search, /discover/topics, /discover/users (VERIFY
      their current request/response shapes in api/app/endpoints/
      discover.py before building; extend nothing server-side in this
      item); (3) results: posts render in the existing TrendingCard
      language, matching users as a compact row above; #tag queries
      filter/route through the existing topic-chip mechanics; (4)
      EVERY result deep-links into web10-social — /u/:username for
      users/authors (route exists), post permalinks once
      D-feed-lightbox's /u/:username/p/:postId route merges (until
      then, link the author profile — degrade, don't dead-click);
      (5) anon-first: search works signed-out (the discovery layer is
      anon-queryable) — that's the whole entry-point thesis. files:
      marketing/marketing-ui/src/pages/Trending.tsx + a new
      SearchBar/SearchResults component, nav touch in the header
      component. read design.md first. acceptance: type a word →
      matching public posts + users; type #tag → tag-scoped results;
      click-through lands on web10-social; works signed-out; empty
      state tells the story ("nothing yet — be the first"), no dead
      spinner; screenshots desktop + 375px. gate: none (post
      permalink deep-links degrade until D-feed-lightbox merges).
      BITES (rule 5 — one PR each):
      bite a, TRENDING SEARCH: the search bar on /trending + post
        results in the existing grid (text + #tag queries), signed-
        out. no nav changes, no user results.
      bite b, USERS + NAV: matching-users row, the compact nav
        search affordance, tag-chip routing polish. gate: bite a
        merged.

[ ] D-graph-app (sub-lane ws-D/marketing, AFTER D-marketing-search —
      same marketing-ui surface; operator, 26.07: "graph.web10.app,
      something wooshy is great marketing, because it is an app
      interoperating on web10 data… one outside analytical app on top
      of your social data"): a standalone, anon-first, read-only
      visualization of the node's social graph — users as nodes,
      follows as edges, force-directed ("wooshy": drag, zoom, physics,
      it breathes), node click → that user's /u/:username on social
      (the funnel again), hover → avatar/name card, maybe a "find me"
      search. THE POINT is the existence proof: a separate app built
      on the SAME public data from OUTSIDE the flagship — "apps are
      just lenses" demonstrated, the wordpress-flywheel slide made
      live. TWO MODES (operator follow-up: "it could render your
      personal graph, not public necessarily only!"):
        (1) ANON NODE VIEW — the whole node's public graph. DATA
            CONTRACT (hard): the PUBLIC follow ledger only (PATCH
            /public/entries, payload.action='follow' — D34, already
            anon-queryable) + /discover/users for names/avatars.
            NEVER private data in this mode — the anon view must be
            the sovereignty story's friend.
        (2) MY GRAPH (signed-in) — the ego graph: log in via the
            normal web10 auth/consent flow, the app requests a SCOPED
            token for the user's own follows (+ contacts if consented)
            and renders YOUR world — followers, follows, optionally
            contact/DM-derived edges. all client-side render under
            your own token, nothing published, nothing written. THIS
            IS THE DEEPER DEMO: an outside app walking the consent
            screen → scoped read → lens pipeline is the third-party
            app contract shown live (I5: least-privilege scoped
            token doing exactly what it consented to). and the
            personalization IS the pitch (operator: "you log in…
            so your graph is different from a friend['s]"): two
            friends logging in see two different graphs, because the
            app holds no global view — it renders only what each
            user's token grants. ownership, felt.
      FRAMING (operator): the anon global graph is the ENTRY POINT;
      "SEE YOUR GRAPH!!!!" is the hero moment and the CTA — "more
      than facebook ever let you in on" is the marketing line
      (platforms always HAD your graph and never let you look; here
      an outside app shows you, because it's yours). EXPLAIN IT
      in-product: a "how does this work?" popover (the D-trending-
      knobs truth-telling pattern) saying plainly: this is a separate
      app reading web10 data — anon mode reads the public ledger,
      logged-in mode reads YOUR records with a token you consented
      to; any developer can build this. + one short paragraph in the
      marketing-ui docs ("outside apps on your data"), manifesto
      voice. wherever else it's told (social app, deck), reuse the
      same copy — write it once.
      HOSTING: v0 is a marketing-ui route (/graph) —
      no new vhost/compose/DNS churn (that's a lane E touch when it
      earns graph.web10.app; leave a .context/ note then). rendering:
      pick a maintained force-graph lib (or hand-rolled d3-force) —
      design.md tokens for node/edge/glow colors, dark-canvas
      spectacle is the bar ("wooshy" is a design requirement, not
      decoration; read design.md first). files: marketing/marketing-ui
      new page + components, nav link. acceptance: the seeded persona
      graph renders alive and interactive, signed-out; node click
      lands on social's /u/:username; empty/sparse state says honestly
      that real follows appear as accounts join (see gate); 60fps-ish
      at demo scale; screenshots desktop + 375px. gate: soft on
      D-ledger-mirror-fix — build against personas now (they have
      cross-follows in the ledger via the seed script); real accounts
      populate as the mirror fix + follows land. D20 check: this is
      marketing/showcase surface, no ranking, no feed — on-thesis.
      BITES (rule 5 — one PR each):
      bite a, ANON GRAPH: the /graph route, force-directed public
        graph (ledger + /discover/users), node click → /u/:username,
        the explainer popover. no auth code at all.
      bite b, MY GRAPH: the signed-in ego view (consent flow, scoped
        token, personal edges). gate: bite a merged.

[ ] D-social-analytics (sub-lane ws-D/marketing, AFTER D-graph-app —
      same showcase surface, sequential; operator, 26.07: "ranked
      chess.com analytics on your social media, you versus friends,
      or the top creators… a cool analytical app interoperating on
      your web10 data"): the second outside-analytical-app beat —
      chess.com-feel stats over social data. signed-in, scoped-token,
      read-only (same contract as D-graph-app mode 2): (1) YOUR
      numbers — followers, posts, reactions/comments received, growth
      over time (charts); (2) RANKED — you vs your friends (the
      people you follow) and vs the node's top creators: percentiles,
      leaderboards, the chess.com rating-page feeling — all computed
      client-side from PUBLIC data (the ledger + /discover/users
      engagement counts), so no one's private numbers leak: you rank
      against what's already public. (3) honest-numbers framing ties
      to the pitch: "your number is real here" (THE STORY) — the
      analytics app is where that line becomes a dashboard. same
      explainer popover pattern as D-graph-app. files: marketing-ui,
      new /analytics route (or a tab inside the graph app's surface —
      decide at build). read design.md AND the dataviz bar: tokens,
      no chart-library default colors. acceptance: signed-in persona
      sees own stats + ranked view vs follows and top creators;
      anon visitors get a tasteful teaser + login CTA (no wall on
      explanation, wall on personal data only); screenshots desktop +
      375px. gate: D-graph-app merged (same chain); counts get real
      for real accounts as D-ledger-mirror-fix + A14/A15 land.
      BITES (rule 5 — one PR each): bite a, MY NUMBERS (own stats +
      growth charts, signed-in); bite b, RANKED (percentiles +
      leaderboards vs follows and top creators; gate: bite a).

[ ] D-appstore-revamp (sub-lane ws-D/marketing — the App Store page
      only, so it may interleave with the search/graph chain if its
      files stay untouched by them; operator, 26.07, screenshot):
      "ALL JUST APPS" — the store currently gives web10 social a
      jumbo FLAGSHIP hero and the node console/importer abstract
      feature-blurb cards, while actual registered apps (fallout
      avatar, translator) look like real apps: icon, visits, Open.
      the operator liked the OLD app store where "they really really
      really looked like apps," and explicitly does NOT want the
      first-party surfaces plugged so hard — "they will naturally get
      landed on" (top of the visit sort). revamp: (1) ONE uniform
      icon-forward app card for everything — real icon (first-party
      apps get proper brand icons per design.md §3, NOT lucide
      glyphs; registered apps already carry theirs), name, one-line
      description, visit count, Open — first-party and third-party
      identical shape; at most a small "flagship" chip, no hero, no
      separate sections; (2) sort by visits (the D16/A7 restored
      counts) — prominence is EARNED, which is itself the pitch (the
      store doesn't rig itself; same trust story as the no-algorithm
      feed — put that line in the page's subtitle if it fits the
      voice); (3) PREMIUM bar — "this is like the apple app store,
      PREMMIUMMM!!!!!": big rounded app icons, generous spacing,
      crisp typography, hover lift, skeleton shimmer, all design.md
      tokens, flawless at 375px. files: the marketing-ui App Store
      page + its card components (find via the D16 curation work,
      1.0.136). read design.md first. acceptance: every entry renders
      as the same app-shaped card with a real icon; first-party has
      no hero treatment; visit-sort puts social/authenticator on top
      naturally; screenshot desktop + 375px and LOOK — the bar is
      "apple app store screenshot" credible. gate: none (independent
      page).
      V2 — THE PRODUCT PAGE (operator follow-up: "when you see the
      apps like it is on your iphone, the big logos… and when you
      click them you see a description with comments! that would be
      so cool"): (1) the grid goes iphone-home-screen icon-forward —
      the BIG logo is the card; (2) click → an app DETAIL page:
      big icon, name, description (from the registered-app record),
      visit count, screenshots slot (empty-graceful), Open CTA, and
      COMMENTS/reviews. BACKEND (the operator flagged it un-figured;
      v0 needs NOTHING new): reviews are D32 public-ledger entries
      targeting the APP id — the exact machinery post comments use
      (anon read via PATCH /public/entries; compose auth-gated, the
      trending-comments funnel pattern; same engagement counting).
      FEDERATION honesty: where an app's reviews live when the app
      runs on many nodes is genuinely unsolved — do NOT invent a
      protocol here (CLAUDE.md rule); v0 renders THIS node's reviews
      with an honest label, cross-node aggregation is an M3
      federation question filed with the rest of M3.
      REGISTRATION REWIRE (operator follow-up: "app registration may
      need to be rewired to go through the same public avenue /
      system as the other!"): today's registered apps live in a
      legacy registration store (the A7-restored records from the
      live app-store era). the on-thesis target: app registrations
      become records on the SAME public data plane as everything
      else — a schema-registered public entry (an `App` schema in the
      registry, like Post/Comment/Follow/Reaction) that the discovery
      layer indexes; then the store is just a lens over public data,
      reviews target app entries natively, and federation inherits
      whatever answer the public layer gets — one system, no side-
      channel. AND IT'S THE MODERATION FIX (operator: "you approve
      [an] app, and then [the] app changes photo to something
      inappropriate — the pwa thing can totally bypass moderation
      loopholes"): listing metadata (icon, name, description,
      screenshots) must be NODE-HOSTED RECORDS, never hot-linked
      from the app's own backend — then approval attaches to a
      VERSION of the metadata, any listing edit is a write the node
      sees, and a changed listing drops back into review
      (approved → pending-on-change). the store NEVER auto-pulls
      assets from the app's origin (a pwa manifest/thumbnail change
      is not silently mirrored — "new thumbnail on a pwa got
      submitted, that kind of thing": updates arrive only as
      explicit re-submissions that enter the review queue).
      bait-and-switch becomes structurally impossible instead of
      policy-patrolled — moderation by architecture, and a phase 12
      t&s input. this is
      a LANE A spec question (write the shape + review-state
      machine + migration of existing registered apps in a
      .context/ note or decisions.md entry BEFORE v2 builds against
      it; v1's visit-sorted card grid reads the legacy store as-is). gate for v2:
      the v1 revamp merged + the registration-shape decision; comment
      compose benefits from D-ledger-mirror-fix landing but the
      funnel pattern works without it.
      BITES (rule 5 — one PR each): v1 is ONE bite (the card-grid
      rework — one page, bounded). v2 splits: bite a, PRODUCT PAGE
      (detail route: big logo, description, visits, Open — no
      comments yet); bite b, APP COMMENTS (the D32 ledger panel on
      the product page; gate: bite a); bite c is LANE A's
      registration-shape spec note (a .context/decisions.md write-up,
      no code — gates the rewire build, which bites separately once
      specced).

[ ] D-poke (sub-lane ws-D/engagement; mostly independent): an "advanced
      poke" — a playful, lightweight nudge primitive (the facebook-poke
      feeling) reimagined as web10's OWN, NOT a clone (name + interaction
      TBD, keep it non-infringing). Could ride the public-ledger
      reaction plumbing (reactions.ts + /public/entries). Files: new
      data/pokes.ts (or extend reactions), a small UI surface on the
      profile / user card. acceptance: send a nudge to another user, it
      shows up for them; screenshot. gate: none (design first — propose
      the concept in a .context/ note for operator sign-off before
      building the surface).

[ ] D-mobile-feature-gap (sub-lane ws-D/shell; operator, 27.07): the
      mobile responsive site has NO access to the new features visible
      on desktop — all "Coming soon" chips and new surfaces are hidden
      on 375px. The mobile user is walled off from the product's
      evolution. Audit every "Coming soon" toggle and responsive guard:
      if a feature is ship-ready on desktop, it should either ship on
      mobile too or have an explicit, documented reason for the
      exclusion. Files: App.tsx routes, sidebar/nav responsive logic,
      any viewport-gated feature flags. acceptance: a 375px viewport
      can reach the same features as desktop (or the gap is documented
      with a migration plan per feature). gate: none.

[ ] D-inapp-discover-knobs (sub-lane ws-D/marketing or ws-D/feed;
      GATE: a D20 REVISIT decision — do NOT build before it): the
      operator wants the /trending "your algorithm" knobs in the SOCIAL
      APP's Discover too. D20 currently says the social feed stays
      chronological and the knobs are marketing-page theater. This is a
      product-decision reversal, not a bug — record the decision in
      decisions.md first. If approved, port KnobRack/RotaryKnob/
      powerMean from marketing-ui into web10-social (separate package —
      copy per design.md §D22 "verbatim copies beat premature sharing")
      and wire them to DiscoverScreen's client-side ranking. Files:
      web10-social components/Discover/**, copied knob components.
      acceptance: knobs re-rank the in-app Discover live; screenshot.
      gate: D20 revisit signed off in decisions.md.

[ ] D-coming-soon-builds (sub-lane ws-D/surfaces; SEVERAL big
      independent surfaces — one workspace EACH, not one item): the
      sidebar "Coming soon" chips (shipped 1.0.170/1.0.172) are stubs;
      these are the real builds, each its own workspace: Flares
      (ephemeral 24h posts — the "stories" shape; provisional name,
      confirm rename), Takes (short vertical video — the "reels" shape;
      provisional name), Livestream (rtc small-scale → hls fan-out —
      already plan.txt phase 8 later-milestone), Games (scope undefined
      — spec first), Groups (multi-user spaces), Marketplace (creator
      commerce). Each: own sub-lane, own components dir, own data
      service. gate: per-surface spec first; none blocks another.

[ ] D-night-owl (sub-lane ws-E/infra — the AI team that moonlights so
      you don't have to; operator, 27.07: "the 10x potential — i am not
      orchestrating, i am in the back giving my two points on SMS").
      PRIORITY (operator, 27.07): ESSENTIAL, priority-zero — the big
      blocker to hyperspeed development; fleet orchestration cost is
      the bottleneck, so this IS the product-velocity work. runs in
      parallel with the product bites (night_owl/** shares no seam).
      the supervisor loop runs every 40min on your Mac via CRON
      (operator correction, 27.07 — a plain cron entry or launchd
      plist; Hammerspoon is NOT required and nothing gates on it).
      screenshots Conductor, an AI vision agent sees the UI and drives
      it — creates workspaces, sends prompts, reads results, kicks off
      K3 bites. on crucial moments (web10web10, gather up, brick),
      texts you like your top engineer. the AI is the eyes — adapts to
      UI changes, no hardcoded coordinates. files: night_owl/ (init.lua,
      conductor_agent.lua, texter.lua, config.json, README.md) — or a
      plain-python/swift equivalent; the .lua names assumed Hammerspoon,
      so with cron the file layout is the implementer's call (keep the
      README + config.json + texter ideas).
      UNBRICK TRIGGER (acceptance criterion, from the 27.07 code-word
      fold): noticing a stalled/looping/errored workspace and raising
      `unbrick!` (text + a prepared kickoff) is a REQUIRED night-owl
      behavior, not a nice-to-have — the fire alarm is its job.
      BITES (rule 5 — one PR each):
      bite a, SCAFFOLD: the loop skeleton — cron entry (40min), AI
        vision pipeline (screenshot Conductor → valid AI actions out),
        texter, config, README. Cmd+Opt+Ctrl+C (or a --once flag)
        triggers a manual cycle that screenshots Conductor and returns
        valid AI actions.
      bite b, CONDUCTOR PILOT: AI navigates Conductor — new workspace,
        model select, type prompt, hit enter, read result.
      bite c, SUPERVISOR LOOP: full "should we do it?" pipeline —
        Fable audit, K3 workspaces created per bite, progress tracked.
      bite d, TEXTING + UNBRICK WATCH: crucial events fire SMS in
        Steve Jobs style; a stalled/bricked workspace raises `unbrick!`.
      bite e, 24/7: cron enabled for real, fleet runs overnight.
      gate: none (cron needs no special permissions; Accessibility/
      screen-recording perms only if the vision path drives the UI
      directly). acceptance: wake up to merged PRs and a summary text.

LANE E — infrastructure / deployment (owns: proxmox provisioning,
    reverse proxy, CI/CD deploy targets)
[✓ 1.0.60] E1. proxmox staging node: Portainer + Nginx Proxy Manager
       infra (prep-vm.sh, docker-compose.staging.yml self-contained stack,
       Cloudflare DNS plan). ubuntu-deploy.sh replaced by Portainer UI
       approach. README.md updated. DEPLOYED 19.07.2026:
       api live at staging.web10.app, ui serves at
       auth.staging.web10.app but is broken by hardcoded origins
       (lane B fix queued in B5); rtc/minio DNS records missing;
       marketing/social not in the stack yet. live triage +
       ops procedures: ubuntu-deployment/AGENT-OPS.md + OPS-LOG.md.
[✓ 1.0.38] E2. marketing deploy: marketing-ui + marketing-api on proxmox,
       caddy reverse proxy with auto-TLS. internal staging complete.
  [✓ 1.0.68] E3. PROD env — the whole ecosystem public (specced in plan.txt
      CROSS-CUTTING deployment "environments"): prod marketing-ui +
      marketing-api + node (api, ui, social, rtc, minio, db) behind
      NPM with public cloudflare DNS + router-forwarded 80/443 +
      real TLS. infra panels stay LAN/VPN-only (E1 security model).
      REPO SIDE DONE (1.0.66): docker-compose.ecosystem.yml (one
      parameterized compose, dev + prod — the staging env was cut),
      env.prod.example, social prod Dockerfile, runbook. REMAINING:
      box execution — create the web10-prod Portainer stack + NPM
      hosts + DNS per README.md and AGENT-OPS.md §4.2 migration
      order (needs SSH/.env + CF creds; watch the
      api.web10.app/auth.web10.app cutover caution).
  [✓ 1.0.68] E5. DEV env — the SAME whole ecosystem, VPN-only, on the same
      linux box ubuntu-deployment/ preps (parallel to E3, and to
      the B5/D12/D13 ui revamp): cloudflare DNS for dev vhosts
      (*.dev.web10.app or similar) pointing at the box's INTERNAL
      LAN ip — resolves publicly, unreachable off-VPN, zero
      port-forwards, TLS still works (DNS-01 needs no inbound).
      second Portainer stack from the same parameterized compose,
      own volumes + env file. promotion flow (dev soak -> prod
      redeploy) documented in README.md.
      REPO SIDE DONE (1.0.66): compose + env.dev.example + DNS-01
      procedure + promotion flow all in README.md.
      REMAINING: box execution — web10-dev stack, *.dev DNS records
      pointing at the LAN ip, DNS-01 certs. ui/social bundles wire
      to dev origins only after B5/D12's env fixes merge (compose
      already passes the build args).
  [ ] E4. automated provisioning: terraform or ssh-api script +
      ansible for idempotent VM/LXC setup. don't rush — manual is
      fine until E2 works.
[✓ 1.0.134] E6. SSH-DEPLOY CI/CD — CORE LANDED: .github/workflows/deploy.yml
       deploys web10-dev on dev pushes and web10-prod on main pushes,
       runs scripts/smoke.sh in the job, and Portainer GitOps
       auto-update was disabled box-side (OPS-LOG 23.07) — deploy.yml
       is the single deployer per env, which is the item's load-bearing
       goal. NOT built from the spec below: the Portainer stack-register
       API call and the tagged-image rollback — re-open as a small
       follow-up only if the operator misses them.
       (original spec, kept for that follow-up:) the current Portainer
       GitOps loop (5-min poll, silent
       retries on failure, zero signal back to GitHub) is brittle —
       when a deploy fails, Portainer retries forever in a loop and
       GitHub has no idea. the replacement: GitHub Actions SSHes into
       the Ubuntu server, runs `docker compose up` directly, captures
       exit codes, and runs the smoke test. GitHub now knows instantly
       if a deploy succeeded or failed. no self-hosted runner needed
       (avoids the public-repo security issue — no runner in the
       docker group on the box).

       AUTH: deploy key (SSH key-pair, not PAT). the private key
       lives as a GitHub Actions secret (`DEPLOY_SSH_KEY`), the public
       key is installed on the box at `~/.ssh/authorized_keys` with a
       command restriction scoped to the deploy script only — the key
       can't be used for general shell access.

       WORKFLOW: new `.github/workflows/deploy.yml`:
         - triggers: push to `dev` → deploy `web10-dev`; push to
           `main` or `v*` tag → deploy `web10-prod`
         - runs on `ubuntu-latest` (ephemeral GA runner)
         - steps:
           1. checkout
           2. SSH→box (`ssh -o StrictHostKeyChecking=accept-new`)
           3. `cd /opt/web10 && git pull origin <branch>`
           4. `docker compose -p <stack> --env-file env.<env> -f
              ubuntu-deployment/docker-compose.ecosystem.yml
              up -d --build --remove-orphans`
           5. wait for health (poll `docker ps` for stable state,
              max 5 min)
           6. `bash ubuntu-deployment/scripts/smoke.sh` (exits
              non-zero on failure → GitHub marks the job failed)
           7. on success: Portainer API call to register/update the
              stack metadata so the Portainer UI reflects the deploy
              (see below)
         - concurrency: `deploy-<env>` group, cancel-in-progress

       PORTAINER INTEGRATION (post-deploy register): Portainer is
       just a UI on top of Docker — it watches the Docker socket.
       when you `docker compose up` on the box, Portainer SEES the
       containers automatically (they show up in the "Containers"
       view). to also have the stack appear in the "Stacks" view
       (for UI management: restart, logs, env-var editing), call the
       Portainer API AFTER a successful deploy to create/update the
       stack entry. PREFERRED approach: FILE-BACKED stack via
       Portainer API — point at the compose file on disk
       (`/opt/web10/ubuntu-deployment/docker-compose.ecosystem.yml`).
       this gives Portainer full stack management (restart, rebuild,
       logs, env-var editor) WITHOUT GitOps polling. the deploy
       script calls `POST /stacks/create/standalone/file` (or PUT to
       update) after docker compose succeeds. keeps Portainer as the
       management UI without making it the deploy trigger.

       DISABLE GITOPS: remove the `autoUpdate` field from existing
       stacks (or delete and recreate as file-backed). the GitOps
       polling is the source of the "retrying" loop — it must be
       killed. `deploy-stacks.py` currently sets `"autoUpdate":
       {"interval": "5m"}` on create; the new deploy script must
       create file-backed stacks (no autoUpdate) or update existing
       stacks to disable autoUpdate via `PUT /stacks/{id}`.

       SECURITY: the SSH deploy key is scoped. even if compromised,
       it can only run the deploy script (command restriction in
       authorized_keys). the deploy script runs as the non-root SSH
       user (jacob), who is in the docker group. no sudo, no root.
       the key is rotated by replacing the GitHub secret and the
       authorized_keys entry.

       ROLLBACK: if smoke test fails, the workflow can automatically
       rollback: `docker compose -p <stack> down` then `docker compose
       -p <stack> up -d` to restore the previous image (Docker keeps
       the old image until the next build). or more reliably: tag
       images at deploy time (`<stack>:<sha>` and `<stack>:previous`)
       and rollback is `docker compose up -d --no-build` with the
       previous tag. the smoke test failure is visible in GitHub
       Actions with full logs — the operator can decide rollback vs.
       fix.

       RATIONALE: this is the standard pattern for teams that want
       CI visibility without self-hosted runners. the SSH-from-
       ephemeral-runner pattern is used by Railway, Render, and
       Fly.io internally. the advantage over Portainer GitOps:
       instant feedback (not 5-min poll), failure is visible in
       GitHub (not a silent retry loop), the smoke test is part
       of the deploy (not a separate manual step), and concurrency
       control prevents concurrent deploys from fighting.

       BLOCKS: none — can start immediately. depends on the operator
       generating an SSH key pair and adding the private key as a
       GitHub Actions secret. the operator must also install the
       public key on the box with the command restriction.
  [✓ 1.0.100] E7. SDK npm publish flow (with C2/D18): cd.yml publishes sdk/
      to npm on a v* tag today — confirm it fires + the package is
      public, and decide whether a version bump on merge to dev/main
      should auto-publish so the sdk stays fresh (operator, 19.07:
      "publish the web10 sdk to npm if it updates"). small.
  [ ] E8. MOBILE ENCRYPTOR → app stores (future, operator 19.07 —
      "just dont want to lose track"): mobile/encryptor is an Expo
      app (the phone-as-keychain seed, D3/D6). when it's ready,
      deploying to the Apple App Store + Google Play becomes real
      work: expo eas build + submit pipeline, store listings,
      signing certs/provisioning, review. NOT now (post-M0, gated on
      the encryptor actually doing e2e key management) — parked here
      so it's on the board, not lost.
  [ ] E-run-discovery-migration (ops, small, HIGH demo impact; operator
      screenshot 26.07: his real public post absent from /trending
      Newest on the deployed app): A13 (✓ 1.0.178) shipped the
      discovery-terms fix as code + ADMIN ENDPOINTS — POST
      /admin/discovery/migrate_terms and POST /admin/discovery/backfill
      — which nobody has RUN. Until they execute per environment,
      every pre-existing account (the operator's included) stays
      undiscoverable: no anon-read term on public_posts → new posts
      not indexed, old posts not backfilled. Task: after the next
      dev→main promotion carries 1.0.178, run both endpoints against
      dev, verify (the operator's post appears in PATCH
      /discover/posts?sort=recent + on /trending Newest), then run
      against prod and verify the same. needs an admin token + box/env
      access — if the workspace has neither, stop and say so (operator
      gate, same as the E box-execution items). record the run in
      ubuntu-deployment/OPS-LOG.md. gate: 1.0.178 deployed to the
      target env.
  [✓ 1.0.71] E9. DEPLOYMENT STATUS PAGE (operator, 19.07 — iterated from
      "portainer should alert github" down to what's actually
      wanted: "all i need is an easy way to know we are deployed, a
      url i can visit ... the version, the commit, the date, the
      PR"). one URL per env — status.web10.app / status.dev.
      web10.app — showing the juicy info about what's live:
        - VERSION: the top entry of CHANGELOG.md at the deployed
          commit (the repo's release number, e.g. 1.0.71)
        - COMMIT: sha + the squash title (which carries the PR #,
          e.g. "... (#130)") + author date
        - DEPLOYED AT: when the stack last updated
        - per-service health dots (reuse scripts/smoke.sh checks)
      preferred design — ZERO new machinery: the stacks already
      rebuild from git on every change (GitOps), so add a tiny
      static status page BAKED AT BUILD TIME (a build step reads
      git sha/date/title + CHANGELOG top and emits status.html/
      status.json; serve via a minimal nginx container or an
      existing frontend's public dir) + one NPM vhost + DNS record
      per env. every auto-redeploy refreshes it by construction —
      no poller, no github token, works for VPN-only dev too.
      (portainer's API does track the deployed sha — verified — so
      a box-side poller remains the fallback design if the
      build-time stamp proves awkward.) json endpoint doubles as
      the version marker future tooling can check. below PRIORITY
      ZERO — do after A7/B6.

WHAT MUST STAY SEQUENTIAL (don't try to parallelize these)
----------------------------------------------------------
- A1 -> A2: both churn api internals; ferretdb testing against a
  moving pydantic target is pain for nothing
- B1 -> B2 -> B3 -> B4: same codebase, one lane, in order
- wave 0 before A1: tests exist before the upgrade they referee
- C2 -> C3.5: the scaffolder's templates are built on the new sdk;
  scaffolding legacy-wapi apps would poison the dev on-ramp
- D1 before C4/D4-schemas/D5: everyone imports the conventions
- P12 trust & safety tooling needs B4's admin panel; but the csam/
  dmca DECISIONS (vendors, process) are d1-style writing -- can
  happen any time, and must before M1 ships hosted media


INTEGRATION MOMENTS (when lanes must sync)
------------------------------------------
M0 "stands on its own"   = A3 + B3 + D4 converge. first real
                           cross-lane assembly; expect a dedicated
                           integration branch + a day of glue.
M1 "import your life"    = C1 + C4 + D5 + M0 node.
M2 "the first creator"   = B4 + A5 + custom domains (A3 packaging).
M3 "the network"         = encryption integration (A rtc + C2 sdk
                           crypto + D3 keychain app) + federation
                           polish. the last big merge.


CURRENT CONDUCTOR BOARD (as of 27.07.2026 — audit #6; keep fresh)
--------------------------------------------------------------------
AUDIT #6 REFRESH (27.07): zero open PRs, zero [~] items — the fleet is
IDLE, not in flight; every kickoff below starts fresh. Gates cleared
since audit #4: D-messages-views merged 1.0.182 → D-message-controls
UNBLOCKED; D-profile-post-experience merged 1.0.180 → the whole
ws-D/profile chain (D-post-visibility-toggle first) UNBLOCKED.
D-ledger-mirror-fix re-verified broken at HEAD (registerDefaultSchemas
never called outside feed.ts; createPublicEntry still POSTs the raw
entry vs public.py's {token, query} shape). D-feedback-contact-privacy
re-verified leaking at HEAD (main.py:487 appends Contact: into an
anon-readable public post; squash title of PR #312 wrongly says "DMs
to operator" — the code does public posts; the PII strip is unbuilt).
D-night-owl re-verified at HEAD as NOT started (no night_owl/ dir).
D-night-owl PRIORITY REVERSED (operator, 27.07): it is ESSENTIAL,
priority-one — the blocker to hyperspeed development; the fleet's
orchestration cost is the bottleneck, so the tool that runs the fleet
is the product-velocity work. It no longer stages behind product
bites — it runs in PARALLEL with them (its files, night_owl/**, share
no seam with any product lane). Scheduler corrected to cron (operator,
27.07) — Hammerspoon is not required, don't gate on it.
The 26.07 night-capture fleet DRAINED SAME-DAY: A13-discovery-terms
✓ 1.0.178, D-profile-post-experience ✓ 1.0.180, D-messages-views
✓ 1.0.182, D-bug-reports-delivery ✓ 1.0.181 (as PUBLIC POSTS, not the
DM option — operator to confirm; privacy fix filed, see PRIORITY 2),
D-join-two-step ✓ 1.0.178, D-poke proposal filed 1.0.179 (awaiting
operator sign-off). PR #311 landed the D-public-media-client DATA
LAYER only — unlogged (retro-logged 1.0.183) and with zero call sites
wired, so cross-user images STILL 403 (item stays open, see its
STATUS). Meanwhile the operator ran a live pass on the deployed app
(screenshots) — captures filed below and in plan.txt PHASE 8.6.

  PRIORITY 0 (operator, 27.07 — ESSENTIAL, the hyperspeed blocker):
    ws-E/night-owl: D-night-owl — the supervisor loop that runs the
      fleet (creates workspaces, kicks off bites, watches for bricks
      and raises `unbrick!`, texts the operator at crucial moments).
      The orchestration cost IS the bottleneck; this tool removes it.
      Runs in parallel with the product bites (night_owl/** shares no
      seam). Scheduler is a plain cron — NOT Hammerspoon-gated.

  PRIORITY 1 (the pitch's numbers are theater for real accounts):
    ws-D/ledger: D-ledger-mirror-fix — the app's public-ledger mirror
      is DEAD (registerDefaultSchemas never called + createPublicEntry
      sends the wrong body vs public.py → silent stub). No real
      account's follows/likes/comments ever hit the ledger: follower
      counts + trending engagement work for seeded personas ONLY.
      Verified live at HEAD 26.07. data/feed.ts + one bootstrap line.
    ws-A: A14-followers-count — the api half (/discover/users returns
      followers_count so /u/:username can show social proof).
    ws-E (operator-gated): E-run-discovery-migration — A13's admin
      migrate+backfill endpoints have never been RUN; until they
      execute on dev/prod, the operator's own posts stay off
      /trending (his 26.07 screenshot). needs admin token + env.

  PRIORITY 2 (small, PII): ws-D/feedback: D-feedback-contact-privacy —
    1.0.181 publishes the bug reporter's CONTACT INFO in an
    anon-readable public post. strip it; fix the stale "DM" docstring;
    flag the public-vs-DM deviation for operator confirmation.

  PARALLEL-SAFE FLEET (26.07 late — one workspace per sub-lane; the
  chains WITHIN a sub-lane are sequential, file seams):
    ws-D/ledger:     D-ledger-mirror-fix (PRIORITY 1)
    ws-A:            A14-followers-count (PRIORITY 1), THEN
                     A15-engagement-count-accuracy (badge vs panel
                     mismatch — same documentdb.py aggregation seam,
                     sequential in this workspace)
    ws-D/feedback:   D-feedback-contact-privacy (PRIORITY 2)
    ws-D/marketing:  D-marketing-search (public search on /trending +
                     nav — tags/text/users via the existing
                     /discover/search + /topics; the youtube-esque
                     entry point into web10-social; anon-first),
                     THEN D-graph-app (the wooshy follow-graph
                     visualization — the "outside app on your social
                     data" existence proof; same marketing-ui surface,
                     sequential), THEN D-social-analytics (chess.com-
                     style ranked stats — same chain). D-appstore-
                     revamp (store page only, no file overlap with
                     the chain) may run as a SECOND marketing
                     workspace in parallel.
    ws-D/feed:       D-public-media-client REMAINDER (wire the call
                     sites — see the item's late-26.07 STATUS; touches
                     ProfileScreen.tsx, run BEFORE the ws-D/profile
                     chain's placement item), THEN D-follow-backfill,
                     THEN D-feed-lightbox (feed posts open the
                     lightbox + Share action — PostLightbox seam with
                     ws-D/profile: coordinate via .context/), THEN
                     D-link-embeds (same seam cluster)
    ws-D/profile:    D-post-visibility-toggle, THEN
                     D-lightbox-media-sizing, THEN
                     D-profile-image-placement (rebase after
                     D-public-media-client's ProfileScreen touch),
                     THEN D-follow-lists, THEN D-text-post-cards
                     (stage LAST — spans grid+feed+lightbox+composer)
    ws-D/messages:   D-message-controls (delete/edit), THEN
                     D-mail-experience (REAL email: folders inbox/
                     sent/spam + subject/reply/cc/bcc/forward —
                     operator's "kickass feature" ask), THEN
                     D-crm-upgrade (old-CRM parity: status colors,
                     bins/sort, contact fields)
    ws-D/settings:   D-settings-tab (v0 sections + .context proposal
                     for the full facebook-style tree)
    GATED (don't pick up until the gate clears):
      D-deep-links (gate: the current fleet merged — it sweeps most
      ws-D files). NOTE ITS WEIGHT: the operator's funnel polish
      ("trending click → the exact post, comment click → that exact
      comment, highlighted") only works with advanced links to
      EVERYTHING — total coverage, not partial ("it only works if
      there are advanced links to everything"). it is the FIRST item
      staged when the fleet drains, and the AGENTS.md url-holds-state
      rule keeps new screens from re-opening the gap meanwhile.
      D-poke build (gate: operator sign-off on the 1.0.179 proposal).
      D-inapp-discover-knobs (gate: D20 revisit — operator sign-off).
      D-feed-avatar-resolution + the D-user-profile-media grid half
      (gate: public-media path END TO END — the D-public-media-client
      remainder merged and verified on dev).
      D-coming-soon-builds (gate: per-surface spec first; one
      workspace each when staged).
      E-run-discovery-migration (gate: 1.0.178 deployed to the target
      env + admin token — operator provides).
  Selection rule (CLAUDE.md): only truly-parallel items at once. The
  PostLightbox/FeedScreen/PostComposer files form ONE seam cluster
  spanning ws-D/feed and ws-D/profile — the two chains coordinate via
  .context/ notes and never edit the same file concurrently.
  BITE RULE (rule 5, operator 27.07): the chains above are QUEUES of
  bites — a kickoff hands an agent exactly ONE bite (one PR, ~20-40
  min); multi-bite items carry a BITES: breakdown in their lane text.
  never kick off "the chain".


PRIORITY ONE (plan.txt, top — supersedes 19.07's priority zero,
whose baseline chain A7/B6/D16.1/D17 is merged): THE KILLER APP,
PROUD AND WORKING, before anything else. the only question a task
must answer: "does this make web10-social something the operator
opens on his phone, in front of someone, and is proud of?" (D29)
[✓ 1.0.140] ws-G1: run THE GAUNTLET (plan.txt top, 8 steps) against dev;
          write the honest fail list (docs/gauntlet-23.07.2026.md);
          fix in order of demo impact. lane D owns app fixes, lane A
          owns api fixes, lane B owns the auth/consent seam.
          HONESTY NOTE (23.07): the 1.0.140 run was a DESK gauntlet —
          api probes + code audit; its own method line says "NOT a
          real phone." the operator's actual phone passes (22-23.07)
          found what the desk run missed (legacy D24/D25,
          D-profile-media-refresh, the trending-card theater). the
          tick stands for the fail list, not for the bar.
  STANDING until the M0 gate: operator phone pass, weekly, a calendar
          event not an intention (timeline.md standing rules). every
          finding is filed as a lane item SAME-DAY — the 22-23.07
          loop (find → file → fix within a day) is the referee that
          works; keep it running.
  [✓ 1.0.145] ws-G2: keep the personas alive (persona-orchestration/) so feed +
         trending never demo empty. Seed script is now idempotent
         (read-before-write on every create; stable origin_id on posts +
         DMs; --cleanup removes dups from prior buggy runs; --verify reports
         state without writing). 2 new posts/persona + 4 reply DMs added.
  [✓ 1.0.145] ws-G3: encode each passing gauntlet step as a playwright journey
         in e2e/ (lane C — this is C6 continued, NOT C2).
GAUNTLET RESIDUE — DRAINED 24.07: all six 23.07 kickoffs merged
same-day (D-profile-media-refresh 1.0.157, D-url-routing +
D-login-cta 1.0.155, D-dm-compose 1.0.155, D-follower-count 1.0.158,
D-trending-comments + D-join-page 1.0.155, A11 1.0.155) plus the
follow-on fixes 1.0.159-1.0.162. zero open PRs at board refresh.

THE 24.07 AUDIT FINDING (drives this round): a real user's post
reaches NO ONE (composer → private_posts, no fan-out anywhere — see
D-post-delivery), and cross-user media has NO read path (owner-only
terms + presign denial — see A12 / D-public-media-client / D35).
gauntlet steps 2-3 pass on seeded personas only; the delivery pitch
is unimplemented for real accounts. this round is delivery + public
media, still squarely priority-one work.

IN FLIGHT NOW (24.07 — one workspace per sub-lane):
  ws-D/feed:      D-post-delivery (composer visibility default-public
                  + inbox fan-out on write; gauntlet steps 2-3 for
                  real accounts)
  ws-D/staging:   D-staging-review (D19 Phase C screen; new sub-lane,
                  components/Staging/** + data/staging.ts)
  ws-D/marketing: D-trending-knobs (gate D-trending-comments merged
                  1.0.155)
  ws-A:           A12 public-media read path (api side of D35)
  ws-C:           gauntlet journeys — [✓ 1.0.167] step-6 refresh
                  regression pin, step-5 fixme flip (D-dm-compose UI
                  flow), step-3 follow journey (follow button +
                  /u/:username + follower count, all UI-driven). Next
                  ws-C item: ws-C/cross demo-node theming + real-takeout
                  seed battle test (NEXT queue above, timeline.md week 3).
  ws-E:           BOX EXECUTION remainder (AGENT-OPS.md §4.2) + the
                  off-box backup + restore drill (plan.txt KNOWN
                  GAPS). operator gate: needs box SSH + .env — if the
                  workspace has no box access, stop and say so.
NEXT (stage as gates merge — don't run now):
  D-public-media-client (gate: A12) → then D-feed-avatar-resolution +
  the D-user-profile-media grid half (gate: public-media path
  end-to-end); D22 vertical feed (gate D21 merged — stage when
  ws-D/feed frees up, same sub-lane); ws-C/cross demo-node theming +
  seed via a REAL takeout import (timeline.md week 3, aug 3-10 —
  first exporter battle test; log every parser break, promote phase
  9's llm-assisted mapping on the first one); then low-hanging fruit
  by demo impact — realtime feed refresh, notifications,
  search-your-own-stuff.
ON DECK — M2 PREREQUISITES (stage when the gauntlet closes; these are
plan.txt KNOWN GAPS with no lane items yet, and all three gate
creator #1): memberships wired to stripe rails (B4.5 rung-0 →
functional — owned revenue in month one is the retention lever);
hosted ops floor decision (a $40 vps + one-container node + caddy is
enough — pick it before the first yes); t&s gate (dmca/csam vendors +
process decided before any hosted community posts media).
PARKED until the gauntlet passes (do NOT pick up — they read as an
infra company, not a social platform): C2 sdk rewrite, C3 mcp,
C3.5 create-web10, D11 ux telemetry, A9 transcode pipeline, E4
provisioning, E8 mobile store submission, docs/knowledge prose
beyond fixes. (D21 media polish + D22 vertical feed are NOT
parked — they are gauntlet-step-2 / demo-impact work.)

previous board (19.07, for history — all merged):
  ws-A: A7 connect the real host mongo — [✓ 1.0.72]
  ws-B: B6 authenticator revamp — [✓ 1.0.72] (+ B7 [✓ 1.0.76])
  ws-C: C6 e2e deep sweep — [✓ 1.0.74]
  ws-D: D17 dev docs [✓ 1.0.72] + D16 app store [✓ 1.0.136 —
        curation landed 23.07]

  ws1: [✓ 1.0.65] B5 level-up ui/ to design.md (lane B — ui/**) — merged
  ws2: [✓ 1.0.64] D12 level-up web10-social to design.md (lane D —
       marketing/web10-social/** only) — merged, see decisions.md D24
       for the alternative.png asset correction (shared with D13).
  ws3: [✓ 1.0.63] D13 rebuild marketing-ui on the design system, bulma
       out (lane D — marketing/marketing-ui/** only) + partially pays
       the shared brand asset debt (design.md §3) — icons + og-image
       done and dropped in .context/brand-assets/ for B5/D12 to apply;
       svg vectorization still open (no tracing tool available).
  ws4: E5/E3 REPO side merged in 1.0.66 (one parameterized
       docker-compose.ecosystem.yml for dev/prod incl. social +
       marketing, env.{dev,prod}.example, edge NPM stack, runbook
       in README.md). staging as an env was CUT 19.07 (dev + prod
       only). what's left is BOX EXECUTION, the full ordered
       migration in AGENT-OPS.md §4.2: stop legacy caddy → edge NPM
       stack → dev/prod DNS (dev → LAN ip, DNS-01 certs) →
       web10-dev + web10-prod stacks + NPM hosts → smoke tests →
       decommission the legacy *.staging stack/records/caddy.
       B5's ui origins fix is MERGED (1.0.65) — a fresh stack build
       serves per-env origins; only social still needs D14. operator
       prerequisites: docker group for the ssh user, rotate the CF
       token exposed in the world-readable Caddyfile.
ws5: C6 e2e deep sweep + bug hunt (lane C — e2e/** only; local
        stack, no deploy gate). signup-as-a-test + persona seed
        fixtures; the honest bug list is the deliverable.
   ws-new: [✓ 1.0.86] D-homepage-feed. Social-media-style tabbed feed
        preview on the marketing-ui homepage. Placeholder content with
        For You / Following / Trending tabs, post cards with avatars,
        media placeholders, engagement counts. Sits between hero and
        reach-gap section. Ready for live backend content.
    [✓ 1.0.130] D-trending-card: TrendingCard component for marketing-ui — media-forward
         card with rank badge (#1 gold, #2-3 silver, #4+ brand), heat glow
         (engagement_score → violet glow intensity), author row, content
         truncated to 3 lines, tags as chips, engagement bar. Hover: 2px lift
         + glow intensify. Skeleton shimmer matches card shape.
         Gate: none. Owns: marketing/marketing-ui/src/components/FeedPreview.tsx
         (new TrendingCard, TrendingSkeleton exports).
    [✓ 1.0.130] D-trending-grid: /trending page layout — responsive grid (1/2/3 col),
         #1 featured card spans 2 cols, topic filter chips (sticky, horizontal
         scroll), hero statement ("What's actually trending. No algorithm.").
         Gate: D-trending-card. Owns: marketing/marketing-ui/src/pages/Trending.tsx
    [✓ 1.0.130] D-trending-sidebar: /trending sidebar (desktop only, wide screens) —
         "🔥 Top 10" ranked list, compact entries, click scrolls to card.
         Gate: D-trending-grid. Owns: new TrendingSidebar.tsx + Trending.tsx
    [✓ 1.0.130] D-trending-polish: /trending loading + empty states — shimmer skeleton
         grid (matches layout), empty state story beat, "Load more" button.
         Gate: D-trending-sidebar. Owns: Trending.tsx + FeedPreview.tsx
     [✓ 1.0.149] D-comments-ledger (social-side prerequisite; decision D32):
         `createComment` (web10-social/src/data/comments.ts:36) mirrors to
         /public/entries UNCONDITIONALLY (like createReaction at
         reactions.ts:35) — no per-post visibility gate; comments are public
         by default (D32). fires the Comment schema (feed.ts:33) with
         payload.action='comment' (what documentdb.py:828 counts). ALSO: the
         `comments` service gains anon-read whitelist in serviceTerms.ts
         (matching public_posts) so any logged-in user reads comments on any
         post via CRUD, not just the post author; AND deleteComment mirrors
         to DELETE /public/entries/{id} (author-only) so removed comments
         drop out of the count. fixes the always-0 trending comment count +
         the test_discovery.py:645 mock that hid it. Gate: none. Owns:
         web10-social/src/data/{comments,serviceTerms}.ts + the FeedScreen
         call site.
     [✓ 1.0.149] D-trending-link: TrendingCard → deep-link to the author's profile on
         web10-social (`${SOCIAL_ORIGIN}/u/<author>`); until social gains a
         per-user profile route (lane-D sibling — App.tsx only has my-bio,
         flagged in gauntlet 1.0.142), link to social's root so the click still
         navigates instead of dead-clicking. Gate: none (degrades to root
         link). Owns: FeedPreview.tsx + Trending.tsx.
     [✓ 1.0.149] D-trending-share: Share2 button has no onClick → navigator.share +
         copy-link fallback (no auth). Gate: none. Owns: FeedPreview.tsx.
    [✓ 1.0.155] D-trending-comments: MessageCircle now expands an inline comment panel
         an inline comment panel (anon read via PATCH /public/entries, which
         is anon-OK per public.py:42; compose auth-gated → funnel-to-social).
         Gate: D-comments-ledger (nothing writes comments to the ledger today,
         so the read returns [] until the social-side mirror lands). Owns:
         FeedPreview.tsx + Trending.tsx.
    [✓ 1.0.164] D-trending-knobs (sub-lane D/marketing): THE ALGORITHM IS YOURS —
         /trending gains synth-styled rotary knobs (recency / likes /
         comments weights, time-horizon half-life, power-mean "character"
         p) that re-rank the grid LIVE, client-side, zero network calls
         per twist. knobs are STEPPED (6 detents) → a knob state
         serializes to a shareable URL mix code (#mix=40213) and gives
         v1 a finite cache keyspace. full spec incl. the math (set-
         independent saturating normalizers so client and server score
         identically), presets (Newest / Most loved · all time /
         Balanced), the accuracy staging (v0 fetch-whole-index = exact
         today; v1 when the corpus outgrows one fetch: true db search
         on cache miss → top-~100 per detent combo cached w/ short TTL,
         debounced local-then-settle — lane-A item then, cache tech
         decided then, NO redis before v1), the tag-scoped-knobs future
         note, and the D20 guardrail (the SOCIAL feed stays
         chronological — this is pitch theater on the marketing surface)
         in plan.txt PHASE 5.5. new lib/powerMean.ts is pure + unit-
         tested (p→±∞ limits, p=0 geometric mean, weight-zeroing).
         GATE: D-trending-comments merged (same files: Trending.tsx /
         FeedPreview.tsx — in flight in ws-D/marketing now). owns:
         marketing/marketing-ui/**. acceptance: design.md §12
         (screenshots desktop + 375px); knob twist reshuffles with zero
         network requests; "Newest" is pure reverse-chron regardless of
         likes, "Most loved · all time" ignores age.
     [✓ 1.0.149] D-trending-reactions: like + repost are fake local increments
         (Trending.tsx:98) → real toggle via /public/entries mirroring
         reactions.ts; signed-out funnels to social. Gate: none. Owns:
         FeedPreview.tsx + Trending.tsx.
[✓ 1.0.93] A5.5. DISCOVERY API — the public layer (Lane A, api/**):
         discovery index (web10.discovery_posts, write-protected,
         background task), schema registry (web10.schemas, CRUD with
         author enforcement, provider.uuid6 IDs), public ledger
         (web10.public, validated entries), discovery endpoints
         (/discover/posts, /users, /search, /topics, /post/{user}/{service}/{id}),
         engagement aggregation (cached counts on index, schema-agnostic).
         See PHASE 5.5 in plan.txt and docs/discovery.md.
    [✓ 1.0.92] D5.5. SOCIAL APP PUBLIC LAYER — split posts into public_posts /
         private_posts, default terms on signup, register default schemas,
         wire reactions to /public/entries, wire feed to /discover/posts.
         See PHASE 5.5 in plan.txt.
    [✓ 1.0.89] persona-orchestration/ — 5 live-testing personas with seed scripts,
         first-week content plans, cross-follows, posts, comments, DMs, and
         reactions. powers C6's e2e persona fixtures and makes the platform
         look alive for dev testing + demos.

  merge-order notes: B5/D12/D13 don't gate each other; merge each as
  it passes design.md §12. D13 owns the cross-app asset generation
  (svg marks, favicons) but replacing assets INSIDE ui/ is B5's to
  apply — D13 leaves the files + a .context note, doesn't reach in.
  C2/C3/C3.5 still parked until after M0.
  (A1/B1/B2/B3/B4/B4.5/C1/C5/D1/D2/D2.5/D3/D4/D5–D10/E1-infra/E2
  are DONE — don't re-pick.)

LANE CI — github actions optimization (owns: .github/workflows/**, cross-cutting)
   goal: reduce from ~$90/month overage to within the 2,040 min free tier.
   each item is a small, independent branch. no lane conflicts.
   [✓ 1.0.99] CI-bun-cache: add bun install caching to js-ci.yml (actions/cache
       keyed on bun.lock + bun cache dir). cuts each js job ~50%.
   [✓ 1.0.99] CI-push-triggers: removed push triggers from all CI check
       workflows (js-ci, api, docker, e2e, marketing-api). code already
       passes PR checks — re-running after merge wastes ~60+ min/merge.
       deploy.yml keeps push (it actually deploys). saves ~80% of CI.
   [✓ 1.0.99] CI-path-filter: per-package path filtering in js-ci.yml so
       touching ui/** only runs the ui job, not all 6 packages.
   [✓ 1.0.99] CI-single-arch: drop linux/arm64 from cd.yml image builds.
       saves ~50% on CD minutes.
   [✓ 1.0.99] CI-small-runners: use ubuntu-latest-small for changelog check.
[✓ 1.0.128] CI-e2e-push-only: skip e2e.yml on pull_request, run only on
        push to dev/main. saves ~15 min per PR.
   [✓ 1.0.99, audit 1.0.140] CI-single-arch: drop linux/arm64 from cd.yml
       image builds if no self-hosters need ARM. saves ~50% on CD minutes.
       AUDIT (23.07, jacoby149/ci-cost-trims): cd.yml + docker.yml on
       origin/dev publish `platforms: linux/amd64` only — arm64 was dropped
       in 1.0.99 (commit 8b2e5aca) and never re-added. README.md +
       ubuntu-deployment/README.md + AGENT-OPS.md document NO arm64/
       aarch64/Raspberry-Pi operator (only "self-hostable" generically; the
       documented deploy target is Ubuntu-on-Proxmox x86_64). An operator
       needing arm64 runs `docker compose up --build` locally — the ghcr.io
       prebuilt images don't gate them. No workflow code change required;
       the trim is in target state on dev.
   [ ] CI-small-runners: use ubuntu-latest-small for lightweight jobs
       (changelog check, js typecheck). docker/e2e builds stay on
       ubuntu-latest.
       BLOCKED (23.07, audit 1.0.140): this org's GitHub plan does not
       provision the `ubuntu-latest-small` runner label. commit 196d908e
       reverted ubuntu-latest-small because it "queued indefinitely (empty
       runner_name, cancelled after ~20min) and showed 'pending' forever
       on every PR"; changelog-check workflow was then removed entirely in
       a3712077 ("a dedicated Actions VM per PR just to run a diff+grep
       isn't worth the wall-clock"). Applying ubuntu-latest-small to any
       PR-gated check (the js.yml typecheck is the only remaining
       candidate) would re-regress the same queue-forever → every JS PR
       red, which violates the "both trims land green" acceptance at the
       top of this lane. Re-open when the operator moves the github org to
       a paid plan that provisions the `ubuntu-latest-small` label (or
       replaces it with a self-hosted small runner). NOT TICKED: the trim
       is infeasible in the current plan, not done.

   from here on: pull the top unticked item of whichever lane's queue
   is unblocked. lanes A and B are long marches; C and D are a stream
   of short branches that keep two slots busy without touching A/B
   files.
