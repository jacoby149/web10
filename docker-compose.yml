version: "3"

services:
  app:
    build: api/.
    volumes:
      - ./api/:/web10   
    ports:
      - "5000:5000"
  auth:
    build: auth/.
    volumes:
      - ./auth/:/auth   
    ports:
      - "3000:8000"
  social:
    build: social/.
    volumes:
      - ./social/:/social   
    ports:
      - "8000:8000"